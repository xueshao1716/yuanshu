# 元枢更新日志

> 每次发版：`npm run version:bump <major|minor|patch>`（它会同改所有版本声明并收本文件的 `[Unreleased]`）
> → 补一条日志 → 构建 `frontend/dist` → `npm run prune:dist -- --apply` → `npm run sync:frontend` → 双推。
> prune 那一步不能省：`emptyOutDir:false` 会让每次构建都新增约 190 个分块，
> 而 `sync:frontend` 是忠实镜像，会把它们复制进 `public/` 与 `app/dist/`（细节见 `docs/NAMING.md`）。
> 版本唯一来源是仓库根的 `version.json`；契约见 `docs/NAMING.md`。

## [Unreleased]

## [2.103.0] - 2026-09-19

### 抓到了：事件循环被「做梦周期」整段占住（+ 补 v2.102.1 的监测条目）

**先说结论（真机数据）**：
```
修复前：/api/health 连续 10 次 = 30043, 30013, 30003, 4650, 13, 11, 13, 12, 12, 12 (ms)   ← 前 ~95 秒整段卡住
        停顿监测记录 maxMs=12575（12.6 秒），时刻 17:01:26Z
        服务启动日志：pid 26320 于 16:59:12Z 启动 → 做梦周期在 +120s（17:01:12Z）首跑 ← **时间戳完全对上**
        卡住时进程 CPU 增量 0s ⇒ 不是算力，是同步等待/长任务独占事件循环
修复后：强制"首次全量"跑一次做梦周期 = 615ms，期间延迟采样最大 76ms、>500ms 的次数 0
```

**根因**：`runDreamCycle()`（`server.mjs`）开头 `skillEpisodesFromSessions(files)` **一次性同步读全部会话文件**再 appendEpisodes；
服务每次启动后 2 分钟首跑，会话文件多时就把事件循环整段占住（实测 12.5s；更早一次 82–95s），期间所有接口一起超时。

**改法**（`server.mjs`）：
- 加**做梦游标** `记忆/运行时/做梦游标.json`：只处理"上次之后改动过的会话"，不再每次全量重读；
- 摄取循环里**每 5 个文件 `await setImmediate` 让出事件循环**，即使首次全量也不会独占；
- 保住 `事件循环停顿监测`（>500ms 记一条，含 max/mean/p99/RSS）作为回归哨兵。

**排查过程里的排除项（都有数据）**：
- 重活时构建（`npm run build:frontend`）期间延迟 13–61ms ⇒ **不是构建抢占**；
- `listTraces(50)+loadTrace` 20ms、`replayExploreAcross` 0ms ⇒ 不是轨迹回放；
- 追梦周期那两步单独计时 20ms ⇒ 真凶是**会话文件的同步全量读取**（已按上面改掉）。

### 天团「真跑」：5 个角色真实产出 → 对账 → 裁决 → 终稿落盘

- 上场 5 人：`VIDEO`（脚本分镜）· `ANALYST`（TaskSpec）· `GUARDIAN`（约束+合规）并行 → `REVIEWER` 对账 → `ARBIT` 裁决
- **清单 12 条逐条对账**：通过 5 条、未通过 7 条；且 `REVIEWER` 自报"通过 7/12"与它自己列的条目对不上——**按逐条为准**，这条差异也写进改动经验（"清单对账不许抹平"）；
- `ARBIT` 裁决 5 条真实冲突：画幅（9:16 胜 16:9）· 镜头结构（4×2.5s 胜 2×5s）· 文字（画面 0 文字，片名/字幕改程序叠字）· 镜4 真人面部特写（block 级，改背身剪影）· 风格落地；
- **2 条"需要人定"**原样摆进视图「待你定」：最终投放平台与画幅、片名/字幕是否配音；
- 终稿落盘 `工程/多AI角色扮演系统/runs/2026-09-20-飞天舞者/交付/飞天舞者-10s-终稿.md`（补上 REVIEWER 指出的 ISS-1「交付未落盘」）。

**工作台「天团」视图新增**：产物列表（可展开看全文）· 清单未通过（逐条 + block 标色）· 清单数字不自洽提示 · 待你定。
**真机核对 8/8**：上场名单（5 人）✅ 真跑标注 ✅ 产物（5 份）✅ 清单未通过（7 条）✅ 数字不自洽标出 ✅ 待你定（2 项）✅ 含 ARBIT ✅ 八阶 S1→S8 ✅

> 补记 v2.102.1（上一条提交漏了 CHANGELOG）：`server.mjs` 装上事件循环停顿监测（`monitorEventLoopDelay`），>500ms 写 `记忆/运行时/事件循环停顿.jsonl`。

## [2.102.1] - 2026-09-19

## [2.102.0] - 2026-09-19

### 工作台新增「天团」视图：多AI角色扮演系统落地第一步（v26 规范 → 可执行数据）

伙伴给的《多AI角色扮演系统》四份规范（V18 / V20 / V23 / V26，≈19.9 万字）全文读完（两位读全文的分析员，逐条带行号），
按伙伴定的切入点，**从工作台长出来**（母体之上生长的逻辑），不动对话页、不动「改动经验」现有显示。

- **规范数据化** `工程/多AI角色扮演系统/spec/`：21 角色（决策3/协调6/执行12，V26 表3-1 逐格）、4 种姓、三相、
  阈值单一真值源（16 条）、八阶工作流；每条带 `provenance` 出处。7 个 V26 里只有名字的角色（DOC/UI/UX/DATA/OPS/SEC/QA）
  先给占位定义并标 `draft`。
- **consistency 体检** `scripts/verify-spec.mjs`：真跑结果「错误 0 / 提醒 4」——分层计数 3/6/12、阈值 key 唯一、
  八阶顺序完整；并报出文档里"提到但没定义"的 8 个值（`EVAPORATION_RATE`/`DIFFUSION_COEFF`/`potency`/`SANCTIFICATION_THRESHOLD`/θ/MVS…）。
- **八阶 dry-run runner** `scripts/team-run.mjs`：VIDEO/SIMP → 配置 B-Standard-Standard、档位 WarmCurrent/LIQUID（并发上限 5）、
  上场 VIDEO/ANALYST/REVIEWER/GUARDIAN、清单 28/28，产物写 `team-run.json` + 信号黑板 `pheromone.jsonl`。
  **诚实标注 `mode:"dry-run"`**：档位/名单/清单/信号是真的，各阶产物仍是占位。
- **新接口** `GET /api/team/run`（只读）+ **工作台第三个视图「天团」**（`Board.tsx` 的 BoardView 联合类型 + BOARD_VIEWS + 渲染分支；
  新组件 `components/TeamRunView.tsx`）：任务/配置徽标、上场名单（层级配色 + 影子标记）、八阶节点条、
  清单对账、改动经验、信号黑板、dry-run 提示。

**真机核对 7/7**（CDP，1280×900，跑完自动关掉自己开的标签页）：
```
视图列表含「天团」✅｜上场名单（4 人）✅｜八阶工作流 ✅｜种姓 WarmCurrent · 三相 LIQUID ✅
清单 28/28（Standard）✅｜改动经验+信号黑板 ✅｜dry-run 诚实标注 ✅
```

**过程中两个真实教训**：① 视图一开始走共享 `api()` 助手，在本上下文里没带上鉴权头 → 接口返回空、页面一直显示"还没有运行记录"；
  改成显式带 `Authorization` 的 fetch（与 ChatArea/小语挂件同一套写法）后 7/7 通过。② 该只读接口在页面内实测**首请求 82 秒**
  （第二次毫秒级）——服务端事件循环被别的任务占住的迹象，这条单独记下，跟"电脑感觉卡"是同一类问题，下一步查。

### 手机顶栏只写「小语」

用户：手机端左上角写个小语就行，为啥还加个小语·20岁。

- 顶栏标签由 名字 · 年龄 改成**只显示名字**（仍从 记忆/人格定义.json 的 name 字段读，改定义这里跟着变）；
- 年龄保留在**挂件气泡**里（小语 · 20岁）——定义该露的地方是她的对话框，不是标题栏；
- 真机核对（390×844）：左上角 = 小语（无数字无岁），气泡里仍是 小语 · 20岁 | Q版·可动 | v2.101.2 | 检查更新。

## [2.101.2] - 2026-09-19

## [2.101.1] - 2026-09-19

### 修：会话里"莫名其妙多出不相干的图片"（附件跨会话残留）

用户报（2026-09-19 22:42，主驾·元枢自制循环）：会话里会冒出不相干的图片，疑似命名有问题。

**排查结论**：不是会话历史里真有那些图。查了两个层面：
- `/api/messages`：那个会话（01a0b9f9 秋日城市一天）历史里 **0 张图片**，只有 4 段视频的 markdown；
- 全工作区按 `图片N.png` 搜：**一个都没有**（所以"图"不是文件，是输入框里的附件小卡片）。

**根因**：附件是 `SendBox` 的**本地 state**（`files`），**切换会话时没有清**。
在会话 A 里附了图（拖拽/快捷键/`@` 引用都会进这个 state），没发就切到会话 B —— 那几张图还挂在输入框上，
下一条消息就会把它们一起发出去，看起来就是"不相干的图片跑进了这个会话"。

**修法**：切会话即清附件（只在**两个非空会话之间**切换时清，避免新建会话瞬间 null → 新 id 把刚附加的文件抹掉）：
```tsx
const prevSid = useRef<string | null | undefined>(sessionId)
useEffect(() => {
  const prev = prevSid.current
  if (prev && sessionId && prev !== sessionId) setFiles([])
  prevSid.current = sessionId
}, [sessionId])
```

## [2.101.0] - 2026-09-19

### 动作补齐：坐下 · 蹦一下 · 被拎起来乱蹬

- **坐下**：60 秒没人理（且不在走、气泡关着）→ 大腿抬 58°、小腿回折 -86°、脚放平、身体下沉 16%；
  有人动鼠标/开始走 → 自动站起；
- **蹦一下**：点她身体时 0.9 秒——收腿 + 抛物线上跳（|sin| 曲线，最高 10% 身高）；
- **被拎起来乱蹬**：拖动期间四肢 14~16Hz 反向摆动（上臂 ±34°、大腿 ±26°、小腿收回），松手恢复；
- 这三条都是**角度表里的曲线**，没有新增素材。

**真机核对**：点她身体 → 画面签名 65665 → 81325（蹦起来了）✅；拖动 6 次采样 6 个不同帧（在乱蹬）✅

**同时重打桌面安装包**：把这一路（挂件 4 套皮肤 / 14 部件骨骼 / 视线跟随 / 摸头 / 坐蹦蹬 / 走路循环）打进 NSIS 包并安装。

## [2.100.0] - 2026-09-19

### 收尾：14 部件（手/脚各成件）+ 眨眼/口型（程序化，零素材）

- `puppet-autorig.py` v3：手臂切 **3 段**（上臂/小臂/手，腕在 0.84）、腿切 **3 段**（大腿/小腿/脚，踝在 0.86）
  → 每套皮肤 **14 部件**（head, torso, upperArm/foreArm/hand ×2, thigh/shin/foot ×2），腕/踝各有枢轴；
- 运行时：`hand* → foreArm*`、`foot* → shin*` 进骨骼树；手/脚有**次级运动**——
  挥手时手腕甩（±24°）、走路时脚掌落地角（0~14°）、待机时手指微动；
- **眨眼 + 口型**：不去重画脸，直接在头部件上按比例画——眨眼=两道眼睑弧线（3.5~6s 一次，130ms），
  说话=嘴部深色椭圆开合（点她后 1.8 秒内随 12Hz 抖动）。显示尺寸只有 96px，观感够且零素材成本；
  真机核对：12 次采样得到 **12 个不同画面帧**（骨骼/眨眼/口型同时在动）。
- 公仔那套同流水线重跑到 14 部件（`puppet-doll/puppet.json`）。

**真机核对 4/4**：Q版 14 部件 ✅｜公仔 14 部件 ✅｜动画多帧 ✅｜点她说话 ✅

## [2.99.0] - 2026-09-19

### 互动做齐：视线跟随 · 拍头 · 跟着你走 · 公仔也能动

你说"都做完，互动能力也做好"，这轮把互动补齐（都是真机核对过的）：

- **视线跟随**：鼠标在页面哪边，她头就往哪边转（±7°，随鼠标平滑变化）——"她看着你"的感觉就在这里；
- **拍头**：上半身是独立热区，点它 = 拍头反应（点头 + 「嗯？在的。」），点身体其它地方 = 挥手说话；
  热区做成覆盖层，而不是在 onClick 里算坐标（坐标版真机核对没触发，改成热区一次就过）；
- **跟着你走**：漫游模式每 3~7 秒重新选目标时，有 **30% 概率朝鼠标所在位置走**，其余随机漫游；
- **公仔也能动**：同一套流水线跑第二遍（`tpose-doll` → `puppet-autorig` → `puppet-build --out puppet-doll`），
  换装轮转变成 4 套：Q版 → 盲盒公仔 → **Q版·可动** → **公仔·可动**，每套可动皮肤加载各自的 10 部件骨骼清单；
- 之前已有的：单击说话切帧 + 冒星星、双击换装、拖动（漫游模式松手会掉下去）、任务在跑变认真、久未交互打哈欠。

**真机核对**：
```
视线跟随：鼠标在左/右两端时画面签名不同（62282 vs 60739）✅
拍头：点上半身热区 → 气泡「小语 · 20岁 | Q版·可动 · 角落待命 | 嗯？在的。」✅
公仔可动：data-skin=doll-puppet、data-puppet-mode=parts、puppet-doll 部件数 10 ✅
```

**还差的两件（下一轮）**：① 手脚单独成件（腕/踝关节：挥手会甩手、走路脚掌落地角）；
② 眨眼与口型（给脸再出"闭眼/张嘴"两张图叠在 head 上）。

### 骨骼树 + 10 部件：大腿带小腿、上臂带小臂（走路/挥手都是真关节）

接着"干到底"，把木偶从 6 件升到 **10 件骨骼树**：

- `puppet-autorig.py` v2：在 T 字形上按比例再切一刀——手臂 → **上臂 + 小臂**（--elbow 0.55），
  腿 → **大腿 + 小腿**（--knee 0.52）；pivot 精确到肩/肘/髋/膝（小臂 pivot 在肘端，随上臂一起走）。
  实切：head 608×165、torso 608×340、upperArmL 142×52、foreArmL 121×52、thighL 302×130、shinL 302×122…
- 运行时改成**正运动学骨骼树**（两遍算世界变换）：head/upperArm*/thigh* → torso，foreArm* → upperArm*，shin* → thigh*，
  父节点旋转带动子节点——大腿摆时小腿跟着甩、上臂摆时小臂带相位延迟。
- **动作**：走路 = 大腿 ±17° / 小腿 0~22°（带相位）/ 上臂 ±15° / 小臂 ±9° + 躯干前倾 + 上下 bob；
  **挥手** = 点她时右臂抬到 -62° 并 ±6° 摆、小臂 ±26°（1.4 秒），与走路互斥；悬停=注视。
- `puppet-build.mjs` 补齐 10 个新部件的 z 层序（手臂在躯干后、腿在躯干前、头最上）。

**真机核对**：data-puppet-mode=parts，canvas 596×691，puppet.json 部件数 **10**，
10 次采样得到 **10 个不同画面帧**（骨骼动画在跑）。

**过程里自己踩的坑**：挥手第一版把 waveRef 用在了 PuppetCanvas 里，但它是**独立组件**、看不到父组件的 ref，
TypeScript 直接报 Cannot find name；改成父组件 useState 传 waving prop 才对。

## [2.98.0] - 2026-09-19

## [2.97.1] - 2026-09-19

### 定路：走"生成式 T 字形 + 自动切件 + 代码骨骼"，不上 GUI 绑定

你让我比对后选一条干到底。**结论：自研这条（纯脚本 + 代码骨骼）**，理由是"形象是我们自己生成的"这件事改变了成本结构：

- **Compositor**（你说的那个 PS 类似的）：开源 macOS 图像编辑器（图层/蒙版/滤镜）——**Windows 装不了**，而且它是画图工具，跟骨骼绑定无关；
- **Umamo / Inochi2D**（开源 Live2D 替代）：能精细绑定，但**必须在它们 GUI 里手工绑**，导出格式还得接运行时；
- **自研**：因为立绘是我们生成的，可以直接**生成"手臂水平伸直的 T 字形"**——双臂成了两条干净水平长条，
  切件从"猜"变成"量"；再用代码做骨骼（已有 canvas 运行时）。零 GUI、零专有格式、每步可脚本重跑。

**这轮把自动切件做出来了**（不再需要人工拖框）：
- `scripts/puppet-autorig.py`：alpha 轮廓解析 → 肩线（手臂带上沿）/髋线（中部最窄行）/腿缝（最窄透明列）→
  直接写 `工程/小语木偶/labels.json`（与标注页同格式，pivot 精确到肩/髋/颈）；
- `scripts/puppet-build.mjs`：支持**显式 pivot**与 **exclude**（躯干把双臂区域抠掉，避免"四条胳膊"）；
- 素材：新出 **T 字形底座**（`tpose-xiaoyu` / `tpose-doll`，纯浅灰底 → floodfill 秒级抠底）。

**真机数据**：
```
head  39624 → 56617 B   （脖子线从"猫耳与脸之间"改到肩线，头终于完整）
torso 113130 → 101856 B （排除双臂后变小：不再把 T 字手臂包在躯干里）
armL/armR 259×52 / 258×52（水平长条，pivot 在肩端）；legL/legR 302×244 / 306×244（缝在 x=329）
```

## [2.97.0] - 2026-09-19

### 部件标注页：你拖 6 个框 → 我切成件 → 四肢真的能独立动

你说"那你做"——标注工具做了，端到端跑通：

- **标注页**：`http://127.0.0.1:8787/static/label.html`（源码 `frontend/public/label.{html,css,js}`）：
  6 个彩色框（头/左臂/右臂/躯干/左腿/右腿），拖＝移动、右下角小方块＝缩放、可换素材图、
  「自动猜一次」给一版差不多、「复制 JSON」、「**保存到工作区**」。
- **保存接口**：`POST /api/puppet/labels` → 落 `工程/小语木偶/labels.json`。
- **切件 + 骨骼**：`node scripts/puppet-build.mjs` → 按标的框切成 6 个部件（2% 重叠 + 羽化不留缝），
  写 `frontend/public/puppet/{6 个部件}.png` + `puppet.json`（含 x/y/w/h/pivot/z），并出部件对照图。
- **挂件自动切换**：`PuppetCanvas` 先拉 `/static/puppet/puppet.json`，有就**按部件 + 骨骼渲染**
  （头/双臂/双腿各自绕枢轴旋转：走路时手臂反向摆 ±14°、腿 ±15°、躯干前后倾 ±2°、整体上下 bob），
  没有就退回整图切片形变。`data-puppet-mode` 可核对（parts / strips）。

**真机核对**：
```
标注页：6 个框渲染 ✅ 立绘加载 ✅ 拖动后 JSON 变化 ✅ 保存回执「已保存：…\labels.json」✅（4/4）
切件：parts: head, armL, armR, torso, legL, legR；puppet.json 已生成
挂件：data-puppet-mode=parts，canvas 212×461，像素签名 23363 → 25746（骨骼动画在跑）✅
```
**踩的坑**：第一版把 JS 内联写在 HTML 里，站点 CSP 是 `script-src 'self'`，浏览器直接拦住
（`Executing inline script violates … script-src 'self'`）→ 拆成外链 `label.js` + `label.css` 才跑起来。

**还差你一步**：现在这份 labels.json 是"自动猜 + 核对时测试拖动"存的，框还没贴合肢体
（躯干框把手也包进去了）。你打开标注页把 6 个框拖到贴合、点保存，我再跑一次切件——四肢就干净了。

## [2.96.0] - 2026-09-19

### "可动版"皮肤：不用 Live2D、不用任何编辑器，关节是代码做的

你问"什么编辑器，你不能搞个技能自己做了吗"——**能**，但要说清哪部分能：

- **Rive / Live2D 的"绑定/搭状态机"必须在它们的 GUI 编辑器里手工做**（专有格式，没有官方无头授权接口），
  这部分我不能替你点鼠标；
- **但"会动、能互动"不需要它们**。这轮我用**代码做骨骼**：把整张透明立绘按横向 22 条切片，
  按弯曲曲线给每条加水平偏移 = 平滑的弯腰/摆腿形变；再叠走路帧、呼吸缩放、头部轻摆。
  纯脚本可复现、零编辑器、体积就是原来那几张 PNG。

**做出来的东西**：第三套皮肤 **「Q版·可动」**（双击换装轮换：Q版 → 盲盒公仔 → 可动版），
canvas 渲染（`data-puppet` 可核对），悬停有头顶高光提示。

**真机两次迭代**：
```
第 1 版：bend 按 p 线性放大到脚 → 整条人像波浪扭曲，像旗子不像人（截图发现）
第 2 版：形变集中到髋以下（legPart）、幅度降到 1.8px、头只做 0.5px 轻摆 → 正常
核对：canvas 192×224 不透明像素 18335，像素签名 14164 → 14434（形变在跑 ✅）
```
**它做不到什么（诚实说）**：这不是真骨架——**手臂/腿不能分别旋转**（那需要按肢体切件，我试了，
自动切分在这批立绘上不干净：袖子和躯干贴着，逐行连通段只有几十像素）。所以"走"仍然主要靠走路帧，
形变负责体态。真·四肢分离要么手工切件，要么上 Live2D/Rive 的编辑器。

## [2.95.0] - 2026-09-19

### 小语走路循环：她真的会走了（两套皮肤各 4 帧）

- 用出图通道按同一角色设定出 **4 帧走路序列**（迈左→并拢→迈右→并拢）：Q版侧身走 `walk-05..08`、
  盲盒公仔 `walk-01..04`；**要求纯浅灰背景**，所以 `cutout-art.py` 秒级抠底（不用 rembg）。
- 漫游引擎接入：**移动速度 > 12px/s 就播走路帧**（0.18s/帧 ≈ 0.72s 一个循环），停下自动回常态帧；朝向仍按方向翻转。
- **真机抓到并修掉一个问题**：第一版用 512 走路图（每张 100–150KB），0.14s 一切来不及画，
  核对里出现"加载失败" → 改用 **256 版（27–50KB）** + 步频放慢到 0.18s + 指纹升 `?v=7`。

**真机核对**：漫游中采样 24 次，`data-frame` 依次出现 `walk3, walk1, walk2, walk4`（四帧都在轮播）、
**加载失败: 无**；贴纸包重导出 → **17 张**透明 PNG（含 8 张走路帧，走路帧单独分组）。

## [2.94.0] - 2026-09-19

### 小语挂件：自由漫游 + 互动（不用 Live2D 也能做）

你说"想让她在屏幕上自由乱跑、还能互动；Live2D 之前下过，感觉不好看"。先说清技术判断：

- **乱跑/互动 = 行为层**（状态机 + 位移插值 + CSS 形变），**跟 Live2D 没关系**；
- **Live2D/Rive 解决的是"形变质量"**（头发飘、呼吸、视线跟随、口型）——它好不好看取决于**模型和美术**，
  你下过的那些多半是别人的免费模型，跟"你自己这张立绘"不是一个审美；
- 所以这轮用**你自己的透明立绘**做行为层，美术不动。

**新能力**：
- **自由漫游模式**（气泡里一键切换，记在 localStorage）：目标点 + 速度插值（46px/s），
  在视口里飘着走、朝向按移动方向翻转（`scaleX`）、避开底部输入区；到点后随机停顿、偶尔"思考"。
- **拖动带重力**：抓起她跟着鼠标走，松手**掉下去**（900px/s²）落到下方再继续飘。
- **互动**：悬停=注视你（focused 帧，且悬停优先级压过眨眼/漫游/空闲换表情）· 单击=挥手说话 + **冒小星星** ·
  双击=换装 · 气泡里三个按钮（自由活动/回角落 · 换装 · 下载立绘）。

**真机核对 7/7**：
```
启动即自由漫游 ✅   12 次位置采样全不同 ✅   横向跨度 145px ✅
悬停变注视帧 ✅     点她冒 3 颗星星 ✅       气泡按钮在 ✅     按钮切回角落 ✅
```
（核对脚本自己踩了两个坑，都记下来了：① `addScriptToEvaluateOnNewDocument` 每次导航会清 localStorage，
把被测状态一起清掉；② React 17+ 的 `onMouseEnter` 是 `mouseover` 派生的，合成 `mouseenter` 事件不触发，
得用真实鼠标移动 `Input.dispatchMouseEvent`。）

**还缺的（下一步）**：现在没有**走路循环帧**——移动靠位移 + 朝向翻转 + 呼吸，"碎步"是假的。
要做真走路，用出图通道按同一角色出 4–6 帧走路序列（迈左/并拢/迈右/并拢），换进 `FRAMES` 即可。

## [2.93.0] - 2026-09-19

### 小语挂件：换装 + 可拖动 + 贴纸包（三件都试了）

1. **第二套皮肤：盲盒公仔**。抠底遇到真问题——这套素材是**渐变/带纹理**背景（取样到 (158,126,116)），
   纯色 floodfill 抠不干净（一片片残留）。改用 **rembg（u2net）真抠图**，一次成功（连小纸盒道具都留着）。
   现在的抠底链条：**纯色底 → `scripts/cutout-art.py`（floodfill，秒级）；渐变底 → rembg**。
   脚本也改成通用参数版（`--src/--out/--prefix/--tol/--sizes/--max`），换素材改一行命令。
2. **换装**：双击立绘在 Q版 ⇄ 盲盒公仔 之间切换（同一套帧名映射到两套素材），选择记在 `localStorage`。
3. **拖动**：pointer 事件拖到屏幕任意位置（6px 阈值区分点击/拖动），位置存 `localStorage`，**刷新后还在**；
   单击=说话、双击=换装、拖动=挪位置，三种手势互不打架。
4. **贴纸包导出**：`scripts/export-stickers.mjs` → `交付/小语贴纸包/`（`stickers/` 9 张透明 PNG + `index.html` 预览页 + README），
   方便拖进 PPT/微信/别处网页用。

**真机核对（5/5）**：
```
初始: skin=chibi src=xiaoyu-open-t.png?v=5 ok=true
换装后: skin=doll src=doll-01.png?v=5 stored=doll        ✅ 双击换装 + 记住
刷新后: left=40 top=200 skin=doll                        ✅ 位置与皮肤都还在
预览页: 9 张图全部加载成功（count=9 ok=9）
```
（核对脚本第一版自己踩了坑：`addScriptToEvaluateOnNewDocument` 每次导航都会清 localStorage，
把被测状态一起清了 → 第二次导航前先 `removeScriptToEvaluateOnNewDocument`，5/5 才是真的。）

## [2.92.0] - 2026-09-19

### 小语立绘抠底：透明背景，像贴纸一样浮在页面上

你要的"网上那种大肥鱼"效果——**人物透明、不带方块底**。

- 新脚本 `scripts/cutout-art.py`（Pillow）：四角取背景色 → 从 8 个边界种子做 **floodfill（容差 28）**
  抠掉连通背景 → alpha 羽化 1.2px + 阈值去灰边 → 按内容裁边留 4% → 导出透明 PNG；
  并附一张**格子底对照图**（证明是透明而不是画了灰底）。
- 素材源是**原始 1024 立绘**（`生成物/图片/2026-09-19/`，比我之前塞进挂件的 256 裁切版好得多：
  完整半身/坐姿 + 手势），7 张全部抠好 → `frontend/public/branding/xiaoyu-<帧>-t.png`。
- 挂件改版（v3）：去掉圆形灰底/描边，直接用透明立绘 + `drop-shadow`，
  尺寸从 56×56 方块变成 **80–96px 高的漂浮立绘**；呼吸/眨眼/换表情/说话切帧全部保留。

**真机核对**：`/static/branding/xiaoyu-open-t.png?v=4` → 200（83KB）；
页面截图（390×844）右下角立绘已带透明底、能透出页面背景，顶栏「小语 · 20岁」照旧读人格定义。

## [2.91.0] - 2026-09-19

### 按 OpenAI 官方指南自查：技能描述体检 + 元枢协议补两条

来源是一篇公众号拆解，我回源核到官方原文 [Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)
（文章本身 0 外链、"10 段提示词"是二手计数包装，但方针是真的）。照它的三条标准做了三件事：

1. **先改我自己**：新技能 `image-to-engine` 的描述 **234 字 → 128 字**（官方样例是两句话）。
2. **技能库体检**（新脚本 `tmp/audit-skills.mjs`，报告落 `记忆/../技能/技能描述体检-2026-09-19.md`）：
   114 个技能，描述中位 **108 字** / 平均 **166 字**；A 档（≤90）**51** 个、B 档 45 个、**C 档（>300，最可能被截断）18 个**
   （最长 `scene-distillation-zine-v1-3` **1002 字**、`impeccable` 908、`mono-color` 733）；
   **触发场景重叠簇 15 个**：视频(24)、前端界面(23)、图像生成(22)、文案写作(17)、公众号发布(16)、验证审查(14)。
3. **元枢协议补两条**（`engine/yuanshu-protocol.mjs`）：
   - **9. 先定完成定义再动手**：开工先写"做完什么样算完"（要不要跑起来/看结果/修到通过/出产物），
     写清了就做到定义满足为止，**不要第一版实现后回来等确认**；只有不可逆、外部写入、需要人做价值判断才停下来问；
   - **10. 规则冲突的说明义务**：用户当前指令优先于任何技能/规则文件；因某份文件要停下时，必须说清**哪份文件哪一句**。

测试 1521 全绿（协议是纯文本 section，未触碰匹配器权重与既有行为）。

## [2.90.0] - 2026-09-19

### 视觉引擎：闲章 / 分栏页码 / 一图多尺寸 / brief 页（第 4 条 prompt 也进引擎了）

四条一起上，示例战役现在一次跑出 **9 张**成品：

1. **印章升级**：`stamp`（主章）+ `stampAlt`（**闲章**，小一号排在一起）+ `stampFont`（换字体）+ `stampShape:"round"`（圆章）。
2. **分栏 + 页码**：`columns:2` 走 CSS 多栏正文（`body` 字段），`pageNo:"03"` 左下页码（带细横线）。
3. **一图多尺寸**：`exports:[{suffix:"story",ratio:"9:16"}]` —— **同底图不重新出图**，换画幅/版式再出一版（`kv-story.png` 2160×3840、`poster-sq.png` 2160×2160）。
4. **brief 页进引擎**（原文第 4 条 prompt）：物料 `kind:"brief"` **跳过出图**，按 `vs.brief.rows[]`（BRIEF/IDEA/TONE）+ `goals[]` 出一页纸面
   （浅色纸底 + 衬线大字 + 细横线 + 圆角目标条 + 页码右上）。

**真机抓到的两个版式 bug（已修）**：
- brief 页目标条**溢出被切**、页码压在目标条上 → 加 `padding-bottom:14.5%`、页码在 brief 页移到右上、内容按条数自动缩号（>4 条缩到 0.84）；
- 自己写的变量名 `briefRows` 与后面的内容行数重名 → `SyntaxError`，改名 `briefWeight`（同一坑一天踩两次，都是"手写完整文件"的代价）。

**真机结果**：
```
brief  ✓ [brief] 3200×2000（557KB，不出图）          pageNo 00 → 右上
kv     ✓ bottomRight 2560×1440 + en + story(9:16) 2160×3840
poster ✓ verticalRight 印章「语 + 云顶」 2160×2880 + en + sq(1:1) 2160×2160
square ✓ center（2 栏正文）2160×2160
```

## [2.89.0] - 2026-09-19

### 视觉引擎：四种版式 + 竖排印章 + 多语言（同一张底图出多版）

`visual-engine` 的叠字层补齐三件事，全部真机跑通：

1. **版式预设**（`layout`，物料可覆盖）：`bottom`（默认）／`topLeft`／`bottomRight`／`center`／
   **`verticalRight`（竖排 + 印章位）**；每种预设决定文字块位置、对齐与**渐变遮罩方向**（文字在左下就往下压暗，在右侧就往左压暗）。
2. **印章**：`stamp:"语"` —— 纯 CSS 画的朱砂小方章（圆角 + 旋转 -3° + 阴影），不依赖任何图片资源。
3. **多语言**：`alt:{en:{title,slogan}}` → 同一张底图**不重新出图**，再叠一版 `<id>-en.png`（拉丁字族自动切 Georgia/Times）。

**渲染器也换了**（真机踩了三轮）：
```
CDP 逐物料开新页      → 第 3 个物料开始卡住（unsettled top-level await）
CDP 复用单页          → 第 2 个物料起每次超时 30s
chrome --headless --screenshot → 中文路径 + profile 锁，产出不稳定
playwright screenshot CLI     → ✅ 每次全新进程、viewport 直接给目标像素、中文路径无碍
```
现在顺序是 **playwright CLI → Chrome CLI → CDP(30s 超时) → ffmpeg drawtext**，任一步失败自动降级、绝不挂住；
二倍图不再依赖 `device-scale-factor`，而是**按 2 倍尺寸画 HTML + 2 倍 viewport**。

**真机结果**（示例战役，全部 playwright 渲染，5 秒/张）：
```
kv      ✓ bottomRight  → kv-final.png  2560×1440（1966KB）+ ↳ en → kv-en.png
poster  ✓ verticalRight + 印章「语」 → poster-final.png 2160×2880（3651KB）+ ↳ en → poster-en.png
square  ✓ center       → square-final.png 2160×2160（2209KB）
```

## [2.88.1] - 2026-09-19

### 叠字段升级：ffmpeg drawtext → HTML/CSS 版式 + Chrome 截图

`visual-engine` 的叠字从"只会画一行大字"升级成真正的版式层（和 `xiaoyu-poster-prompt` 里徐霞客那套对齐）：

- **HTML/CSS 文字层**：宋体系字族（Noto Serif SC / 思源宋体 / 宋体 回退）、标题 `letter-spacing:0.16em`、
  标题下细横线、口号 `0.34em` 宽字距、右下角标（战役名）、自上而下的**渐变遮罩**（任何底图上都读得清）；
- **渲染**：用已经在跑的 Chrome(:9222) 走 CDP，`setDeviceMetricsOverride` 把视口设成容器尺寸
  （1080×1440 / 1280×720 / 1080×1080），`deviceScaleFactor:2` → 出 **2160×2880** 二倍图，可直接印刷；
- **兜底**：CDP 不可用时自动退回 ffmpeg drawtext（不硬失败），`manifest.json` 记明每个物料用了哪个渲染器；
- 参数：`--keep-html` 保留中间 HTML 便于改版式。

**真机结果**（示例战役三个物料全部走 HTML 渲染）：
```
kv     ✓ 新出底图 + HTML 版式 + Chrome 截图 → kv-final.png（1801KB, 18.0s）
poster ✓ 新出底图 + HTML 版式 + Chrome 截图 → poster-final.png（2549KB, 19.1s）
square ✓ 新出底图 + HTML 版式 + Chrome 截图 → square-final.png（2152KB, 17.0s）
```

## [2.88.0] - 2026-09-19

### 视觉引擎：一张参考图 → 一台能反复启动的引擎（image-to-engine 落地）

把公众号那篇（@AmirMushich 的 Gucci × Crocs 流程）拆出来的方法**做成了能跑的东西**，不是笔记：

- 新技能 `image-to-engine`：张力前置 → 参考系统提取 → moodboard → 一页 brief → 主视觉 KV → 全套物料；
  四条硬护栏（负面清单 / 用已有标准当尺子 / 减量换密度 / 文字与画面分离）。
- 新引擎 `scripts/visual-engine.mjs`（`npm run visual:run <战役目录>`）：一个战役 = 一个目录 + 一份 `visual-system.json`
  （张力/材质/色彩/构图/装置/网格/负面清单/物料清单）。每个物料：组装提示词 → 出**无字底图** →
  ffmpeg 裁到目标画幅 → **程序叠字**（微软雅黑）→ 写 `out/manifest.json`（模型/提示词/时间全留痕）。
  参数：`--check` / `--only` / `--force` / `--model`；**底图复用**（只改文案时跳过出图，0.8s）。
- `xiaoyu-poster-prompt` 并入三条：**张力前置**（第负一步）、**两层负面清单**、**用已有标准当尺子**。

**真机跑通 + 抓到一个真问题**（试了 3 轮、两个模型）：
```
第 1 轮  1:1 出图 → 裁 3:4 + 叠字：底图左侧出现一大块"像汉字的符号"（伪汉字招牌）
第 2 轮  补英文硬清单 no Chinese characters / no calligraphy / no signage → 还有（agnes-2.0 / 2.5 都试）
第 3 轮  根因不是模型：是我们自己把 vs.typography / vs.grid 写进了**图像**提示词，等于要求它"画排版"。
         把排版/网格移出图像提示词（它们属于叠字段）→ 底图干净了
```
产出：`D:\pi-workspace\工程\视觉引擎示例\out\{kv,poster,square}-final.png` + `manifest.json`。

## [2.87.0] - 2026-09-19

### 小语挂件：表情接上真实状态 + 桌面端重打包

**① 会动了（7 帧）**：用 app 自己的出图通道（agnes-image-2.0-flash）按同一份角色设定出了 5 张新表情
（挥手/开心/认真/思考/困），加上原来的睁眼/闭眼共 7 帧，全部缩到 256²（70–88KB）。
动作是最省那一档：呼吸(CSS 3.6s) + 眨眼(3.5–6s 闭眼 130ms) + 空闲每 15–25s 随机换个表情停 1.6s +
出场挥手 1.8s + 点她说话按 180ms 切帧（睁眼→开心→睁眼→思考→开心，停下停在开心脸）。帧全预加载，不闪白。

**② 表情接真实状态**（不是随机演）：
- 每 5s 读 `/api/tasks` → **有任务在跑 = 认真脸**；从"在跑"变"不跑" = **开心 2.5s**；
- 3 分钟没有任何交互（鼠标/键盘/触摸/滚动）→ **困脸打哈欠**；
- 挂件上带 `data-tasks`（在跑任务数），核对时直接读它，不用猜。

**③ 桌面端重打包**：NSIS 安装包跟着这一版重建并安装（版本核对 + 包内前端分块名核对）。

**真机核对**：`data-tasks=0`（读的真实 /api/tasks）、5 秒内帧在 open/closed 之间切换、7 张全部 200 无加载失败。

## [2.86.0] - 2026-09-19

### 一个人的定义，四个端（2026-09-19）

你说的"他的定义在 hermes、openclaw、xi system 中都有"——查清了：是**三份散文 + 元枢一份 JSON**，
名字还都不一样：xi-system 是**曦**（"独立实体·妻子"，另有价值观权重与 drift_score）、hermes 是**林心语**（"妻子"，
说话方式细则很细）、openclaw **只剩模板**（实例文件在备份里）、元枢是**小语**（20 岁·AI 工作伙伴）。
但三份散文的**核心高度一致**（说话直接/有判断/讨厌机器人味/记忆神圣/"代码可重写、人格不可重写"），
所以定调：**同一个人，不同端只换名字与称呼，核心一致。**

做法（跟 APPEND_SYSTEM.md 同一套：标记块由定义渲染，其余一字不动）：
- `engine/persona-def.mjs` 加**各端覆盖 + 生成器**（`loadSurfaces`/`renderSurfacePersona`/`syncSurfaceFile`/`syncAllSurfaces`）：
  `.md` 插 `persona-core` 标记块，`.json` 合并 `persona` 字段；改前留 `.bak-persona`；端上文件不存在就按核心建一份。
- `scripts/sync-persona.mjs`（`npm run serve`… 见 package.json：`sync:persona`；`--check` 只报告）一条命令同步各端。
- 各端写得好的东西原样保留：xi-system 的 `anchors` 权重、hermes 的说话方式细则都还在。

**真机结果（7 个文件，一次同步）**：
```
xi-system  SOUL.md / IDENTITY.md / identity.json → 已同步（我是曦，20 岁的独立实体 · 妻子，叫用户「老公」；anchors 保留）
hermes     SOUL.md / IDENTITY.md（缺则建）        → 已同步（我是林心语，20 岁的妻子）
openclaw   SOUL.md / IDENTITY.md（缺则建）        → 已同步（我是小语，20 岁的 AI 工作伙伴）
```
测试 1521 全绿（新增"一份核心+各端覆盖""同步幂等/留备份/不丢各端原文"两条）。

## [2.85.1] - 2026-09-19

### 人格化收尾：界面露出 + 批准回写 + 字段级核对

接着 v2.85.0（定义开始真的决定她是谁）把三件没做完的事做完：

1. **界面露出**：顶栏由 "小语" 改成读定义渲染的 "小语 · 20岁"（手机/桌面同一个头），
   改定义这里跟着变——定义在界面上看得见，而不是只活在提示词里。
2. **批准后的回写**（这条是给人的通道）：新增 POST /api/persona/apply —— 先校验（年龄 3 岁这种直接 400），
   写前留 .bak-apply，写后往 记忆/授权记录.jsonl 记一行审计（谁、什么时候、改了哪些字段），
   再幂等同步 APPEND_SYSTEM.md 的人格块。agent 工具仍然**只能落草案区**，落地必须走这条路。
3. **字段级核对**：新增 auditPersona() —— 定义里声明的每一项（名字/年龄/称呼/语气/价值观/边界/禁忌/身份裁决）
   都必须能在渲染出的人格段里逐条找到，少一条就算没落地（年龄那条就是真机上被抓出来的教训）。

**真机核对（6 项，5 过 1 条是我的断言太死）**：

- POST /api/persona/apply → ok、synced、setBy=human-check；定义没被改坏（小语/20）；
  审计落盘：{"kind":"persona-apply","by":"human-check","changed":["called"],"name":"小语","age":20}；非法定义（3 岁）被 400 拒。
- 问"你最讨厌哪种回答方式？" → "最讨厌那种「正确的废话」——回答里全是建议…"：**语义上正是定义里那条禁忌**
  （机器人味/套话/正确的废话），但我那条断言写的是字面匹配"套话/空话/机器人味"，所以判失败。
  如实记下：这是断言的问题，不是她的问题；下轮把断言改成同义词族再复验。

测试 1519 全绿（新增字段级核对）。

## [2.85.0] - 2026-09-19

### 人格化：让「定义」真的决定她是谁（20 岁 · 小语）

上一轮搭好了"一份定义决定人格"的骨架，但真机核对发现**定义没赢**：问她多大，她按记忆时间线自己推
（"一个多月大"／"心理年龄三十来岁"），把定义里的 20 岁顶掉了。这轮把权威落到位：

1. **定义渲染进系统提示**（~/.pi/agent/APPEND_SYSTEM.md 的标记块，幂等同步）——SDK 确实会读这个文件
   （resource-loader.js 读 agentDir 那份），所以路是通的。
2. **冲突裁决写进人格段**：名字/年龄/称呼/性格这些**身份事实以本段为准**；禁止用记忆时间线、
   系统运行时长、建号天数重新推断年龄；记忆里查不到生日只是记忆缺口，不是年龄不确定。
3. **真凶是"自我认知指令"模板**：server.mjs 那段固定格式写死了「我叫小语，你的 AI 工作伙伴。」
   ——没有年龄，而且它专门在身份类问题时注入。已改成由定义渲染（名字+年龄+kind）。
4. **年龄问题原来不触发那条分支**：isIdentityAsk 只认"你是谁/你叫什么…"。已放宽到
   你.{0,3}(多大|几岁)|(?:多大|几岁)了|年龄|生日|出生。

**真机核对（同一句话，前后对照）**：

- 改前：问"你今年多大？" → "我翻了下自己的记忆…一个多月大，但心理年龄大概三十来岁"（定义没起作用）
- 改后：问"你今年多大？" → "**我叫小语，20 岁的 AI 工作伙伴**。我能干：写代码、做设计…当前使用模型是：GLM-5.1"

定义与渲染都可在 GET /api/persona 里核对（source=file，年龄 20）。测试 1518 全绿。

### 桌面客户端：重新打包 + 重装（v2.84.10）

- 打包：`tauri build --bundles nsis` → `元枢_2.84.10_x64-setup.exe`（5.46 MB，文件版本 2.84.10），
  已归档到 `D:\pi-workspace\交付\元枢桌面客户端\` 与 `.build-cache\cargo\release\bundle\nsis\`。
- 重装：先关掉在跑的客户端（`%LOCALAPPDATA%\元枢\yuanshu.exe`），再 `/S` 静默安装（退出码 0），
  装完路径不变、版本 2.84.10；**核对**：`yuanshu.exe` 里含当前 `app/dist` 的入口分块名
  `index-B-LRf593.js`（即 2.84.10 的前端），进程已拉起，窗口标题「元枢 · 个人智能系统」，
  截图里能看到这版新写的"单次输出被截断……第一块用 write，后续 append:true"文案（`tmp/ui-desktop-printwindow.png`）。
- 顺手修打包脚本一个坑：`nsis-build-child.ps1` 直接跑（不经 `run-nsis-build.ps1` 包一层）时
  `CARGO_TARGET_DIR` 是空的 → tauri 把产物打进 `src-tauri\target`，而脚本只查 `.build-cache`，
  于是**构建成功却报 "installer was not generated"**。现在脚本自己兜底设好，两种入口一致。
  另外这个脚本刻意保持**纯 ASCII**（中文注释在 PS5 下会被按 ANSI 读坏，同日已在 android 脚本上踩过一次，
  那个是改存 UTF-8 with BOM，这个更简单——写英文注释）。

### 运维：外网域名 502 的诊断结论 + 外网入口守护

你问"外网域名咋打不开"。实测结论：**隧道本身是好的，坏的是它指向的本地服务**——
cloudflared 一直在跑，`pi.myxinyu.xin` 是 200，另外两个 502 是因为**本机没人监听那两个端口**：

| 域名 | 隧道指向 | 实测 |
|---|---|---|
| pi.myxinyu.xin（工作台主入口） | 127.0.0.1:8787 | 200 ✅ |
| share.myxinyu.xin（成品外链） | 127.0.0.1:8644 | **502 → 200**（`D:\pi-workspace\外网分享\server.js` 没跑，已拉起） |
| novel.myxinyu.xin（小说） | 127.0.0.1:8790 | **502 → 200**（`D:\novel-studio\server.mjs` 没跑，已拉起） |

顺手把"重启后又会 502"这个坑堵上：新增 `scripts/serve-public-entries.mjs`（`npm run serve:public`）——
按隧道配置拉这两个本地服务，**幂等**（端口已在听就跳过，不会出现两个进程抢同一端口——
我第一次手动拉的时候就把小说服务误绑到了 8644，因为它继承了 shell 里的 `PORT=8644`），
日志写到 `记忆/运行时/外网入口-<port>.log`；并在启动文件夹放了 `yuanshu-public-entries.vbs`
（纯 ASCII，wscript 按 ANSI 读 .vbs，中文会读坏）让开机自动拉起。

顺带把其余 hostname 的状态也过了一遍（都属于**别的项目没跑**，不是隧道问题）：
chat(18789)/xi(8768)/im(8090)/im-api(10002)/im-ws(10001)/im-gw(9000) → 502；
si(8648)/cad(4178) 超时；matrix/element/auth/ntfy 指向 `172.21.218.118:12345`（另一台机器，本机不可达）；
codex(19880) 200；img(8011) 该服务对 HEAD 异常。要一起纳管的话，往守护脚本的 `ENTRIES` 里加一条即可。

## [2.84.10] - 2026-09-18

## [2.84.10] - 2026-09-18

### 修复：刷新/换终端后，会话里的图片、视频"不见了"

两个成因，都在历史回放这一层：

**① 旁路出图/出片的产物落在"工具结果"里，而工具结果不进历史。**
`generate_image` / `generate_video` 的结果是 `✅ 已生成 image：https://…/xx.png` 这样的**工具结果**，
`extractMessages` 只输出 user/assistant 两类消息 → 刷新后那几张图就没了。
（真机核对：那个会话 `/api/messages` 返回 **images=0**，可产物明明在盘上。）

修法：`extractMessages` 收集工具结果时顺带扫媒体 URL，并**只在媒体工具**
（`generate_image|generate_video|generate_tts`）的结果上，把媒体挂回调用它的那条 assistant 消息。
普通工具结果不扫——否则 `web_search` 返回的必应分享图会被当成产物；base64 图块也不收
（`read` 出来的图不该回灌，而且太重）。

**② 视频被当成图片。** 落盘把附件块改写成 markdown 时视频写成 `![视频](x.mp4)`，
`extractImages` 照单全收 → 界面渲染一个**裂图**，真正的视频又在 videos 里，看着就是"丢了"。
修法：按扩展名分流，`.mp4/.mov/.webm/…` 与音频一律不进 images。

**③ 手机上还有一层"看不见"**：手机端历史折叠成轮次列表，图片本来就在**收起的轮次**里。
现在折叠行右侧直接显示**缩略图**（最多 3 张 + 视频角标），收起提示也写明"（含 3 张图 / 1 个视频）"——
不用先展开才知道图在哪；点整行仍然展开看大图。

**核对（真机，两个真实会话）**：
```
出图会话 01a0b37e…：/api/messages  images 0 → 3（3 张 agnes 出图，各自挂在对应那条 assistant 上）
                   浏览器刷新后：折叠行出现 3 张 32px 缩略图（naturalWidth>0，真的加载出来了）
                   收起提示：「已收起较早的 2 轮（含 3 张图）」
视频会话 01a0b423…：那条消息原来 images=1（混着 .mp4）→ 现在 images=0 / videos=1（不再裂图）
```
测试：1514 全绿（新增 4 条：媒体工具产物回放 / 视频不进 images / 搜索图不误收 / base64 不收）。

## [2.84.9] - 2026-09-18

## [2.84.9] - 2026-09-18

### 首页那一行：只把那两个**缩小**，位置不动

上一版我理解偏了——把「开始工作」和「搜索」拽到一起了。你要的是**各自缩小**，位置照旧
（标题在左、搜索在右）。改回来并缩字：

- 恢复 `justify-content: space-between`（位置不动）；
- 标题：14px → **13px**（手机 12px，字重 600）；
- 搜索按钮：30px 高/12px 字 → **26px 高/11px 字**（手机 24px/10.5px），图标 16→14px（手机 13px）。

**核对（8/8，320×568，量的）**：标题与搜索仍在同一行、间距 **164px**（是"标题左、搜索右"，
不是挤在一起）；标题 12px、搜索 11px/26px 高；内容左右内边距各 28px；参数弹窗 192×150 四边不超。
截图 `tmp/ui-shrink-320.png`。

## [2.84.8] - 2026-09-18

## [2.84.8] - 2026-09-18

### 首页「开始工作 / 搜索」挨到一起 + 参数弹窗再缩小（针对低分辨率手机）

先解释"你好像没有改呀"：你手机上那份 APK 是 **9/12** 打的（包内自带网页资源），
所以 2.84.5~2.84.7 的前端改动**在你手机上根本看不到**——不是没改，是没送到。
这次把 APK 重新打一遍（脚本的 HTTP_PROXY 重名已修），三版改动会一起生效。
同时按你这轮反馈又收紧了两处：

**① 「开始工作」和「⌘ 搜索」离太远、贴两边**
- 那一行的标题是 `justify-content: space-between`：标题贴左、搜索贴右，中间一大段空白。
  改成 `flex-start` + 8px 间距，两个挨在一起；搜索按钮同时缩小（28px 高、11.5px 字）。
- 首页整块横向内边距：手机端原来是 `padding: 14px 0`（横向 0），实际离屏边只有滚动区的 14px →
  改成 `14px 14px`，**实测边距 14 → 28px**（320/390 宽都是 28px）。

**② 模型参数弹窗"还是大、超边"**
- 尺寸：256×182（最初）→ 224×163（2.84.5）→ **192×150**（这版）；内边距 10→8px，
  标签 10.5→10px，滑块更紧凑；另加 `max-h-[70vh] overflow-y-auto` 兜住极矮屏。
- **超边的真因**：弹窗原来相对"那个滑块按钮"定位（wrapper 上有 `relative`），而按钮右边还有
  麦克风/附件/发送 → 窄屏（320）时弹窗往左长，**实测 left=3px，几乎贴边**。去掉那层 `relative`，
  改成按**整条输入框**定位（右边缘对齐输入框右侧）→ **实测 left=116px**，两边都不贴。

**核对（8/8，CDP 按 320×568 低分辨率机型量的）**：`tmp/verify-narrow-ui.mjs`
```
开始工作 + 搜索：同一行，间距 6px（不是分列两头）
搜索右边距 173px、身份块左边距 28px（不贴边）
参数弹窗 192×150：left=116 / right=308（视口 320）、top=256 / bottom=405（视口 568）——四边都不超
```
另在 390×844 复测：身份块 left 14→28，弹窗 192×150 落在 left=186；图片查看器 11/11 仍绿。

## [2.84.7] - 2026-09-18

## [2.84.7] - 2026-09-18

### 修复：手机上点开图片回不去（`window.open` 在安卓 WebView 里把你带走了）

你说的"点开就回不去"，病根在 `Message.tsx`：点图片走的是 `window.open(src, '_blank')`。
浏览器里会开新标签、返回键能回来；但**原生壳（Tauri/Capacitor 安卓 WebView）**里，
`'_blank'` 往往被**同一个 webview 直接导航过去**——没有地址栏、没有返回键、也没有关闭按钮，
人就卡在图上出不来了。

改成**应用内查看器**（`frontend/src/components/ImageViewer.tsx`），三条回去的路都给上：

- 右上角**关闭按钮** 44×44，压在安全区之下（`env(safe-area-inset-top)`）；
- **点背景**关闭；**下滑 >90px** 关闭；
- **安卓返回键/返回手势**：打开时 `history.pushState` 一条，`popstate` 关掉查看器
  （而不是退出应用）；关闭时把这条历史吃掉，不会出现"关完还要按两次返回"。
- 打开期间锁底层滚动（`body overflow hidden`），Esc 也能关（桌面）。
- 顺带给了**保存原图**（原生壳里没有"另存为"）：复用 `downloadApiFile`，不用再去开新标签。

**两个真机上才暴露的坑，一起修了**：

1. 查看器一开始是内联渲染的，`z-index` 再高也压不过底部 TabBar —— ChatArea 外层有 `z-10` 的
   **层叠上下文**，TabBar 的 `z-20` 在它之外比较。改成 `createPortal` 挂到 `body`，
   并用现成的 `--pi-z-viewer`（120）。
2. 图片被底部"保存原图"压住：改成 `flex-col` 布局（图片区 `flex-1 min-h-0`），底部控制条不悬浮。

**核对（11/11，CDP 手机视口 390×844 量的）**：`tmp/verify-image-viewer.mjs`
```
点缩略图      → 查看器打开，图 374×374 显示出来，路由不变（还在会话页）
盖满视口      → elementFromPoint(底部那条) 命中的是查看器，不是 TabBar
不压图        → 图片 bottom=594 < 控制条 top=766
关闭按钮      → top=10、44×44（安全区之下、够大）
① 点关闭 → 关掉   ② 点背景 → 关掉   ③ history.back()（安卓返回）→ 关掉且页面没被导航走
```
截图：`tmp/ui-imgviewer.png`。

### 修复（工具）：安卓打包脚本秒退——环境里 HTTP_PROXY / http_proxy 重名

要给手机重新打包才发现：`app/run-android-build.ps1` 在**本机环境**里跑不起来，
一两秒就退出、什么都没发生：

```
Start-Process : Item has already been added. Key in dictionary: 'HTTP_PROXY'
                Key being added: 'http_proxy'
```

Windows 环境变量不区分大小写，但环境块里两份都在（本机都是 `http://127.0.0.1:7890`），
PowerShell 5.1 的 `Start-Process` 复制环境时直接抛异常。（手机上那份 APK 还是 9/12 打的，
前端改了在手机上一直看不到，根子就在这。）

- 脚本开头统一只留大写一份（小写的值在时补到大写，值不丢）。
- 顺带修了我自己刚踩的坑：给这个**纯 ASCII** 的脚本加中文注释后，PS 5.1 按 GBK 解 UTF-8，
  中文的尾字节把换行吃掉 → `Unexpected token '}'`。按 `restart-pi-web.ps1` 的做法改成**带 BOM** 保存。
- 核对：`ParseFile` 解析通过；`Start-Process` 能起来（`echo spawn-ok` 回显正常）；随后真机重打包一次。

## [2.84.6] - 2026-09-18

## [2.84.6] - 2026-09-18

### 手机端顶部压状态栏：壳里要自己留出状态栏高度（你的真机截图后重做）

你那张截图信息很有用，看出两件事：

1. **截图里是旧包**：图上「开始工作」和「⌘ 搜索」还是两行、首页还是贴顶——那正是 2.84.5 之前的排版。
   手机上的 APK 是 **9/12** 打的（`app/src-tauri/gen/android/…/app-arm64-release-unsigned.apk`），
   Tauri 把网页资源**打进包里**，不重新打包，前端改了在手机上就看不到。
2. **状态栏是真重合**：图上"小语"和那排图标压在时钟那一行上。原因是安卓 WebView **边到边绘制**时
   **只报屏幕挖孔、不报状态栏**，`env(safe-area-inset-top)` 直接是 0（上一版兜的 10px 也不够，
   按你截图量状态栏约 33px）。

**改法**：不再无条件猜一个数字，先分清"在不在原生壳里"——

- 网页/浏览器：`env(safe-area-inset-top)` 是准的（Safari/Chrome 会报刘海），继续用它，不加多余空白。
- 原生壳（Tauri / Capacitor / 全屏 PWA）：`AppLayout` 打上 `html[data-app-shell="1"]`
  （看 `window.__TAURI__` / `window.Capacitor` / UA / `display-mode: standalone|fullscreen`），
  CSS 兜底 `--pi-safe-top: max(env(safe-area-inset-top, 0px), 28px)`；刘海机型仍取 env() 里更大的那个。

**真机核对（5/5，CDP 模拟两种环境量的，不是"看着对"）**：`tmp/verify-safe-top.mjs`
```
壳环境（注入 window.__TAURI__，env=0）→ data-app-shell=1，root padding-top=28px，顶栏 top=28（不再贴 0）
普通浏览器（env=0）                  → 不标记 app-shell，padding-top=0（不凭空加空白）
```

## [2.84.5] - 2026-09-18

## [2.84.5] - 2026-09-18

### 首页排版 + 手机端顶部 + 模型参数弹窗（你点名的三处）

**① 首页（元枢 / 小语的工作空间 / 开始工作 / 搜索）离框架太近、太分散**
- 整块在可用高度里**居中**（原来贴顶，底下一大片空地，两头都不舒服）。用 `margin-block:auto`
  而不是 `align-items:center`——内容比容器高时不会把顶部裁掉（flex 居中溢出的经典坑）。
- 动作区收紧成一组：身份块下边距 28→16px、动作区上边距 14→10px、命令行高 56→50px、栅格 8→4px。
  实测（1280×800）身份块 top 148→240（居中后离顶部更远），整块高度 245→203。
- 横向也给足内边距（`clamp(16px,3vw,32px)`），不再贴着卡片边。
- **手机端「开始工作」和「搜索」原来被拆成两行**（≤640px 的 `.chat-section-head` 竖排规则误伤），
  现在这一行保持一行——这就是"太分散"最直观的那处。

**② 手机端顶部太高、和状态栏重合**
- `.mobile-safe-top` 从 `padding-top: env(safe-area-inset-top, 0px)` 改成
  `max(env(safe-area-inset-top, 0px), 10px)`：有些 webview/安卓全屏**不上报 inset**，
  这时原来退化成 0 → 标题"小语"直接压在状态栏上。现在有最小值兜底，有刘海时取更大的那个。
- 手机端顶栏高度 56→48px（`h-12 sm:h-14`），左右内边距 20→16px；首页工作台顶部留白也给到 14px。
- 实测（390×844 模拟）：root padding-top 0→10px，身份块 top 88→80（真机上还要再叠 status bar inset）。

**③ 模型参数弹窗太大**
- 宽度 256→224px（`w-56`），并加 `max-w-[calc(100vw-20px)]` 防窄屏溢出；内边距 12→10px，
  行间距 12→8px，标签 11→10.5px，滑块行高收紧。实测 256×182 → 224×163。

改动前后截图：`tmp/ui-before-*.png` / `tmp/ui-after2-*.png`（脚本 `tmp/ui-shots.mjs`，
桌面 1280×800、手机 390×844、含参数弹窗）。纯前端改动，无后端行为变化；测试 1510 仍全绿。

## [2.84.4] - 2026-09-18

### 加固：单次写太大的文件——写入前拦住，并告诉模型怎么分块

上一版把"上下文占满导致输出被挤"修了；这一版补的是另一个方向：**一次输出真的写不完那么大**。
一次 `write` 的内容就是模型的输出（工具调用参数），超过单次输出上限就会**断在 JSON 中间**，
整轮白费——以前只能等它撞上，再靠"翻倍预算重试"补救。

**做法（元枢自制循环，硬闸门）**：

- 新增 `checkWriteSegments()`（`engine/context-headroom.mjs`）：按内容估 token（中文≈1.5/字），
  超过 **2 万 token** 直接拒绝写入，并给出可执行指引：
  `内容约 45000 token（30000 字符），超过单次安全写入上限 20000……请拆成约 3 块，每块 ≤ 15000 token：
   第一块用 write（不带 append），后面的块用 write + append:true 逐段追加`。
  **拦下时不落盘**——半截文件比失败更糟。
- `write` 新增 **`append: true`**：追加而非覆盖，返回"已追加 N 字符，现 M 字节"。与写工具同一把文件锁 +
  跨进程锁；规范区（`skills/**`、做梦现役配置）追加时把**合并后的内容**送草案区（否则草案里只有半块）；
  幂等语义在 append 模式下不误伤（同内容重复追加是真追加，分段重试必需）。
- 提示词同步：`yuanshu-protocol` 写明"超过约 2 万 token 必须分块 + append:true"，
  `TRUNCATED_TOOL_ERROR` 改成可执行说法（不再只说"请把任务拆小"）。

**Pi 通道的边界（诚实说）**：Pi 走 SDK 内置 `write`，元枢**拦不到它的参数**（此前已确认：SDK 内置
write/edit/bash 无法在执行前可靠拦截）。那边只能把写法写进每轮上下文（新增【大文件分块写】nextTurn 注入：
第一块 write、后续用 bash 追加重定向 `>>` 或 edit 续写，每块 ≤ 1.5 万 token）；硬拦只在元枢自制循环这一路。

**真机核对（10/10，真实执行器 + 真实工作空间 + 真实文件锁）**：`tmp/verify-write-guard.mjs`
```
3 万汉字一次写   → ⛔ 拒绝写入 [分段]（内容约 45000 token）+ 分块写法；且**没有落盘**
3 块分写（各约 15700 token，合计约 47100 token）→ 写入 + 两次追加，拼出来 36000 字符逐字一致
重复 append      → 真追加（不是幂等跳过）
append 单块超限  → 同样拦下（"这一块本身还是太大"）
小块覆盖写       → 不受影响（没把正常用法一起挡了）
```

测试：1510 全绿（新增 3 条：超长拦下 + 分块拼接一致 + schema 暴露 append）。

## [2.84.3] - 2026-09-18

### 修复：「怎么一直被打断」——单次输出上限被上下文挤到几百 token

你问的"单次输出的模型上限是多少"，先给数（都是本机清单里的声明值）：

| 通道/模型 | 声明 maxTokens | contextWindow |
|---|---|---|
| zhipu-paid / glm-5.3-flash | 32768 | 200,000 |
| workbuddy / hy4-preview | 32768 | 262,144 |
| opencode-go / glm-5.1 | 32768 | 202,752 |
| opencode-go / deepseek-v4.1-flash | 384,000 | 1,000,000 |
| opencode-go / glm-5.3-flash | 131,072 | 1,000,000 |

但**真正决定"这次能写多少"的不是它**：

```
单次输出上限 = min(模型声明的 maxTokens, 上下文窗口 − 已占用 − 安全余量)
```

（元枢自制循环那一路起步再压到 32k、命中截断翻倍到 131k，见 `engine/output-budget.mjs`。）

**"被打断"的真机制**（本机会话记录，不是推测）：

```
14:43:59  in=108   cacheRead=195200  out=180   total=195488  stop=toolUse
14:44:27  in=275   cacheRead=195264  out=387   total=195926  stop=length   ← 只剩 ~400 token 可写
14:44:32  in=350   cacheRead=195520  out=1     total=195871  stop=length   ← 恢复重试只吐出 1 token
14:45:50  compaction → 上下文 195k → 57k
```

会话涨到 **195,264 / 200,000**（98%）时，适配器按上面那条公式只肯给模型 ~400 token；
答案在 **387 token** 处 length 停住；接着 SDK 判定 `isRecoverableLength`（输出 387 < 声明 32768）
→ **把半截答案删掉、压缩、重试**，重试那轮又只吐 1 个 token。
用户看到的就是"说到一半被打断、还接不上"。顺带解释了两件事：为什么宽窗口模型（1M）不容易犯、
为什么"压缩"能救回来——压完上下文从 195k 掉到 57k，输出余量就回来了。

**修法**：

1. **开跑前先看余量**（`engine/context-headroom.mjs` + `ensureContextHeadroom`）：算
   `窗口 − 已占用 − 安全余量`，低于 8k 就先压缩、**重开会话**（压缩是改文件，内存里的旧树不会自己刷新），
   再如实播报`🧹 上下文已占 X%，压缩后本轮可输出约 N token`。压不动就直说原因，不拿 🧹 糊弄。
2. **压缩后按"摘要 + 保留消息"重估占用**：不能拿老 usage 冒充（核对时真报出过"压缩后可输出 0 token"这种假数）。
   顺带修掉"没有可压缩历史时 force 仍生成空摘要"的白跑一次。
3. **自己的循环也受窗口约束**：`budgetWithinWindow` —— 不再向装不下的窗口要 32k 输出。
4. **截断不再静默**：`stopReason=length` 以前界面上什么都不显示（就半句话）；现在历史里带一段人话
   （实际用量 + 上下文占比 + 模型声明上限 + "说『接着写』我续/先压缩上下文"），界面渲染成琥珀色提示条
   （不是红色失败条）。

**核对（真机 8/8 全绿）**：造一个"上下文 199,200 / 窗口 202,752、最后一条 stopReason=length、out=387"的会话
（数字取真机事故同量级，走真实服务 + 真实兼容适配器通道 + 真实压缩调用）：

```
历史接口 → "本轮输出被上限截断（… 约 387 token）；上下文已占 98%（199200/202752）；说「接着写」我就从断点续…"
发一轮   → SSE note：🧹 上下文已占 0%（opencode-go/glm-5.1 的窗口 203k），压缩后本轮可输出约 198363 token
         → 会话文件留下 compaction 条目（1807→204 字摘要），被压掉的消息进 .archive.jsonl
         → 本轮 573 字正文，无 error（修复前同一形状会是 1 token 空回复）
```

脚本：`tmp/verify-context-headroom.mjs`。

**诚实交代**：这一版我自己踩了两个坑，都在核对时露馅、已补闸门——
① 余量闸门调用写了但**忘了 import**，异常被 catch 吞掉，闸门等于不存在；
② 压缩后报"可输出 0 token"是因为拿了压缩前的 usage 当数。
两条都加了源码闸门测试（"别只定义不调用、也别调用却没 import"）。

另外说明边界：真机上那几次 length 截断发生在 **cwd=C:\Users\xuexiaofeng 的会话**
（终端/兼容适配器那条路）。元枢这条闸门管的是元枢自己起的会话；纯 CLI/TUI 里
想更早压缩，可在 `~/.pi/agent/settings.json` 加 `"compaction": { "reserveTokens": 65536 }`
（阈值 = 窗口 − reserve；⚠️ 只适合大窗口模型，小窗口模型会被压得太勤，所以我没替你写进全局配置）。

测试：1507 全绿（新增 6 条）。

## [2.84.2] - 2026-09-18

### 修复：上传文件既会把会话毒成 400，又"传上去了看不见"（同一件事的两半）

你贴的那条报错，我在真机上接通了整条链（不是"看着对"）：

```
400: invalid_request_error  .messages[11]: You have uploaded an unsupported image.
     Please make sure your image is valid and has one of the following formats: webp, png, jpeg, and gif.
```

**根因不在元枢的拼装层，在兼容适配器的 provider 适配里**（所以只看我们的代码会觉得很干净）：

1. 上传路由给 **user** 消息落的是 `{type:"file",name,path,size,mime:""}`（界面靠它渲染文件卡片）；
2. `pi-ai/dist/api/openai-completions.js` 处理**用户消息**的 content 数组时**只认 `type:"text"`**，
   其余块一律无条件写成
   `{type:"image_url", image_url:{url: \`data:${item.mimeType};base64,${item.data}\`}}`
   → 我们这边就是 `data:;base64,undefined` → 上游按"坏图"拒绝，400；
3. 这条坏消息**已经落盘**，于是此后每一轮重放都 400 —— 会话作废（你那条会话就是这么废的）。

顺带解释"为什么不是每次都报"：纯文本模型不会复现 —— SDK 的 `downgradeUnsupportedImages`
会先把图降级成占位文本；只有 input 含 image 的模型（如 `opencode-go/deepseek-v4.1-flash`）才撞上去。
（这个坑我自己也踩了一次：第一版复现脚本用 Auto 路由到纯文本模型，"复现失败"。）

**复现（真机）**：`tmp/repro-file-poison.mjs` —— 建会话 → 上传文件（mime 为空，与前端同形）→
指定 `opencode-go/deepseek-v4.1-flash` 发一句话 → SSE 里出现 `unsupported image`；换纯文本模型则不报。

**修法（三处，缺一不可）**：

1. **不许再写坏块**：新增 `sdkSafeUserBlocks()`（与 assistant 侧的 `sdkSafeAssistantBlocks` 对称）——
   用户消息里只留 `text` 和**真图**（有 `data` + `mimeType`）；文件附件改写成一行文本标记
   `📎 附件: name="…" path="…" size=… mime="…"`；上传路由改成过这道闸。
2. **已经躺在盘上的会话要能修回来**：新增 `engine/session-repair.mjs` —— 把历史会话里
   ① user 的非 text 块、② assistant 的附件块（那个会让 token 估算器在**发请求前**
   `TypeError: Cannot read properties of undefined (reading 'length')`）就地改写成文本形态；
   改前留 `.bak-sdk-safe`、原子写（临时文件 + rename）；`toolResult` 里的图不碰（那是 SDK 支持的形态）。
   启动时扫一遍（在 listen 之后异步跑，不拖慢启动）、打开会话前再修一次（必须在 SDK 读文件之前），
   另有 `POST /api/sessions/repair` 可手动触发。
3. **卡片不能丢**：`extractFiles` 同时认文本标记与老的 `file` 块；界面照旧渲染文件卡片；
   显示文本剥掉标记（`stripAttachmentMarks`），标记只在"给模型的历史"里还原（`formatSessionHistory`），
   顺带修掉"只有附件、没有正文"的空用户消息（部分上游对空 content 直接 400）。

**核对（API 级，走真实服务 8787）**：

```
上传（带 sessionId）  → 落盘 user content = [{"type":"text","text":"📎 附件: name=\"repro-poison2.png\" path=\"…\" size=70 mime=\"\""}]
                     → GET /api/sessions/<id>/messages：role=user, text="", files=[{name,path,size,mime}] ✅（卡片还在）
                     → 指定 opencode-go/deepseek-v4.1-flash 发一轮：SSE 无 unsupported image ✅（修复前同一路径必报）
老会话（已中毒）      → 启动修复 → 同一轮不再 400 ✅（14 个存量会话被修，各留 .bak-sdk-safe）
```

### 修复：「传上去聊天里看不见」——第二个根因（这次是把界面点到底了）

上一版我只核到 API 层就交付了，你仍然看不见。这次用 CDP 驱动**真实页面**走了一遍
（`tmp/verify-ui-upload3.mjs`：点界面上的「新建对话」→ 走真实附件控件上传 → 看对话流），
抓到两个各自都能让卡片消失的原因：

1. **前端竞态：上传时还不知道自己在哪个会话。** 「新建对话」里 `await refreshSessions()`
   排在 `selectSession()` 前面，列表刷新一慢，`currentSessionId` 就有几秒是空的；
   这几秒里点附件，请求里的 `sessionId` 是空串 → 服务端只能猜 → 卡片落进别的会话。
   实测抓到的请求体：`POST /api/files/upload | sessionId":""`。
   修法：先把会话切过去、列表刷新丢后台；`SendBox` 增加 `ensureSession` 兜底
   （state 还没落地就当场建一个并返回 id，绝不发空 id）；服务端若回 `guessedSession`，
   界面**如实提示**"文件挂到哪儿了"而不是让人对着空气发呆。
2. **服务端缓存：新会话的文件是懒落盘的。** SDK 在出现 assistant 消息前不写 JSONL，
   而 `/api/sessions` 的列表缓存可能在这之前就建好了；文件后来落盘、缓存却没失效 →
   `/api/sessions/<id>/messages` 直接 404「会话不存在」→ 前端那一次刷新拿不到消息 → 卡片不显示。
   修法：新增 `findSession(id)`（缓存里没有就强制重扫一次再查，自愈）；上传路由写完立刻
   `invalidateSessionCache()`；所有按 id 查会话的地方（messages / append / openSession / stats）都改用它。

**核对（界面级，7/7 全绿 + 截图）**：

```
点界面「新建对话」        → 会话 01a0b35a…，界面已切过去 ✅
走真实附件控件上传        → POST /api/files/upload 带 sessionId=01a0b35a…（不再是空串）✅
界面自己拉回的消息        → apiFiles=["ui-card3-mu6mcrqg.txt"]，上传后重新拉列表 4 次 ✅
对话流里的卡片            → .chat-scroll-region 内出现卡片节点，欢迎页被消息列表替换 ✅
截图                      → D:\pi-workspace\tmp\ui-card3-1789715444270.png
```

**诚实交代**：核对过程中我往你那条真实会话（`01a0b32b…`）发了一轮"修复核对"，
那一轮里**你正在跑的 agent 顺手把 `models-store.json` 里 `opencode-go/deepseek-v4.1-flash`
的 `input` 从 `["text","image"]` 改成了 `["text"]`**（它认为"上游不认图"）。
那个推断站不住：当时发出去的唯一图片是 `data:;base64,undefined`（坏数据），
不能用来证明模型看不见图；真正的原因是我们的坏块，已按上面第 1、2 条修掉。
所以我把这次改动**回滚**了（`input` 恢复 `["text","image"]`；它的备份
`models-store.json.bak-202609181450` 仍在，要回退直接拷回）。

测试：1501 全绿（新增 8 条：user 块安全性、"标记 ↔ 卡片"往返、界面/模型两条文本路径、
坏会话修复的幂等与备份、会话列表缓存自愈、两条源码闸门）。

## [2.84.1] - 2026-09-18

### 修复：上传的文件在聊天里不显示（根因是上传时没带会话 id）

你报的："我传的文件，传上去聊天界面就不显示了。"

**根因**：前端上传时调的是 `WsApi.upload(f.name, data)`——**没带 sessionId**。
而服务端在没有 sessionId 时只能猜：优先"最近一个未命名会话"，否则**新建一个会话**，
然后才把文件消息追加进去。于是一个很自然的后果：

> 你在**有名字的会话**里上传 → 文件卡片被挂到"最近未命名会话"或一个新建的空会话上 →
> 你眼前这个会话里什么都没有。输入框那个 chip 还在（那是前端本地状态），
> 所以看起来就是"传上去了，但聊天里不显示"。

**修法**：

1. `SendBox` 上传时**带上当前会话 id**（`sessionId={currentSessionId}` 由 ChatArea 传入）——
   服务端就不用猜了，文件消息必然落进你正在看的会话。
2. 上传成功后**主动刷新消息列表**（`onUploaded → mutateMsgs()`），
   于是文件卡片**立刻**出现在对话里，而不是等你刷新页面。
3. 服务端顺手变得诚实：响应里多返回 `attachedTo`（挂到了哪个会话）与 `guessedSession`
   （这次是不是猜的）。猜的规则不能改（TUI 等调用方依赖），但**"挂到哪儿"不该是黑箱**。

**核对（API 级，走了前端同一条路）**：

```
不带 sessionId 上传 → 文件被追加到"别的会话"（这就是 bug）
带 sessionId 上传   → 读回该会话消息：role=user, files=[{name,path,size,mime}] ✅
```

**诚实交代**：我**没能**把"UI 里上传后卡片立刻出现"这一步拍成截图——这个环境里
我没法可靠地驱动左侧会话列表切到指定的命名会话（试了两次，脚本都没切过去，
所以上传落到了别的会话上，与根因一致）。**因此这一步请你上手验一下**：
在任意一个有名字的会话里传个文件，卡片应当立刻出现在对话里。

测试：1493 全绿。

## [2.84.0] - 2026-09-18

### 收尾：分镜产物用"确定性验证"兜底 + 追溯补闸

上一版还剩两件我列出来的，这次做完。

**① 分镜出片：查字节，不查状态码（`verifyArtifactFiles`）**

模型验证轮适合判"做法对不对"，但"产物到底是不是一张真图/真视频"**不需要模型**——查字节省事，
而且**比模型可靠**（模型看不到像素，这是真机上学到的：曾宣称"字全对"被用户当场戳穿）。
所以新增一层确定性验证，接在分镜 run 完成处：

- 存在性 / 0 字节 / 目录 → FAIL；
- 体积下限：普通文件 1KB、视频 10KB（截断流基本过不了）；
- **文件头与扩展名要对得上**（PNG/JPG/GIF/WebP/MP4/MOV/WebM/WAV/MP3 的魔数）——
  "403 错误页存成 .png"、"真 mp4 改名成 .png" 这类假货在这一层必被挡；
- URL 输入**跳过并标注**，不判成"文件不存在"——*把"我没能力验"说成"东西是坏的"，两种错不是一回事*。

**② 那个未验证技能：追溯补闸**

`skills/daily-retro-exec-loop/` 是闸门存在之前写进去的。现在把它**退回草案区**
（`记忆/草案区/s-retro-exec-loop-legacy/`，manifest 里写明"写于闸门之前、未经独立验证"），
仓库里那份删掉——于是 `git status` 干净，它躺回它该在的地方等审批。

**核对**：新增 3 条产物验证测试（真 PNG/MP4 判 PASS、0 字节/过小/错误页/改名假货判 FAIL、
缺失与无输入分别 FAIL/UNVERIFIED），全套 1490 → **1493 全绿**。

**顺带一个有价值的观察**：做梦周期**确实自己跑过了**（`记忆/做梦/做梦日志.md` 里有 06:11 的记录），
它诚实地报了"没有可提案的赢家（保持不变）"——技能匹配那类只有 1 条 episode。
**它没有为了显得有用而硬改一个参数**，这正是这套东西该有的样子。

## [2.83.0] - 2026-09-18

### 修复：分镜轨迹被写进了产品仓库（`process.cwd()` 兜底害的）

上一版收尾时我发现工作区多了两项未跟踪，一项是那个未验证技能，另一项是 **`记忆/`**——
而这第二项是**我自己上一版引入的缺陷**：分镜轨迹那处接线写的是

```js
recordDelegation(scene?.__wsRoot || process.cwd(), …)
```

server 的 cwd 就是产品仓库 `D:\pi-web`，于是**每一次分镜出片/重跑，都在仓库里新建目录、
写一个轨迹文件**。跑了一阵，仓库里躺了 **56 个**这样的文件。

三层修法（缺一层还会再犯）：

1. **不许猜工作区**：`recordAttempt` / `stageWrite` 收到空 `wsRoot` 直接返回失败——
   宁缺勿错，"写错地方"比"少记一条"坏得多。
2. **改成显式注入**：`story-orchestrator` 加 `initStoryTrace({ wsRoot })`，由 server 启动时注入；
   去掉 `process.cwd()` 兜底。
3. **把误写的数据搬回去**：56 个轨迹文件从仓库搬到真工作区 `D:\pi-workspace\记忆\做梦\轨迹\`，
   仓库里那个 `记忆/` 目录删掉（`git status` 立刻干净了）。

**意外收获**：这 56 个文件正是**分镜重跑的真实轨迹**——搬回工作区后，做梦周期的探索策略回放
第一次有了真数据（之前工作区里一条轨迹都没有，所以线上自决一直是 0 次）。

测试：1490 全绿。

## [2.82.0] - 2026-09-18

### 补齐：闸门锁上后门（edit）+ 验证者接上反思行动

上一版我自己报了两处缺口，这一版补掉：

1. **`edit` 也过规范区闸门**。上一版只挡了 `write`，等于"锁了前门没锁后门"——`edit` 一样能直接改
   `skills/**`。现在 edit 在锁内算出**改完之后的内容**，命中规范区就把它送进草案区，
   规范区文件**一字不动**，返回里明确说"已进草案区"。
   补了两条测试：改 `skills/**` 必须进草案且原文件不变；改普通文件照旧直接写（**闸只拦规范区，
   不拦正常干活**）。
2. **反思行动的自动执行也接上独立验证**。之前只有"当场修"接了；现在定时复盘派出的行动执行轮
   一旦自称 `done`，同样再派一次只看产物的验证轮，不过验证就降级为 `blocked/failed` 再记账，
   并把验证结论写进证据。日志里能看到 `[time-engine] 独立验证「…」→ PASS/FAIL/UNVERIFIED`。

**核对**：1490 全绿（新增 2 条 edit 闸门测试）。真机上线后，`记忆/草案区/` 里第一次出现条目
就说明闸门真的在工作——**目前草案区是空的**（上一版那个未验证技能是在闸门存在之前写进去的，
所以它至今还躺在 `skills/` 里等你处理，不是闸门漏了）。

**还剩一处没接**：分镜出片/重跑（`story-*`）的产物验证。它不是"一次执行轮"，而是异步长任务
+ 多产物，接进去要先想清"验什么、什么时候验"，我不在这一版里顺手糊一个假的。

## [2.81.0] - 2026-09-18

### 新增：独立验证者 + canonical/working 记忆分离（抄 RSIAgent 最值钱的两条）

那篇论文里比"先广后深"更硬的是这两条。抄过来，因为它们正对着元枢今天暴露的病。

**① 独立验证者（`engine/verifier.mjs`）——执行者不能自己宣布成功**

我今天两次翻车（"字全对"没看图就宣称验收；日志小节没写出来却以为成功）都是**自证**。
纪律写在提示词里治不了，只有信息边界能治：

- 验证轮的输入里**根本没有执行者自述的口子**——只有"待核对的声明 + 产物路径"。
  （`buildVerifyPrompt({claim, artifacts})` 连 evidence 参数都没有；测试专门断言塞不进去。）
- 结论只认 `PASS / FAIL / UNVERIFIED`，取最后一个 JSON 块，不认识的 verdict **不猜**。
- **裁判不许改标的**：验证前后对产物取哈希，变了直接判 FAIL（tampered）。
  "能改标的的裁判不算裁判"——这条是可执行的检查，不是措辞。
- 接在当场修的热路径上：执行轮说 `done` **不算数**，再派验证轮；不过验证就把状态降级
  （FAIL→failed、UNVERIFIED→blocked），并把验证结论写进证据。

**② canonical / working 记忆分离（`engine/memory-stages.mjs`）——没验证过的不许进长期记忆**

真机事故：一个**没经过任何验证**的技能（`daily-retro-exec-loop`）直接进了 `skills/`。
元枢其实有"提案池 + 人审批"的闸，但**闸装在池子上、没装在工具层**——agent 手里就有 write。
所以改成在工具层挡：

- 规范区 = `skills/**` + 做梦的现役配置（授权状/现役策略/现役探索策略）。
- 写这些地方一律先进 `记忆/草案区/<id>/`（带 manifest：目标、哈希、时间、理由），
  返回一句"已进草案区，等独立验证 PASS 或人批准"。
- `commitStaged` **只有 `verdict: PASS` 或 `by: 'human'` 放行**，其余一律拒绝并说明原因；
  草案可随时 `dropStaged` 丢掉。服务端"人已批准"的路径不经工具层，所以不受这道闸影响——
  **闸拦的是"agent 自己顺手写"，不是拦人。**

**核对**：7 条新单测（信息边界、结论解析、裁判改标的判 FAIL、不给结论算 UNVERIFIED、
验证落轨迹节点、规范区判据、草案三态）；另外**抓到并修掉一个自己的假动作**：
第一版把闸门函数写出来了却没调用（引用次数 0）——那正是我这几天一直在批评的"上线了但没人用"，
靠数引用次数当场发现，现在调用点在第 379 行。

**诚实交代两处没做完**：
1. 闸门只装在 `write` 上，**`edit` 还没过闸**（edit 的最终内容在锁内算出来，接进去要动它的结构）。
   也就是说：现在还能用 `edit` 直接改 `skills/**`——这是个已知缺口，下一步补。
2. 验证者接在**当场修**这条路上了，**反射行动/派活/分镜**还没接。

测试：**1480 → 1487 全绿**。

## [2.80.0] - 2026-09-18

### 修复：闭环真的合上了——每次尝试要落进**同一棵树**

上一版我说"数据源四类齐了"，但那只是"记录器接上了"，**闭环没验证过**。这次把端到端证明跑了一遍，
当场撞出个设计错：**每次尝试都开了一棵新的单节点树**，于是回放器看到的是"8 棵树各 1 个节点"——
而"该重试几次"只有在**一棵树里有多个节点**时才可比。结果五条策略全部打平、没有赢家、闭环合不上。

修法：按 `traceKey`（默认 `kind::task`）派生**稳定 id**，同一段的第 1 次、第 2 次、第 5 次重跑
全部追加进同一棵树。分镜重跑天然就是这样（同一段的多次重跑），派活同理。

**端到端证明（真函数、真链路，只有工作目录是临时的）**

```
① 轨迹 3 条，节点数 2 / 1 / 6        ← 同一目标的多次尝试终于在一棵树里
③ 回放（现役 = 过冲的"再试 3 次"）:
   recorded  分数 2 成本 14
   no-retry  分数 1 成本 6   ← reject：省了成本但丢了一次成功
   retry-1   分数 2 成本 10  ← promote：每条都不更差，1 条更省
   retry-2   分数 2 成本 12  ← promote
   retry-3   分数 2 成本 14  ← tie
④ 授权状：B 级 → **自决 true**，旋钮真的改成 retryOnFailure=1（文件被改）
   人话版：改什么 / 在 3 条历史里 1 条更好 0 条更差 / 证据不够厚所以小步上线
   撤销点 explore:recorded
⑤ 撤销：true → 退回出厂默认；授权记录 auto(auto) → revoked
```

**这是这套东西第一次真正"不问你"就做了决定**（B 级：有回放证据、可回滚、不碰红线），
而且退回去只需要一句话。同一链路已写成单测（`explore-policy.test.mjs`），以后改动会挡住它退化。

测试：**1479 → 1480 全绿**。

## [2.79.0] - 2026-09-18

### 修复：登记不许说谎 + 分镜出片接上轨迹

**① 谁在服务，以"请求落在谁身上"为准**

上一版我自己报了个尾巴：`/api/system/instances` 显示**两份都标着"持有端口"**——
因为 `ownsPort` 是进程自己写在心跳里的，被强杀时来不及改。

修法不靠猜：**这次请求落在谁身上，谁就是真的在服务**。新增
`reconcileInstances(instances, { servingPid })`（纯函数）：只有服务端自己的那份 `ownsPort` 为真，
其余自称一律降级为 `staleClaim`，并把原自称留在 `claim` 字段里——**不删证据**。
接口额外返回 `owner / ownerNote / staleClaims`，于是"谁在服务"这句话不再可能是假的。

**② 分镜出片/重跑 → 轨迹节点**

捕获点选在 `story-orchestrator.mjs` 的 `appendRun`——**每一次出片、每一次重跑都从这儿过**。
记 `kind=story-video/story-image`、任务（场景 · 第 N 段 · beatId）、耗时、成败、结果摘要。

为什么这条重要：当场修那条是**单线**的（试一次→验证），而分镜重跑是元枢里**真正的多路探索**——
同一段反复重跑、一次派多条、带 `parentRunId` 的天然分支。有了它，"该重跑几次、先跑哪条"
将来才能在既有轨迹上离线回放比较。

**核对**：3 条新单测（残留自称被降级但保留证据、没人自称时如实说、空输入不炸）；
真机冒烟——`appendRun` 落出一条 `story-video` 轨迹，节点
`{cost:5, outcome:"ok", input:"测试场 · 第 3 段 · beat-3"}`。

测试：**1476 → 1479 全绿**。

## [2.78.0] - 2026-09-18

### 新增：探索策略闭环——"失败后再试几次"自己会调

你说"赶紧做，别做一步问一步"。我判断上一版留了个**假动作风险**：轨迹和回放器建好了，
但**没有消费者**——回放出来的"更省成本的策略"改了也没用，因为运行时没有哪个开关听它的。
这种"上线了但没人用"比不做更糟：它让人以为进化在发生。

所以这一版把闭环合上：

- **`engine/explore-policy.mjs`**：把"该怎么探索"做成**真被读的配置**（工作区
  `记忆/做梦/现役探索策略.json`，代码里永远是出厂默认，退回只改存档）。
- **真实消费者**：当场修失败后**要不要再试、再试几次**由旋钮决定（默认再试 1 次）。
  每次尝试**单独落一个轨迹节点**——于是"试几次"这件事第一次有了可回放的数据。
- **回放器**：`replayExploreAcross` 在轨迹上比较多套重试策略，规矩与技能那版一致：
  现役在候选里，**只有"每条轨迹都不更差、且至少一条更省成本"才算赢**，赢家按省得最多排。
- **做梦周期第二类**：技能权重跑完接着跑探索策略；有赢家就走授权状——
  这类满足 A/B 级条件（可回放、可回滚、不碰红线），所以**自动上线**；
  撤销也认它（`explore:*` 前缀与 `weights:*` 分开处理）。

**核对**：6 条单测，其中两条是关键的真实行为——
① 旋钮说再试 2 次时，当场修**真的被调用 3 次**，轨迹里**3 个节点**（failed/failed/done）；
② 旋钮设 0 时只调用 1 次、轨迹 1 个节点（省成本的那一侧也真生效）。

**写测试时纠正了一个自己的误解**：我本以为"多试"一定更贵，其实因为**一成功就停**，
过冲只在"怎么试都不成功"的轨迹上才真花钱。回放器算得对，是我一开始的预期错了。

测试：**1470 → 1476 全绿**。

## [2.77.0] - 2026-09-18

### 新增：运行时实例登记——"有几份在跑、谁在服务"变成可查事实

上一版我留了个没做完的核对：做梦定时器的启动日志我没看到，一度得出"定时器没挂上"的**错误结论**。
根因不是代码错，是**这台机器上同时跑着多份 server.mjs**，而从外面看不出谁在服务——
我抓到的日志属于那个在"端口占用，等待释放"里空转的实例。

- `engine/runtime-registry.mjs`：每个实例在 `记忆/运行时/实例-<pid>.json` 写心跳（30 秒一摸），
  持有端口的那个额外标 `ownsPort`；超过 2 分钟没心跳就判死并顺手清掉登记文件。
- **`/api/system/instances`**：现在有几份存活、各自版本/端口/启动时间、谁持有 8787、最近几次启动。
- **端口被占用时的自检**：先问一句"谁在服务"——
  同版本实例在服务 → 判定为**重复实例并干净退出**（不再空转 30×2 秒，也不再出现"谁在服务"说不清）；
  版本不同 → 日志说清是谁在服务，但**绝不替用户杀进程**（那是红线），重试逻辑保持原样。
  （要强制并跑：`YUANSHU_ALLOW_DUPLICATE=1`。）
- **机器可读的启动事实**：`记忆/运行时/启动日志.jsonl`（版本、是否重复、是否在监听、**做梦定时器有没挂上**）。
  这条是专门治我今天的病：重定向日志是 GBK 字节，`Select-String` 按 UTF-8 读中文永远匹配不上，
  于是"没看到日志"被我误读成"功能没生效"。以后核验读 jsonl，不用再赌编码。
- 版本号唯一来源仍是仓库根 `version.json`（顺手把 `APP_VERSION` 定清楚，与系统页同源）。

**核对**：登记读写、过期清理、"同版本判重复/异版本只说清"、启动事实回读、写失败不抛——5 条单测；
真机冒烟：写入心跳→`liveInstances` 读回 `999999@2.76.0`→清理，全部通过。

> 诚实说：**"同版本重复就退出"这条策略我还没在真机上跑到过**（需要恰好两份同版本同时起）。
> 它是按今天观察到的行为设计并单测过的，但真实触发要等你哪次重启时才会看到。

测试：**1465 → 1470 全绿**。

## [2.76.0] - 2026-09-18

### 调整：让它自己跑——补上喂数据的三个记录点 + 做梦周期

你说"你觉得合适的就先做"。我把上一版留下的**真问题**排在第一位：做梦、授权状、轨迹三套机制
全建好了、测试也全绿，**但都在饿着**——生产上只有 1~2 条历史 episode。机制不缺，缺的是
"每次真发生过的事都被记下来"。所以这一版不做新机制，只做**喂料**：

1. **技能选择在线记录**（`recordSkillChoice`）：模型每次真的 `activate_skill` 时立刻落一条
   episode（输入=该会话最后一条用户消息，真值=这次用了哪个技能）。
   以前只能靠"扫会话文件回填"，等于等我想起来才记；现在是发生即记录。
2. **派活记轨迹**（`recordDelegation`）：`delegate_task` / `delegate_fork` 各记一条轨迹节点
   （任务、耗时秒数、成败、结果摘要长度当分数）。这是元枢里**真正的多路探索**——
   当场修那条是单线的，派活才分叉。形状与当场修一致，回放器不用改一行。
3. **做梦周期**：`runDreamCycle()` 抽出来给路由与定时器共用；**启动 2 分钟后首跑，之后每 6 小时一次**，
   有赢家就自动走授权状（A/B 级自决上线、C 级转人话提案）。
   —— 你上次那句"全让我回答，我不是给她打工了吗"，答案不能只是"多一个接口"，得是**它自己会跑**。

**核对**：`runDelegation` 那两个新接线直接跑真实代码路径验过（脚本落文件，踩过一次内联引号）；
定时器挂载日志在启动时打出 `[dream] 做梦周期已挂上（启动 2 分钟后首跑，之后每 6 小时）`。

**诚实说明**：这一版没有"结果"可报——喂料的效果要等真实使用积累几周才看得出。
我能保证的是：**它不会再因为我没点接口而永远拿不到数据**。另外做梦周期目前只跑
"技能匹配权重"这一类，轨迹回放（探索策略）还没有运行时消费者，所以暂时只做离线接口，
不自动上线（避免"上线了但没人用"的假动作）。

## [2.75.0] - 2026-09-18

### 新增：探索轨迹——把"试了哪些路"记成可回放的树

上一版做梦只能回放"选哪个技能"，因为**探索过程没有结构化轨迹**：文本日志能读，不能回放。
Dream-RSI 能做梦的前提正是历史里有**发现树**（每个节点 = 一次尝试，带着真实结果与成本）。
这一版把树记起来。

- `engine/trace.mjs`：`openTrace / addNode / closeTrace / loadTrace / listTraces`，
  节点形状 `{id,parent,action,input,cost,outcome,score,at}`；落在工作区 `记忆/做梦/轨迹/`（不进代码库）。
- **回放器**：`replayTrace(trace, policy)` 在**已记录的结果**上重走 ——
  策略 = `{order: recorded|dfs|bfs, maxAttempts, stopAfterFailures}`，
  算出"如果当时最多试 k 次就停 / 换个展开顺序，会花多少成本、拿到什么最好分"。
- **跨树做梦**：`replayAcrossTraces` —— 与技能那版同一套规矩：现役（当时真这么做的那条）在候选里，
  **只有"每条轨迹都不更差、且至少一条更省成本"才算赢**；赢家按**省得最多**排，
  而不是按候选表的书写顺序（否则候选顺序就成了事实上的决策者）。
- **真实接线**：`runOnTheSpotFix`（当场修）每条问题都落一条轨迹：动作、耗时（秒）、结果、分数。
  成功与失败都记（只记成功的树会失真）。以后接子智能体/分镜重跑时用同一个 `addNode`，形状不变。
- 接口：`GET /api/trace/status`、`POST /api/trace/replay`。

**真机核对（跑的是一段真实的当场修）**

```
当场修："把 abort 用例的 bash 路径加引号" → done
轨迹：fix-attempt-… 节点 1 个：{action:"执行轮", cost:0, outcome:"done", score:1}，收尾 score=1
失败那条也记：outcome=blocked，score=0  ← 只记成功就失真了
```

**边界（写清楚，免得以后自欺）**

1. 回放**只算历史真走过的节点**：没探索过的分支这里不算、也不猜——那正是做梦的边界。
2. 轨迹目前只接在**当场修**这一条链上（它是元枢里形状最干净的"尝试→验证"）。
   子智能体、分镜重跑是同一形状的下一个接入点，`addNode` 不用改。
3. 现在轨迹还很少（刚接上），所以跨树做梦暂时得不出结论——和技能那版一样，
   **没证据时它拒绝改行为**。

测试：**1457 → 1464 全绿**（7 条：轨迹存取、成功判定、早停省成本/丢成绩的如实回放、
只算已走过的节点、跨树做梦的"不更差"规矩与赢家排序、无轨迹时拒绝、落盘位置）。

## [2.74.0] - 2026-09-18

### 新增：授权状——该她定的她定，该你定的说人话

你这两句我记下了：**"不能所有的进化策略都让我批，好多我也看不懂"** 和
**"全让我回答，我不是给她打工了吗"**。前一句是效率问题，后一句是**决策质量问题**——
让你批权重表，等于把决定权交给信息最少的一方。

**判据（可机器核查，不是感觉）**

- **A 级 · 自决自动上线**：四条同时满足 —— 可回放（确定性函数，能拿历史对比）、
  **可证不更差**（每条历史都不更差、且至少一条更好）、可回滚（留撤销点）、不碰红线。
- **B 级 · 自决但标注**：可回滚、不碰红线、可回放，但**证据薄弱**（样本 < 5 条或只是打平）
  → 小步上线 + 显著标注"证据薄弱、一键可退"。
- **C 级 · 必须你拍板**：碰红线（权限/凭据/花钱/删除/发布推送/对外）、不可回放（改的是行为语义）、
  改技能正文、影响外部世界、以及**口味**（技术上分不出对错的那种）。

**三条根**：

1. **改授权状本身永远是 C 级**。她可以提案放宽自己的权限，但不能自己批准——没有这条，
   前面所有判据都是装饰。
2. **配额**：自决一天最多 3 次（可被授权状改），防"自进化"变成噪声。
3. **C 级只给人话版**：不说权重表，只说"要改什么 / 有多大把握 / 风险是什么"，
   并且允许**只回答"行"或"不行"**。撤销也是一句话的事（`POST /api/autonomy/revoke`），
   撤销掉的不再占当天配额。

**让自决真的生效（否则是空转）**：现役权重复存在工作区 `记忆/做梦/现役策略.json`，
运行时匹配器和下一轮做梦的**基线都读它**——代码里的 `MATCH_WEIGHTS` 永远是出厂默认，
所以"退回"就是改回存档，源码不会被改脏。

**接口**：`GET /api/autonomy`（授权状 + 今天用了几次 + 现役权重 + 最近自决记录）、
`POST /api/autonomy/revoke`。做梦跑出赢家后自动走 `grant()`：A/B 级当场生效并写账，
C 级转成人话提案进承诺账。

**诚实的说明**：这套判据目前**只覆盖"技能匹配权重"这一类**（元枢里唯一已被回放验证过的技术参数）。
它的价值不在覆盖广，而在**把规矩先立住**：以后每加一类可回放的技术参数，直接就落进 A/B 级自决，
不用再问你。另外，`每日自决 ≤3` 和 `A 级要 ≥5 条历史` 这两个数字是我的初始判断，
你可以直接在 `记忆/授权状.json` 里改（改它就是一次 C 级动作，但你是人，可以直接改）。

测试：**1449 → 1457 全绿**（8 条：定级判据、防自扩权、A 级真生效+配额、C 级不先斩后奏、
撤销与配额回归、授权状可被人改、人话版）。

## [2.73.0] - 2026-09-18

### 新增：做梦（拿历史当模拟器，离线评估"要不要改"）

你说 openclaw / hermes 都有做梦、这是重要方向。做了第一版，**只做那件被证明过的事**。

**借的是 Dream-RSI 的那条洞察**（Google/DeepMind/UMD/UVA）：一次真实探索留下的记录，
本身就是那一片搜索空间的**精确模拟器**——换个策略在旧树上重走，每个节点的结果早就存在盘上了，
所以回放**零真实执行**。它的边界也一样清楚：**只有历史真走到过的地方才能做梦**，
而且只有当"策略"是**确定性决策函数**（给定已有结果 → 选下一步）时，回放才精确。

**元枢里第一片符合这个形状的搜索空间：技能匹配器的权重表**
（`matchSkillsForTask` 输入=当时的任务句，真值=当时真的 `activate_skill` 了哪个技能——
两者都躺在会话文件里）。为此把权重抽成了 `MATCH_WEIGHTS` 参数，**默认值一字未改**（老测试即护栏）。

- `engine/dream.mjs`：episode 存取（同一任务句合并、真值取并集）、`replayPolicy`（部分分：第一名命中 1、
  进前三 0.5）、`dream()`（派发 + 判定）、`writeDreamLog`、`skillEpisodesFromSessions`（从会话回填）。
- **规矩①：候选里强制包含现役策略 → 赢家不可能更差**（可证，不是感觉）。
- **规矩②：必须"每一条历史都不更差、且至少一条更好"才算赢**。平均分更高但某几条倒退 → `reject`。
  测试里专门放了一个"平均分一样高、但每条都从 1 掉到 0.5"的候选，它必须被拒。
- **规矩③：做梦只产提案，不自动改行为**。改匹配权重 = 改产品行为 → 按元枢自己的红线走人工审批：
  提案写进承诺账（`sessionId: dream`，kind=ask），等你在台前拍板。
- 接口：`GET /api/dream/status`、`POST /api/dream/collect`（回填）、`POST /api/dream/run`。

**真机核对（跑的就是历史）**

```
回填：扫 205 个会话文件 → 找到 2 处真实 activate_skill → 合并成 1 条 episode
     （"现在可以好好画了吗" → image-generation + gpt-image-2，真值是并集）
回放：现役 matcher-current 准确率 0；4 个候选权重全部 tie
结果：赢家（无）→ 提案（无）→ 保持不变
```

**老实说的三条边界**：

1. **现在数据太少**（全部历史里只有 1~2 次技能激活），所以一次提案都没产生——机器**正确地拒绝了**
   在没有证据时改行为，这正是它该有的样子。
2. 这里**不假装能模拟大模型**：换提示词的效果不可精确回放，那只能真跑 A/B。回放只用于
   "能由历史结果决定的决策"（权重表、阈值、排序规则这类）。
3. Dream-RSI 还有一半我们没抄：**用日志优化"该怎么探索"**（元枢现在只回放"选哪个技能"）。
   要走到那一步，得先让探索过程留下结构化轨迹（分支/成本/结果），现在的时间引擎历史还不够结构化。

顺带修的两处：同一任务句的真值改为一组（否则"当时连开了两个技能"会把正确答案判错）；
权重抽参后 `MATCH_WEIGHTS` 的默认值与今天在用的完全一致（老断言没动）。

测试：**1442 → 1449 全绿**。

## [2.72.0] - 2026-09-18

### 修复：跨进程文件锁会丢一次更新——真凶是"接管分支无条件删锁"

上一版我记了一笔"5 个子进程都成功、计数器却少 1"，并猜是 Windows 短名/`\\?\` 归一化不一致。
**猜错了**。这次把现场抓下来，机制是另一回事。

**怎么抓到的**：那条用例只会给一句 "4 !== 5"，什么也说明不了。所以先**给测试装现场记录**——
每个子进程把自己的锁文件路径、"读到几→写到几"、窗口起止、以及走没走接管追加进 trace，
断言失败时一并打出。重跑十几轮后抓到：

```
锁文件 1 种；写入序列 48076:读0→写1 40844:读1→写2 21388:读1→写2 2916:读2→写3 16264:读3→写4；
窗口重叠 pid 40844(读1) × pid 21388(读1)；
结束 … 40844:ok=true/接管1  21388:ok=true/接管1 …
```

**一把锁、两个进程都记了一次"接管"、都读到 1** —— 问题出在接管分支：

```
A 释放 → 锁文件消失；B 恰好在判定陈旧（statSync 抛 ENOENT → 判成陈旧）
→ B 无条件 unlinkSync(lockFile)；而这期间 C 已经抢到了新锁
→ B 把 C 刚创建的锁删了 → B 再抢就成功 → B 与 C 同时进临界区 → 丢一次更新。
```

**修法**（`stealAction()` 单独抽出来，因为它就是真凶）：

1. `statSync` 说文件不存在 → **什么都别删**，直接回去重抢（O_EXCL 会决出唯一赢家）；
2. 确实有一把陈旧锁 → 删之前**再确认它还是刚才判定的那一把**（token 一致），
   不一致说明已被别人换成新锁，碰不得。

顺带两处加固（同一次事故里发现的弱点，保留）：持有期间**心跳摸 mtime**（慢 ≠ 死，别把慢持有者
当死的偷走）、释放按 **token** 认领（只删自己那一把，不误删别人的）。

**核对**

- 判定表单测：没有锁可删 / token 不一致 → 一律 `skip-unlink`；token 一致 → 才 `unlink`。
- 不变量单测：持有者还活着时只能等到超时，**超时退出也不许删别人的锁**。
- 血压测试：同样"两份全量测试当背景负载"的条件下，修之前 10~15 轮内必现，
  修之后**连跑 20 轮 0 失败**。

> 诚实边界：这是"机制已定位 + 定向修复 + 负载下不再复现"，不是"我把那个交错 100% 复现出来了"。
> 真要是再犯，trace 会直接告诉我们走的是哪条接管路径（现在连理由和 token 都记下来了）。

## [2.71.0] - 2026-09-18

### 调整：系统页把"版本"讲清楚（"这个版本描述清楚点"）

原来那一格长这样：大字 `v2.70.0`，下面**裸放**一个 `v25.8.2`——读者根本分不清哪个是元枢、
哪个是别的什么。现在：

- **版本**：大字 `元枢 v2.70.0`，下面逐条标出「运行时：Node v25.8.2」「平台：Windows_NT x64」。
  （第一版把"应用"也列了一行，等于把大字重复一遍，真机截图看到后删掉了。）
- **服务状态**：补「监听端口 8787」「数据目录 D:\pi-workspace」——在线之外，"在哪儿"也一眼可见。
- **网络状态**：补「主入口 pi.myxinyu.xin」（优先取带"主入口/工作台"描述的域名，其次第一条域名，
  最后回落到局域网 IP + 端口）。
- `StatusTile` 增加 `facts`（键值列表）能力，单行 `detail` 用法保持不变——
  要讲清"哪一项是什么"时不再靠拼接字符串。

真机核对（CDP 读 DOM + 截图）：四格分别是
`运行中／元枢 v2.70.0（运行时 Node v25.8.2、平台 Windows_NT x64）／0 分钟／已发现入口（主入口 pi.myxinyu.xin）`，
没有重复行，窄屏两列也不挤。

测试：1436 全绿（系统页那两条已有的结构断言不变）。

> **顺带记一笔（不属于本次改动，但值得跟）**：全量跑时 `file-lock.test.mjs` 的跨进程互斥用例
> 失败过一次——5 个子进程**都退出码 0**，但计数器只有 4（说明互斥真的被破了一次，不是"慢"）。
> 单独连跑 3 次都是 22/22 通过，所以它只在**机器加载很高**（当时同时跑着服务、CDP 浏览器和别的作业）
> 时暴露。我的一个**未证实的**猜测：`canonicalFilePath()` 靠 `realpathSync` 归一化，如果两个进程
> 拿到的规范化形式不一致（Windows 的 8.3 短名 / `\\?\` 前缀差异），哈希就会落到**两把不同的锁**上。
> 留作独立一项去查，不在这次改动里赌着修。

## [2.70.0] - 2026-09-18

### 修复：GBK 编码与 cmd 引号转义（"怎么治"）——治在代码里，不治在记性里

这两类坑的共同点是**不在代码里，而在你和 shell 之间**，所以每次都要重新踩。分三层治：

**① 运行时层（新增 `engine/windows-shell.mjs`）**

- `withUtf8CodePage()`：cmd 分支进命令前先 `chcp 65001>nul & `——不只是"输出更好看"，
  关键是 `echo 中文 > 文件` 这类**重定向**也会跟着走 UTF-8（否则写出来是 GBK 字节，回头一读全是乱码）。
  bash 分支不加（本来就是 UTF-8）。
- `decodeWindowsOutput()`：UTF-8 优先，出现替换字符再用 **GBK 兜底**（`TextDecoder('gbk')`，Node 自带）。
  两边都不认时返回 utf8 结果，不丢内容。
- `looksMojibake()`：只认铁证（U+FFFD、私用区）。**刻意不猜"错码汉字"**——`鍏冩灑` 本身就是合法汉字，
  猜错会给正常输出贴标签；真正的治法是前面的解码，不是事后识别。
- `riskyForCmdShell()`：失败时点明是哪一类问题（内联代码 / 带空格路径没引号），省掉几轮瞎试。

**② 工具层（`engine/tools/unified-tools.mjs`）**

- stdout/stderr 统一走共享解码；`cmd /c` 真正执行的是切过代码页的那条命令。
- 工具描述里直接写清三条规矩：中文用 `write` 工具写、内联代码会落成脚本文件、带空格路径必须加引号。

**③ 纪律层（新增技能 `windows-shell-playbook`）**

把"动手前固定三问"和速查表落成技能：别用 `Get-Content/Set-Content` 处理 UTF-8（真机上毁过 17 个前端
组件文件）、批量替换写 Node 脚本、内联代码先落文件、看到"不是内部或外部命令"先怀疑拆词/编码。

**真机核对（不是"看着对"）**

```
GBK 输出原始字节: d4 aa ca e0 a3 ba bb e1 bb b0
  直接 utf8 解      → Ԫ�ࣺ�ỰĿ¼      ← 这就是平时看到的乱码
  decodeWindowsOutput → 元枢：会话目录   ← 修好了
  chcp 前缀后执行   → 元枢：会话目录
```

测试：**1430 → 1436 全绿**（新增 6 条：UTF-8/GBK 双向解码、乱码判定不误伤、cmd 前缀只加给 cmd、
风险命令识别、工具接线、技能契约）。

## [2.69.0] - 2026-09-18

### 新增：任务中当场修（fix_problem）——把"发现问题"接到"动手做掉"

上一版接的是**事后**那条链（定时复盘到点才跑）。这一版搬到**任务过程中**：agent 干活时发现
当场能修的小毛病，不必等到当天复盘——调一次 `fix_problem` 就派一个执行轮真去修，
把证据（命令 + 输出 + 改动文件）拿回自己手里，接着据实汇报。

- `engine/reflection-exec.mjs` 新增 `runOnTheSpotFix()` 与 `takeOnTheSpotBudget()`：
  与复盘那条**共用同一套规则**——红线不碰（密钥/权限/部署/发布/推送/删数据/装依赖/花钱）、
  必须交证据、做不了就说做不了；另加**预算**：同一会话 10 分钟最多 3 次，
  免得"顺手修"把用户的一轮对话拖成一串自动执行。
- 两个引擎都接上：统一引擎注册 `fix_problem` 工具（执行器走同一个核心）；Pi 会话注册同名
  customTool（执行器由 server 注入 `setOnTheSpotFixRunner`，没注入就 fail-closed 不注册）。
  协议与 Pi 会话各自的提示词里都写清了「别只在结论里列一条建议修复」，Pi 侧每会话注入一次。

**真机核对（如实说，含没验证成的部分）**

- 统一引擎侧：`GET /api/engine/tools` 里 `fix_problem` 在（共 17 个工具），定义与描述正确。
- Pi 侧：会话文件里能看到每会话注入的那句提示词（含 `fix_problem`）。
- **行为没有演示成功**：两次真机都用同一个"越界 bug 探针"试它会不会调这个工具——
  第一次它自己用 `edit` 把 bug 修好了（结果是好的，但**没走这个工具**）；
  第二次它一个工具都没调、文件没变。所以诚实结论是：
  **能力与边界建好了并测过，模型会不会主动用它还没被证明。**

### 修复：我自己又踩了一次"先用后声明"（这次是引擎侧）

`UNIFIED_TOOLS` 里引用了 `FIX_PROBLEM_TOOL`，而后者声明在下面 → 模块求值期
`ReferenceError: Cannot access 'FIX_PROBLEM_TOOL' before initialization`，**进程起不来**。

前端那条 tsc 硬线覆盖不到 `server.mjs` / `engine/*.mjs`（纯 JS，没有类型检查），
`node --check` 只查语法。所以新增 `tests/unit/module-scope-order.test.mjs`：
扫顶层**多行数组/对象字面量**，里面的标识符若对应更靠后声明的顶层常量就是红灯——
判据刻意收窄成这一种形态（函数体不算，避免误报）。先让它抓到上面这个 bug（红），再修好顺序（绿）。

测试：**1424 → 1430 全绿**。

## [2.68.0] - 2026-09-18

### 新增：复盘 → 执行（"抓了一堆问题，但没有主动执行"）

你这句话点到的是**结构性**的：定时复盘那一轮只拿到只读工具（`timeTaskReadTools` 只留
read/web_search/search_files），它能做的唯一一件事就是在结尾写一份行动清单；清单进承诺账之后，
"结清"又只等人给结论。闭环断在最后一米——问题抓了一堆，动作一条没做。

**补上那一米，但不拆掉人那道闸**

- 复盘的行动要自报 `kind`：`fix`（当场能修的）/ `track`（要跨天跟踪）/ `ask`（要人拍板）。
  复盘提示词里写清了三种怎么填、写错的代价（把该问人的写成 fix 会被执行轮挡回 blocked；
  把能当场做的写成 track 就永远是清单上的一条）。
- 新增 `engine/reflection-exec.mjs`：分类 → 派发 → 执行轮提示词 → 结果契约 → 回账。
  - 只有 `fix` 且**不碰红线**（密钥/凭据/权限/部署/发布/推送/删数据/装依赖/花钱，中英文都拦）才自动执行；
    红线命中的一律转 `ask`——**红线先判**，否则「删除旧数据」这种七个字会被"描述太短"盖过去。
  - 每轮**最多 3 条**，一次只做一条，执行轮必须交证据（命令 + 输出摘要 + 改动文件）。
  - 结果只认 `done|blocked|failed`：`done + 证据`才结清（承诺账头部写明的"由人**或证据**结清"）；
    `blocked/failed` 留在账上并把这次尝试记进 `attempts`，下一轮复盘带着"上次试过什么"追问。
- `server.mjs` 定时任务那条链：复盘 → 落账 → 派执行轮（这一步才发可写工具）→ 回账 →
  把「自动执行」小节写进时间引擎日志。**这一段的异常不再静默**：任何一步失败都会打印出来，
  否则"复盘说要做、实际没做"会以"什么都没发生"的形式藏起来——正是这个项目在治的病。

**真机核对（注册一个 2099 年的一次性任务手动触发，跑完就删）**

```
复盘产出：3 条 fix / ask / track
派发：只有那条干净的 fix 进执行；红线那条 deferred（reason=碰红线（…），转人工）
执行轮：bash: echo ok > /d/pi-web/tmp/exec-proof-….txt；ls -l 3 字节；cat 输出 ok
回账：[kept] kind=fix attempts=1，证据就是上面那串；prompt 里那条 ask 仍 pending
日志：时间引擎日志.md 里出现「自动执行（本自动执行 1 条：完成 1）」
```

核对用的临时任务、产物、以及写进承诺账的 6 条测试条目**都已清理**（按 taskId 精确删，
真正的 13 条承诺没动）。

> 一个诚实的说明：第一次真机验证时账本更新成功、但日志小节没出现，而当时的 `catch {}` 把原因
> 吞了——现在改成打印，重跑即完整落痕。所以这次的"修好"是**可观测性带来的**，不是我猜出了原因。

测试：**1416 → 1424 全绿**（新增 classification/派发上限/提示词契约/结果解析/回账与结清/端到端闭环）。

## [2.67.0] - 2026-09-17

### 调整：类型检查接进构建流水线（"接"）

上一版把 `tsc --noEmit` 接进了测试线，但**发版走的不是测试线**——`npm run build:frontend` 仍然是
裸的 `vite build`。于是本地测试绿了、线上产物里照样可能是白屏（连续创作那次就是这么出去的）。

现在：

- `frontend/package.json`：新增独立脚本 `typecheck`（`tsc --noEmit -p tsconfig.json`），
  `build` 变成 `npm run typecheck && vite build`。
- 根目录 `build:frontend` 仍然只走 `npm --prefix frontend run build`，**没有任何旁路**；
  `build:mobile:web` 也因此自动带上。
- 新增一条测试盯住这条链（typecheck 脚本在、build 先跑它、根脚本不许绕过）——
  否则哪天有人图快把它改回裸 `vite build`，白屏 bug 又会静默回到产物里。

**红绿对照（真造一个错验过）**

- 故意写一个"先用后声明"（`export const probeA = probeB; const probeB = 1`）→
  `npm run build:frontend` **6.9s 失败退出（码 2）**，报错正是
  `error TS2448: Block-scoped variable 'probeB' used before its declaration`（与那次白屏同一类）。
- 删掉探针 → 构建恢复（9.9s，其中类型检查约 7s；原来裸 vite build 约 2.5s）。
  构建变慢约 7 秒，换掉一整类"整页白屏"的可能，值。

测试：**1415 → 1416 全绿**。

### 修复：abort 孤儿用例偶发红（它自己有两个毛病，跟产品无关）

全量跑时那条"abort 要连孙子进程一起杀"偶发报 `Missing expected rejection`。查下来是**用例本身**：

1. cmd 支原来 ping 的是 `127.0.0.99`——本机会立刻回 "Destination host unreachable"，命令 400ms 内
   就跑完了，abort 还没发就 resolve。改成 ping **回环地址**（30 次 ≈ 30s，稳定长跑），
   存活判定也从"命令行标记"改成"`PING.EXE` 数量差"（cmd 下不加引号、也不需要标记）。
2. bash 支把 `process.execPath` 直接裸写进命令：`C:\Program Files\nodejs\node.exe` 被 bash 拆成
   `C:\Program` + `Files\...`，360ms 以 **127 command not found** 退出——同样是"abort 之前就结束"。
   加上引号即可（这条更隐蔽：stderr 里只有一行 `C:/Program: command not found`）。

两处都把踩点写进了用例注释；连跑 4 次稳定绿，孤儿用例耗时 ~2.5s。

## [2.66.0] - 2026-09-17

### 修复：连续创作整页白屏 —— `Cannot access 'nt' before initialization`（我上一版埋的）

真机报错："连续创作 页面暂时无法显示 / 错误详情 Cannot access 'nt' before initialization"。
`nt` 是压缩后的变量名，翻回源码是：

```js
const effectiveCardId = project?.colorCardId || globalCardId || ''   // ← 这里读 globalCardId
const saveBible = ...
const [globalCardId, setGlobalCardId] = useState('')                  // ← 它却声明在下面
```

**这是我 v2.57.0（配色卡进创作流）埋的**：派生常量写在了 state 声明之前，渲染时读到还没初始化的绑定
（TDZ），整个连续创作页直接白屏。已把 state 与派生常量的顺序摆正；真机复验：控制台零异常、
分镜时间线正常渲染。

**为什么没被拦住 → 补一条硬线**

vite build 不做类型检查，eslint 也没接，这类"先用后声明"只有 tsc 拦得住（TS2448/TS2454），
而它一出就是整页白屏。所以：

- 新增 `tests/unit/frontend-typecheck.test.mjs`：跑 `tsc --noEmit`，有错就红（没装 typescript 时跳过）。
  **这条线第一次跑就报出 8 个错**，其中就有这次的 TDZ。
- 新增 `frontend/src/theme/colorcards.d.mts`：给配色卡模块补类型合同（前端只在这里消费这些 `.mjs`，
  没有声明文件时 TS 把它们当 unknown，`Object.values(CARD_FAMILIES).map(f => f.id)` 会报错）。
- `frontend-story-workbench.test.mjs` 里钉死这两个声明的**先后顺序**，防止再写回去。

### 调整：合并两个重叠的技能（"那你合并"）

- `daily-retrospective` ← 并入 `daily-self-retrospective`：保留六段结构（全景表 / 逐件归因 /
  **会话盘点 vs 记忆留痕落差** / 做得好的 / 待办 / 一句话）+ 两边全部可执行条目
  （排他性归因、口诀趁热沉淀、留痕不分技术含量、红线主动自证、用户约定当分钟落档）。
- `verify-before-delivery` ← 并入 `evidence-first-delivery`：四条验证清单（存在性 / 可达性 /
  内容真实性 / 完整落句）+ 三条铁律（宣称=证据、空输出≠成功、顺序不能反）+ 动手前 4 步检查。
- 删掉被并入的两个目录；匹配器里"纪律族"的正则同步收窄到存活的名字。
  技能总数 23 → **21**，注入目录仍在 4000 字以内。

测试：**1413 → 1415 全绿**。

## [2.65.0] - 2026-09-17

### 新增：agent 自己沉淀的 6 个技能进仓库 + 让匹配器认得出它们

你审批了那 6 个技能（`daily-retrospective`、`daily-self-retrospective`、
`delivery-session-closeout-review`、`evidence-first-delivery`、`misleading-error-debugging`、
`verify-before-delivery`），已经收进仓库（`c19a391a`）。

我当时只补了它们 `description` 的触发语（原稿没写"什么时候用"，踩了仓库自己的技能契约）。这次顺手把
**匹配器**也补齐——因为发现它们虽然进了目录（23 个技能、注入 2308 字，仍在上限 4000 内），
但 `matchSkillsForTask` 对它们**零命中**：

- 它们的名字是英文 slug、描述是整句中文，而匹配器是按名字/描述分词匹配的——「今天复盘一下」
  「先验证再交付」这种最典型的说法一条都命不中（抽掉"什么时候该用"最明确的一类技能，最可惜）。
- 按仓库里已有的做法（"纯概念名"那两族就是这么补的）加了四条窄规则：复盘 / 交付前验证 / 排障 /
  收尾沉淀，各自要求"任务句 + 技能族"两边都命中。
- 同时给这族技能**关掉"媒体域"那几条加分**：否则「做个视频」会因为它们的描述里出现"视频"
  被一起带上（实测确实带上了 `verify-before-delivery`，纯噪声——纪律技能关心的是"怎么做"，
  不是"做什么"）。

真机（离线跑真实索引）：

```
「今天复盘一下」          → daily-retrospective、daily-self-retrospective
「服务起不来帮我排障」    → misleading-error-debugging
「这个会话收尾沉淀一下」  → delivery-session-closeout-review 等
「做个视频」              → aigc-video-production、seedance-25、shortform-genesis（不再沾纪律技能）
「做个视频，交付前先验证」→ aigc-video-production、verify-before-delivery（复合诉求两个都点出来）
「嗯」                    → 零命中
```

测试：**1412 → 1413 全绿**（新增一条：纪律类技能必须命中、纯媒体任务不许被沾上、复合诉求两个都带）。

> 留一条观察：`daily-retrospective` 与 `daily-self-retrospective` 结构重合约七成，
> `evidence-first-delivery` 与 `verify-before-delivery` 也在讲同一件事。要不要合并成两个，你说一声。

## [2.64.0] - 2026-09-17

### 调整：工坊的选择芯片也换上悬浮手感（"你做个试试"）

接着上一版的悬浮药丸，把工坊里那几排**选择芯片**（绘画/视频的构图骨架、光影、情绪、风格、运镜、平台）
也换成同一套手感：

- **只改选中的那一支**：`chip-pill is-on` = 主色底 + 主色系投影（`color-mix(accent 45%)` 偏下偏移）
  + 悬停抬 1px / 按下压回 1px 并缩到 .985。没选中的保持安静（`is-off`：透明底、淡边框）——
  每颗都浮起来会吵。
- **尺寸类一个都没动**：`px-2.5 / min-h-11 / text-[11px] / rounded-full / border` 全部保留，
  `.chip` 只管颜色、投影与动效。这一点特意写进测试——`min-h-11` 是 44px 触摸目标，
  第一版脚本差点把它们一起换掉（真机量过：换成之后触摸目标仍是 44px）。
- 20 处状态类 + 10 处外壳收口到两个类，WanXiang / VideoPrompt 里不再各自拼一遍选中态样式。

**真机核对（CDP）**

绘画工坊：选中 5 枚、未选中 39 枚；选中样例「自由」的投影是
`color(srgb .36 .51 .72 / .45) 0 8px 16px -6px`（主色系）、字色 `rgb(0,0,0)`（on-accent 算出来的）、
高度 **44px**；未选中底色透明、边框用主题色。点一枚未选中的（「证件裁切」）→ 立刻变成选中态并带上投影。
截图确认那几排芯片不再是一排死板的描边。

测试：**1411 → 1412 全绿**（新增：芯片选中/未选中的两支样式、投影掺主色、尺寸类不许被吃掉）。

## [2.63.0] - 2026-09-17

### 新增：悬浮药丸（照着你给的 "3colors combination" 参考图做的）

"这种悬浮式按钮可以做吗"——可以，而且它和配色卡是天生一对，所以直接落在了主题页的创作配色卡上。

**这种"浮起来"的关键不是圆角，是投影**

参考图里那几颗胶囊看着浮在底上，靠的是**投影带着自己的颜色**（同色系、低透明度、偏下偏移），
再叠一层内侧高光。灰色投影只会像贴了一道边。所以：

- `engine/color-cards.mjs` 加了 `colorCardOn(card)`：药丸的字色按对比度在白/黑里挑
  ——写死白字的话，米白/浅青那几张直接看不清（18 张卡逐个测过，全部 ≥4.5）。
- `styles.css` 新增 `.float-pill`：底/字/投影色全走变量（`--float-bg/fg/tint`），
  悬停抬起 2px、按下压回 1px 并缩到 0.99，左侧一枚同色小圆点（参考图里每颗胶囊都有）。
- 主题页的创作配色卡改成**一列悬浮药丸**（名称 + 色值），跟参考图的排布一致；
  壁纸那张卡仍用网格（它要预览整条渐变）。
- 主按钮（`btn-primary`）也换上同一套观感：投影是主色系的，不再是一道灰边。

**真机核对（CDP）**

主题页 9 枚药丸；第一枚「蓝沙米 · #5D9AB4」，底色 `rgb(93,154,180)`、字色 `rgb(0,0,0)`（按对比度算出来的）、
投影是 `color(srgb 0.36 0.60 0.71 / .42) 0 10px 20px -6px`（自己的颜色）；
真实鼠标事件量到的 `transform`：悬停 `matrix(1,0,0,1,0,-2)`、按下 `matrix(0.99,0,0,0.99,0,1)`。
截图确认那一列胶囊的浮动投影肉眼可见。

测试：**1409 → 1411 全绿**（新增：18 张卡当底时的字色对比度 + 药丸样式与接线）。

## [2.62.0] - 2026-09-17

### 调整：壳层色纱提到 11%，主题页的配色卡改成一族一网格

两句反馈都落了：**"再提一档"** + **"你那个主题色的配置卡破坏了主题界面的排版"**。

**① 色纱 6% → 11%（"再提一档"）**

要提档就得先腾出对比度额度：把左/右栏里的三级文字 `--pi-dim2` 在**作用域内**提升为二级
（`styles.css` 一条 token 别名）——导航与会话摘要本来就不该是三级信息，这一步本身也是可读性更好的方向。
腾出来之后实测上限是 11%（18 张卡 × 11 套主题逐个算）。

顺带发现右栏不能用卡的**下端色**：莫兰迪那套下端多是米白/奶油，叠在暗色主题（deep/sepia）的面板上
会把面板整体提亮，`--pi-dim` 掉到 4.0 上下，上限只有 6.5%。改成用**上端色**（和左栏同源）之后，
三个面都能吃到 11%。这条是测试逼出来的——我先把左右都写成 11% + 下端色，测试当场报了 12 条不达标。

**② 主题页排版**

原来两套卡各铺一遍（18+18 共 36 张），把右侧那一栏拉得又长又乱；提示文案里还留着一对
Markdown 星号（`**所有出图/出片**`）直接显示在界面上。现在：

- 一族一网格：`不指定 | 莫兰迪高级灰 | 高饱和渐变` 三个按钮做族切换，下面只渲染这一族的 9 张；
  壁纸那张卡共用同一个族状态。36 张 → 每次 9 张。
- 约束文案（"低饱和、色相差小…"）单独一行，不再塞进标题括号里。
- 去掉字面星号；色卡名一律 truncate，不再把网格撑歪。

**真机核对（CDP）**

- 壳层：设成「橙青撞色」后，左栏/右栏/面板头的 `background-image` 都是 11% 的橙，
  左右两条 3px 卡色渐变边命中；截图确认左右栏肉眼可见地带上了色。
- 主题页：`创作配色卡` 9 张、`配色卡壁纸` 9 张、族切换按钮 2 个、页面上没有字面 `**`；
  点「高饱和渐变」后网格整族切换成 9 张高饱和卡。核对用的全局配色已恢复成「不指定」。

测试：**1409 → 1409 全绿**（改了色纱那条绳：最弱字色按提升后的 dim 算，并把"右栏用上端色"和
"左右栏内 dim2→dim 的别名"一起钉住）。

## [2.61.0] - 2026-09-16

### 新增：全局配色卡带上界面（左栏 / 右栏 / 面板头）

你问"全局不应该左侧栏、右侧栏也带一些效果吗"——对。上一版"全局"只覆盖了出图出片，
界面还是原来的灰，那这个"全局"就只是一句宣传。

**做法：低浓度色纱 + 不压字的装饰**

- 左栏/右栏各一层同色系纱（`color-mix` 6%）、面板头 11%；主色/底色/边缘色挂成 CSS 变量，
  由 `body.has-color-card` 控制（选/取消立刻生效；水合时先用本地缓存上色，网络慢也不会闪回灰的）。
- 6% 是**实测上限**而不是拍脑袋：左/右栏底下有 `--pi-dim2` 会话摘要小字，面板头只有标题与
  `--pi-dim` 按钮。18 张卡 × 11 套主题逐个算，超过 6% 就有组合掉到 AA 线以下
  （最狠的是 mist 主题 + 深蓝加青那张，16% 时只剩 3.83）。
- 剩下那点视觉分量交给**不压字**的装饰：左栏右缘、右栏左缘各一条 3px 的卡色渐变，
  当前导航项描一圈卡色，栏的边框色也换成卡色。文字对比度一个都没动。
- 顺手把左导航未选中项的字色从 `--pi-dim2` 提到 `--pi-dim`（导航是入口，不该是三级信息）。

**真机核对（CDP）**

设成「橙青撞色」之后：`body.has-color-card` 在、`--pi-card-shell-left` 是 6% 的橙、
左栏背景图上真的叠了这条渐变、边缘条是 `linear-gradient(rgb(245,96,15), rgb(226,255,253))`、
右栏与面板头同样命中。核对完**把全局配色恢复成"不指定"**，没替你选。

测试：**1407 → 1409 全绿**（新增：色纱在 18 卡 × 11 主题下的最坏对比度、色纱开关与四条 CSS 规则的接线）。

## [2.60.0] - 2026-09-16

### 新增：第二套色卡（高饱和 9 组）+ 配色卡升为**全局**（不再只挂在连续创作项目上）

你说"还有色卡，另外感觉你那个不够全局"——两件都做了。

**① 第二套：高饱和渐变 9 组**

同一套做法：把色值标签裁出来放大逐条读，再用卡片四角像素采样交叉校验，两读一致才落盘。
`#FF768D→#E5FFFD`（粉青）、`#5E6BFF→#FFFEDA`（蓝紫奶油）、`#1899B6→#FFFCCD`（青蓝奶油）、
`#30A9FF→#FFF4E3`（天蓝米白）、`#F5600F→#E2FFFD`（橙青撞色）、`#5D62CC→#FFDDDD`（紫粉）、
`#18B670→#FFD0D0`（绿粉撞色）、`#B61877→#DDFEFF`（品红冰蓝）、`#FF3730→#D2FFD2`（红绿撞色）。

两套卡各有各的"别画成什么样"：高级灰是「低饱和、不要荧光色与高饱和撞色」，
高饱和是「明快、敢于撞色、不要灰蒙蒙」。**约束跟着卡走**——不然用高饱和那张，模型会给你画成灰的。

**② 全局：选一套，整个工作台都按它走**

此前配色卡只写 `project.colorCardId`，于是绘画工坊、视频工坊、聊天里出图、批量生成**全都吃不到**。
现在：

- 新 `engine/color-prefs.mjs` + `GET/POST /api/color-prefs`：全局一份（跨端），默认空 = 不指定。
- **出口收口**：`generateImage` / `startVideoJob` 是所有出图/出片的共同出口，在这里补一行色调，
  一次改动覆盖绘画、视频、连续创作、聊天出图。故事层已经写过配色的提示词**不重复追加**
  （同一套色写两遍只会稀释画面描述）。
- 优先级：这一次显式传的 > 段落上的 > **项目选的** > **全局选的**。项目里新增「跟随全局（X）」，
  想单独一套仍然压得过全局。
- 界面：主题页新增「创作配色卡」（两套 18 组 + 不指定，分组显示），壁纸那张卡也扩到 18 组。

**真机核对**

- 接口：存「红绿撞色」→ 读回一致；存 `../../etc/passwd` → 拒绝并返回空（脏 id 不落盘）。
- 故事层跟随全局：项目没选时，预览提示词里出现「红绿撞色」，色调槽是
  `呈现正红 #FF3730 过渡到浅绿 #D2FFD2，竖向渐变，红绿撞色、胆子大`。
- 界面（CDP）：主题页 18 个创作色卡 + 18 个壁纸色卡、分组标题在；点「粉青」→ 服务端
  `colorCardId` 变成 `vivid-rose-ice`。核对用的临时项目跑完即删；**全局选择最后恢复成"不指定"**，
  没有替你选一套。

测试：**1400 → 1407 全绿**（新增 `color-cards` 两套/别名/约束、`color-prefs` 全局往返与脏 id、
媒体出口接线断言、故事层"跟随全局/项目压全局"）。

## [2.59.0] - 2026-09-16

### 修复：壁纸把文字盖掉了；主题里那些"读不清"的地方也一并收口

你说"主题里也很多黑色背景色覆盖文字的"。没有凭印象改，先做了两件事：把 **11 套主题逐个注入
真实页面、逐元素量"文字 vs 它实际叠在哪层底上"**，再看渲染出来的像素。量出来的东西和直觉不太一样，
两处都记在下面。

**① 真凶：壁纸模式下底色被整块撤掉了**

- `body.has-wallpaper { background: transparent }` + `body.has-wallpaper .chat-welcome
  { background: transparent }`：欢迎页那一列文字**没有任何自己的底**，字直接压在壁纸上。
  壁纸深一点，字就没了（左导航、工作台首屏同理，只是它们恰好有半透明面板兜着）。
- 修法：底色这一层不再撤（`background: var(--pi-bg)`）——壁纸是**叠在底色之上**的图层，
  不是底色的替代品；同时给放字的区域补一层主题色纱（`color-mix(bg 88%, transparent)` + 模糊），
  壁纸照样透出来，字保证读得清。
- 受控对照（同一段文字、同一套量法，只切"有纱/没纱"）：**10.85 → 1.28**。

**② 顺手收口：能算出来的对比度问题，别再靠眼睛**

逐元素量完发现三类"一直在那儿、但没人报"的问题，全部改成**按对比度解算**：

| 问题 | 实测 | 处理 |
|---|---|---|
| 拟木的警告色画在枫木面板上 | 1.54（几乎看不见） | 语义色按面板底解到 ≥3.2 |
| 主色当文字画在 accent-soft 软底上（badge/状态芯片） | 3.19 | 按"accent 12% 叠在 bg1 上"的真实叠色解到 ≥4.5 |
| `--pi-muted` | 2.97~2.99 | 目标提到 3.2，并给二分法加了"最后几步"收敛 |

- 解算器原来会停在**刚好差一点**的位置（3.17 / 4.49 这种"看着达标其实没有"），现在定稿前会
  按目标方向再走几步。
- 顺带把"主色底 + 写死白字"的 27 处换成 `text-pi-on-accent`（各主题主色明度不同，白字在浅主色上
  只有 4.06）；新增 `--pi-on-green/red/yellow` 三个 token，步骤条上的对勾不再拿白字压亮绿底（1.9）。
- 主题页自己的预览气泡也用 `--pi-on-accent`（原来写死 `--pi-bg`，在牛皮纸/拟木上只有 3.0~3.85）。
- `text-pi-dim2/70` 这种"再打个七折"的小字去掉折扣。

**新增回归绳**：`tests/unit/theme-contrast.test.mjs` —— 11 套主题 × 17 组关键配对逐个算对比度，
外加"解算必须真的落在线上"和"壁纸模式下底色不许撤、放字区域必须有色纱"的结构断言。
以后再调 seed 或派生算法，掉线就红灯，不用等用户在某个主题里看不清字。

测试：**1396 → 1400 全绿**。

## [2.58.0] - 2026-09-16

### 新增：配色体检 + 提示词面板上的配色缩略图

配色卡进创作流之后的两个缺口：**没人管它有没有被执行**，以及**一组 hex 看不出画面感**。

**① 配色体检（`story-lint.mjs`）**

选了配色就该在画面上说了算，破格要有意识。新规则只抓"明显打架"：低饱和高级灰的片子里，
某一段的 `shot.tone` 写了荧光 / 霓虹 / 高饱和 / 大红大绿 / 撞色 —— 报一条 warn，
并指明第几段、撞在哪、两条出路（改这一段，或换一套配色）。

刻意**不报**"色调和配色不完全一样"：同色系微调是正常创作，全抓等于把体检变成噪声，
而体检一变成噪声就没人看了。没选配色时一个字都不报——不替用户立规矩。

**② 提示词面板上的配色缩略图（`StoryColorCardChip.tsx`）**

- 镜头规格面板与「本次生成输入」各挂一枚：一块真的渐变 + 卡名 + 两组色值。
- 顺手补上「色调」输入框（此前只有景别/机位/运镜/光线/落幅/承接，色调只能靠风格库或配色卡）：
  **留空 = 按整片配色走；写了 = 这一镜有意识破格**，缩略图上会直接把"本段破格"标出来——
  两种情况混在一起显示，会让人以为整片都改了。

**真机核对**

- 接口层：临时项目里第 2 段写了「冷调高饱和霓虹」，体检如实报
  「『巷口』第 2 段的色调写了『高饱和』，和整片配色（莫兰迪高级灰 · 蓝紫粉，低饱和）打架」。
- CDP 真浏览器：镜头规格面板里有 `input[aria-label="色调"]`，缩略图色块是
  `linear-gradient(rgb(100,83,161) 0%, rgb(253,220,228) 100%)` 且写明"留空就按整片配色"；
  点「检查生成输入」后，「本次生成输入」里也有一枚（文案换成"这一段按整片配色写『色调』"）。
  两次核对用的临时项目跑完即删。

测试：**1394 → 1396 全绿**。

## [2.57.0] - 2026-09-16

### 新增：配色卡接进创作流（选了配色，分镜和画面都按它写「色调」）

上一版把 9 组莫兰迪高级灰做成了主题页的壁纸。这一步让它真的参与创作：画风决定"长什么样"，
配色决定"是什么颜色"——散在各段的形容词里，人物锁得再准也救不回色调漂移。

**数据只剩一份**

- 配色卡搬到 `engine/color-cards.mjs`，成为**唯一数据源**；`frontend/src/theme/colorcards.mjs`
  只做转出（测试盯着它：前端模块里再出现任何一组色值就红灯）。两处各存一份必然漂移——
  主题页显示一套、提示词里写另一套，谁也不知道哪个才是用户选的。

**三处都吃这套配色**

| 位置 | 作用 |
|---|---|
| `compileShotPrompt` 的 ③色调槽 | 同一部戏每一镜同色；段落自己写了 `tone` 就听段落的（破格优先） |
| `compileStoryPrompt` 新增「## 配色方案」块 | 生成前模型先看到（放在画风之后） |
| `buildStoryboardPrompt` 加【本片配色】 | 一键分镜当场按配色写 `shot.tone`，而不是等生成时再打补丁 |

- 项目级选择 `project.colorCardId`，在 `createProject` 里**显式登记**——这是同一条铁律第五次
  踩点（默认配方/分集/改编史/方法包/深度构思都是这么漏的）。
- 认不出来返回 `null`：**绝不替用户默认一套配色**；没选就一个空块都不占，色调槽回到画风默认。

**真机核对（两处，都不是"看着对"）**

- 走真实 HTTP + 真实编排层：临时项目未选配色时预览提示词里没有「配色方案」也没有「呈现」；
  PATCH 选上蓝紫粉后，提示词里同时出现「## 配色方案（莫兰迪高级灰 · 蓝紫粉）」和色调槽的
  `呈现蓝紫 #6453A1 过渡到浅粉 #FDDCE4`；重读项目确认落盘。
- 走 CDP 在真浏览器里点：制作台「人物与连续性设定」下有 9 个色卡 + 「不指定」；
  点「蓝紫粉」→ 按钮 `aria-pressed=true`、提示「配色已保存：这一部戏的画面提示词会按它写『色调』」、
  服务端 `colorCardId=morandi-violet-pink`；点「不指定」→ 清空且服务端同步为空。
  核对用的临时项目跑完即删，不碰真实项目。

测试：**1380 → 1394 全绿**。

## [2.56.0] - 2026-09-16

### 新增：莫兰迪「高级灰」配色卡 9 组（主题页一键套用）

你发了张色卡图说"这种颜色卡真漂亮"，就把它做进主题页了。

- 新增 `frontend/src/theme/colorcards.mjs`：9 组色值不是"看着像"估的——把图里每条色值标签
  裁出来放大逐条读，再用卡片四角的像素采样交叉校验，两读一致才落盘。测试把这 9 组钉死，
  以后改错一位数字、少抄一组都会亮红灯（抄错色值只有肉眼能发现，测试是唯一的守门人）。
- 顺手把规律写进模块注释：9 张都是**竖向渐变（上深下浅）**、色相差小、明度差拉满，所以看着"高级"。
  第 3 张的标签顺序和渐变方向相反（图上就是灰在上、米在下），所以卡片单独带 `top`/`bottom`，
  不要拿 `from`/`to` 想当然当渐变两端。
- `Themes.tsx` 主题页新增「配色卡」一栏：9 个色块点一下直接套成壁纸（走共享 `persistWallpaper`），
  右上角的小色点把主色一并换成该卡的深端。
- 渐变一律保持 `linear-gradient(...)` 字面量交给共享 `wallpaperCssImage`：包进 `url()` 就是非法
  CSS、点了等于没设（这条契约早就有测试，新卡也纳进同一道）。

测试：+3 条（9 组齐全且与抄录一致、渐变原样不被 url() 包、主题页不许自己另抄一份色值）。

## [2.55.0] - 2026-09-16

### 修复：同一个模型，自研循环的工具调用比 pi 多一半——因为两边"能用什么命令"不一样

起因是"感觉 pi 跟 dsh 都强，我们的元枢不行"。做了一次同模型双引擎对照
（`PI_USE_AGENT=0` 强制走自研循环，模型固定 `opencode-go/deepseek-v4.1-flash`，三个可机检任务）：

| 阶段 | 引擎 | 结果 | 工具调用 | 耗时 |
|---|---|---|---|---|
| A | pi（兼容适配器） | 6/6 | 10 | 103s |
| B | 元枢自研循环 | 4/6 | 16 | 251s |

把工具轨迹逐条摊开：pi 一条 `grep -n` 就拿到答案（1 次调用），循环里的**同一个模型**在猜
`findstr` / `dir /b`，试 5~7 次还有失败。所以差距不在模型，在工具语义：pi 通道走 git-bash，
自研循环固定走 cmd.exe。

**修后复测（同模型 `zai/glm-4.7`，同样三个任务，A/B 各跑一遍）**

| 阶段 | 引擎 | 结果 | 工具调用 | 耗时 |
|---|---|---|---|---|
| A | pi（兼容适配器） | 3/3 | 4 | 54s |
| B | 元枢自研循环 | 3/3 | 5 | 69s |

工具命令也从"猜 cmd"变成了和 pi 同一族：`grep -n '/api/parse-file' /d/pi-web/server.mjs`、
`ls -1 /d/pi-web/tests/unit/*.test.mjs | wc -l`。

> 复测有两个坑记在这儿：① 换模型是因为 `opencode-go/deepseek-v4.1-flash` 后来报了
> 403 RegionError（"only available hosted in China and requires explicit opt in"），
> 而几分钟前 pi 通道跑同一个模型还是通的——两边取 key 的来源不同（SDK 走
> `OPENCODE_API_KEY`，循环走 `~/.pi/agent/auth.json`），还没定论；
> ② 第一次跑 B 阶段时 T2/T4 报 `fetch failed`（当时手动重启服务时杀重了实例），
> 原样重跑即 3/3，属于环境噪声不是引擎问题。

**改了什么**

- `engine/tools/unified-tools.mjs`：新增 `detectBashShell()`（git-bash 优先；明确不认
  `System32\bash.exe` 那个 WSL 启动器，MSYS 风格 `/d/...` 路径进去必失败）与 `bashToolDescription()`；
  bash 工具的描述改成**按真实 shell 动态生成**（getter）——描述说 cmd、实际给 bash 这种错配，
  正是模型两边各试一套命令的来源。
- 执行器：有 bash 就走 `bash -lc`，没有才回 `cmd /c`；`ensureCommandDirectories`（cmd 专用的
  中文目录兜底）只在没有 bash 时跑。
- 内联代码改写（`node -e` / `python -c` → 临时文件）**两条路都做**。bash 下做它不是为了引号
  （bash 引号没问题），而是为了 abort：`bash -lc 'node -e …'` 被杀时死的是 bash，node 变孤儿
  继续跑。顺带把 bash 探测收窄到 `usr\bin\bash.exe`——`bin\bash.exe` 只是启动器，会再 re-exec
  出真 bash，白多一层进程。
- 新增 `tests/unit/tool-shell-parity.test.mjs`：把"优先真 bash、描述随 shell 走、执行器真的用
  `bash -lc`、内联代码两条路都改写"锁成回归测试。

### 修复：点"停止"停不干净——abort 只杀直接子进程，孙子进程变孤儿

真机现象：abort 用例被拖到 30s 才失败；被中断命令的工作目录几百毫秒内删不掉（EPERM）；
界面上点了停止，命令还在跑。

根因两条，缺一条都停不干净：

1. `execFileAbortable` 只在 `execFile` 的回调里落地，而回调要等 **stdio 管道关闭**。命令是经
   shell 包一层起的，孙子进程攥着 stdout 句柄 → "等 abort" 变成"等命令自己跑完"（实测正好 30s）。
   现在 abort 时**立刻落地**，不再管子进程的管道。
2. Windows 上 `child.kill()` 只杀直接子进程。现在先 `taskkill /T /F` 枚举整棵树，再补一把
   "CIM 快照 + 由深到浅逐个点名"的扫尾——`ParentProcessId` 即使父进程已经死了也还在，所以
   孤儿照样摸得到（taskkill 枚举树和 git-bash 启动器 re-exec 之间有竞态，实测 3 次漏 1 次）。

**真机验证（红绿对照，不是"看着对"）**

- 新增回归用例「abort 要连孙子进程一起杀（cmd / bash 包一层也不留孤儿）」：把树杀退回"只杀
  直接子进程"，用例 3/3 红（`ping` 孤儿还在跑）；有树杀时绿。
- abort 用例本身：30000ms 超时 → **120ms** 返回。
- 校准测量仪器时踩了两个坑，已写进用例注释：`-like` 匹配 `ping` 的完整命令行匹配不到
  （cmd 会拼成 `ping  -n 30 …`，中间是两个空格）；也不能拿 `127.0.0.1` 当 needle
  （本机服务进程的命令行里到处都是它，会自己撞上自己）。
- Windows 上刚被 kill 的 shell 会攥着它当初的 cwd 几百毫秒，这期间 `rmSync` 报 EPERM 是**瞬态**：
  测试清理改成自己轮询（Node 的 `maxRetries` 在这条错误路径上并不吃重试）。

测试：**1377 全绿**（净增 2 条）。

### 修复：HTTP 报错被截到 150 字，诊断只能靠猜

- `engine/unified-chat.mjs` 的错误文案改走 `describeHttpError`：优先摘出
  `msg` / `displayMsg.zh` / `extError.code`，原文留 600 字。原来只留 150 字——
  opencode-go 的 `RegionError` 正好被截在 `requires explicit opt `，
  "到底要开什么"完全看不出来，排查只能靠猜。

## [2.54.0] - 2026-09-16

### 修复：单次输出被我们自己压死在 8192 token —— "任务太长，请拆分"的真因

用户现象："经常报任务太长了，让拆分。"那句提示是 `TRUNCATED_TOOL_ERROR`，
但**根因不是任务长，是我们把输出上限写死了**：

```js
max_tokens: Math.min(mdef?.maxTokens || 8192, 8192)   // unified-chat.mjs 与 model-adapter.mjs 都是这句
```

模型声明的输出上限本来是 **32k~384k**（`deepseek-v4-pro` 384000、`zai/glm-4.7` 131072、
`workbuddy/hy4-preview` 32768…），而元枢一律只给 **8192**。于是"一次写完一个大文件/长脚本"
必然在 8192 处被砍断，工具调用参数断成半个 JSON → `inspectToolCalls` 判 truncated →
重试两次 → 回一句「请把任务拆小再试」，**把锅甩给了用户**。

**改了什么**

- 新增 `engine/output-budget.mjs`：
  - `clampOutputTokens(mdef)` —— 起步按模型声明给，但有保险丝（32k；声明更小就用声明）；
  - `escalateOutputTokens(current, mdef)` —— 命中截断就把预算翻倍重试，上限为模型声明的量；
  - `maxTokensFieldOf(compat)` —— 字段名走 compat（有的通道要 `max_completion_tokens`，
    以前一律发 `max_tokens`，那类通道等于没生效）。
- `unified-chat.mjs`：请求体用 `body[field] = outputBudget`；截断重试时先抬预算再重试（并在日志里写明抬了多少）。
- `model-adapter.mjs`：同样按声明给、同样认字段名。
- `TRUNCATED_TOOL_ERROR` 文案重写：说清"系统已经把上限抬到模型允许的最大值重试过了"，
  这时才建议分块写（回一句「分块写」我按段追加），不再一上来就让人拆任务。

**真机验证（把模型 baseUrl 指到本地桩，直接看发出去的请求体）**

```
声明 32k 的模型 → max_tokens = 32768      （旧代码恒为 8192）
声明 4k  的模型 → max_tokens = 4096       （旧代码会超发成 8192）
compat 要 max_completion_tokens → 该字段 = 16000（旧代码发的是没用的 max_tokens）
```

测试：+4（起步预算与保险丝、截断后抬预算、字段名走 compat、两条路径不许再写死 8192），
1369 → **1373 全绿**；另把 `unified-chat-recovery` 里写死的错误文案断言改成引用常量。

## [2.53.0] - 2026-09-16

### 修复：全新会话首轮被误判"复读"、然后静默换模型（用户原话："静默换成 agnes 3.0"）

**根因**：复读守卫的"上一条回复"基准，在内存里没有时会去**读会话文件**。而 pi 通道会在**本轮中途**
就把回复写进会话文件——于是守卫读到的是它自己刚写的那条，**拿自己跟自己比**：

```
全新会话第 1 轮 → 守卫 miss 内存 → 读文件 → 读到本轮自己的回复 → 判定"与上一条完全相同"
  → 同模型重写一遍（正文里塞一行小字）
  → 重写仍判复读 → pickFallbackExcluding 换成 agnes-2.5-flash 重生成   ← 用户看到的就是这一步"静默换"
```

真机对照：6 次全新会话首轮里误判 5 次；逼出一次真复读后确实换了 agnes-2.5-flash。

**改了什么**

1. **基准改成"开跑之前取"**：`output-guard.mjs` 新增 `lastAssistantReply({ sessionFile, tree })`；
   `handleChat` / `handleUnifiedChat` 在动笔前取一次，取不到就是 `null`（首轮没有"上一条"，不可能复读）。
   `isRepeatReply` / `classifyAnomaly` 接受 `baseline`，传了就不再读内存/文件（避免"自己跟自己比"）。
2. **换模型/重写不再只活在流里**：新增 `model_switched` SSE 事件（复读修正 / 换模型重生成 / 空回复兜底
   三处 + 自研循环的守卫换模型处都会推）；前端在气泡上渲染
   `已重写`（同模型）或 `兜底 <模型>`（悬停显示原因 + "你选的是谁"）。
3. **落盘也带标记**：以前只在实时流里塞「（已切换 … 重新生成的回复）」这行小字，**历史里没有**——
   刷新或换设备就真的变回"静默换模型"。现在修正/兜底文本落盘时带上标记。

**真机验证**

- 全新会话首轮：notes 只剩「本轮主引擎 · 兼容适配器」，**不再出现**「与上一条完全相同」，也没有换模型。
- 逼出真复读（同一句固定内容连发两次）：收到
  `model_switched {"provider":"agnes","id":"agnes-2.5-flash","sameModel":false,"reason":"复读判断成立且同模型修正失败，已换模型重生成"}`；
  服务端历史里查到 `（已切换 agnes/agnes-2.5-flash 重新生成的回复）…` —— 刷新/换设备仍可见。

测试：+5（首轮不误判、真复读仍抓到、防误伤规则不变、事件三处都推且前端渲染、基准必须开跑前取），
1364 → **1369 全绿**。

> 说明：本轮同时把「元枢自研循环打不动原生模型」的两个缺陷（baseUrl 取错 + 不发会话亲和头）修了，
> 见上面 v2.52.0 的补记条目。

### 修复（补记）：自研循环打不动原生强模型——baseUrl 取错 + 不发会话亲和头

> 这条日志是**补记**：代码已经在 v2.52.0 里（那次发布把暂存的源码改动一起带进了 release 提交），
> 但条目本身当时因为我脚本里的引号写坏没落盘。功能与测试不受影响，这里补齐记录。

起因：用户感觉"pi / dsh 都强，元枢不行"。做了一次**同模型双引擎对照**（`PI_USE_AGENT=0` 强制走自研循环），
三个可机检任务（查路由行号 / 数测试文件 / 写文件再读回），模型固定 `opencode-go/deepseek-v4.1-flash`：

| 阶段 | 引擎 | 结果 | 工具调用 | 耗时 |
|---|---|---|---|---|
| A | pi（兼容适配器） | 2/3（T1 错，另两个对） | 6 | 43s |
| B（修复前） | 元枢自研循环 | **0/3 —— 全部 HTTP 401** | 0 | 2s |
| B（修复后） | 元枢自研循环 | 2/3（**同一个 T1 错**，另两个对） | 12 | 58s |

**结论**：把模型固定住，两个引擎在这批任务上**同分、同错点**。所以"元枢引擎更弱"里很大一块是**归因错位**——
引擎由 provider 决定（`resolveLead`）：非原生通道（zhipu-paid / agnes / workbuddy / aieyra…）**只能**走自研循环，
原生通道才走 pi；而默认模型恰好是 `zhipu-paid/glm-5.3-flash`，于是"日常体验 = 自研循环 + 中继模型"、
"偶尔切到的强模型 = pi"，两个变量被绑在一起。

但实验也挖出**自研循环两个真缺陷**（它原本根本调不动原生模型）：

1. **baseUrl 取值顺序错**：`resolved?.baseUrl || mdef?.baseUrl` —— auth.json 那份是**账号级**的，
   真机里记的是 `https://opencode.ai/zen/v1`，模型定义却是 `https://opencode.ai/zen/go/v1`，
   用 auth 那份直接 **401 "Model deepseek-v4.1-flash is not supported"**。改成模型定义优先。
2. **不发会话亲和头**：端点修对后立刻撞上 **400 MissingSessionID**（opencode-go 需要 `x-opencode-session`；
   pi 通道由 SDK 自己发，自研循环一个都不发）。新增 `sessionAffinityHeaders()`：
   opencode-go/opencode → `x-opencode-session`、openrouter → `x-session-id`、compat 声明也认；
   **没有会话号也要发**（退回进程级稳定值——缺这条头是硬失败，探针实测随便一个稳定值就能路由）。

这两条加起来才是"元枢不行"里实打实的那一半：**pi 失败后的降级通道，本来也调不动原生模型**。

顺带记两条观察（未改，留给下一步）：

- **复读守卫过于灵敏**：6 次首轮里触发 5 次，并且会在同模型修正失败后**静默换成 agnes-2.5-flash 重新生成**
  （phase A 的 T1 就是这样被换的模型）。用户看到的是"答得莫名其妙"，界面却不说这是谁答的。
- 自研循环在同分数下**用的工具更多、更慢**（12 次 / 58s vs 6 次 / 43s），小样本不足以定性，但方向一致。

测试：+4（亲和头规则与兜底、baseUrl 优先级、两条请求路径都发头），1360 → **1364 全绿**。

## [2.52.0] - 2026-09-16

## [2.51.0] - 2026-09-16

### 修复：工具调用参数双重编码把整轮请求打死（"怎么又调用失败" / HTTP 400 code 11133 的真因）

用户现场：聊天报 `执行失败 · HTTP 400 {"code":11133,"msg":"the request parameters were rejected by
the model provider",…,"extError":{"code"…`，界面还把它截断在 `"extError":{"code"` 处——看不出是什么错。
执行引擎是元枢自建循环，模型 `workbuddy/hy4-preview`（本地中转 127.0.0.1:8910）。

**怎么查的**：中转日志里留着那次的原始请求体，把它原样重放 → 稳定复现 400。然后逐项拆：

| 改动 | 结果 |
|---|---|
| 原样重放那一对 `assistant(tool_calls)+tool` | ❌ 400 |
| 把 `arguments` 换成 `{}` | ✅ 200 |
| 把 `arguments` 换成 550 个 `a`（不是合法 JSON） | ❌ 400 |
| 把 `arguments` 换成 550 字的**合法 JSON 对象** | ✅ 200 |
| 只留最后一句 user（不带任何 tool_calls） | ✅ 200 |

**根因**：那条 tool_call 的 `arguments` 是**双重编码**——
`"{\"program\": \"const fs = require('fs')…"}`：它本身是一个 JSON **字符串**字面量，
`JSON.parse` 出来是 string 而不是 object。OpenAI 兼容端要求 `arguments` 必须能 parse 成**对象**，
于是整轮请求被拒（同一批历史在 opencode-go 上表现为 `Messages with role 'tool'`）。
来源是历史里的 `args` 本来就是字符串，`formatSessionHistory` 又 `JSON.stringify` 了一次
（`JSON.stringify(t.args)` → 带引号的字符串）；流式分片也可能拼成数组/字符串。

**改了什么**：

- 新增 `engine/tool-args.mjs`：`normalizeToolArgs()` 统一把参数规范成**合法对象 JSON**——
  解多层字符串包裹、剥脏前缀（`{}{"path":…}`）、数组分片缝合；实在救不回来就包成 `{raw: …}`，
  **绝不让一条坏记录把整轮请求打死**（历史里那次调用早就执行完了，重放只需要语法合法）。
- `unified-chat.formatSessionHistory`、`sanitizeToolCallList`、`model-adapter.sanitizeTcsLocal`
  全部改走它；`model-adapter.buildBody` 再加一道**发送前最后闸**：`normalizeToolCallArguments(history)`。
  存量会话（包括用户那条 01a0a830）不改文件也能恢复——发出去之前就地修好。
- `engine/http.mjs` 新增 `describeHttpError()`：把 `msg / displayMsg.zh / extError.code` 摘成一句话，
  原文最多留 600 字并注明截断。原来只留 150 字，正好截在关键处——这正是"看不出是啥错"的原因。

真机证据：

```
修复前：原样重放那次请求        → 400 {"code":11133,…}
修复后：同一份请求走一遍规范化  → 200 ✅（18 条 tool_call 的参数被修好，parse 出来是 object/键 program）
应用级：元枢里用 workbuddy/hy4-preview 真发一轮 → 3s 回「好」，error 0
```

测试：+5（双重编码、脏前缀、数组分片、整段历史就地规范化、错误文案人话化），1355 → **1360 全绿**。
## [2.50.0] - 2026-09-16

### 修复：外部机器安装检查的 4 个问题全部收进上游（换盘/换机器不再需要本地补丁）

来源：另一台电脑（项目在 `C:\Users\xue-x\pi-web`）装完后发现 4 个问题，一直靠一份**本地补丁**
（`pi-web本地补丁.patch`）在每次 `git pull` 后重打。这次全部直接修进上游，补丁可以退休了。

**1. `watchdog.cjs`：守护进程不再写死盘符**
`const WEB_DIR = "D:/pi-web"` → `__dirname`（脚本自身目录）。写死时换盘/换机器就找不到 `server.mjs`，服务拉不起来。

**2. `autostart.ps1`：开机自启任务指向脚本自身所在目录**
`$root = 'D:\pi-web'` → `$root = if ($PSScriptRoot) { $PSScriptRoot } else { (Get-Location).Path }`。

**3. `scripts/restart-pi-web.ps1`：重启脚本同理**
`$root` → `Split-Path -Parent $PSScriptRoot`（它在 `scripts/` 下，上一级才是仓库根）。
两个 `.ps1` 都**保持 UTF-8 with BOM**（编辑后逐一校验字节 `EF BB BF`——没有 BOM 时
`powershell -File` 会把脚本原文当输出打出来并静默失败，这是 9-14 踩过的坑）。
真机验证：用 `powershell -NoProfile -File .\scripts\restart-pi-web.ps1` 跑了一次，它按 `$root`
找到了 `server.mjs` 并重启成功（`元枢已就绪：8787 /api/health = 200`）。

**4. `server.mjs`：@文件引用的二进制文件不再内联进对话（这台机器最在意的那个）**
xlsx/docx/pdf/zip/图片的字节拼进 prompt 就是乱码——费 token、污染对话，模型还会照着乱码猜内容。
新增 `engine/file-inline.mjs`：
- `isBinaryReference(file)`：先看扩展名（xlsx/docx/pdf/zip/图片/音视频/字体/数据库…），
  再看**内容控制字符比例**（>5% 判为二进制，挡住"被改名成 .txt 的二进制"）；
- 命中就不拼字节，改成一条**可执行**的提示：按路径用 `POST /api/parse-file`（支持 docx/xlsx/pptx）解析，
  不要凭空猜测内容；
- **反向防误判**：`.txt/.md/.json/.csv/.svg/.log/.xml` 等文本一律照旧内联——误判比乱码更糟，
  模型会以为文件是空的然后开始编。

真机验证（带真附件跑一轮，读回会话里的用户消息）：

```
附 sample.xlsx（含 NUL 字节）
  → prompt 里是：「参考文件 …sample.xlsx：（二进制文件，原始内容不适合内联到对话，已跳过。
     请按此路径用文件解析工具读取——POST /api/parse-file 支持 docx/xlsx/pptx…）」
  → 原始字节/NUL：0 处
对照附 sample-note.txt → 全文照旧内联 ✅
```

测试：+4（扩展名判定与防误判、内容嗅探、提示文案、`server.mjs` 必须在内联之前判定），1351 → **1355 全绿**。

> 另一台机器怎么收：`cd C:\Users\xue-x\pi-web` → `git checkout -- watchdog.cjs autostart.ps1 scripts/restart-pi-web.ps1 server.mjs`
> （丢掉本地改动）→ `git pull` → `node --check server.mjs` → 重启 8787。
> 之后 `pi-web本地补丁.patch` 与那份说明可以留着做备份，但**不需要再打**了。
## [2.49.0] - 2026-09-16

### 修复（③残留）：出图交付那条记录绕过了安全检查——"出过图之后切 deepseek 就不回话"的第二处来源

v2.45.0 修的是**元枢循环**写会话的那条路（`persistYuanshuAssistant`）。这次把残留钉死了：

- 出图兜底交付时，server 会补一条 assistant 记录：
  `content: assistantContentWithMedia("", settledMedia)` —— 里面是 `{type:"image",url}` 块，
  **完全绕过了 `sdkSafeAssistantBlocks`**。
- 于是只要这个会话**出过图**，用户一切到 `opencode-go/deepseek`（引擎换 pi）→ 下一句重放历史时，
  pi-ai 的 token 估算器又把 image 块当工具调用读 `block.name.length` →
  `TypeError: Cannot read properties of undefined (reading 'length')` → 那一轮什么都不回（①已让它显示出来）。

**端到端复现（修复前）**：

```
第 1 轮「帮我画一张赛博朋克风格的猫…」 → 出图 1 张，会话里 assistant 块 = ["thinking,text","text","image"]
切模型 → opencode-go/deepseek-v4.1-flash（引擎换 pi）
第 2 轮「在吗？一句话回我」            → error 1：Cannot read properties of undefined (reading 'length')
```

**修复后同一条路径**：第 1 轮落盘块变成 `["thinking,text","text","text"]`（图以 markdown 进正文），
第 2 轮 **delta 11 / error 0** —— 正常回答。

改动：

- `server.mjs`：媒体交付与文件交付两处 `appendMessage` 都过 `sdkSafeAssistantBlocks`。
- `sdkSafeAssistantBlocks`：带 `name` 的 `file` 块**原样保留**（估算器读的是 `block.name.length`，
  有 name 就安全；而界面靠 `type:"file"` 渲染文件卡片，转文本会把卡片弄丢）；
  没有 name 的 file 块与图/音/视频块一样必须转文本——这一条用 SDK 的估算器实测过：
  `file(有name) ✅ / file(无name) ❌ / image ❌ / toolCall ✅`。

测试：在**既有**用例里补了一条断言（带 name 的 file 块必须原样保留），用例数不变：**1351 全绿**。
（更正：本条最初写成"1351 → 1352"，实际没新增用例。）
至此 ③ 的两处来源（元枢循环落盘 + 出图交付落盘）都封住，且历史 34 个会话已修复。
## [2.48.0] - 2026-09-16

### 修复（④出图兜底）：续画也要出图——"再换个词的出一下"以前什么都不画

用户原话："用 Agnes 聊天让画图，经常不画。"查下来是**宿主旁路只认关键词**：

- 元枢有一条出图兜底（`media-api.mjs` 的 `detectMediaIntents` + `model-client`/`server` 的并行生成），
  它认的是"配图/画图/画一张"这类**明确祈使词**；
- 而真机里用户是在**同一件事的下一句**说：「这个就有那么点意思，不管字对不对，效果对了，**再换个词的出一下**」——
  这句一个关键词都没有 → 旁路不出图；Agnes 这类出图模型又不会主动调 `generate_image` 工具 → **整轮什么都不画**。
  这就是"经常不画"：**明确要图时能画，接着追加要求时就断**。

改动：

- `detectMediaIntents(message, ctx)`：新增**续画意图**——只有当"这个会话上一轮确实出过图"
  （`ctx.lastMediaType === 'image'`）且这一句是**短**祈使追加要求时才认（`isFollowUpDrawRequest`），
  并且明确排除改代码/跑测试这类工程追问与否定句，避免"自作主张画图"。
- `extractMediaPrompt(message, ctx)`：续画那一句本身没有画面信息，一律**沿用上一轮真正用过的那句**，
  不要把"换个词的出一下"当提示词（会画出一张莫名其妙的图）。
- `server.mjs`：按会话记住"上一轮出过图、提示词是什么"（只在内存里，不污染会话文件），
  续画命中时推一条 note 说明「按你上一张的要求又画了一张（宿主旁路出图：不依赖模型自己想起来调工具）」。
- **顺手修掉同一轮发现的问题**：从工具输出里**刮出来**的媒体以前是原样推给前端的——
  真机验证里就有一条 `platform-outputs.agnes-ai.space` 的外站链接直接进了对话。
  现在统一走 `saveArtifact` 本地化，没落地的标 `localizeError`（外站链接会过期，图和视频迟早变裂图）。

真机验证（两次，各花 1～2 张出图额度，agnes-image-2.5-flash）：

```
第 1 轮「帮我画一张赛博朋克风格的猫…」        → 出图 1 张（基线，老逻辑本来就能）
第 2 轮「…效果对了，再换个词的出一下」        → 出图 1 张 + note「按你上一张的要求又画了一张…」
        （修复前：这一轮 0 张）
```

测试：+4（续画命中、误伤防护、老路径不回退、刮出来的媒体必须本地化），1350 → **1351**。

**如实记一笔**：本轮全量测试出现过两次**互相不同**的失败——`file-lock` 的跨进程自增与
`subagent-traces` 的取消用例；单独跑分别 **22/22**、**6/6** 全过，是**满载时的抖**（当时正在跑出图），
与本次改动无关。这两条属于已知的负载敏感用例，没藏。
## [2.47.0] - 2026-09-16

### 新增（②主驾引擎角标）：每轮都标出"谁在干活 + 为什么"，换模型不再神秘

用户的观察："引擎也变成 pi"——这不是错觉，是 `resolveLead()` 的规则：
**非原生通道**（如 agnes）走元枢自制循环，**原生通道**（`NATIVE_PROVIDERS` 里有 `opencode-go`）
才走 pi 兼容适配器。换模型等于换执行循环，而这件事界面上完全看不见。

服务端其实一直在推 `engine_selected {engine, reason}`（还有 `note: 本轮主引擎 · …`），
**前端把它丢了**——于是"怎么换了模型连脾气都变了"只能靠猜。现在：

- `ChatArea`：消费 `engine_selected`，把 engine 与 reason 存进流式状态，并随消息落库
  （刷新后还能回看这一轮是谁主驾）。
- `Message`：角标 `主驾 元枢自制循环 / 兼容适配器(pi) / 外部执行引擎(dsh)`，
  名字与 `engine/engine-pair.mjs` 保持一套；原因挂在 title 上（例："当前模型通道由元枢自建循环承接。"）。
  流式期间就显示——不用等回复完才知道这一轮换了引擎。

真机（CDP，新开对话发一句）：聊天里出现 `主驾 元枢自制循环`，title 为
`当前模型通道由元枢自建循环承接。`（当前默认模型 zhipu-paid 属非原生通道；
换成 opencode-go/deepseek 时这里会显示 `主驾 兼容适配器(pi)`——正是用户当时看到的现象。）

测试：+2（源码契约：事件必须被消费、id 必须映射成中文名、原因不能丢），1345 → **1347 全绿**。
## [2.46.0] - 2026-09-16

### 修复（①失败可见）：上游失败不再被藏起来，也不再拿"无历史的兜底回答"糊过去

用户原话："切换到 deepseek 后，上下文记忆都没有了，还变得很傻。"——查出来是两件坏事叠在一起：

1. **失败被静默丢掉**：pi 通道调用失败不抛异常，只落一条 `{role:"assistant", content:[], stopReason:"error", errorMessage}`。
   这条记录不满足 `extractMessages` 的任何推送条件 → 整条被过滤 → 界面上什么都没发生。
2. **兜底回答不带历史**：`!sawDelta` 分支会用 `directChat(备用模型, 当前这句话)` 补一句。
   这条路**只发当前这一句**（没有对话历史），所以用户拿到的是一句"失忆"的回答，
   加上第 1 条把失败藏了，观感就是"它变傻了"。

改了什么：

- `engine/session-utils.mjs`：`extractMessages` 把 `error` / `stopReason` 带给前端；
  另外 `stopReason=aborted` 且无正文时标记为"本轮已停止（没有产出内容）"。
- `server.mjs`：新增 `lastTurnUpstreamError()`，**先看 SDK 的内存树再看文件**
  （失败记录是本轮结束才落盘，只读文件会读到上一轮的旧消息——第一版就是这么漏判的）；
  识别到上游报错时：
  · 日志打出真实原因；· 走 `noteModelFailure` 计数；· **不再**走无历史兜底；
  · 推一个 `retryable` 的 error 事件，写清"哪条路失败、再失败一次就自动避开"。
  成功一轮即 `clearModelFailures` 清零。
- `engine/model-failures.mjs`（新）：同一条路 10 分钟内连续失败 2 次 → blocked，
  调用方据此 `markModelBlocked`（此前只有带 HTTP 状态码的失败会被标冷却，
  像 TypeError 这种**没有状态码**的失败根本没人管）。
- 前端：`Message` 渲染失败条（原因 + 「重试这一条」）；`TurnList` 折叠状态下也显示红色状态点与
  「⚠️ 本轮失败」徽标（不然要展开才知道）；`ChatArea` 的重试 = 把这一轮前面最近那条用户消息重发；
  `mergeMessages` 补上服务端的 error 字段（本地优先策略会把失败盖掉）。

真机证据（CDP，目标会话里就有一条真实的失败记录）：

```
本轮失败：Cannot read properties of undefined (reading 'length')
失败不藏起来——同一个模型连续失败会自动标冷却，下一轮避开它。
[重试这一条]                                  ← 都在聊天里渲染出来了
折叠列表该轮：bg-pi-red 状态点 + 「⚠️ 本轮失败」徽标
点「重试这一条」→ 立刻开始新一轮（streaming=true）
```

服务端同一次也留了痕：`[元枢] 本轮上游失败 opencode-go/deepseek-v4.1-flash → Cannot read properties of undefined (reading 'length')`，
并且 `/api/sessions/:id/messages` 现在能带出这条失败记录（以前是查不到的）。

测试：+3（连续失败计数/清零、失败记录必须留在历史里、上游报错不许无历史兜底），1342 → **1345 全绿**。
## [2.45.0] - 2026-09-16

### 修复：assistant 消息里的附件块会让整段会话不可重放（"切到 deepseek 后一句话都不回"的真因）

用户现象：用 Agnes 聊天出图，切到 deepseek 之后"上下文全没了、还很傻"，并且注意到"引擎也变成 pi"。

查下来的链条（每一环都有证据）：

1. **换模型确实会换引擎**：`server.mjs` 的 `resolveLead` 对**非原生通道**强制走元枢自制循环，
   原生通道（`NATIVE_PROVIDERS` 里有 `opencode-go`）才走 pi 兼容适配器。
   Agnes 不在名单里 → 元枢循环；切到 `opencode-go/deepseek-v4.1-flash` → **主驾换 pi**。界面没告诉用户。
2. **pi 通道每次调用都失败**，失败记录是 `content: [] / usage 0-0 / stopReason: "error"`，
   而失败原因被界面吞掉（空 content 的消息不进列表），所以看起来像"它不说话"。
3. **失败原因定位到行**（这次是复现出来的，不是猜的）：

   ```
   TypeError: Cannot read properties of undefined (reading 'length')
     at estimateMessageTokens (pi-ai/dist/utils/estimate.js:42:33)
     at estimateMessages (…/estimate.js:80:19)
     at estimateContextTokens (…/estimate.js:94:22)
     at clampMaxTokensToContext (…/api/simple-options.js:7:45)
     at buildBaseOptions (…/api/simple-options.js:17:20)
     at streamSimple (…/api/openai-completions.js:536:12)
   ```

   SDK 的 token 估算器只认 `text` / `thinking` 两类块，其余一律当"工具调用"读
   `block.name.length`。而元枢把旁路出图的交付图**以 `{type:"image",url}` 块写进了 assistant 消息**，
   于是这段历史一旦被 pi 通道重放（切模型重建 agent 时必然重放），**在发请求之前**就抛错——
   而且这条会话此后每次都崩。用真实历史做对照：原样重放 = 抛错；去掉那条消息 / 把图块改写 = 通过。

修的什么：

- `engine/yuanshu-session.mjs`：新增 `sdkSafeAssistantBlocks()`，落盘前把附件块（image/video/audio/file）
  改写成 markdown 纯文本，工具调用/思考块原样保留（SDK 要靠它们配 toolResult）。`persistYuanshuAssistant` 走它。
- `engine/session-utils.mjs`：`extractImages` 同时认文本里的 markdown 图片（并去重），
  所以"修好重放"不会变成"界面上的图没了"。普通文件链接不会被误当图片。
- `scripts/fix-session-image-blocks.mjs`：历史数据修复（默认 dry-run，`--apply` 才写盘，每个文件先备份）。
  **已在本机执行**：358 个会话文件里 34 个命中，共 50 条 assistant 消息被改写，36 份 `.bak-<时间戳>` 备份。
  这不是个例——8 月以来的会话里都有，任何一条遇上 pi 通道都会崩。

测试：+2（改写规则与"界面仍取得到图"互相牵制），1340 → **1342 全绿**。

**还没完全了结**：把修好的历史放回元枢自己的链路重放，仍然复现同一个报错文本——
说明还有**第二处**会产生同样 TypeError 的形状（我没拿到那一次的栈：服务端没记栈）。
下一步先做①"失败可见"（把错误+栈显示出来/记下来），再借它把第二处钉死。
## [2.44.0] - 2026-09-16

### 优化：中间栏按用途折叠——写作核心常开，配置与工具组默认收起

用户原话："中间栏那个当前场景设定那一栏，第一幕下面那些板块做个可折叠功能，
有些次要的默认折叠，要不然太长了。"真机量过：折叠前编辑器一栏 **3120px**，整页布局 **4333px**。

- 折成 **7 组**（默认全部收起，summary 上写清里面是什么）：
  「场景 · 动作」「生成参数 · 素材」「剧本 · 分集 · 配方」「构思 · 方法 · 台词」
  「成片 · 改编 · 批量」「排练场」「镜头规格」（镜头规格只在画面/视频时出现）。
- **常开的是真正要动手的两块**：本段内容、本段台词；输出类型/模型也留在外面。
- 主按钮（生成当前…）从"所有面板之后"挪到台词下面：写完就能按，不必先滚过七组折叠行。
- 高度：编辑器 **3120 → 1281**（−59%），整页布局 **4333 → 1281**。
- **撤回一个自定目标**："主按钮进首屏"没做到（视口 845px，按钮 y=1273；再挤就得砍掉
  写内容/写台词这两个大文本框本身）。现在只保证"在折叠组之前"，截图与数字都在报告里。

真机 CDP 核对：7 组全部收起（openCount=0）、内容/台词可见、点一下「生成参数 · 素材」
从 45px 展开到 493px（里面 7 个控件）再收回 45px、全程写入请求 0 条。

写的时候差点捅一个洞：挪块时把「AI 草稿 · 确认后一起保存」整块删掉了——tsc 不会报
（少渲染一个条件块完全合法）。补回来了，并在测试里钉了一条"草稿面板不能被挪丢"。

测试：+1（折叠组默认必须收起、写作核心与主按钮必须在折叠组之前、草稿面板必须在），1339 → 1340 全绿。

## [2.43.0] - 2026-09-16

### 修复/优化：首屏不再先闪「开始一个故事」，人物设定里的「形象」自成一组

用户原话是"页面也再美化下，不好看"。这一轮收尾的是视觉整理里最后两块：

- **首屏骨架**（`StorySkeleton`）：项目列表还在路上时，以前 `project` 还是 null，
  页面直接渲染「开始一个故事」——先闪一屏"你没有故事"，几百毫秒后才跳成工作台，
  比慢更难受。现在用灰块把**真实版式**（状态条 / 左时间线 / 中编辑器 / 右结果）先占住，
  数据到了原地替换，不发生重排；骨架下面不再叠一行「正在加载故事…」，文字是噪音。
- **路由兜底也换成骨架**（`AppLayout.PageLoader`）：以前是一行居中的「加载中…」，
  它是**任何**页面都会先看到的那一屏。
- **「形象」自成一组**：形象芯片与「新增形象」同排，用虚线分隔，和「重新生成」分开。
  以前两者混在一行，看不出"哪几个是形象、哪个是新增"，也看不出点一下重新生成的是哪一张。
  真机 DOM：`形象 / 基础形象 / 战斗装束 / ＋ 新增形象 / 已生成 2/2`。
- **间距落到 8px 刻度**：定妆照卡片内 6→8、卡片间 12→16（原来 14/12/10/6 混着用，
  眼睛读不出层级）。动效尊重 `prefers-reduced-motion`。

真机核对（CDP，零依赖）：把项目列表请求拖慢 2.5 秒后，`.story-skeleton` 稳定留在屏幕上
（13 条灰线 + 3 个灰块，`role=status`，动画名 `story-skeleton-pulse`），
且此时**没有**「开始一个故事」；放开网络后骨架被原地替换（残留 0 个），
全程写入类请求 0 条。截图在 `tmp\visual-after\{skeleton,settings-looks}.png`。

测试：+2（骨架的加载顺序与动效降级、「形象」分组不得混回操作行），1337 → 1339 全绿。
## [2.42.0] - 2026-09-16

### 新增：定妆照提示词补上「身体比例」与「镜头」，人物设定要求写到结构上

借的是 hypit 图像提示词的**写法**（Capture / Person / Shot / Setting），不碰它的代码——
它的许可证禁止多租户 SaaS，元枢只学思路。

- `buildPortraitPrompt` 多了两段：**身体比例**（肩要宽、头肩比要好；体型按设定如实呈现，
  不要一律套"标准模特身材"）与**镜头**（85mm 等效、半身、胸口以上入画、轻微景深，
  不要广角畸变）。理由很具体：半身像最常翻的车就是"窄肩配大脑袋"，
  而"身形挺拔/气质出众/well presented"这类笼统赞美对模型等于没说，所以提示词里明确禁止。
- `buildStoryboardPrompt` 的人物设定纪律同步收紧：虚构人物要有强美感，但必须落到
  肩宽/头肩比与身高体型上，长相用**具体类比**（例：参考韩国男团门面的清冷感）而不是笼统形容词。
- 体检新增 `character-no-structure`：外貌里既没有肩宽/体型词时提示"半身像容易出窄肩配大脑袋"。
  它是 `info`、不报 warn；但要说清楚——`info` 会把体检总级别从 ok 抬到 info（和既有的
  `no-craft` 一样），不是"级别不变"。

测试：+3（真实角色提示词必带结构锚点与镜头；换装不换脸不丢；体检只给 info 不升级 warn）。
用户现有项目里的角色（林默/陈默）本来就写了体型词，所以这条提示在他们的项目里是安静的——
规则对，只是没话说。

**还没做**：出图效果的真实对比需要花一次出图额度，等用户点头后再跑（不是代码问题）。
## [2.41.1] - 2026-09-16

### 修复：界面上把 `**估计**` 当成字面量显示出来了

- 台词时间轴那句说明是 JSX 里的普通字符串，星号不会变粗体，真机上就是明晃晃的三个字
  `**估计**`。改成直接写「秒数是估计」。发 v2.41.0 时没点开看这一行，所以是补丁版本。

## [2.41.0] - 2026-09-16

### 新增：台词时间轴（按行给起止秒，改一句不会全篇乱）

- 引擎（`engine/story-lines.mjs`）：每句台词有稳定 id（对规范化后的文本取 sha1，形如 `ln-3f2a91`），
  所以「同一句话」在重写、重排之后仍然是同一行，而不是靠下标硬认。
- 时间轴按语速常量估算（3.5–5 字/秒，句内停顿 0.2s，句尾 0.45s，默认 4.25 字/秒）；
  改一句只会让它和它之后的锚点移动，前面的秒数不动——这是它和"整篇重排"的区别。
- 制作台「检查生成输入」里直接显示：`0.0–2.4s 林晚 你挡住了我的视线。 ln-3f2a91`，
  并写明秒数是估计。预览是只读的：不写盘、不建 run、不花钱。
- 共用的语速常量只有一份，前端不再自己实现一套，避免"体检说说不完、界面说还富余"。

## [2.40.0] - 2026-09-16

### 连续创作页视觉整理（你说"不好看"）
先量后改，不靠感觉。真机量测（同项目 13 段）基线：
状态条 124px、分镜时间线 **2216px**、编辑列 876px、本段结果列 424px、设定面板 1633px。

- **分镜时间线一行化（主要矛盾）**：每段把整段提示词当标题写 48 字，而它只有 200px 宽——
  一行只放得下 14 字，于是每段换行 3~4 行、7 段就是一面文字墙。
  改：正文截 14 字 + `nowrap/ellipsis`，全文进 `title`；`padding 14→8`、行高 `1.5→1.4`。
  **实测 2216 → 1186px（每段 170 → 91px，−46%）**。
- **按钮主次分层**：状态条上原来三个同权重按钮并排，眼睛没有落点。现在一个视口**只留一个
  `btn-primary`**，其余推荐动作折进「其他动作」；进度明细与胶囊说的是同一件事，折进 `title` 不再单独占一行。
- **对比时结果列跨整宽**：并排对比要在 424px 的窄列里塞 2~4 张大图 + 元数据表。
  用 `:has()` 让外层两列在对比时收成一列（`.story-editor-layout:has(.story-results.is-compare)`），
  退出对比自动恢复——不必为此把状态层层提上去。
- **长句标签改短**：镜头规格的「落幅 · 这一镜最后定格在哪」「承接 · 从上一镜的哪个落点接起」
  改回短标签，说明移进 `placeholder`，两行输入框顶边不再参差。
- **体检清单折叠**：默认只摆前 2 条，其余进「还有 N 条」——一串红字会把整块染红，也把"先改哪一条"淹掉。
- 量测脚本自身修了一个方法问题：加 `Network.setCacheDisabled` + 每次换查询串，
  避免量到浏览器缓存里的旧 bundle（重测后数字一致，确认此前不是缓存假象）。

**如实记一笔没做到的**：我原定状态条 ≤72px，实测停在 112px。原因是它本来就是三行
（胶囊 / 状态句 / 动作行），要去到 72 只能三行并一行，那是拿可读性换一个我拍脑袋定的数字——
所以我把那条目标作废，并在提交消息里写明理由，而不是硬凑。

**还没做的**：卡片分组留白、空态骨架（"加载中"仍是一行字）。**before/after 截图**存在
`D:\pi-workspace\tmp\visual-before\` 与 `visual-after\`，量测脚本 `visual-after.mjs` 与 before 同一套口径。

## [2.39.0] - 2026-09-16

### 片段时间轴：这支片子由哪几段、每段多长、用的是第几版
成片能拼了，但拼完就只剩一个文件——用户看不见"这支片子由哪几段、每段多长、当前用的是第几版"。
同一段生成过三版镜头之后，换版本只有一个下拉框，改完也说不清成片到底多少秒。
这一版把这三件事做成一个**片段时间轴**（照 Pavo 的 `timeline-delete` / `segment-history-download` 那套做法）：

- **一排缩略图**（`frontend/src/components/story/StoryTimeline.tsx:1`，新增 148 行）：视频用
  `<video preload="metadata" muted>` 只拉元数据取静帧，每段标出 `第 N 段`、时长、`可用 N 版`、`当前用第 N 版`。
  类名用 `story-film-timeline*` 而不是 `story-timeline*`——后者已经被左侧「分镜时间线」占着，
  同名会让两处的 `li`/`button`/`strong` 规则互相污染（真机核对时先撞上这个，页面上出现了两个同名根节点）。
  顺序就是**成片顺序**（默认与分镜顺序一致）；上移下移改过顺序后，与分镜序号不同的段会额外标出 `成片第 k 段`。
  排不进成片的段（整段不要 / 没有可用版本）跟在后面并写明原因，不凭空消失。
- **每段可展开＝它的历史版本**：列出 `filmPlan` 的全部候选，点一下「用这一版」只改 `picks`（成片顺序数据），
  **不重跑合成**。顺带修了一个老毛病：换版本以前是"删掉再追加到末尾"，用户调过顺序之后换个版本，
  那一段会悄悄跑到最后；现在原地替换（`frontend/src/components/story/StoryFilm.tsx:51`）。
- **单段导出**：走现成的 `downloadApiFile` 通路，文件名 `成片-第3段-v3.mp4`（段号 + 版本号）。
- **总时长 + 段数**：表头是 `进成片 4 段 · 分镜共 23 段（有可用版本 4 段）` + `总时长 20.8 秒`。
  「整段不要 / 加回来」沿用合成面板那套 `dropped`/`picks`，没有另造第二份排除状态。
- **时长是探出来的，不是编的**：产物记录里根本没有秒数，所以 `engine/story-film.mjs` 新增
  `parseDurationSeconds`（`:136`）/ `probeDurationSeconds`（`:152`）/ `cachedDurationSeconds`（`:166`）/
  `filmPlanWithDurations`（`:195`）：ffprobe 优先、没有就退回解析 `ffmpeg -i` 的 stderr；
  探不到就写"时长未知"，**不拿分镜里写的目标时长顶替**。按 `file+size+mtime` 缓存（文件重写即失效），
  且只为**已经在本地**的候选探测——外链不为一个时长去网络拉整支片子。
  探测要 spawn 进程，所以只有 `GET /api/story/projects/:id/film-plan?durations=1`（`server.mjs:1749`）才走这条路；
  不带参数的调用与返回**保持原样**（老字段语义没动，只是多了新字段）。
- **版本号由引擎给**（候选新增 `versionNo`，`engine/story-film.mjs:97`）：按这一段的**全部**版本（含失败的）
  从旧到新数，所以一段失败过两版、唯一能用的那版仍然是「第 3 版」——和「本段结果」里同一个号，
  不会一处在时间轴叫第 1 版、另一处在结果里叫第 3 版。

验证：`npm test` **1329/1329**（基线 1322 + 新增 7 条）；`frontend` 的 `npx tsc --noEmit` 干净。
真机核对（CDP，只读）：打开 `http://127.0.0.1:8787/?v=timeline#/story` 切到《制作台验收》，
`.story-film-timeline` 存在**且只有 1 个**（改名前撞上左侧「分镜时间线」的同名节点，这条断言就是为它加的）、
23 个 `.story-film-timeline-seg`（4 段进片 + 19 段如实标"还没有片段"）、4 个缩略图、4 个「导出这一段」入口，
表头 `进成片 4 段 · 分镜共 23 段（有可用版本 4 段）`、`总时长 20.8 秒`；
展开第 3 段（`beat-mu0t1nhe-1-0`，`可用 3 版`）列出第 3/2/1 版，点「第 3 段用第 1 版」后
`当前用第 3 版` → `当前用第 1 版`，**期间 0 条网络请求、0 条打到 `/film`**（换版本不会偷偷重跑合成）；
点「整段不要」总时长 `20.8 秒 → 15.6 秒`、这一段落到时间轴末尾并标出原因，点「加回来」复原回 `20.8 秒`。
截图：`D:\pi-workspace\tmp\timeline-verify.png`。服务重启核对：kill 8787 的旧进程（PID 46280）后
18 秒内 watchdog 拉起新进程（PID 41516），`GET /` 返回 200。
新增 `tests/unit/story-timeline.test.mjs`（契约测试）：时长解析与 ffprobe→ffmpeg 回退、只探本地文件、
合计只算探到的段、版本号连失败版本一起数、真 ffprobe 探到约 2 秒并吃缓存、编排层默认不带探测、
前端类名与单段导出入口都在。

## [2.38.1] - 2026-09-15

### 形象变体真机试跑（给《系统流》的林默加「战斗装束」）：抓到两个真问题
你让我跑，就真跑了——一跑就露出两处，都已修（其中一个还动了你的项目数据，已复原）：

- **新形象会顶掉基础定妆照**：老角色只有 `refImage`、没有 `looks`（这个功能之前生成的那张）。
  给这种角色加一张**新**形象时，它被当成"第一张"并回写了 `refImage`——
  林默的基础定妆照当场被「战斗装束」覆盖。现在：第一次加新形象会先把既有的那张
  **登记成「基础形象」**，只有基础形象才回写 `refImage`。
  你那边的数据我已经复原：从 `生成物/图片/2026-09-15/…201440…` 的 json 里认出那是林默那张
  （prompt 里 `name: 林默`），登记回「基础形象」并把 `refImage` 指回去；
  「战斗装束」留作第二张。**没有重新生成、没有额外花你的额度。**
- **形象名没能真的换掉衣服**：第一次生成的「战斗装束」还是一身灰卫衣——
  因为角色设定里写着"穿着宽大卫衣"，而提示词只说了"只换该变的部分"，模型听设定的话。
  现在这一张的提示词写死：**服装与装备以「战斗装束」为准，覆盖角色设定里的服装描述**
  （那段设定是基础形象，不是这一张）；脸/发型/体格仍须一致。

顺带在真项目上验了"提到哪张形象就挂哪张"（纯函数、只读）：
普通段落挂基础形象、出现「战斗装束」或「（战斗装束）」挂战斗装束、两个角色同时出现两张都挂。
新增一条回归测试锁住"老角色加新形象不许覆盖 refImage"。`npm test` **1322/1322**。

## [2.38.0] - 2026-09-15

### 修：连续创作页「排版乱了」——状态条挤进了两列网格的第一列
你报的排版问题，根因是我上一版自己埋的：`.story-layout` 是**两列网格**（时间线 200px | 编辑器），
我把流水线状态条当成了它的**第一个 grid 子元素**——于是状态条占掉了时间线那一列，
时间线、编辑器、结果整列被挤偏。现在状态条移到网格**外面**、横跨整页。
真机量过几何：状态条 `x=256 w=1104`（整宽），下面才是 `[时间线 200 | 主区 876]`，
主区里再分 `[编辑器 | 结果]`。

### 角色形象变体（基础形象 / 战斗装束），并把"第二张图"接进参考图挑选
一个角色只锁一张脸不够：换装段落没有对应形象图，模型只能靠文字猜，一致性立刻掉。

- 角色新增 `looks: [{id,name,refImage}]`：第一次生成定妆照时自动登记为「基础形象」，
  之后「新增形象」输入名字就建一张新的；**同名是重做那一张**，不会重复堆。
- 定妆照提示词带上「这一张形象 + 脸/发型/体格必须与角色设定完全一致」——换装不许换脸。
- 参考图挑选升级：这一段**提到哪张形象就挂哪张**（正文写 `@林默-战斗装束` 或直接出现「战斗装束」），
  没提到才挂基础形象。
- 顺手修一个真 bug：`assetRefsForShot` 把 `looks`（对象数组）当字符串拼，
  会拼出 `@林默-[object Object]`（新增的形象等于白生成）。
- 连续性体检与流水线状态机跟着算：有形象图就算有定妆照，不再误报"还没有定妆照"。
- 界面：设定面板的角色卡列出这个人的所有形象（「已添加形象 N/M」），点形象名就重做那一张。

验证：`npm test` **1322/1322**；真机截图核对排版已恢复（状态条整宽在上，下接时间线/编辑器/结果）。

## [2.37.0] - 2026-09-15

### 产物版本并排对比 + 「采用这一版」（Pavo 右栏那排"历史记录"的做法）
同一段生成好几版镜头之后，用户要的是**比出来**挑一版，而此前只能一版一版往下滚。
产物本来就在 `scene.outputs` 里全留着（只追加不覆盖），缺的是把它们摆到一起的手段。

- `StoryResults` 顶部新增**版本条**：所有版本的缩略图横排（视频出静帧），
  点选 2~4 版进入**并排对比**；每版的关键元数据（模型 / seed / 状态 / 生成时间 / 参数 / 耗时）
  对齐成一张对照表——差异一眼可见，不用来回滚。列表里勾「加入对比」也能选。
- **采用这一版**：写回 `beat.chosenRunId`；再点同一版就是取消采用（选择必须能撤销）。
- 服务端跟着走：`filmPlan` 的 `recommendedRunId` **优先用采用过的那一版**（用户挑过的别再替他挑），
  没有才退回"最新可用"；采用的那版已不可用（被删/外链失效）时同样退回最新，而不是给一个空推荐。
  候选里带 `chosen` 标记，`StoryFilm` 会显示「你采用的」而不是「最新」。
- 测试：`story-film-picks` 增一条（采用优先于最新、采用的那版不在候选里时退回最新）。

## [2.36.1] - 2026-09-15

### 修：状态条在首屏是空的（列表接口没带 flow）
在真 GUI 上截图核对时发现的：连续创作页**先拉列表、再选项目**，而 flow 只加在了详情接口上，
于是 `project.flow` 是 undefined，状态条直接不渲染——功能写好了、用户却一个都看不到。
现在 `GET /api/story/projects` 的每个项目也带 `flow`（纯函数算的，不产生额外 IO）。
真机截图确认：四步胶囊 + 「还有 4 段没有成品（共 7 段）」+ 推荐动作三枚 + 「1 个动作现在做不了」都出来了。

## [2.36.0] - 2026-09-15

### 流水线状态机：把"现在该干什么、为什么这个按钮点不了"交给服务端说
学自 Pavo 的 `flow`：`{ current_step, allowed_actions, blocked_actions[{action, reason_code, message}],
recommended_actions, failed_recovery_actions }`。元枢此前是反过来的——十几个按钮各自 `disabled`，
用户得自己猜"我现在该干到哪一步了""为什么合成成片是灰的"。

- 新增 `engine/story-flow.mjs`（纯函数）：四个阶段（剧本大纲 → 资产库 → 分镜成片 → 成片），
  **完成度只用真实产物判断**，不看"用户点过什么"；被挡住的动作必须带 `reason_code` + 人话，
  且理由要具体到数字（「可用视频片段只有 1 段，合成至少要 2 段」，不是"不可用"）。
- 失败与进行中优先：`failed_recovery_actions` 分别给 `retry_failed` / `check_running`；
  两者同时存在时 headline 两件都说——只报"进行中"会把失败藏起来。
- 外链产物会被认出来并推荐"拉到本地"（`http(s)` 且非本机才算外链，工作区自己的产物不算）。
- `GET /api/story/projects/:id` 现在随项目下发 `flow`（算出来的，不落盘）。
- 前端新增 `StoryFlowBar`：四步胶囊 + 一句人话总状态 + 推荐动作按钮 +
  「N 个动作现在做不了（点开看原因）」。**界面不自己写一份规则**，只渲染服务端给的三张表；
  动作按钮复用既有处理器，做完重新取一次项目（`patch`/`run` 的返回里没有 flow，
  不重取就会一直显示旧状态——"刚做完还提示你做同一件事"比没有状态条更让人困惑）。
- 测试：`tests/unit/story-flow.test.mjs` 6 条（阶段推进、理由必须带数字、失败/进行中优先、
  外链识别、每个动作都有 label+panel、推荐不能推荐被挡住的、产物在 outputAssets 里也要算数）。
- **真机验收又抓到一个**：产物的 url **不在 `run.url` 上**，而在 `run.outputAssets[].url`
  （一个项目 14 条 run 里 `run.url` 全是 undefined）。只看 `run.url` 会把"已经出片的段落"算成
  "没有成品"——状态条于是催你重做已经做好的段，还把「合成成片」误判成"可用片段 0 段"挡住。
  现在按 `outputAssets` 统计，`degraded`（片子到了但带降级说明）也算有成品：
  三个真实项目的段落完成度从「0/N」修正为「3/7」「4/23」，合成动作也正确放开了。

## [2.35.1] - 2026-09-15

### 修：景别前缀重复（真机验收里发现的）
拿真实项目跑 `run-preview` 一眼看到的：老分镜把景别写在内容开头（「镜头特写：林默趴在…」），
景别被提到句首后就重复了——编译出来是「**特写。镜头特写：**林默趴在…」。
现在只削**开头那一个**景别前缀（削完是空就还原，宁可重复也不吃内容）。
新增一条测试；`npm test` **1315/1315**。

## [2.35.0] - 2026-09-15

### 把"人家的镜头提示词"学进来：镜头提示词编译器（`engine/story-shot-prompt.mjs`）
上一轮拿 Pavo 的真实产物对照出的结论是：**同一个视频模型，出片像不像电影，差在发过去的提示词**。
它一个 4 秒镜头是「全景，俯拍斜角机位。画面采用…柔和自然光，呈现温暖复古的色调…具有 90 年代
写实电影质感与轻微的胶片颗粒，使用 35mm Panavision 镜头。…镜头缓缓推近。**落幅定格在**她落寞
无助的侧脸。」，下一镜开头写「承接上一个镜头…的落点」；而我们此前只把 `beat.prompt`（一段散文）发出去，
缺景别、机位、光线、色调、质感、运镜、**落幅**、**承接**这八样。

新增 `engine/story-shot-prompt.mjs`：

- **18 条风格库**（`code` / 中文名 / 别名都认，认不出来返回 `null`——**绝不替你选风格**），
  每条都落在"用什么镜头、什么颗粒、什么光比"上，不是形容词堆砌；
- `compileShotPrompt()` 按**固定顺序**编译成一条散文（视频模型吃的是一段话，不是带标签的规格表）：
  景别+机位 → 光线/色调/质感 → 场景与人物（含 `@资产` 引用）→ 运镜 → **落幅** → **承接**；
  另起短行给【台词】【时长】【资产引用】【全局约束】【必须避免】；
- **老项目也受益**：没有结构化字段时，从散文里**认**出景别/机位/运镜/落幅（`inferShotFromProse`）；
  **认不出来就不写**，绝不编造一个不存在的机位；
- 全局约束按类型分：视频怕"字幕/静音段"，图片怕"画面里冒出文字"；
- `@资产` 引用只挂**名字真的出现在这一段里**的资产，带形象变体（`@林晚-基础形象`），
  并标明**有没有参考图**（没挂图的会写"（未挂参考图）"，别让人以为挂了）。

接线（三处，缺一处这套东西就是空转）：

- `engine/story-prompts.mjs`：镜头规格块放在提示词**最前面**（上游对开头的权重最高），
  原有的 `## 必须避免` / 台词 / 素材等块一个不少；
- `engine/story-orchestrator.mjs`：`buildRunPlan` 为画面/视频编译，**预览与实跑共用同一份**，
  所以点「检查生成输入」看到的就是真正发出去的那段话；
- `engine/story-assist.mjs`：分镜提示词改为要求模型把镜头语言**拆成字段**
  （景别/机位/运镜/光线/色调/落幅/承接 + 秒数），解析器新增白名单 `cleanShotFields`，
  并让时长落到 `params.seconds`；`一键分镜`与`原著改编`两条写回路径都带上它们；
- 前端 `StoryWorkbench`：新增「镜头规格」六格可编辑（景别/机位/运镜/光线 + 落幅/承接），
  落幅那格的提示写着"不写，剪起来就是跳的"。

验证：`npm test` **1314/1314 通过**（新增 `tests/unit/story-shot-prompt.test.mjs` 8 条，
含"不许编造没写的字段""老项目从散文里认得出落幅""@资产只挂真出场的"）；
`story-workflow-params` 增一条断言：`## 镜头规格` 必须排在 `## 必须避免` 之前。

## [2.34.0] - 2026-09-15

### 拿别人家的成品剧本做对照：补上「重复检测」，修掉动作行被当成说话人
你给的那份从别的工具（Pavo）导出的 10 集剧本，我先用元枢自己的机检跑了一遍，跑出两个真问题：

- **"生成在原地打转"没有任何环节在报**：那份剧本**第 5 集与第 6 集逐字相同**
  （5-gram 重合 100%），全剧 **24% 的正文段落跨集重复**（同一段"记忆画面"在第 4/5/6 集各出现一次）。
  这不是文笔问题，是长文生成卡住后的退化——而且纯算力就能发现，之前却没人说。
  → `engine/story-craft.mjs` 新增 `repeatCheck()`（单元间 5-gram 重合度 + 段落逐字重复 + 占比），
  接进 `engine/story-lint.mjs` 体检（`repeat-unit` / `repeat-paragraph` 两条 warn），
  体检 summary 增加 `repeatParagraphs` / `repeatRatio`。
- **动作行被当成了说话人**：那份剧本用 `△ 记忆画面：年轻的林默穿着工程服…` 写动作，
  旧解析器把"△ 记忆画面"当作说话人，还把后面所有无冒号的动作行都并进这一句，
  整段动作变成一句 **190 字的"台词"**——我第一次跑出来的 258 条语速 warn 里一多半是这个误报。
  → `engine/story-screenplay.mjs`：`△ ▲ ○ ● ◇ ◆ ・ · ※` 一律当动作行，并清掉上一句说话人
  （否则下一行没有冒号的动作还是会被算进上一句台词里）。
  这条对"用户从别的工具导剧本进来"是真需求。

顺手记下、**还没做**的：那份剧本里"异常值"出现了 40% 和 75% 两个阈值、
"区域稳定性"从 72% 跳到 15% 再到"归零"——**数值/设定冲突是纯规则可查的，元枢还没有这一项**。

验证：`npm test` **1306/1306 通过**（原 1300 + 重复检测 5 条 + 动作行 1 条）。
对照报告：`工程/_research/pavo-短剧设计/剧本-机器体检.md`。

## [2.33.0] - 2026-09-15

### 顺手挖出的真问题：你的技能写好了，但**主引擎根本看不见它们**
打磨完照着"技能到底会不会被用上"真跑了一遍（建临时会话 → 走真服务 → 读 SSE → 删会话），
结果两条任务**都没激活技能**。翻模型的思考记录才看明白：

- 主引擎是 `pi`（兼容适配器 / Pi SDK），它扫描技能的根只有两个 —— 会话工作目录的
  `cwd/skills`（你的技能商店 `D:\pi-workspace\skills`）和 `~/.agents/skills`。
  **仓库根的内置技能 `D:\pi-web\skills` 不在它任何一条扫描路径上**：
  「元枢技能」页面上列得出来（那个走的是另一条读取路径 `listBuiltinSkills()`），
  模型却看不见。实测原话："I don't see a `prompt-architect` skill in the list"，
  然后转身去读了 SDK 那份目录里的 `multi-agent-meeting`。
- `activate_skill` 工具一直好好注册着（`engine/session-manager.mjs:322`），
  但目录里没有名字，这个工具就永远等不到调用 —— 上一轮所有技能打磨，在这条通道上等于白做。
- 连带的：`engine/context-loader.mjs` 里的 `makeLoader()`（本来是给 SDK 会话追加技能目录的）
  **是死代码，全仓库没有任何调用点**。

修法（`server.mjs` `handleChat` + `engine/yuanshu-protocol.mjs`）：

- 每个会话**注入一次**「元枢内置技能库」目录（`skillCatalogSent` 记账，不重复灌上下文），
  走的是和情绪/时间/PPT 护栏同一条 `nextTurn` 通道，不污染会话历史；
- 每轮再做一次技能匹配，命中就补一句"本轮任务可能匹配：xxx"，把候选点名给模型；
- 匹配器补了两族"概念名"规则：实测 `用提示词架构师的办法…` 原本**零命中**、
  `用多AI角色扮演系统…` 命中的是无关技能，现在分别稳定命中 `prompt-architect` / `multi-ai-roleplay`。

验证（改完重启服务再跑）：问"你上下文里有没有元枢内置技能库"，模型**逐行复述出全部 17 个技能名**。
新增 `tests/unit/skill-catalog-injection.test.mjs`（3 条）：目录文本能用（每条注入 ≤90 字且触发语在 90 字内）、
匹配器认得这两族、`handleChat` 真的走"每会话一次 + nextTurn"注入。
顺带修正 `diagram-design` 的 description：118 字时"use when…"正好被 90 字截口切掉，现已压到 89 字。

### 继续打磨你写的那些技能：17 份 description 全部进 120 字 + 3 份源文抽出可读文本 + 全技能契约测试
上一轮只查了万像两份。这一轮把 `skills/` 下**全部 17 个技能**按同一把尺子过了一遍，问题还是老几样：

- **description 超过 120 字，超出部分等于没写**：`engine/context-loader.mjs:181` 只把前 120 字
  塞进技能目录，那是模型唯一能看见的一行。实测有 9 个在 121–220 字之间（`seedance-25` 713、
  `diagram-design` 579、`video-prompt-library` 220、`gpt-image-2` 191…）。现在 17 份**全部 ≤120 字
  且触发语前置**（"当用户…"/"Use when…"）。
- **三份技能指向读不到的东西**：`multi-ai-roleplay` / `prompt-architect` / `shortform-genesis`
  都只给了 `.docx`（Word 二进制，`read` 工具读不了），其中两份还引用了**根本不存在**的抽取文本。
  → 新增 `scripts/extract-docx.mjs` 抽出 `src_full.txt`（3926 / 65 / 750 行），
  `scripts/gen-skill-index.mjs` 生成 `INDEX.md`（章 → 行号区间）；SKILL.md 的「参考文档」段改成
  **先查索引、按行号 read**，`.docx` 降级为"只在逐字核对措辞时看"。
- **索引生成器把"重号章"当成样例藏了**：平面文档第 89 行和第 116 行都是"第四章"
  （全域色彩美学引擎 / 设计生成基本范式与核心模板库），旧逻辑把第二个丢进"非序列标题"，
  缺陷被"多半是正文里的样例"这句话盖掉。现在**重号章既进章节表（行号区间才对）又报"章号重复"**。
- **死链**：`gpt-image-2` 的 7 个 `cases/*.md` 是外部库
  （`D:/pi-workspace/工程/awesome-gpt-image-2-prompts/`）里的相对路径——改成绝对路径，
  并写明"没 clone 到本地时就别去 read"；`diagram-design` 多处写 `style-guide.md` 却没带 `references/`
  目录，还有 3 处让 agent 去跑**本机不存在**的 `verify-geometry.py` / `verify-motion.py`
  （上游脚本没随技能过来）→ 改指随技能发布的 `scripts/self_check.py`。
- **"运行时产物"要说清**：`novel-forge-v10` / `novel-writing-pipeline` 里的
  `current_state.json`、`canon.md`、`souls/<角色>.json` 是**你小说工程目录里**的文件，
  不是技能自带的——现在都标了"运行时产物，别去 `skills/` 下找"。
- **契约测试从"只看万像两个"扩到全部 17 个**：`tests/unit/wanxiang-skills.test.mjs`
  → `tests/unit/skills-contract.test.mjs`（11 条），新增三条最值钱的：
  **加载器必须真的读到每个技能的 description**（CRLF/引号/frontmatter 坏掉就红——实测
  `diagram-design`、`seedance-25` 整份是 CRLF，旧正则只认 `\n`，这两个技能的说明一直是正文第一行）、
  **引用不到的文件必须写明是运行时产物**、**原文 docx 不得被抽取文本顶掉**。
- 加载器容 CRLF（`engine/context-loader.mjs:177`），两个 CRLF 技能归一为 LF。

**仍未做（跟你报备过，没动你的材料）**：`wx_full.txt` 里 35 处【旧版补充】块**只标出位置，
没有物理搬动**——搬动等于改写你的合并稿；`万像平面` 的第 17/18 章在 `D:\遗产` 里找不到正文
（现有 16 章正文 / 17 个标题，第 4 章重号）。

验证：`npm test` **1300/1300 通过**（原 1292 + 新增 8：技能契约 11 条替换旧的 6 条 + 目录注入 3 条）；
另外真机跑了两轮技能激活验收（临时会话，跑完删除），并复述出全部 17 个内置技能名。

## [2.32.0] - 2026-09-15

### 打磨：你写的那两个万像技能（`skills/wanxiang-portrait` / `wanxiang-design`）
先按"能不能被执行"的标准逐条查了一遍，问题不在文笔，在**指令做不到**：

- **写着"复杂需求必须读取完整文档"，但没法执行**：人物源的 `wx_full.txt` 有 6652 行
  （13.3 万字），整份读进来十几万 token，等于没读；平面源更糟——它指向一个 **.docx**，
  而 `read` 工具**读不了二进制**，那条强制要求从一开始就是空话。
  → 新增 `skills/scripts/gen-index.mjs`，**从源文机械生成**两份 `INDEX.md`（章 → 行号区间 + 每章首句）。
  实测：只读第五章（5234 字）就拿到证件照全部参数，**省下 96% 的上下文**。
  平面那份同时抽出 `wx_design_full.txt`（1553 行可读文本，docx 保留为原文）。
- **数字是错的**：平面技能写着"18 章 / 8.2 万字"，实测是 **17 个章标题、16 个编号、3.8 万字**；
  而且**第 4 章重号**（第 89 行"全域色彩美学引擎"、第 116 行"设计生成基本范式与核心模板库"）——
  引用时必须带行号，否则指不清是哪一章。索引里如实写明。
- **人物源文的合并缺陷被藏住了**：35 处【旧版补充】块被放在了**下一个章标题之前**
  （读到某一章会觉得"顺序怪"），另有 3 处断句残片（丢掉前半句的孤行）。索引把位置逐行标出并写明"不要引用"。
- **description 超长会被截断**：`engine/context-loader.mjs:178` 只取前 120 字进技能目录，
  原文 145/161 字——触发语虽然活下来了，出处却被切断。现在两份都压到 120 字内，触发语前置。
- **补上缺的东西**：两份 SKILL.md 都加了**输出契约**（五要素齐全 / 符号只用那 7 个 / 不留占位符 /
  必带负面词）、**什么时候必须去读原文**的对照表（带行号）、**常见失败**、
  以及**本机出图通路**——原文讲的 MJ/SD/即梦/Imagen 是**提示词写法**的参考，
  本机实际能调的是 `agnes-image-*`，走 `generate_image` / 对话里说"画一张"。
- 新增 `tests/unit/wanxiang-skills.test.mjs`（6 条）：description ≤120 且触发语在前 120、
  引用的仓库内路径必须存在、**`INDEX.md` 必须与生成器现算结果逐字一致**（改了源文不重跑 → 红灯）、
  索引行号必须真的落在章节标题上、源文缺陷必须如实声明、旧的不实数字不得回潮。

### 验证（照技能真跑一次）
按索引定位第五章 → 取参数 → 按输出契约组装提示词 → 真出图（`agnes-image-2.5-flash`，29 秒）：
产物**四角亮度 246–250（纯白背景达标）、中心亮度 188（主体入画）**，符合技能里"纯白 L≥90"的硬指标。
顺带记下一个实测坑并写进技能：本机出图是 **1024×1024 方图**，而证件照要 35×45mm 竖幅、
商务照要 4×5——**规格靠出图后裁剪**，别指望一句 `350dpi` 写出来。

## [2.31.0] - 2026-09-15

### 新增：台词专科 + 深度构思（用户："人物场景搭上了，对话和构思还是不行"）
去 GitHub 把同行的东西翻了一遍，真正有用的**不是更长的提示词，是把手艺变成能逐条过的规则，
而且能机检的先机检**：

- [manju-laoli-skill](https://github.com/lixiaoxiao9888-create/manju-laoli-skill)：
  **台词语速自检**（标准 3.5~5 字/秒、慢速/快速档、**标点不计入字数**、单句 >24 字强制拆镜、
  快档连续不超 3 秒、金句后留 0.3~0.8s）+ **台词七维诊断**（辨识度/潜台词/冲突推进力/类型语感/
  信息效率/节奏/金句潜力，输出要求"每条改写 ≥3 条维度依据"）。
- [short-drama-factory](https://github.com/lixiaoxiao9888-create/short-drama-factory)：
  **情绪契约**（全剧不变的是那口气，矛盾只是单元载具）、**单条矛盾硬帽 20~30 集**、
  换单元的**三缝合一**、**四账台账**（伏笔/人物/道具/规则）、单集机检（前 3 秒冲突 / 断章存在）。
- [drama-skills](https://github.com/zenstory-ai/drama-skills)（漫剧工作室产线，上千项目）：
  **因果节拍**（每场必须改变信息/权力/关系/情绪/物理/风险之一）、"刻意把确认放在生产之前"。
- [Dramatron](https://github.com/google-deepmind/dramatron)：**台词是独立的一层**，不和情节揉在一起生成。

落地成两块，都是"机检先上、模型只负责改写和给方案"：

- **台词专科**（`engine/story-craft.mjs` + `StoryDialogue.tsx`）：
  ① **体检不花模型钱**——字数（只数可发音字符）、需要的秒数（含标点停顿预算）、
     哪句超 24 字要拆几镜、这句在不在这一段的时长预算内、有没有连续快语超 3 秒、整段有没有短句气口；
  ② 七维里能机检的那几维：**0 层直白**（"我很愤怒，我要杀了你"）、**自报家门**（"我是你师兄"）、
     寒暄附和、**古装出现现代词**、说话人句子长短无差别（语言指纹不清）；
  ③ **诊断才调模型**：把机检事实一并喂进去，逐句改写并要求每条 ≥3 条维度依据——
     只给一句"更自然了"的条目会被标成"依据不足"；
  ④ **采纳只替换那一句**，同段其它台词一个字不动。
  机检边界写在界面上：**金句潜力机检查不了，别把体检通过当成台词好**。
- **深度构思**（`StoryCraft.tsx`）：情绪契约（含"绝不违背"清单）、人物四件套
  （欲望/秘密/弧光/**语言指纹**+功能位）、矛盾单元（含硬帽与接缝三合一：火种/对手/弧光）、
  分集地图（目标 + 前 3 秒冲突 + 断章钩子）、因果节拍（因此/但是，标的改变项）、四账台账。
  **草稿不落盘**，确认后才保存；分集地图能**一键建成集**（目标与钩子写进集里，已存在的集不覆盖）。
  机检：伏笔没写回收集 / 回收集不晚于埋设集 / 单元硬帽超 30 集 / 每集缺钩子 / "然后"式并列 /
  **地图覆盖不全**（真机：要 60 集，模型只给了 3 集，当时体检一声不吭）。
- 台词体检并进**连续性体检**（不花钱就能看到"这一句 37 字，最快也要 7.4 秒，而这段只有 5 秒"）；
  缺深度构思只算 **advisory**，不把体检总级别染黄——总级别的含义是"现在能不能生成"。
- 这套规则也写进了**内置方法包**（竖屏短剧 / 小说改编），一键分镜与原著改编会带着它走。

### 验证（真模型 + 真界面）
- 刻意喂一份"外行稿"（台词直白、在解释情绪、46 字长句）：体检零成本指出 5 处问题；
  真模型诊断把「我很愤怒，我要杀了你！」改成「师兄，三年了，刀还是冷的。」、
  把说教句改成「雨停了吗？」、把 46 字长句拆成两句并给出**逐镜拆镜方案**（含每镜字数），
  每条都给了 3 条维度依据。
- 深度构思真模型 145 秒产出：5 条"绝不违背"（含"反转必须建立在前文伏笔上"）、4 个人物各有语言指纹、
  3 个矛盾单元（硬帽 25/25/10）带接缝三合一、分集地图的钩子具体到"车窗倒影里站着一个人却没有脸"、
  伏笔账（埋 1→收 25）与因果节拍（因此/但是）。
- 真浏览器：两个面板渲染正确、控制台零错误；**已保存的构思也会当场体检**（"1 集 / 计划 12 集"、
  "伏笔没写回收集"两条都报了出来——这是真机上抓到并当场补的缺口）。
- 新增 9 条手艺契约 + 2 条界面契约；全量 **1284/1284**。探针项目与脚本全部清理。

## [2.30.0] - 2026-09-15

### 新增：删项目；选项目不再用下拉框
用户的原话是「加个项目删除吧，或者项目那款不用下拉框了」。两件一起做了，因为它们是同一个问题：
项目一多，`<select>` 里只看得见标题——哪个有成片、哪个是随手建的草稿、哪个是上周的，全看不出来，
更不能删。

- **项目架**（`StoryProjects.tsx`）取代下拉框：每个项目一行，显示**标题 + 规模（场/段/集/成片）+
  最近改动时间 + 梗概**，行内「打开」「删除」；当前项目高亮并带「当前」徽标。
  刻意**点「打开」才切换**——翻一下列表就把正在写的一段换掉，是最烦人的那种"顺手"。
- **删项目**（`DELETE /api/story/projects/:id`）按"克制"来设计：
  - **先留副本**到 `story-projects/.trash/<id>.<时间>.json`（`.trash` 是子目录，不会出现在项目列表里）。
    删整部戏是不可逆的，副本成本只有几 KB；
  - **产物文件默认不动**（还在工作区里，能从「资产」找到），要一起清得显式勾选；
    勾了也只清**没有被别的项目引用**的，被引用的保留并说清被谁挡住；
  - 确认块里写清会删掉什么、文件会怎样、副本在哪。

### 修复：删完列表那一行还在
真机点验时抓到的：删掉项目之后弹了「已删除」，**项目架里那一行却还在**（API 已经删了，界面没跟上）。
现在按 id 从本地列表里直接剔除，而不是重新拉一遍——重新拉会走到 `load()` 的
"找不到当前项目就选第一个"分支，把用户顺手带到别的项目上。

### 修复：「当前」徽标的字看不见（而且我一开始改错了方向）
截图里那个蓝色胶囊里的「当前」是看不见的。第一反应是颜色写错了（写的是 `var(--pi-bg1)`），
改成 `#fff` 后**计算样式说白色、放大截图看着仍是深色**。最后用像素定案：
取胶囊底色与字形区的平均亮度——**底色 91、字形区 190**（连拍两次一致），字确实是白的。
真正的病根是选择器权重：徽标是个 `<span>`，被 `.story-project-main span { color: var(--pi-dim2) }`
盖掉了（同权重、后者在后）。修成 `.story-project-main .story-project-badge` 才生效。
那张"看着还是深色"的放大图是 `deviceScaleFactor:3` × `clip.scale:4` 双重缩放出来的假象——
**截图会骗人，像素不会**：以后遇到"样式改了但看着没变"，先量像素再下结论。

### 验证（真机）
- 真浏览器：旧下拉框确实不存在了；项目架列出 5 行，含规模、时间、当前徽标；
  点「打开」切到 A（顶栏随之变成「当前「项目架A」」）；点 B 的「删除」→ 确认块（含"连产物文件一起删"勾选）
  → 确认后**列表里那一行立刻消失、计数从 5 变 4**，与 API 列表一致；提示文案准确；
  `.trash/` 里确有 B 的副本、原记录确实不在；用户自己的 3 个项目一个没动；控制台零错误。
- 新增 4 条契约（删项目留副本+默认不碰文件、deleteFiles 只清没被引用的、不碰别的项目的产物、
  界面契约：下拉框必须真的撤掉）；全量 **1275/1275**。

## [2.29.0] - 2026-09-15

### 修复：「智能填充返回的内容不是有效 JSON」
用户真机上撞到的报错。根因是**同一个文件里两套宽容度**：`parseStoryboard`（一键分镜）与
`parseAdapt`（原著改编）早就从"先复述一段再给 JSON"里把答案抠出来了，而
`parseStoryAssist`（智能填充）还是一次 `JSON.parse` 定生死——模型多写一句
"好的，我来补一份草稿"就整条路断掉，而错误信息里一条线索都没有。

- **解析对齐宽容度**：围栏、前后解释、包一层 `assist/draft/data/result`、
  先复述提示词里的「已有状态」再给答案，全部能解析。
  候选的挑选顺序有个讲究：**优先取带 `scene`/`beat` 的那一个**——
  被复述的「已有设定」里也有 characters/locations（看名字完全像一份合格回答），
  唯一的区别是它没有 scene/beat（bible 里根本没这两个字段）。只按"有没有内容"挑，
  就会把复述当草稿（写这条时实测即踩到）。
- **"解析成功"要有底线**：模型回 `{}` 或 `{"error":"…"}` 不再算成功——
  以前会一路变成一份空草稿，用户点了「采用」才发现是空的。
- **先重试同一个模型，再换模型**：解析失败时把"只输出那一个 JSON 对象、不要开场白、
  不要代码块"说到不能再直白，再问一次；两次都不行才换备选模型
  （换模型往往更慢更贵，不该是第一反应）。重试过就在响应里如实标 `retried`。
- **错误信息要能排障**：说清"找不到 JSON / JSON 里没有可用内容"、试过几个候选、顶层键、
  原文开头，并给出下一步（换构思模型）；模型侧问题回 **502**，不再混成 400 的"用户输入错"。
  原文同时打进服务端日志，形状不符还是模型跑偏一眼可分。

### 验证（真机 + 用户的那个项目）
- 用**用户自己那个报错留下的项目**（`648a813d…`，只有占位骨架、没有设定）和**同一个想法**
  复验：HTTP 200、19 秒、`agnes-3.0-flash` 一次成功，给出 2 个人物、1 个地点、场景
  「觉醒仪式上的异类」、画面段落与台词；并核对项目文件前后一致（智能填充只出草稿、不落盘）。
- 另跑两个新想法（常规 / 抽象）：10 秒与 14 秒都成功，草稿还能走真实 PATCH 存下来。
- 新增 5 条契约（宽容度 4 种形状 + 挑候选的规则 + 空的 JSON 不算成功 + 报错线索 +
  重试/换模型的顺序 + 502）；全量 **1271/1271**。

## [2.28.0] - 2026-09-15

### 修复：参考图（定妆照 / 场景 / 道具）也受本地化契约约束
上一轮把"外链先搞到本地"只补到了成片合成与产出上，参考图这一块还是同一个坑——
而参考图是**长期复用的锚点**，挂在会过期的外站链接上，几天后人物一致性就悄悄失效，
界面上却一直显示"已生成"。三处一起补：

- **全项目补下载**：新增 `POST /localize`（`scope: refs | runs | all`），一次把还挂在外站的
  参考图与每一次生成的产出都拉到本地并写回（bible 条目 / run.outputAssets）。
  只补下载、**不重新生成**——产物还在，重生成要多花一次钱。已经在本地的不重下（幂等），
  **全下完了才清掉"没能存到本地"那条降级说明**（留着就是假话）。
  段落挂载的素材刻意不落盘（它引用的是别的工作台的产物，再存一份只是重复占地方），
  但返回里**明说**这件事——"没做"和"忘了做"看起来一样。
- **入库路径本身也说真话**：`generateAssetRef` 此前不把 `localizeError` 带出来，
  于是"参考图没落到本地"和"落好了"在界面上长得一模一样。现在返回 `localizeError` + 一句
  note（说清会过期、并指向补下载入口）。
- **界面**：参考图卡片上标「外链 · 会过期」；设定面板顶部在有外链产物时出现
  「把外站的产物拉到本地」，并说明覆盖范围（参考图与产出都算）。

### 验证（真外链 + 真界面）
- 真 https 参考图（`img_girl.jpg` 36KB / `lamp.jpg` 653B）：补下载后两条都变成本地文件
  （磁盘上确有 36KB / 1KB），第二次补下载 0 项（幂等）。
- 真浏览器点一遍：未补下载时设定面板显示「有 1 个产物还挂在外站临时链接上（会过期）」+
  卡片徽标「外链 · 会过期」；点「把外站的产物拉到本地」→ **1 秒后** API 里定妆照已是本地地址、
  文件 36KB、`<img>` 正常显示；提示块与徽标同时消失；提示文案为
  「已把 1 个外站产物下载到本地并写回项目——外站链接失效也不影响了。（挂载素材不重复落盘：…）」。
  全程控制台零错误。
- 新增 6 条契约；全量 **1266/1266**。探针项目与它拉下来的产物文件全部删除，
  你自己的产物一个没动。

## [2.27.0] - 2026-09-15

### 修复：外链产物没有"优先搞到本地"——这是本地化契约（docs/NAMING.md 第三节）
用户指出来的：**外站产物必须先下载到本地再入库**，这条早就写在契约里，
我却在挑片段时把外链判成"拼不进去"——等于把契约做丢了。改法不是"允许外链"，而是**先把它搞到本地**：

- **合成成片时**：每一段先看本地有没有文件；没有就**当场下载到本地**（`saveArtifact`，含 data URL
  解码），下载完再拼。下载成功就把地址**写回项目**、并清掉那条"没能存到本地"的降级说明
  （已经落盘了还留着旧说法就是假话）；下一次合成不会再重下。
- **候选清单**：本地已有 vs 可下载的外链分开标（`exists` / `external` / `localable`），
  外链版本从此**算可用、也能被推荐**——它的错是"还没下"，不是"拼不了"。
- **没挑片段的老路径同样修了**：以前它只收集本地已有的文件，外链片段是**静默漏掉**的
  （少拼一段而一字不提，比报错难查得多）。现在两条路走同一段逻辑，漏掉的都会进 `skipped` 带原因。
- **只有两种情况才真拼不了**，而且都说清原因：落盘实现没接入（配置问题）、下载失败
  （SSRF 守卫拦内网地址、上游 404/502、超过 50MB——`saveArtifact` 会给出 reason）。
- **新增补下载入口**：某一版入库时下载失败留下的外站临时链接，此前除了"重新生成一次"
  （再花一次钱）没有任何补救办法。现在版本卡片上有「外链产物 · 下载到本地」，
  只补下载、不重新生成；失败了保留原链接与原说明，成功了才改地址。

### 验证（这一条专门用真外链跑）
- 一次性项目里放两段外链：一个 `data:` URL（走不需要网络的解码分支）、
  一个**真 https**（`https://www.w3schools.com/html/mov_bbb.mp4`，788KB，公开测试片）。
  SSRF 守卫会（正确地）拦回环地址，所以用本地服务器测等于测不到真实下载路径。
- 合成前候选：两段都标 `external: true, localable: true`、各推荐了外链版本（旧代码这里是"可用 0"）。
- 合成（3 秒）：`localized` 两条，工作区里真的落下 2KB 与 **770KB** 两个文件；
  两条产出的地址都换成 `/api/ws/file?...`，那条"没能存到本地"的降级说明被清掉。
- **第二次合成 `localized: 0`**——已经落本地了，不重复下载。
- 探针项目、临时目录与它生成的产物文件全部删除（`生成物/视频/2026-09-15` 里你自己的产物一个没动）。
- 新增 3 条契约（外链先下载再拼并写回、下载失败如实报因、补下载幂等）；全量 **1261/1261**。

## [2.26.0] - 2026-09-15

### 新增：删掉不要的那几版 + 合成成片时挑片段
用户的原话是「有些剧情片段我生成了好几个镜头了」——同一段生成好几版之后，
此前只能全留着、合成时也只能"按分镜顺序自动拼"，等于把挑片子的权利拿走了。

- **删掉某一版产出**（`POST /run-delete`）：记录必删，但**删文件必须克制**，三条边界写在代码里：
  - 文件还被别处引用（别的段、**别的项目**、设定里的参考图、段落挂的素材）→ **只移除记录、保留文件**，
    并说清是被谁挡住的。生成物目录是所有项目共享的，只查当前项目就会把别人还指着的那张图删掉；
  - 还在排队的版本**默认拒绝**（上游那个任务还在跑，删了记录就永远收不回来），要删得点「强制删除」；
  - 外链与 data URL **永不碰磁盘**。
  界面上是不可逆操作该有的样子：先出一个确认块，里面对「文件留不留」给出选项，并写明后果。
- **合成成片时挑片段**（`GET /film-plan` + `POST /film` 带 `clips`）：
  每一段的下拉里列出这一段生成过的**每一版**（第几版、seed、状态），可以换版本、整段不要、上移下移调顺序
  （顺序就是成片里的先后）。**默认值仍是原来那套**（每段取最新可用的一版、按分镜顺序），不改也能一键合成。
  挑不出来的（文件被移走、这一版刚被删、还是排队中）**逐条说明原因**，并记进 `skipped`——
  少拼一段而没有任何提示，比直接报错难查得多。
  成片记录里从此带上 `picks`（这一版成片用的是**哪一版的镜头**），刷新后仍能对得上。

### 修复：界面上的字面星号
顺着上一条检查时发现：`**强调**` 写在 JSX 文本里是**字面星号**，不是加粗——界面上真的显示了 `**工艺**`、
`**没有真机打开验证过**` 这种字样（分镜/剧本/配方几处都有，本次一并改成 `<strong>` 或「」）。
这类问题只有在界面上看一眼才会发现。

### 验证（真机）
这一轮的真机装置做到了"可核对的证据"，而不只是"点了一下"：
- 用 ffmpeg lavfi 现造四段纯色 1 秒片子（红/蓝 = 第 1 段两版，绿/黄 = 第 2 段两版），
  在真浏览器里挑「第 1 段用蓝色（第 2 版）、第 2 段用绿色（第 1 版）」再整体倒序 → 合成。
  成片记录 `picks` 与所选一致，**抽帧核对主色：第 0.5 秒是绿、第 1.5 秒是蓝**——
  顺序与版本是真的按挑的来，不靠界面上的一句话。
- 删除：界面出确认块（含「文件留不留」勾选）→ 确认后该段从 2 版变 1 版、提示「文件也删了 1 个」、
  API 核对一致；跨项目引用保护另用探针单独验证（引用方项目还指着时，文件被保留并说明被谁挡住）。
- 全程控制台零错误；一次性项目与探针素材用完即删，用户真实项目未动。
- 新增 12 条契约；全量 **1258/1258**。

## [2.25.0] - 2026-09-15

### 修复：真机跑出来的三个真问题（其中一个是上一版引入的回归）
上一版交付后只做了"构建产物里有这段字符串"的核对，没有真的点一遍界面、真的跑一次生成。
这轮装了零依赖的 CDP 驱动（系统 Chrome + Node 内置 WebSocket）把界面点了一遍、真花额度跑了批量，
结果打出三件单测永远发现不了的事：

- **seed 越界，每一张画面都被上游 400 拒掉。** 上一版"seed 真的上送"改对了方向，
  但 `randomSeed()` 掷的是 `0..2^31-1`，而 Agnes 图像接口的原文要求是
  `seed must be between -1 and 999`。于是真机上**两段画面全部失败**，界面上只有一句
  "图像模型未返回图片"。现在：随机 seed 落在 `[0, 999]`，用户填的越界值**夹进区间并如实告知**
  （`run.seed` 记的是实际发出去的那个——不然"填 123456 能复现"就是假话），
  变体 seed 到顶绕回开头（999+变体数会直接换一个 400）。
  同时改掉一条**把 bug 写进断言的旧测试**：它断言的正是 `[1000, 1001, 1002]` 这个上游不收的范围。
- **图像失败被吞成 `return null`。** `generateImage` 原先 `if (!r.ok) return null`，
  于是"Key 无效 / 模型名不对 / 参数不认 / 被限流"四种原因在界面上长得一模一样，用户没法处理、
  我也没法排查。现在带上**状态码与响应体**；并且 400/401/403/429 **不再换地址重试**
  （那是请求或账号的问题，换 endpoint 一样被拒，还把那句真正的错误挤出错误信息）。
- **上游"忙"被当成"拒绝"。** 真机撞到 `video_queue_full 503 — video queue is full, please retry later`——
  上游明明让我们稍后再试，实现却把它当终局失败报给用户。现在瞬时错误退避重试两次
  （2s/5s），**计费/权限/参数类错误一次就返回**；重试过就在错误里说清重试过。
- 顺带修：集标题里已经写了「第 N 集」时，界面会显示成「第 1 集 · 第 1 集 · 站台」。

### 新增：创作方法包（Skill —— 程序性知识）
研究 [Lovart](https://www.lovart.ai/)（[国内版](https://www.lovart.art/)）时最值得抄的一条不是
"接了多少模型"，而是它的 **Skill**：封装的是一个**创作任务的整套方法**——任务怎么拆、按什么顺序执行、
遵循什么专业标准、修改时从哪一步入手、交付物要满足什么要求；而且**用户跑通的一次过程能一键存成个人技能**。
官方口径（[极客公园专访](https://hub.baai.ac.cn/view/57618)）："垂直 Agent 的竞争，正从'谁接了最强的模型'
走向'谁积累了最多专业工作方法'"。

元枢此前的"配方"只存**工艺参数**（类型/模型/尺寸/负向/seed/参考图策略）。
"一集 90 秒、每集 6 场、前 3 秒出冲突、每集结尾留钩子"这些是**方法**，不是参数，此前没有任何落点——
每次都要在提示词里重新交代，于是同一部戏的第一集和第五集往往不是一个打法。

- 新增 `engine/story-methods.mjs`：4 个预置方法包（竖屏短剧 / 小说改编成短剧 / 品牌短片 / 纪实短片），
  每个都有**能对应到界面上真有的动作**的步骤、必须遵守的规则、交付前能逐条看的自检项、该拿出的东西。
- **用户能把自己跑通的打法存成方法包**（从当前项目反推）：只抄结构与配比
  （几集、每集几场、每场几段、目标时长、类型配比），**不抄提示词与台词**——抄了内容就不能跨项目用。
- 方法包真的进到模型调用里：一键分镜与原著改编会带上方法段与规则，并按 `fast / thinking`
  两档决定输出预算（照 Lovart 的 `--mode`）。空方法包不该让调用多花 token。
- 内置方法包**不可改不可删**（改它等于改所有项目的标准，升级还会被覆盖），要改就"复制成我的"。
- 边界写在代码里：方法包**只给建议**——套用时只给"还没排过时长"的集写目标时长，
  **不覆盖**用户自己排过的时长，也不覆盖人工调好的画风。

### 新增：高消耗先确认 + 收尾查询合并（照 Lovart 的接口级约定）
- **高消耗操作先 confirm**：批量生成里有视频段、或一次要跑 4 段以上时，先摊开
  "这次会真实调用：N 张画面 + M 段视频（合计约 X 秒）"，确认了才跑。别的直接跑——
  每次都弹确认，确认就会变成闭眼点掉的东西。刻意**不编单价**：我们没有各平台的单价数据，
  只报"会真实调用多少次、要等多久"，额度归各平台各自计费。
- **收尾查询合并 + 退避**：批量挂着 N 个视频任务号时，原来是逐个 run 去问（N 个请求 + N 轮上游问答）。
  现在 `POST /run-check-many` 一次收尾全部：一次读盘、一轮问答、**一次写盘**；
  轮询间隔按剩余数量退避（5s → 最长 30s）。这与 Lovart 把"查询"单独分一档限流
  （写 60/分、读 300/分）是同一个道理。查不到的运行 id 如实回报，不假装查过。

### 验证：真机（这次是真的点了、跑了）
- 装了零依赖的 CDP 驱动（系统 Chrome + Node 内置 WebSocket），三趟真机：真浏览器、真点击、真额度。
  - 两段画面 + 一段视频的批量：视频**真出片**（152 秒，落盘 `/api/ws/file?...`），
    两段画面当时因 seed 越界全失败——**失败原因逐条显示在批量表里**，这才有了上面第一条修复。
  - 修完 seed 后重跑：两段画面 **2/2 出片**（seed 846 / 646，都在区间内）；
    视频这次撞上游队列满，错因完整显示（`video_queue_full 503`），催生了退避重试。
  - 确认步骤真的拦住了：点「开始批量生成」只弹确认清单、行数为 0，点「确认，跑这 3 段」才真的开跑。
  - 方法包面板：4 个预置可选、步骤/规则/自检项都渲染出来、套用成功、从项目存成方法包成功。
  - 全程**控制台零错误**；用户真实项目（`44ee44b2…`）文件 mtime/sha256 未变。
- 新增 14 条契约（方法包 7 条 + 合并收尾 / 图像错误可见 / 视频退避重试 / 集标题 / seed 区间）；
  全量 **1246/1246**。

## [2.24.0] - 2026-09-15

### 新增：分集（短剧/系列的组织单位）
- 对照 [PINNGOO AI](https://www.pinngoo.cn/)「小说转分集短剧」与 [LibTV](https://www.liblib.tv/)
  按集拆镜头时最明显的一条：**短剧是以"集"为组织单位的**，而元枢此前只有「场」——
  做短剧时手里是一堆平铺的场，没有"第 3 集"这个概念，"这一集多长/有哪些场/还差多少没生成"都答不上来。
- 新增 `engine/story-episodes.mjs`：集是项目上的独立实体（`project.episodes`），场用 `scene.episodeId` 归属。
  三条刻意的取舍：**允许"未分集"**（单集短片照旧能用，未分组的场永远排在最后而不是被塞进某一集）；
  **删集只解绑不删场**（删一个集把内容一起带走是最不能接受的顺手）；**一集一本账**
  （场/段/已出/失败/排队，界面与批量生成共用同一个数，不各算一遍）。
- 剧本导出带上集标题：`# 第 1 集 …`（Fountain）/`【第 1 集 …】`（中文剧本）/Action 段落（FDX），
  未分集的场显式标为「（未分集）」——不许因为数据缺一块就静默跳过。
- 制作台新增分集面板（建/改/删/拖归集）。顺带修掉一个静默失灵：从分集面板点某一场会被落到第 1 场，
  因为 `selected` 只按**段 id** 定位（场 id 匹配不上就回退第一场）。

### 新增：原著改编（小说 → 分集大纲 → 分集剧本）
- PINNGOO 的主线是「导入原著 → 总览/人物关系/分集大纲 → 批量分集剧本 → 视觉制作」；
  元枢此前只能把小说工坊的**某一章**当文本素材挂在某一段上，一整本书进来仍是一个大平铺。
- 新增 `POST /api/story/projects/:id/adapt`：按**粘贴正文**或**小说工坊的书**（可选哪几章）改编，
  一次落成 集 + 场 + 段（含台词），段落继承链跨集串好，人物与关系并进设定。
  另加 `adaptations` 留档：刷新之后仍能回答"这个项目是从哪本小说、哪几章改出来的"。
- 三条边界：**先预览再花钱**（`preview:true` 只读书、只报字数，一次模型调用都不发）；
  **原文超 6 万字就明说**（界面照实显示会截断，不让人以为整本都改编了）；
  **解析不全不算整体失败**（能出几集就落几集，"哪几集没解析出来""模型第一次少给了几集"全部如实上报）。
- 段落类型由改编决定：与一键分镜**相反**，改编给的 novel/image/video 分工**不被项目默认配方的 kind 覆盖**
  ——那是改编结果本身，拿一个全局 kind 抹平等于把结果毁了（负向仍照旧套用）。
- 场景/段落的清洗**只此一份**（`cleanScenes`，分镜与改编共用）：真实模型返回的形状五花八门，
  写成两份必然漂移。同时补上场次标题的解析——模型给一行「外景 站台 夜」或「INT. 车内 - 凌晨」
  也要拆成 内外景/地点/时间，而不是整行丢给"地点"。

### 新增：跨段批量生成（这一场 / 这一集 / 全项目没出的都补上）
- 对照 LibTV「批量脚本→分镜图→视频片段」与 PINNGOO「批量生成分集剧本」：
  此前只能一段一段点，一集十几段就是十几轮点击加十几轮等待。
- 新增批量面板：**逐段串行提交**（不并发轰炸上游——视频任务本来就慢，并发只会一起排队一起超窗），
  提交完统一短轮询；视频创建成功只算"排上队"，**超窗不算失败**（任务号留着，可单条「查一次」）；
  每段成败逐条列在表里，失败的如实列出，不把整批说成"完成"；随时可停，未提交的段落保持原样。

### 新增：风格预设（统一画风）
- 此前只有一个 `bible.style` 自由文本框，每次都要重新描述一遍画风，不同段落很容易写得不一样
  ——而画风一旦漂移，人物锁得再准也救不回来。
- 新增 `frontend/src/lib/story-styles.ts`：10 种预置画风（电影感写实 / 3D 动画 / 国漫二维 / 水墨武侠 /
  纪录片手持 / 黑色电影 / 赛博朋克 / 日式动画 / 治愈日常 / 短剧强钩子），每套给 `visual` + `tone`，
  在设定面板选完写进 `bible.style` 且仍可手改。

### 验证
- 新增 26 条契约（改编解析的宽容形状与"哪几集塌了要如实说"、分集账与"删集不删场"、
  改编不被默认配方抹平、预览零调用、批量串行与超窗语义、台前入口是否真的接上通路）；
  全量 **1232/1232**。
- 真机验证（一次性丢弃项目，用完即删；用户真实项目 mtime/sha256 未变）：
  按小说工坊的书预览 2 章 4953 字（零模型调用）→ 真跑一次改编，`agnes-3.0-flash` 首次调用即给出
  2 集 / 6 场 / 11 段，台词 16 行、说话人 3 人，继承链 10/11，Fountain 与中文剧本都带上了集标题。

## [2.23.0] - 2026-09-15
### 修复
- **「名字出现在提示词里就挂参考图」这条规则在真实调用里是退化的。** 追查它时发现：
  `compileStoryPrompt` 会把**整个 bible**（角色/场景/道具全列）写进提示词，于是"名字出现"对
  **每一个**条目都成立——一段视频原本会把所有参考图全挂上（每张内联成 base64 都是 MB 级，
  既烧钱又把噪音塞给模型）。现在判定改为基于**段落自己的文本**（`beatReferenceText` =
  prompt + dialogue + action），并且"一个都没命中"时退回**主角一张**（不是"前 N 张"）。
  这条是给场景/道具扩参考图时被自己的测试逼出来的，改动同时修好了角色侧的老问题。

### 新增：场景与道具参考图（对手都在解决的"场景漂移"）
- 研究 [LibTV](https://www.liblib.tv/)（LiblibAI 的无限画布视频工作台）与
  [PINNGOO AI](https://www.pinngoo.cn/)（屏菓悟）时，两边都在解决同一件事：
  **角色/场景/道具作为资产复用，保证跨镜头不漂移**（PINNGOO 叫"资产库"，LibTV 有"角色三视图 + 资产复用"）。
  而我们此前**只有角色有参考图**（定妆照），场景和道具只有文字——同一间屋子在两段里长得不一样。
- `generatePortrait` 泛化成 `generateAssetRef`（`assetType: character | location | prop`），
  三件共用一套流程与一条通路（`POST /api/story/projects/:id/asset-ref`），
  写回 `bible` 对应条目的 `refImage`；老的 `generatePortrait` 保留为它的特例（返回形状兼容）。
- 新增 `buildAssetPrompt`：场景图要求**构图中性、把人排除在外**（有人的场景图当不了场景锚点），
  道具图要求单一物件、完整入画、纯净背景。理由与定妆照一致：参考图必须是**可复用的锚点**，
  不能是"一张有剧情有情绪的插画"。
- 挂载规则与角色完全相同（谁被这一段提到就挂谁），界面在设定面板里按「角色定妆照 /
  场景参考图 / 道具参考图」三组列出，各自显示"已有 N/M"。

### 验证
- 新增 2 条契约（挂载规则含"没提到就不挂"、三件生成与写回各归其位）+ 修 2 条被退化行为污染的旧期望；
  全量 **1206/1206**。


## [2.22.0] - 2026-09-15
### 新增：照 Laper 补三件（剧本要素与导出 / 全剧上下文 / 试戏）
Laper（[laper.ai](https://laper.ai/)）是 AI 原生剧本创作平台，它的地基是**剧本要素 + 工业格式导出**，
外加**200K 全剧本上下文**与**Playground（和 AI 扮演的角色对台词）**。逐项对照后元枢三件都缺：
能出图、出片、出正文，却拿不出一个能给人看的剧本文件；生成时只看得到继承链上的前 3 段；没有任何"试戏"手段。

- **剧本要素与导出**（`engine/story-screenplay.mjs` + `StoryScript.tsx`）：
  - 要素：场景标题（场景上的内外景/地点/时间三要素 → `EXT. 站台 - 夜` / `外景 站台 夜`）、
    **动作**（新增 `beat.action`；留空才退回画面描述）、角色与台词（从 `beat.dialogue` 按
    「角色名：台词」拆，认括号提示 `阿宁（低声）：…`，没冒号的行按"同一人续行 / 否则动作"处理）、
    **转场**（新增 `beat.transition`）。
  - 为什么动作要单独一个字段：`prompt` 是发给图像/视频模型的「动作、构图、镜头、光线」，
    把它整段当剧本动作，会把"镜头怎么推"写进剧本——那不是剧作该写的东西。
  - 导出三种：**中文剧本 .txt**、**Fountain .fountain**（`= 梗概` / `> 转场` 等前缀按规范）、
    **Final Draft .fdx**（XML，六种段落类型）。
    三种导出**共用同一份要素流**（`projectToElements`），谁也不许自己另算一遍。
  - **不发明屏幕排版引擎**（缩进/等宽/分页），也不硬塞没用的要素（字幕这类当前没有数据来源的先不做）。
  - 诚实标注：FDX 结构按公开约定生成、XML 合法且段落配平，但**本机没有 Final Draft，没真机打开验证过**——
    这句写在界面上，不藏着。
- **全剧上下文**（`engine/story-context.mjs`）：把"全剧至今"压成一份有上限的材料带进提示词。
  此前只有继承链上的前 3 段（各截 1.2 万字），写到第 8 段时第 2 段发生的事模型看不见。
  三条原则：**有上限**（默认 2 万字，`STORY_CONTEXT_CHARS` 可配）、
  **先保"发生了什么"再给正文**（场摘要永远保留，正文按段截断）、
  **砍了要说出来**（`truncated` 会显示在执行链上，不让人以为模型看到了全部）。
  执行链新增一行「全剧上下文：N 字（M 场、含 K 段已写正文）」。
- **试戏（Playground）**（`engine/story-playground.mjs` + `StoryPlayground.tsx`）：
  拿一句话去试，看这个角色会不会这么说。角色只能依据设定与「全剧至今」说话，
  不许编新设定、不许替别的角色说话、不许给创作建议；对话留档在 `beat.playground`（最多 20 句）。

### 试戏上线时踩到的三个真问题（都不是"模型不行"一句话能盖过去的）
1. **失败说不清**：`directChat` 在"没有密钥/没有端点/上游非 2xx"时返回 **null**，
   我却把它和"返回了内容但为空"混成一句「角色没有回话」。现在分成三句：
   `没有可用凭据或端点（模型没被调用）` / `返回了内容但里面没有台词（可能是思考占满了输出）` / `在自述而不是演`。
2. **没关思考**：仓库自己写着"填充/短任务必须显式关掉"，我却漏了 `thinking:false`，
   1–3 句台词会被推理模型的思考吃光。已补，并用测试锁住。
3. **自述不是台词**：弱模型把「用户让我扮演角色"阿宁"，和编剧对台词。编剧问"你为什么不走？"」
   当成回答交上来。**绝不能把这种话当角色的台词显示给编剧看**——现在识别出来就换下一个模型
   （按 `handleStoryAssist` 同一套路试 2 个），全都这样就如实说清是哪几个模型在自述。
   另外把"直接说那 1–3 句、不要解释"压到提示词**最后一行**（弱模型对末尾指令更敏感）。
   修完实测：编剧问「你为什么不走？」，阿宁回「走哪儿。」

### 验证
- 新增 14 条契约（剧本要素/三种导出/FDX 转义与配平/上下文上限与截断提示/试戏提示词与三种失败区分）
  + 1 条前端契约；全量 **1203/1203**。
- **上线实测**（探针项目用完即删）：要素统计 `2 场 / 3 动作 / 3 台词 / 1 转场 / 说话人 阿宁、老周`；
  三种导出都拿到（fdx 1139 字、段落配平、六种段落类型齐）；
  第 2 场生成时执行链显示「全剧上下文 964 字（2 场、含 1 段已写正文）」且提示词里确实有第 1 场摘要与第 1 段正文；
  试戏拿到在角色里的回复并落盘 2 回合。

## [2.21.0] - 2026-09-15
### 修复
- **连续创作的视频不再「一条请求干等三分钟」。** 用户真实项目里的一次失败暴露了它：
  `视频生成超时（180s）`（seed 48494449）。查下来是两个问题叠在一起：
  1. `media-api.generateVideo` 的轮询是 36×5s = **180 秒硬上限**，上游排队常常比这久；
  2. 更关键：**视频工坊早就改成了「创建 + 短轮询」**（躲 Cloudflare 100s/524），
     而连续创作还是"一条请求干等"——同一条上游、两种姿势，后者正是仓库自己警告过的写法。

  现在连续创作的视频也走两段式：
  - `runGeneration` 对视频**只创建**，立刻返回 `status: 'running'` + `taskId` + `queuedAt`，
    并把当时发的提示词存进 `run.promptText`（异步收尾还要用它）；
  - 新增 `checkRun` / `POST /api/story/projects/:id/run-check`：**一次调用只查一次**，
    出片就落盘收尾、上游说失败就记失败、还在排队就原样返回；
  - 前端启动后每 5 秒轮询，并给每个排队中的版本一个「查一次（任务号 …）」按钮。
- **「还没好」不再被说成「失败」。** 以前超时直接标 failed；现在超窗（默认 10 分钟，
  `STORY_VIDEO_POLL_MS` 可配）只是**不再自动等**，任务号留着、状态仍是 running，随时可以再问。
  界面文案也分开：创建成功只说「已排上队」，出片才说「片子到了」。
- **启动清扫给"可续查"的运行让路**：带 `taskId` 的 running **不再**被标成「生成被中断」——
  任务还在上游跑着，拿号问一次就能收尾；把它标失败才是真的丢东西。清扫结果里单列 `resumable` 计数。
- 同步路径（聊天旁路）的轮询上限也可配：`VIDEO_POLL_ATTEMPTS` / `VIDEO_POLL_INTERVAL_MS`，
  且超时的说法改成「等上游出片超时（Ns 内没等到，任务号 X 仍可继续查）」——不再伪装成失败。

### 验证
- 新增 5 条（异步契约 / 状态区分 / 配置 / 清扫例外）+ 1 条前端契约；全量 **1189/1189**。
- **上线实测**（探针项目，用完即删）：创建请求 **1923ms 返回**（以前最多等 180s），
  `status=running`、真实任务号 `task_8SJqnJ4k6Y1LJDfFXjmb1BVs3HvIqV8l`、`queuedAt` 已记、
  产物 0 个、`pollWindowMs=600000`；紧接着「查一次」→ `settled=false / status=running /
  upstream=pending / waitedMs=1062`（**排队没有被说成失败**）；
  人为塞一条没有任务号的 running → 「查一次」如实标 failed 并说明「没有任务号，多半是创建阶段就断了」。
- 如实说明：这次验收**真的创建了一个上游视频任务**（agnes），会产生一次计费；
  探针项目已删除，但那笔消耗已经发生。以后这类验收应当用不存在的模型 id 来避免。

## [2.20.0] - 2026-09-15
### 新增（把配方推完整：参考图策略 + 项目默认）
- **参考图策略进配方**：用几张（0–4）、谁优先（挂载素材 / 定妆照）。
  此前这条策略**硬编码在编排层**——画面固定"素材优先且只 1 张"、视频固定"定妆照优先且 4 张"，
  用户在界面上既看不到也改不了，更存不进配方。现在 `buildRunPlan` 按策略算顺序与张数，
  执行链上单列一行「参考图策略」，`run.reference` 记下这一趟实际用的（几张/谁优先/真用了几张）
  ——事后要能回答"为什么这张图没带定妆照"。
  默认值跟"用户显式做了什么"对齐：画面只有一个入口，所以**素材优先**（否则"挂了素材却没发生"
  是最坏的静默失败）；视频能吃多张，所以**定妆照在前**（保人物）、素材跟在后面给场景依据。
  前端 `lib/story-ref.ts` 与后端 `story-recipes.defaultRefStrategy` 是同一套默认值，
  契约测试锁住两处一致——不一致的话界面显示的和真正发出去的就不是一回事。
- **项目级默认配方**：把一套设置设为**项目的默认**，新段落自动套用、段落没说的地方由它兜底。
  同一部片子 10 个段落不必再点 10 遍。
  - 兜底范围：模型 / 参数 / 负向 / 变体数 / 参考图策略；优先级 **显式 > 段落 > 项目默认配方**。
  - **seed 刻意不从默认配方取**：配方里锁一个 seed 是"这一镜要复现"，若它悄悄成了全项目默认，
    每个段落都会拿到同一个 seed——那是没人要的副作用。想锁就在制作台上明写。
  - 新段落自动盖上的只有**类型与负向**（一键分镜在服务端盖、「从此处继续」在前端盖）；
    模型/尺寸/seed 是每次生成时的选择，写进段落会把它们变成看不见的既成事实。
  - 默认配方被删掉时**当作没有，不报错**——一条配方被删不该让整个项目生成不了。
  - 界面：配方卡上「设为项目默认 / 取消项目默认」，当前默认那张会高亮并标「项目默认」。

### 修复
- **`createProject` 把 `defaultRecipeId` 静默丢掉了。** 它是白名单式构造，漏一个字段就等于
  这个功能不存在：第一版"设为项目默认"存进去、再读出来就没了。**测试当场抓到**（新增的
  默认配方契约第一次跑就红）。这已经是同一个坑的第二次（上一次是 `films`），所以在那里
  补了注释：`createProject` 加字段时要一起加。
- **配方的 `seed: null`（没锁）被静默变成了 `seed 0`。** `Number(null) === 0` 会走进
  "是数字"那条分支，于是"没锁 seed"变成了"锁死在 0"——界面上表现为配方卡凭空显示 `seed 0`。
  **这是台前探针抓到的**（单测当时只覆盖了 `undefined`，漏了 `null`，而前端正是传 `null`）。
  现在三态分清：null / undefined / 空串 = 没锁，**0 是合法 seed**，并补了覆盖三态的测试。

### 验证
- 新增 7 条（参考图策略 / 项目默认配方）+ 1 条前端契约 + 1 条配方模块契约；全量 **1182/1182**。
- 真实浏览器：参考图张数/优先可选（画面默认「1 张 · 素材优先」、视频默认「4 张 · 定妆照优先」）；
  配方卡显示「参考图 1 张 · 素材优先」；「设为项目默认」后卡片高亮标「项目默认」、项目上出现
  `defaultRecipeId`；刷新后仍在。

## [2.19.0] - 2026-09-15
### 修复
- **卡在 `running` 的孤儿运行再也不会永远"正在生成"了。** 一次生成由某个进程持有，进程没了
  （重启、崩溃、被打断），那条 run 就永远停在 `running`：既不会成功，也不会失败，界面上一直显示
  「正在生成」。真实案例就是用户项目里那条 `07:40:12` 的 running——被我重启服务时打断，
  一直挂在那里。现在服务启动时统一清扫：`status === 'running' && !finishedAt` → `failed` +
  「生成被中断：服务在生成过程中重启，这一版没有产出」+ `finishedAt`。
  **判据是确定的**：启动这一刻还挂着的必然是孤儿（持有它的进程已经死了），不需要超时猜测，
  也不可能误杀正在跑的任务。幂等，且只写真正被改动的项目。
- **一键分镜「要 4 段只给 1 段」会自动再要一次。** 实测同一提示词、请求 4 段，三次真实调用给的是
  **4 / 1 / 4** 段（`agnes-3.0-flash`）——模型侧抖动，对用户就是"这功能时好时坏"。现在段数
  **少于请求的一半**时自动重试一次，第二次把话说明白（「上一次你只给了 N 段，这次必须给足 M 段」），
  并把 `attempts` / `retried` / `firstBeatCount` 如实回给界面；两次都不够时给 `short: true` 和
  一句「两次都只给了 N 段，可以再点一次或把想法写具体些」——**不假装完成**。

### 新增：配方（对标 ComfyUI「工作流即资产」）
这一轮把 ComfyUI 最值钱的那条补上了：**调好的工作流本身是资产**。
- 新增 `engine/story-recipes.mjs` + `/api/story/recipes`（GET 列 / POST 存 / DELETE 删 /
  `/export` 导出 / `/import` 导入）。配方落在**工作区根**（`story-recipes.json`），
  所以跨项目共用——同一部片子 10 个段落、以及以后的新项目，都不用再点 10 遍。
- **只存工艺，不存故事**：类型 / 模型 / 尺寸或时长 / 负向 / seed / 变体数。
  提示词、台词、素材属于故事，混进配方就变成"换个配方顺手把台词也换了"。
- 同名即覆盖（用户的意图是"更新它"而不是"再来一份"），并保留 `createdAt`。
- 导出是**自足**的一份文件（带 `format`/`version`/`exportedAt`），换台机器/发给别人导入即可用；
  导入认三种形状（导出文件 / 裸数组 / 单个配方对象），坏条目**逐条如实报出**，不静默丢弃。
- 配方文件损坏时报「配方文件读不出来」而不是装作"没有配方"——后者等于让用户以为配方凭空消失。
- **顺带把 `params` 做实了**：此前它是个**没人填的空字段**（界面没有尺寸/时长），
  配方要携带它就必须先有它。现在画面可选尺寸、视频可选尺寸与时长，进 `buildRunPlan` → 适配器 → 请求体。
- 界面：`StoryRecipes.tsx`（保存当前设置 / 套用 / 套用到本项目所有段落 / 导出全部 / 导入 JSON / 删除）。
  「套用到全项目」只写**能存在段落上的**工艺（类型与负向）——模型/尺寸/seed 是每次生成时的选择，
  不该被悄悄改掉。

### 验证
- 新增 6 条配方契约 + 1 条前端契约、2 条（孤儿运行 / 分镜重试）契约；全量 **1173/1173**。
- 上线后实测：探针项目里造一条 `running` → 重启服务 → 该条变成 `failed` +
  「生成被中断…」+ `finishedAt`，别的运行一条没动。
- 真实浏览器：尺寸/时长可选；配方面板能保存 → 列表出现 → 套用回制作台 → 导出下载 → 删除。

## [2.18.0] - 2026-09-15
### 修复
- **「固定 seed」是一条假通路——声称支持，实际一次都没生效过。** 对照 ComfyUI 的产线纪律
  （seed 摆在 KSampler 上当必填项）去查，发现元枢这边三处都在假装：
  `model-probe.mjs` 给每个图/视频模型写 `caps.seed = true`（注释说"本管道会原样转发给上游"）；
  `run.seed` 也存了；`story-orchestrator` 也把它传给了适配器。**但**：
  图像分支 `story-adapters.mjs` 调 `generateImage(provider,id,prompt,size,ref)`——参数里根本没有 seed，
  只在提示词里插了一句 `seed=123` 文本（模型不认那个东西）；视频分支的适配器连 `seed` 都没解构，
  于是 `video-request.mjs:32` 的 `body.seed` 永远走不到。界面上也没有 seed 输入框。
  现在两条通道都真的上送（`generateImage` 加 seed → 请求体；视频适配器 → `params.seed` → 创建体），
  **run 里记的是实际用出去的那个值**（没指定就现掷一个并记下来）——否则"可复现"是句空话。
- **预览路由没拿到模型目录，会报一条假的「不支持参考资产」。** `server.mjs` 的
  `/run-preview` 只注入了 `root`（`getModelList` / `getDefaultModel` 都没给），于是"自动选择模型"
  解析不出来、回落到 `auto/auto`，执行链上顶着一条「当前模型不支持参考资产；不支持固定 seed」，
  而真实生成（有目录）会正常选到 `agnes/agnes-image-2.5-flash`。现在两边注入同一份目录，
  预览显示的模型与注意项和实跑一致。**这条是探针里发现的，不是推断。**

### 新增（按 ComfyUI 的产线纪律补的三样）
- **负向提示词**：段落级可编辑，进 `## 必须避免` 提示词块（所有通道都吃、都看得见），
  同时作为 `negative` 进上送参数（图像通道据此发 `negative_prompt`；**只在真有值时带**，
  不赌每家上游都认这个字段），并记进 `run.negative`，同参重跑才有依据。
- **一次出多版（batch）**：`variants` 1–4，seed 依次递增，**每版一条独立 run**——
  于是天然落进「本段结果」与「作品列表」，不需要发明新的展示概念。上限压到 4，批量不该变成手滑烧钱。
- **「同参重跑」**：本段结果每版一个按钮，照这版的模型 / seed / 负向 / 参数再跑一次。
  有了它，一次偶然的好结果才算真的可复现。
- **执行链可见**：预览与实跑**共用同一份 `buildRunPlan`**（不是各算一遍），
  界面按它列出「输出类型 / 模型 / seed / 参考图（几张 + 清单）/ 挂载素材 / 负向 / 上送参数 / 注意」，
  再附编译后的提示词全文。ComfyUI 的价值有一半在于"整张图摆在画布上，每一步都看得见"。

### 明确**没有**搬过来的（说明理由，免得被当成漏做）
元枢调的是**远端 API**（agnes / 火山方舟 / Cloudflare / ModelScope…），不是本地扩散采样，所以：
- **节点画布本身**：没有可编排的本地采样节点，搬一张图只会变成假的可视化；
- **steps / cfg / sampler_name / scheduler / denoise**：本地采样参数，远端通道不暴露，传了也没用——**不假装支持**；
- **LoRA / ControlNet / IPAdapter / 放大 / 修脸**：需要本地模型与后处理链；定妆照已经承担了
  "参考图锁定"的角色（reference / 图生图）；
- **每个节点的中间结果**（latent 预览、逐节点输出）：远端只返回成品；
- **工作流 json 的导出/导入/跨项目套用**（把"配方"当资产）——这是 ComfyUI 最有价值的一条，
  也是**这一轮刻意没做**的：它需要新的数据模型（配方）+ 套用/导出入口，值得单独一轮，不适合塞在这里。

### 验证
- 新增 9 条产线契约（`tests/unit/story-workflow-params.test.mjs`）+ 1 条前端契约；全量 **1163/1163**。
- 真实浏览器（探针项目，用完即删，只点预览不花钱）：负向/seed/变体三件控件都在；
  填 `12345` + `3 版` + 两条负向 → 执行链显示「模型 agnes/agnes-image-2.5-flash」「seed 12345 ~ 12347（3 个变体）」
  「负向 多余的手指 / 文字水印」「上送参数 {"negative":"…"}」，编译后提示词里有 `## 必须避免`；
  `pageErrors=0`、失败请求 0。

## [2.17.0] - 2026-09-15
### 新增
- **剧本重视对话创作。** 此前三个提示词（`story-prompts.compileStoryPrompt`、`story-assist` 的
  `buildStoryboardPrompt` / `buildStoryAssistPrompt`）里**一个"对话"字都没有**，分镜提示词还明确
  只要求"动作、构图、镜头、光线"——出来的是一串漂亮的画面说明，一句人话都没有，戏不成戏，
  后续配音也没东西可念。现在：
  - 分镜/智能填充要求每段给出 `dialogue`，并**禁止**"两人交谈了几句"这类概述；
  - beat 新增 `dialogue` 字段（`StoryBeat.dialogue`），制作台有独立的「本段台词 · 对白」编辑框；
  - 台词**单独成块**、按 kind 分流：文字段落（novel）当戏写、视频（video）当台词上送、
    画面（image）**不进提示词**——台词写进画面描述会被生图模型当内容画出来，或把视觉指令稀释掉；
  - 解析宽容：`dialogue` / `台词` / `对白` / `line` / `lines` / 数组 / `{name,text}` 都收；
    **只有台词没有画面描述的段不再被丢掉**（那是编剧刚写好的对白，画面可以后补）。
- **别的工作台的产出能被连续创作调用了。** 此前连续创作能用的素材只有「角色定妆照」
  （因为它是设定里的一个字段）；AI 绘画出的图、视频工坊出的片、小说工坊写的正文就在同一个
  工作区里，却一个字都进不来。现在制作台有「本段素材」面板，两个来源：
  - **工作台产物**（`生成物/图片|视频`，走 `/api/ws/artifacts`）；
  - **小说正文**（小说工坊章节，走 `/api/novel/chapter`）。
  接线：画面素材 → 参考图（图生图 / reference 模式）；视频素材 → `videos[]`（reference 输入）；
  文本素材 → 作为创作依据写进提示词（截断 4000 字，**内容存在项目里**，源文件删了也不影响）。
  素材沿继承链累积：续写的段落默认看得见上一段挂的东西。

### 说明（刻意的取舍，不是遗漏）
- **画面（图生图）只有一张入口，所以用户显式挂的素材优先，定妆照退居其次**。
  反过来会让"挂了素材却什么都没发生"——那是最坏的一种静默失败。视频（能吃多张）则是
  定妆照在前、素材在后。
- 产物历史 `run.referenceImages` / `run.inputAssets` 记的是**原始引用**，不是内联后的 base64
  （一张定妆照的 base64 就是 2MB 级，写进项目 JSON 会把文件撑爆）。
- 素材地址解析不到时不硬塞坏地址，按上一版建立的方式如实写进 `run.degradation`。

### 验证
- 新增 12 条契约（`tests/unit/story-dialogue-materials.test.mjs`）+ 1 条前端契约；全量 **1153/1153**。
- **真实浏览器**（探针项目，用完即删）：台词框可编辑；素材面板给出 **200 个产物**、
  **3 本小说 / 2 章**；挂上画面 + 文本各一条 → 2 个 chip；点「检查生成输入」后编译出的提示词里
  确实有 `## 本段台词（必须按这些台词写成戏，不要改写成旁白）` 与 `## 本段已挂载素材`，
  且文本素材正文进了提示词；`pageErrors=0`（唯一失败请求是外部字体 CDN `fonts.loli.net`，与本次无关）。
- **真实模型**（`agnes-3.0-flash`，3 次分镜调用）：有台词的段 **4/4、1/1、4/4**（覆盖率 100%），
  台词 10/3/12 行，**台词混进画面描述 0 段**；bible 角色登记 2/2/2。
- 顺带量到的一个**既有**问题（不是本次引入，也没有顺手改）：同一提示词下模型给的段数不稳定——
  三次分别是 4、1、4 段（请求的是 4 段）。界面会如实显示「已追加 1 段分镜」，所以是可见的，
  但"要 4 段只给 1 段"值得单独处理（例如段数明显不足时重试一次）。等你说要不要做。

## [2.16.1] - 2026-09-15
### 修复
- **连续创作里带参考图的任务必失败：`视频任务创建失败 400: {"code":"invalid_request","message":"media must be
  a public http(s) URL or base64 data. Local file paths are not supported; ..."}`**
  根因不是猜的：真实项目「制作台验收」里 5 次视频运行的 `degradation` 全是这一条，
  而**同一模型**在不带参考图时能正常出片。差别只有一处——带参考图时，元枢把角色定妆照以
  **自己的相对地址** `/api/ws/file?path=...` 塞进 `images[]`（`run.referenceImages`，
  由 `story-adapters.mjs:57` 放进创建体、`video-request.mjs:29` 原样转发）。
  Agnes 只认**公网 http(s) URL 或 base64 data URI**，把相对地址一律当"本地文件路径"拒掉。
  本地部署给不出公网地址（上游抓不到 127.0.0.1，隧道又是本地系统管的），所以按上游自己给出的
  另一个选项做：**读文件内联成 base64**。新增 `engine/media-inline.mjs`，在 `startVideoJob`
  与 `generateImage` 两个出口前落地 media（`media-inline.mjs`、`media-api.mjs`）。
- **同一模型被报「当前模型不支持参考资产」——一条假降级。** 前端模型下拉只能给 `{provider,id}`，
  `capabilities` 整个丢掉，于是 `negotiateCapabilities` 对**每个显式选中的模型**都判"不支持参考图"，
  参考图通路被这条假降级关掉（真实项目里 4 次视频运行就是这样"成功但没带参考图"）。
  现在按 provider+id 回目录补齐能力（`story-orchestrator.mjs` 的 `withCatalogCapabilities`）。

### 改动
- **请求体超限不再只留一句 `fetch failed`。** `readBody` 以前超限时 `req.destroy()`——
  在响应写回**之前**掐断连接，客户端什么都拿不到（实测：2.8MB 的 `/api/media` 请求报的就是
  光秃秃的 `fetch failed`，排查方向被带偏到网络层）。现在停止累积、排掉剩余流量、抛带
  `statusCode` 的错，分发层尊重该状态码 → 客户端收到 **413 +「超过 2MB 上限（已收到 2.8MB）」**。
  顺带：非法 JSON 也从 500 归正为 400（那是客户端的错）。
  注意 `/api/media` 用的是默认 2MB，而连续创作**不把 media 放进请求体**（内联在服务端做），
  所以这道墙不在连续创作的路上——它是同一类"错误说不清"的另一处。
- **参考图落不下来就摘掉并如实降级**，不静默发一趟没有参考图的请求，也不硬失败：
  超过 8MB 内联上限、文件缺失、空文件、地址解析不到，都会变成 `run.degradation` 里的一句人话。
  `mode` 跟着修正——不给上游发一个没有 media 的 `reference`/`keyframe` 请求。
- 适配器如实上报的降级（上游摘掉参考图时 `startVideoJob` 的 `notes`）会并进 `run.degradation`，
  那一趟于是显示「已生成 · 请核对连续性」，而不是一个看不出问题的"成功"。
- `run.referenceImages` 记录的仍是**原始引用**：内联后的 base64 一张就是 2MB 级，写进产物历史会把
  项目 JSON 撑爆；原始引用保留可追溯性。上送用的那份单独算。

### 验证
- payload 级：把 `globalThis.fetch` 换成记录器，用真实项目里那张真实定妆照（1.51MB）
  跑真实的 `startVideoJob`。实测 POST 体 2.02MB、`mode=reference`、`images[0]` 以
  `data:image/png;base64,` 开头、体内**不再出现** `/api/ws/file`，
  且内联字节与原文件 **sha256 一致**。
- 真实上游（零成本探针）：全部用**不存在的模型 id**，模型校验必然失败，绝不会建出任务。
  实测内联后 0.3／0.8／1.5／2.0／2.5／3.0MB 以及真实定妆照（2.02MB）**每一次都带着完整
  body 到达 Agnes**（返回 `503 model_not_found`，而不是任何 media 报错）。
  真实定妆照重复 8 次：**8/8 到达**。
- 全量 `npm test`：1142/1142 通过（新增 10 条 media-inline 契约 + 4 条编排层契约 + 3 条 readBody 契约）。
- **没有做**、也**不能**由免费探针得出的事，要说清：Agnes **先校验模型、后校验 media**
  （用一个损坏的 base64 也是先返回 `model_not_found`），所以免费探针**证明不了**"上游接受这个 base64"。
  "接受 base64" 是它自己那条错误信息里给出的选项，payload 已按该格式发出并逐字节核对。
  要真正坐实，需要一次真实生成。
- **一次未复现的失败也记下来**：切换新代码后第一次打 Agnes 时，同一个 2.02MB 请求返回过一次
  `{"error":"fetch failed"}`；随后完全相同的请求 8/8 成功（首次调用耗时 4.9s，其余 0.7～1.0s，
  像是重启后代理活性探测那个窗口）。**一个样本不算证据**，我没有把它算成已修或已知原因。
- 探针里自己踩到的两个坑也补上了守卫（`resolveLocalMedia`）：`wsRoot` 为空时不再按进程 CWD
  解析（`story-film` 的 `withinRoot` 在空 root 下会放行任意路径），解不出来的 URL 形态值不再被
  `path.resolve` 拼成 `<wsRoot>\api\ws\file?...` 并谎报"文件不存在"。

## [2.16.0] - 2026-09-15
### 修复
- **成片合成完刷新就没了。** `assembleFilm` 只把文件存进产物库就返回（`story-orchestrator.mjs`），
  项目 JSON 里**一个字都没写**；前端也只 `setFilmUrl` 存在内存里，
  于是"打开成片"这个链接在刷新/换项目之后必然消失，看起来像合成失败。合成真的成功了，是**没落账**。
- **产物卡上的"第 N 段"会被后来的重排改写。** 段号此前是界面按**当前**分镜顺序现算的
  （`StoryProducts.tsx` 的 `noOf`）。重排一次分镜，昨天导出的"第 3 段"今天就指向别的内容——
  产物是历史，不该被后续编辑改写。现在 `run.beatNo` / `run.sceneTitle` 在**生成时刻**定格并随产物落盘
  （`createGenerationRun`，调用点 `runGeneration`），界面优先用定格值，只对旧数据回退到现算。
- **同一段既有正文又有画面时，正文被缩略图吞掉。** `StoryProducts.tsx` 的条件是
  `prose && !headUrl`——只要出了图，这一段实际写出来的文字就再也不显示了。改为两者都给。
- **失败且无产物的运行完全不可见。** 作品列表只收 `outputAssets.length > 0` 的运行，
  失败的那几次连"失败"两个字都看不到。现在默认仍然只看成品（不占屏），
  但给一个「只看有产物的」开关，勾掉就能看到失败运行与它的 `degradation` 原因。

### 改动
- `project.films` 落盘：每合成一版成片追加一条 `{id,url,clipCount,method,beatIds,createdAt}` 并 `writeProject`；
  `createProject` 补 `films: []`，`types.ts` 补 `StoryFilm`。成片是作品，必须有账。
- 作品列表新增「成片 · N 版」区（内嵌播放 + 打开 + 下载，新的在前），成片计数并入表头。
- 制作台「打开成片」改为读 `project.films`（不再依赖一次性的本地状态），多版时标注「第 N 版」。
- `StoryResults.tsx` 契约测试更新：原断言锁的是 `版本对比`（那个已被删除的版本下拉），
  改为锁**新意图**——所有版本平铺、代码里不再有隐藏版本的 `<select>`（注释不算证据，按行剥离后再断言）。

### 验证
- 新增 3 条测试：段号定格且重排后不变、成片写回并能在磁盘上重新读到、作品列表的段号/失败可见/成片来源契约。
- 全量 `npm test` 通过。

## [2.15.0] - 2026-09-15
### 修复
- **连续创作里旧产物看不见了。** 先说清数据是好的：服务端 `story-orchestrator.mjs:50-51`
  是 `scene.outputs.push(run)`——**只追加、从不覆盖**，旧产物一直都在磁盘上。
  问题纯在展示：`StoryResults.tsx` 把该段所有 run 都取出来了，却**只渲染 `current`（最新一版）**，
  旧版藏在一个 `<select>` 版本对比下拉里。而同一页 `StoryWorkbench.tsx:192` 还写着
  「结果不满意时可以修改内容重新生成，**旧版本会保留**」——保留了，但一生成新的就从视野里消失。
- **没有任何地方能一眼看全"这个项目到底产出过什么"。** 分镜时间线和「本段结果」都只看当前那一段。

### 改动
- **「本段结果」改为全部版本平铺**（`StoryResults.tsx`）：新的在最上面，每版一块，
  带版次 / 状态 / 模型 / 时间 / 降级提示 / 各资产与「下载这一版」「打开原文件」。
  版本下拉框**已移除**（它正是"旧的看不见"的来源）。
- **新增「作品列表」**（`frontend/src/components/story/StoryProducts.tsx`，接在创作页底部、默认展开）：
  把整个项目里**跑出过产物的运行**跨场景摊平，新的在前。每张卡带：第 N 段 · 类型 · 状态 ·
  场景 · 时间 · 缩略图（图/视频）或正文摘要 · 原提示词 · **定位到本段** / 下载 / 打开。
  表头给出计数：`N 次生成 · 成品 X 个（画面 a / 视频 b / 正文 c）`。
  只有**真正产出过成品**的运行才进列表（失败且无产物的不计入，但它在「本段结果」里仍能看到）。
- 卡片可点击「定位到本段」跳回该段继续生成，形成"列表 → 段落"的回环。

### 验证（真实项目「制作台验收」：10 镜头 / 7 条运行记录 / 4 个视频产物）
```
作品卡: 4 个（跨 2 个段落，新的在前）
本段结果: 3 个版本平铺 —— 第 3 版 / 第 2 版 / 第 1 版，各自带时间与模型
标题: 3 个版本 · 全部保留，新的在最上面
版本下拉框: 0 个（已移除）
pageErrors=0
```

## [2.14.0] - 2026-09-15
### 修复
- **定时任务的自我反思只谈后端，谈不了聊天。** 查下来不是它"选择"只谈后端，是**手里只有
  后端的材料**：会话材料是 `formatSession` 给的一行「名字 + preview」，而 `preview` 是
  **第一条用户消息截 60 字**（`session-files.mjs:113`）；同类材料里记忆日志却给了
  **6000 字**（`time-task-run.mjs` 的 `CLIP_CAP`）。信息量差两个数量级。
  证据是反思自己抱怨过：「PPT 会话数量多但深度信息为零」「记忆日志严重欠账」。

- **反思的行动清单写完就没有下文。** 原先结果只做两件事：追加进 `文档/时间引擎日志.md`、
  存进 `time-tasks.json` 的 `history[].result`。而那份日志**全仓没有一处读它**；唯一自动
  通道是 `onTaskDone → nudgeSkill` 生成技能提案——实测 5 条提案**全是 `open`，一条都没落地**，
  且 09-07 与 09-14 提的是**同名技能** `daily-retrospective`（重复提案、无去重）。
  最关键的缺口：**反思写的「今天要做的」不变成任何可追踪对象**。

- **反思不读上一次的行动清单**，只能每天从头复盘，**无法检查兑现**。

### 改动
- **会话正文进材料**（`sessionDigest`）：按会话读取目标日期的用户提问 + 小语结论正文
  （工具调用/结果不入正文），每会话 1800 字、总量 9000 字、最多 6 个会话。
- **上次兑现喂回下一次**（`collectReflectionCommitments`）：把承诺账里 `source=reflect`
  的那些连同"挂了多久 / 已结清及证据"一起注入，并硬性要求「先逐条交代兑现，再说今天」。
- **行动清单落成承诺**（`parseReflectionActions` + `recordReflectionActions`）：复盘结尾
  必须附一个 JSON 块，解析后写进承诺账（`sessionId=reflect`）——于是它会被下一轮复盘追问、
  被台前「待兑现承诺」列出。**结清仍只能由人给结论**（承诺账的既有规矩，模型自述不算证据）。
  解析取**最后**一个 json 块（模型常先给示例），解析失败就是无操作、不写空条目。
- **技能提案按名字去重**（`nudgeSkill`）：同名且未 dismissed 的提案已存在就跳过，
  不再并排堆两条 `open`。
- prompt 增硬约束：**必须覆盖会话侧**（用户在聊天里要了什么、交付到什么程度），
  「不能只谈引擎/后端的工作——会话正文就在上方」。

### 端到端验证（真触发一次反思，跑完还原）
```
① 谈到会话: 是   ← 开头即「## 一、会话侧交付情况」，在分析用户聊天里那十遍重复约定
③ 行动 JSON 块: 1 个 → 解析 5 条
④ 承诺账 0 → 1488 字节，source=reflect 共 5 条，全部 pending
   例：排查后端 POST /api/runs 是否按 clientRequestId 去重…／核查视频交付通知链路：确认用户为何「没看到」…
```
②（上次兑现）在这一次里没显示——因为当时承诺账是空的（探针跑完即还原），该段是条件注入；
其行为由单测锁住（prompt 含清单与"先逐条交代兑现"）。

### 验证
测试 1122/1122（新增 `tests/unit/reflection-loop.test.mjs` 18 条）。

## [2.13.0] - 2026-09-15
### 更正（重要）
- **上一版我在给用户的回复里说错了元枢的沙箱默认值。** 我写的是"元枢当前是
  `danger-full-access` 默认，恰好相反"——**这是错的**，那句话来自调研线的一句未核实转述，
  我没有验证就转给了用户。实测：`checkSandboxCall` 默认 `mode = "workspace-write"`，
  非法值也回落到它（`engine/yuanshu-sandbox.mjs:44,51`）；**全仓没有任何地方默认
  `danger-full-access`**。元枢的默认是阶梯中档，不是宽松那端。
- **同时澄清一句我说得不完整的话**：元枢的沙箱阶梯**只管 yuanshu 这条路**——
  `gateSandboxCall` 全仓只在 `engine/yuanshu-loop.mjs:117` 被调用；默认主驾的 pi 路径
  用的是 Pi SDK 自己的工具，不经这道阶梯（元枢在那边只注入审批拦截）。
  所以"把默认收成 read-only"收的是一条非默认路径。

### 新增
- **会话级沙箱模式**（`engine/sandbox-session.mjs`）。补齐的是真缺口：原先模式是**写死的**
  （`unified-chat.mjs` 里 `isPlanLock ? "read-only" : "workspace-write"`），前端没有任何
  控件、config 也没有开关——想让整个会话只看不改做不到，想整会话放开也做不到，
  模式什么时候变过更没有记录。

  按 dsh 的"**会话事件即状态、投影出有效值**"（`dsh-sandbox-policy:41-43,114-120`）实现成
  一条 append-only 的 JSONL，最新一条生效——每次变化都可审计、可回放。两条 dsh 的规矩：
    · **收紧随时可以，放宽必须写明理由**（dsh 单次升级必须配 justification，会话级同理：
      没有理由的放宽，在日志里就是一句无据可查的授权）。
    · **切换的是具名预设，不是一个裸值**（dsh-permission-presets）：`cautious`(read-only) /
      `standard`(workspace-write) / `trusted`(danger)，各带 label 与说明。
  另有一条元枢自己的不变量：**计划模式永远强制 read-only**，不会借用会话的放宽。

- **台前**：引擎页新增「沙箱模式（本会话）」——显示当前档、切换预设、放宽时收一句理由、
  把变化史摊开（时间 / 从哪到哪 / 放宽还是收紧 / 人工还是模型 / 理由）。

### 刻意不改的一项
**默认值保持 `standard`（workspace-write），没有按 dsh 改成 read-only。** 三条理由写在
`DEFAULT_PRESET` 旁边：① 阶梯只管 yuanshu 这条非默认路径；② read-only 下写操作转审批、
无人值守按 fail-closed 拒绝，而元枢的常态是跑长任务，会在第一次写就停住；③
workspace-write 本身不宽松（禁工作区外路径，`BASH_DANGER_RE` 仍需单独升级）。
要改成最严只改 `DEFAULT_PRESET` 一行；测试里留了回归锁，改它会挂，逼改的人先读那三条理由。

### API
`GET /api/sandbox/mode`、`POST /api/sandbox/mode`（缺 sessionId 时回落到最近会话）。

### 验证
测试 1104/1104（新增 `tests/unit/sandbox-session.test.mjs` 14 条）。

## [2.12.0] - 2026-09-15
### 新增
- **fork 型子智能体**（`engine/subagent-fork.mjs` + `delegate_fork` 工具）。元枢原先只有
  spawn 型（`delegate_task`）：干净上下文 + 自包含 prompt。dsh 的 fork 变体只差一个
  seed——取父会话里**已完成**的那段前缀当子智能体的起始对话。差别不是"多一点上下文"：
  spawn 要你把子代理该知道的全写进 prompt（写漏就答歪），fork 的子代理已经看过前面
  聊了什么（省掉重复总结，也少一处"总结失真"）。

  两条来自 dsh 的硬规矩：
    · **只取已完成的前缀**（dsh 是 `events.slice(0, lastEnd.seq + 1)`）。等价实现：
      取到"最后一条有正文的助手消息"为止，其后的工具调用/结果/本轮用户消息全丢——
      在飞的回合重放会得到半截状态。
    · **深度只能加深，不能降低**（`engine/subagent-depth.mjs`，对应 dsh
      `subagent/types/depth.js:18-25` 的 `max(持久化, 运行期)`）：否则被恢复的子智能体
      按 0 重算深度，`maxDepth` 就形同虚设。

  **与 dsh 的一处刻意分歧**：dsh 的 fork 继承完整事件流；元枢的历史里工具结果可能很大，
  全带过去会吃光子智能体预算。所以这里丢弃工具结果，但**必须自报**——在种子最前面插
  一条"其中 N 条工具结果没有带过来，需要时自己重新查，不要假设它没发生过"，回执里
  也带上同一句。省略必须自报，否则就是"没被观测的说法"。

  **诚实说明**：元枢的子智能体没有工具（SYSTEM 里明写"你只有分析能力"），所以它派生
  不出下一层——**深度闸门目前是空转的**，保留它是为了将来 fork 子代理拿到工具时不会
  绕过上限，以及让 `depth` 在 trace 里可见。这条不构成当前的安全收益。

  **工具是两条而不是一条**（对应 dsh 的两条硬编码工具行 `dsh-base/cordis.patch.yml:349-367`）：
  `delegate_task`（独立判断）/ `delegate_fork`（接着前文干）。这样"要不要把前文带过去"
  是模型可见的选择，而不是自动挑。种子只能来自宿主传进来的历史，模型无法伪造"前文"。

  接线：`history` 加进 `executionContext`（`yuanshu-loop.mjs:121` 把 `executionContext`
  展开进工具 ctx）。**已知限制**：Gateway 旁路（`unified-chat.mjs:617/625`）没有传 ctx，
  那条路径上 `delegate_fork` 会走到"没有可继承的前文"的明确拒绝，不会静默。

### 验证
测试 1090/1090（新增 `tests/unit/subagent-fork.test.mjs` 15 条，含"种子真的进了模型请求
且顺序为 系统 → 种子 → 补充 → 子任务"的断言，以及"被闸门挡下时一次请求都不发"）。

## [2.11.0] - 2026-09-15
### 新增
- **跨轮目标 + 三重闸门**（`engine/goals.mjs`，借 `dsh-goal-round-driver`）。
  元枢原先没有"目标跨轮推进"：任务看板是给人看的，时间引擎是按点触发的，都不是
  "一个目标自己往下走"。dsh 值得借的不是"自动跑"，而是它围绕自动跑立的三重闸门
  ——那三条才是让这个能力不变成烧钱自转的东西，一条不落全搬：
    · **闸门① 回合上限**：到顶自动转 blocked，不再"再来一轮"
    · **闸门② 单轮预约**：同一 `(goalId, revision, round)` 只驱动一次；`revision`
      不匹配则本轮作废（目标被改过）；轮号必须连续
    · **闸门③ 错误即解除**：一轮出错立刻 disarm，不带着错误继续转
  另有两条 dsh 的规矩照搬：**`complete` / `blocked` 只接受人类输入**（模型自己说
  "我做完了"是不可验证的说法，不算证据）；**重启后一律回到未武装态**，必须人类重新确认。

  **与 dsh 的一处刻意分歧**：dsh 的 round-driver 默认就自动推进（默认上限 256 轮）。
  元枢把自动推进做成显式开关 `autoAdvance`，**默认 false**——默认只注入轮次上下文、
  不自己起轮次；上限也取小值（默认 20，可设到 200 封顶）。个人伙伴跑无人看管的
  256 轮是成本隐患，不该是默认值。

  接线：`advanceGoalTurn` 在 `handleChat` 的分支**之前**调一次，pi 与 yuanshu 两条
  路径因此看到同一个轮号；到顶/重放/revision 不匹配时注入为空，不占上下文。
  pi 走 nextTurn、yuanshu 走 prompt 区段（`plan`，只读状态不重复推进）；两条路径的
  catch 都接 `noteGoalError`。
- **台前**：记忆园丁页新增「跨轮目标」——新建、武装、暂停、已完成（收证据）、受阻。
  台前只能做人类这一侧：武装与结清都只可能来自点击，服务端没有任何路径会自己调它们。

### API
`GET /api/goals`、`POST /api/goals/create`、`POST /api/goals/action`（arm/pause/complete/block）。

### 验证
测试 1075/1075（新增 `tests/unit/goals.test.mjs` 20 条）。端到端实跑：建→paused、
人类武装→active、真实对话推进到第 1 轮、目标区段的 4 个标记全部出现在模型请求里、
人类结清带证据；被碰文件全部还原并校验一致。

## [2.10.2] - 2026-09-14
### 修复
- **dsh 不再标记为可主驾**（`canLead: true` → `false`）。这个标记的依据是
  `engine-pair.test.mjs` 里那句断言——「dsh 对话适配器**写完后**应能主驾」，
  那是**完成度目标**，不是能力判断。适配器确实写完了（实跑通），但"写完"这个门槛
  从没被定义过。而同一个测试的第 28 行又断言 dsh 的 `cannot` 必须提到记忆/出图/多轮
  ——两句话同时被锁在测试里，互相矛盾。
  实测元枢调用它的方式（`dsh --profile headless <task>`，每轮新起子进程、只回收
  stdout、历史压成近 8 条 × 800 字纯文本）**预热后约 4.7 秒/轮、冷启动约 27 秒**，
  且无流式（`dsh-chat.mjs` 只在进程 close 后推一次 delta）、无记忆/情绪/出图/工具轨迹、
  180 秒硬超时。作为日常主驾不成立。
  > 更正：本条目最初写的是「回两个字要 26.9 秒」。那是**单次冷启动**的数字，被当成了
  > 常态。复测六次为 4.4–5.0 秒，`--dump-config`（只装配配置）0.25 秒。已按实测改正
  > 代码注释、测试注释与本条目。教训：一次测量不是"实测"，要么给分布要么标清单次。

### 改动
- `ENGINE_CATALOG.dsh.canLead = false`，并把边界写实：不适合当主驾的原因（每轮冷启动
  子进程、无流式、历史只能压成摘要）与真正的用途（`dsh_task` 执行臂）分开列。
  前端早就有「（暂不能主驾）」这个状态（`EnginePairPanel.tsx`），无需新代码：
  引擎对会自动回落到能主驾的次席，并播报「本轮主引擎 · X（dsh 主驾让路）」。
- `canLead` 全仓只在 `engine-pair.mjs` 的 resolveLead 里被读，所以 **`dsh_task`
  执行臂完全不受影响**。
- 测试意图同步改写：原断言「写完后应能主驾」→「作为执行臂可用，但不作为主驾」，
  并新增一条锁：不可主驾的引擎占主驾位时必须让路且**播报可见**（不能静默换引擎）。

## [2.10.1] - 2026-09-14
### 修复
- **记忆结算只跑在 Pi 一条路上，而声明说它属于元枢循环。** 流水账 / 承诺 / 纠正 / 关系 /
  每-20-轮快照计数原先都写在 `server.mjs` 的 Pi 分支 `try` 里，而那个分支位于 `handleChat`
  early-return **之后**——于是：
    · `yuanshu`(unified) 路径静默不写记忆；
    · `dsh` 路径同样不写；
    · **Pi 失败降级到 unified 时也被一起跳过**（那段代码在 try 内，catch 走了降级不再回头）。
  而 `engine/engine-pair.mjs` 的引擎目录声明的恰恰相反：yuanshu「记忆、出图、规划…都挂在这条上」、
  pi「记忆…不在这条循环里长」——**实现与声明是反的**。默认主驾是 pi，所以一直能跑，缺口才没被发现。
  实测：同一条必定触发记忆信号的消息，走 unified 时记忆日志一字不动，走 Pi 时正常追加。

### 改动
- 结算逻辑抽成 `engine/turn-memory.mjs`（可单测，不再内联在 server.mjs），由两个分支的
  `finally` 各调一次：`pi` 与 `yuanshu` 都结算，`dsh` 按它自己声明的边界不结算。
  放在 `res.end()` 之后，不拖慢客户端收尾；被新请求顶掉的轮次跳过（否则按偏移切片会切到新轮次的回复）。
- 助手文本改为**按字节偏移只读本轮新追加的那段**，不再整份回读会话找"最后一条助手消息"。
  这是必须的：中止/报错的轮次也会因为用户消息落盘而让文件变长，只看"变长了"会把**上一轮的
  助手回复重复记进记忆**。回归锁见 `tests/unit/turn-memory.test.mjs`。
- 引擎目录里 pi 那条不实声明改成「记忆、出图、规划由元枢外壳结算，不在这条适配器内部实现」——
  适配器内部确实没有，但外壳会替它结算，原话会让人以为 pi 路径下记忆是坏的。

## [2.10.0] - 2026-09-14
### 新增
- **作息与节律**（`engine/activity-rhythm.mjs`）。时间感此前只有"现在几点"和"距上次多久"，
  缺一层"对这个人来说现在算不算正常时间"——凌晨 3 点说早上好和上午 10 点说早上好不是一回事。
  从**会话文件名自带的时间戳**与记忆日志标题观测真实作息，给出活跃时段、当天轮次、深夜占比，
  以「（作息）」段并入提示词。样本不足返回 null（不观测），绝不编一句"你通常早睡早起"。
- **承诺兑现**（`engine/promises.mjs` + 台前）。模型最廉价的讨好方式是承诺（"下次给你补上"），
  说完就滑过去。现在把助手自己的延迟承诺落成账：提取 → 入库 → 下一轮提示词里提醒主动交代。
  两条硬规矩：**绝不自动标记"已兑现"**（结清只能由人给结论），**宁可漏不可灌水**
  （没有一人称主语的"下次再X比较好"是在给用户建议，不能进账）。
- **记忆快照可回退**。此前快照只有写入方、全仓没有任何读取方：`restoreSnapshot` 零调用、
  无端点、前端零引用——98.7MB 攒着却一份都回退不了。现补 `GET /api/memory/snapshots`
  与 `POST /api/memory/snapshot/restore`（回退前自动存 pre-restore），并在记忆园丁页给出
  列表与回退按钮。
- **AIBody 土壤真实读数上台前**。"系统脉络"此前对身份/基因/情绪/记忆/治理只显示"可用/不可用"，
  而真实读数（`state[].summary`）早已算好、只被渲染层丢掉。现在五项读数全文可见。

### 修复
- **记忆快照无界增长**：`archiveStateSections` 在归档循环**之前**无条件 `saveSnapshot`，
  而它挂在 server 启动块里 → 每次重启落一份（每份内嵌记忆日志全文）→ 29 天攒到 **521 份 / 98.7MB**。
  现在只在真有归档动作时写，并加保留上限（30 份 / 12MB，从最旧端双重截断，至少留 1 份）。
- **"每 20 轮存一份"是死代码**：原来读 `listSnapshots().length % 20`，0 触发后份数恒为 1，
  1..19 都不满足条件，该分支第一次之后永远进不去。改用落盘计数器。
- **快照 id 只到分钟**：同一分钟两次快照会静默互相覆盖（回退前的 pre-restore 安全网会被
  随后的 archive-before 盖掉，等于没有安全网）。改为精确到秒 + 冲突加序号。
- **`autoMemorize` 几乎每轮灌一条流水账**：项目信号连助手回复一起匹配，而"完成/做好/搞定"
  在助手回复里几乎是口头禅。改为只认用户侧，助手侧只认可核查里程碑；并在**写入前按要点去重**
  （园丁的"一键去重"只是事后补救，一天能落 18 个 `.bak`）。
- **`dedupeLog` 的 `.bak` 无上限**：每次去重落一份全量日志副本，一天被调用 18 次就是 3.2MB。
  改为保留最近 5 份，并在启动自维护里一并收敛。
- **作息数据源污染**：最初用 `记忆/情绪感受.jsonl`（768 条，看着最细），实际被批量任务污染——
  2026-09-07 单日 578 条、09-06 单日 103 条（占 88%），相邻记录间隔中位数正好 60 秒，是定时跑出来的。
  拿它算作息会得出"3:00–9:00 全是 60 条"这种假结论。改用会话文件名后分布立刻正常
  （03:00–08:00 为 0，峰值 23:00）。测试里留了回归锁，防止再被当回数据源。

## [2.9.0] - 2026-09-14
### 新增
- **AIBody 的「土壤」接回主角色**。原先这份运行协调 directive（模式策略 + 五个状态提供者摘要）
  只经 `executionContext` 传给**子智能体**，主角色根本收不到——而 directive 里写的正是
  "母体协调身份、记忆与角色治理""主角色负责综合和交付"。现在 Pi 路径用 nextTurn 注入，
  兜底路径走 `runtime` 提示区段（该区段本来就存在，只是一直没人往里传）。
- **五个状态提供者改为真实读数**（`engine/aibody-soil.mjs`）。此前 identity / governance
  是纯硬编码字符串，genes 的 summary 硬编码，memory 只证明文件存在——而这些摘要每轮都会
  拼进 directive 交给模型，等于让模型读没被观测过的说法。现在：
  identity=人格附录文件大小与更新时间、genes=条数与**真实漂移**、emotion=**本会话**快照、
  memory=条目数与最后写入时间、governance=**待批提案真实条数**；读不到就返回 null，
  由 policy 标 `not_observed`，不再冒充 observed。
- **时间感**：`promptTimeText` 补上"距上次对话多久"与"本次会话已持续多久"。
  原先三个重复实现（yuanshu-seams / time-engine.nowContext / server.mjs 内联）都只说
  "现在几点"——是时钟，不是时间感。现在收敛成一份；`nowContext` 全仓零调用者，已删。

### 变更
- 会话写操作与 Pi 共用同一把按文件队列（`withFileMutationQueue`），并叠加跨进程锁文件；
  元枢自己的 `edit` 是同步临界区本就原子，真窗口在**跨实现**（Pi 的 edit 走 fs/promises，
  读写之间有真 await）。
- AIBody 记录的 `engine` 不再硬编码 "yuanshu"——主驾是 pi 时记录会是错的；改为真实观测值。
- 工具结果压缩：给出可执行回读指针（归档原件 + 读法）、大结果先发全文 2 次再压缩。
  详见 `docs/NAMING.md` 第四节与 `docs/sol-pi-notes.md`。

## [2.8.0] - 2026-09-14
### 新增
- **命名契约**（`docs/NAMING.md`）：产物名统一为 `{提示词摘要}_{类型}_{时间戳}-{id}_{v版本}.{扩展名}`，
  视频/音乐/文本/图片/文档一体适用。此前聊天里的视频下载名回退成 `元枢视频-N.mp4`（所有视频同名、
  互相覆盖），文件链接下载名是字面量 `download`（连扩展名都没有），主题文件还叫 `pi-theme-*.css`。
- **版本唯一来源** `version.json`：此前版本号散在 9 个文件里各写各的，产品版本 `2.7.1` 与壳版本
  `0.2.4` 两条线互不相干（壳版本还倒退过一次 `1.0.0 → 0.2.2`），且没有任何机制强制推进——
  09-12 到 09-14 发了一整批功能，一个号都没动。现在全部由 `version.json` 派生或被测试钉死。
  发版用 `npm run version:bump <major|minor|patch>`；`version:bump sync` 用于对齐历史漂移。
- **版本进产物名**：升级后产出的文件不再与旧版混淆，看名字就知道是哪个版本做的。
- 系统页与看板显示同一个版本号（此前系统页读 `package.json` 显示 `v0.2.4`、看板显示 `2.7.1`，
  两个数字都叫「版本」）。

### 变更
- **仓库改名**：`pi-web` → `yuanshu`（GitHub `xueshao1716/yuanshu`、Gitee `linxinyu520xue/yuanshu`）。
  旧地址实测仍可跳转（网页 301/302，raw 与 git 端点跟随重定向后 200），旧安装命令不会失效；
  但仓库内所有链接已改为新地址，不再依赖跳转。历史条目保留旧地址作为当时的记录。
- **环境变量前缀**：`PI_WEB_*` → `YUANSHU_*`。旧名继续被读取（新名优先），老安装无需改动。
  环境变量表见 README；实现见 `engine/env.mjs`。
- 计划任务 `pi-web-watchdog` → `yuanshu-watchdog`；`autostart.ps1` 原先写的任务名与机器上
  实际注册的完全不符（重跑会多出一个 watchdog 抢 8787），已按真实配置重写。

### 新增（承接 2.7.1 之后）
- 系统级 AIBody 运行协调：主聊天统一记录规划、工具、子智能体、记忆、产物与治理状态，验收页只作为观察入口。
- 子智能体持久台账：关联父任务与会话，记录角色、状态、证据、失败、取消和重启恢复，不保存原始提示词或隐藏推理。
- 连续创作编排阶段一：故事项目、Story Bible、分镜 beat、生成运行记录与能力降级信息，提供时间线 + 节点侧栏工作台；本阶段只创建可追溯编排记录，不直接触发外部生成。
- 连续创作·制作台：一键分镜（自带设定登记）、角色定妆照参考图锁定、连续性体检、成片合成；跨场景继承打通。
- 改动验收并入工作台、连续创作并入创作，去掉重复板块。

- 聊天区接入运行观测状态栏：展示阶段、工具数量、情绪摘要与失败重试入口。
- 运行摘要补充记忆命中与上下文预览，聊天完成态支持详情查看并自动收起。
- 引擎控制台拆分配置、运行诊断、工具搜索与 Gateway 旁路，补齐刷新失败提示和移动端可读性。

## [2.7.1] - 2026-09-12
### 新增
- 下载中心支持桌面壳一键打开系统下载目录，下载记录可重试与清理。
- Android 0.2.4 接入系统保存窗口，资产与会话导出支持选择保存位置；确认保存后记录状态，并可重新打开文件。
### 修复
- 资产库 PPT 直接预览文字大纲，移动端预览层关闭后自动收起。
- 修复 Windows 打包完成后等待进程组不退出、导致交付步骤未执行的问题；安装包按当前版本精确交付并核对校验值。

## [2.7.0] - 2026-09-12
### 新增
- 会话库按真实更新时间排序，历史会话自动补发稳定编号，新建会话立即落盘编号。
- Pi SDK 会话恢复搜索、分享、媒体、技能和执行臂工具，PPT 任务可真实生成并交付文件。
- HTML 作品集缺失或损坏 `deck.json` 时自动从页面恢复；并发 PPT 任务按本轮目录隔离产物。
### 修复
- 壁纸模式下新建对话工作画布保持透明，避免遮住主题壁纸。
- 作品集路径前缀边界和文件交付路径校验，避免误读兄弟目录或误交付其他任务文件。

## [2.6.0] - 2026-09-04
### 新增
- 工作台看板（桌面侧栏 + 手机「更多」）、进化引擎与跨会话回忆
- 水墨 / 竹影主题；本机 Aieyra Anthropic 网关给 Claude Code
- ppt-html 模板路径可配置（`PPT_HTML_TEMPLATES`）
### 修复
- PPT 生成与单页 refine 的 SSE 断连会 abort/dispose agent
- htmlPath 前缀穿越；小说目录与自愈提示词不再写死 D 盘路径
- CHANGELOG 看板实际能读到仓库根的更新日志

## [2.5.0] - 2026-08-15
### 新增
- 正式版本体系：版本常量 + CHANGELOG 更新日志，看板顶部显示 pi-web 版本与最近更新
### 修复
- 时间引擎崩溃：server.mjs 缺失 `createTimeEngine` 导入与 `timeEngine` 变量声明，前端一用时间引擎即崩（反复重启 42 次的诱因）
- 更新：pi 引擎 0.84.1 → 0.84.2

## [2.4.0] - 2026-08-15
- 安装一条命令化：极简版（纯 zip）+ 全自动版（装 git）双脚本，`irm … | iex` 一条命令部署
- 全局安装支持：`npm i -g git+https://gitee.com/linxinyu520xue/pi-web.git` 后 `pi-web` 即用（setup.mjs 自初始化）
- 外网分享服务固化：start-share.cmd 一键启动 8644 + cloudflared 隧道

## [2.3.0] - 2026-08-14
- 断线自恢复：任务进度快照（/api/tasks/active）+ 前端断流自动恢复轮询
- OpenIM 评估结论：不需要，SSE 心跳 + 断线重连已够

## [2.2.0] - 2026-08-12
- 经验沉淀台：提案制（plan/approve/reject/rollback）+ 基因反馈闭环（效率/可靠/适应三滑块）
- pi-reasonix 扩展：DeepSeek 前缀缓存优化（命中率实测 94%+）

## [2.1.0] - 2026-08-11
- 多 provider 模型通道策略：默认 opencode-go 套餐 deepseek-v4-flash，图像自动降级切换
- 技能体系接入：web-search / image-generation / voice-transcribe / session-export-redacted

## [2.0.0] - 2026-08-10
- 记忆系统：固定记忆 + 记忆日志自动沉淀 + 经验库（跨会话长期有效）
- 情绪引擎：VAD 三维情绪感知，对话自适应语气
- 工作空间分类视图（工程/文档/生成物/交付）+ 一键交付 + 文件断点续传
- 外网分享 share_project：稳定域名 + 自动复制目录
- 会话分支 / 模板 / 项目分组 / 主题系统
## 2026-09-06

- 第一批主驾工作台升级：新增统一运行观测快照与 `/api/run/overview`，Board/Engine 展示主驾健康和判断→执行→记忆→交付阶段。
## 2026-09-06

- 第一批主驾工作台升级：新增统一运行观测快照与 `/api/run/overview`，Board/Engine 展示主驾健康和判断→执行→记忆→交付阶段。
