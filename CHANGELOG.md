# 元枢更新日志

> 每次发版：`npm run version:bump <major|minor|patch>`（它会同改所有版本声明并收本文件的 `[Unreleased]`）
> → 补一条日志 → 构建 `frontend/dist` → `npm run prune:dist -- --apply` → `npm run sync:frontend` → 双推。
> prune 那一步不能省：`emptyOutDir:false` 会让每次构建都新增约 190 个分块，
> 而 `sync:frontend` 是忠实镜像，会把它们复制进 `public/` 与 `app/dist/`（细节见 `docs/NAMING.md`）。
> 版本唯一来源是仓库根的 `version.json`；契约见 `docs/NAMING.md`。

## [Unreleased]

## [2.43.0] - 2026-09-16

### 修复/优化：首屏不再先闪「开始一个故事」，人物设定里的「形象」自成一组

用户原话是"页面也再美化下，不好看"。这一轮收尾的是视觉整理里最后两块：

- **首屏骨架**（`StorySkeleton`）：项目列表还在路上时，以前 `project` 还是 null，
  页面直接渲染「开始一个故事」——先闪一屏"你没有故事"，几百毫秒后才跳成工作台，
  比慢更难受。现在用灰块把**真实版式**（状态条 / 左时间线 / 中编辑器 / 右结果）先占住，
  数据到了原地替换，不发生重排；骨架下面不再叠一行「正在加载故事…」，文字是噪音。
- **路由兜底也换成骨架**（`AppLayout.PageLoader`）：以前是一行居中的「加载中…」，
  它是**任何**页面都会先看到的那一屏。
- **「形象」自成一组**：形象芯片与「新增形象」同排，用虚线分隔，和「重新生成」分开。
  以前两者混在一行，看不出"哪几个是形象、哪个是新增"，也看不出点一下重新生成的是哪一张。
  真机 DOM：`形象 / 基础形象 / 战斗装束 / ＋ 新增形象 / 已生成 2/2`。
- **间距落到 8px 刻度**：定妆照卡片内 6→8、卡片间 12→16（原来 14/12/10/6 混着用，
  眼睛读不出层级）。动效尊重 `prefers-reduced-motion`。

真机核对（CDP，零依赖）：把项目列表请求拖慢 2.5 秒后，`.story-skeleton` 稳定留在屏幕上
（13 条灰线 + 3 个灰块，`role=status`，动画名 `story-skeleton-pulse`），
且此时**没有**「开始一个故事」；放开网络后骨架被原地替换（残留 0 个），
全程写入类请求 0 条。截图在 `tmp\visual-after\{skeleton,settings-looks}.png`。

测试：+2（骨架的加载顺序与动效降级、「形象」分组不得混回操作行），1337 → 1339 全绿。
## [2.42.0] - 2026-09-16

### 新增：定妆照提示词补上「身体比例」与「镜头」，人物设定要求写到结构上

借的是 hypit 图像提示词的**写法**（Capture / Person / Shot / Setting），不碰它的代码——
它的许可证禁止多租户 SaaS，元枢只学思路。

- `buildPortraitPrompt` 多了两段：**身体比例**（肩要宽、头肩比要好；体型按设定如实呈现，
  不要一律套"标准模特身材"）与**镜头**（85mm 等效、半身、胸口以上入画、轻微景深，
  不要广角畸变）。理由很具体：半身像最常翻的车就是"窄肩配大脑袋"，
  而"身形挺拔/气质出众/well presented"这类笼统赞美对模型等于没说，所以提示词里明确禁止。
- `buildStoryboardPrompt` 的人物设定纪律同步收紧：虚构人物要有强美感，但必须落到
  肩宽/头肩比与身高体型上，长相用**具体类比**（例：参考韩国男团门面的清冷感）而不是笼统形容词。
- 体检新增 `character-no-structure`：外貌里既没有肩宽/体型词时提示"半身像容易出窄肩配大脑袋"。
  它是 `info`、不报 warn；但要说清楚——`info` 会把体检总级别从 ok 抬到 info（和既有的
  `no-craft` 一样），不是"级别不变"。

测试：+3（真实角色提示词必带结构锚点与镜头；换装不换脸不丢；体检只给 info 不升级 warn）。
用户现有项目里的角色（林默/陈默）本来就写了体型词，所以这条提示在他们的项目里是安静的——
规则对，只是没话说。

**还没做**：出图效果的真实对比需要花一次出图额度，等用户点头后再跑（不是代码问题）。
## [2.41.1] - 2026-09-16

### 修复：界面上把 `**估计**` 当成字面量显示出来了

- 台词时间轴那句说明是 JSX 里的普通字符串，星号不会变粗体，真机上就是明晃晃的三个字
  `**估计**`。改成直接写「秒数是估计」。发 v2.41.0 时没点开看这一行，所以是补丁版本。

## [2.41.0] - 2026-09-16

### 新增：台词时间轴（按行给起止秒，改一句不会全篇乱）

- 引擎（`engine/story-lines.mjs`）：每句台词有稳定 id（对规范化后的文本取 sha1，形如 `ln-3f2a91`），
  所以「同一句话」在重写、重排之后仍然是同一行，而不是靠下标硬认。
- 时间轴按语速常量估算（3.5–5 字/秒，句内停顿 0.2s，句尾 0.45s，默认 4.25 字/秒）；
  改一句只会让它和它之后的锚点移动，前面的秒数不动——这是它和"整篇重排"的区别。
- 制作台「检查生成输入」里直接显示：`0.0–2.4s 林晚 你挡住了我的视线。 ln-3f2a91`，
  并写明秒数是估计。预览是只读的：不写盘、不建 run、不花钱。
- 共用的语速常量只有一份，前端不再自己实现一套，避免"体检说说不完、界面说还富余"。

## [2.40.0] - 2026-09-16

### 连续创作页视觉整理（你说"不好看"）
先量后改，不靠感觉。真机量测（同项目 13 段）基线：
状态条 124px、分镜时间线 **2216px**、编辑列 876px、本段结果列 424px、设定面板 1633px。

- **分镜时间线一行化（主要矛盾）**：每段把整段提示词当标题写 48 字，而它只有 200px 宽——
  一行只放得下 14 字，于是每段换行 3~4 行、7 段就是一面文字墙。
  改：正文截 14 字 + `nowrap/ellipsis`，全文进 `title`；`padding 14→8`、行高 `1.5→1.4`。
  **实测 2216 → 1186px（每段 170 → 91px，−46%）**。
- **按钮主次分层**：状态条上原来三个同权重按钮并排，眼睛没有落点。现在一个视口**只留一个
  `btn-primary`**，其余推荐动作折进「其他动作」；进度明细与胶囊说的是同一件事，折进 `title` 不再单独占一行。
- **对比时结果列跨整宽**：并排对比要在 424px 的窄列里塞 2~4 张大图 + 元数据表。
  用 `:has()` 让外层两列在对比时收成一列（`.story-editor-layout:has(.story-results.is-compare)`），
  退出对比自动恢复——不必为此把状态层层提上去。
- **长句标签改短**：镜头规格的「落幅 · 这一镜最后定格在哪」「承接 · 从上一镜的哪个落点接起」
  改回短标签，说明移进 `placeholder`，两行输入框顶边不再参差。
- **体检清单折叠**：默认只摆前 2 条，其余进「还有 N 条」——一串红字会把整块染红，也把"先改哪一条"淹掉。
- 量测脚本自身修了一个方法问题：加 `Network.setCacheDisabled` + 每次换查询串，
  避免量到浏览器缓存里的旧 bundle（重测后数字一致，确认此前不是缓存假象）。

**如实记一笔没做到的**：我原定状态条 ≤72px，实测停在 112px。原因是它本来就是三行
（胶囊 / 状态句 / 动作行），要去到 72 只能三行并一行，那是拿可读性换一个我拍脑袋定的数字——
所以我把那条目标作废，并在提交消息里写明理由，而不是硬凑。

**还没做的**：卡片分组留白、空态骨架（"加载中"仍是一行字）。**before/after 截图**存在
`D:\pi-workspace\tmp\visual-before\` 与 `visual-after\`，量测脚本 `visual-after.mjs` 与 before 同一套口径。

## [2.39.0] - 2026-09-16

### 片段时间轴：这支片子由哪几段、每段多长、用的是第几版
成片能拼了，但拼完就只剩一个文件——用户看不见"这支片子由哪几段、每段多长、当前用的是第几版"。
同一段生成过三版镜头之后，换版本只有一个下拉框，改完也说不清成片到底多少秒。
这一版把这三件事做成一个**片段时间轴**（照 Pavo 的 `timeline-delete` / `segment-history-download` 那套做法）：

- **一排缩略图**（`frontend/src/components/story/StoryTimeline.tsx:1`，新增 148 行）：视频用
  `<video preload="metadata" muted>` 只拉元数据取静帧，每段标出 `第 N 段`、时长、`可用 N 版`、`当前用第 N 版`。
  类名用 `story-film-timeline*` 而不是 `story-timeline*`——后者已经被左侧「分镜时间线」占着，
  同名会让两处的 `li`/`button`/`strong` 规则互相污染（真机核对时先撞上这个，页面上出现了两个同名根节点）。
  顺序就是**成片顺序**（默认与分镜顺序一致）；上移下移改过顺序后，与分镜序号不同的段会额外标出 `成片第 k 段`。
  排不进成片的段（整段不要 / 没有可用版本）跟在后面并写明原因，不凭空消失。
- **每段可展开＝它的历史版本**：列出 `filmPlan` 的全部候选，点一下「用这一版」只改 `picks`（成片顺序数据），
  **不重跑合成**。顺带修了一个老毛病：换版本以前是"删掉再追加到末尾"，用户调过顺序之后换个版本，
  那一段会悄悄跑到最后；现在原地替换（`frontend/src/components/story/StoryFilm.tsx:51`）。
- **单段导出**：走现成的 `downloadApiFile` 通路，文件名 `成片-第3段-v3.mp4`（段号 + 版本号）。
- **总时长 + 段数**：表头是 `进成片 4 段 · 分镜共 23 段（有可用版本 4 段）` + `总时长 20.8 秒`。
  「整段不要 / 加回来」沿用合成面板那套 `dropped`/`picks`，没有另造第二份排除状态。
- **时长是探出来的，不是编的**：产物记录里根本没有秒数，所以 `engine/story-film.mjs` 新增
  `parseDurationSeconds`（`:136`）/ `probeDurationSeconds`（`:152`）/ `cachedDurationSeconds`（`:166`）/
  `filmPlanWithDurations`（`:195`）：ffprobe 优先、没有就退回解析 `ffmpeg -i` 的 stderr；
  探不到就写"时长未知"，**不拿分镜里写的目标时长顶替**。按 `file+size+mtime` 缓存（文件重写即失效），
  且只为**已经在本地**的候选探测——外链不为一个时长去网络拉整支片子。
  探测要 spawn 进程，所以只有 `GET /api/story/projects/:id/film-plan?durations=1`（`server.mjs:1749`）才走这条路；
  不带参数的调用与返回**保持原样**（老字段语义没动，只是多了新字段）。
- **版本号由引擎给**（候选新增 `versionNo`，`engine/story-film.mjs:97`）：按这一段的**全部**版本（含失败的）
  从旧到新数，所以一段失败过两版、唯一能用的那版仍然是「第 3 版」——和「本段结果」里同一个号，
  不会一处在时间轴叫第 1 版、另一处在结果里叫第 3 版。

验证：`npm test` **1329/1329**（基线 1322 + 新增 7 条）；`frontend` 的 `npx tsc --noEmit` 干净。
真机核对（CDP，只读）：打开 `http://127.0.0.1:8787/?v=timeline#/story` 切到《制作台验收》，
`.story-film-timeline` 存在**且只有 1 个**（改名前撞上左侧「分镜时间线」的同名节点，这条断言就是为它加的）、
23 个 `.story-film-timeline-seg`（4 段进片 + 19 段如实标"还没有片段"）、4 个缩略图、4 个「导出这一段」入口，
表头 `进成片 4 段 · 分镜共 23 段（有可用版本 4 段）`、`总时长 20.8 秒`；
展开第 3 段（`beat-mu0t1nhe-1-0`，`可用 3 版`）列出第 3/2/1 版，点「第 3 段用第 1 版」后
`当前用第 3 版` → `当前用第 1 版`，**期间 0 条网络请求、0 条打到 `/film`**（换版本不会偷偷重跑合成）；
点「整段不要」总时长 `20.8 秒 → 15.6 秒`、这一段落到时间轴末尾并标出原因，点「加回来」复原回 `20.8 秒`。
截图：`D:\pi-workspace\tmp\timeline-verify.png`。服务重启核对：kill 8787 的旧进程（PID 46280）后
18 秒内 watchdog 拉起新进程（PID 41516），`GET /` 返回 200。
新增 `tests/unit/story-timeline.test.mjs`（契约测试）：时长解析与 ffprobe→ffmpeg 回退、只探本地文件、
合计只算探到的段、版本号连失败版本一起数、真 ffprobe 探到约 2 秒并吃缓存、编排层默认不带探测、
前端类名与单段导出入口都在。

## [2.38.1] - 2026-09-15

### 形象变体真机试跑（给《系统流》的林默加「战斗装束」）：抓到两个真问题
你让我跑，就真跑了——一跑就露出两处，都已修（其中一个还动了你的项目数据，已复原）：

- **新形象会顶掉基础定妆照**：老角色只有 `refImage`、没有 `looks`（这个功能之前生成的那张）。
  给这种角色加一张**新**形象时，它被当成"第一张"并回写了 `refImage`——
  林默的基础定妆照当场被「战斗装束」覆盖。现在：第一次加新形象会先把既有的那张
  **登记成「基础形象」**，只有基础形象才回写 `refImage`。
  你那边的数据我已经复原：从 `生成物/图片/2026-09-15/…201440…` 的 json 里认出那是林默那张
  （prompt 里 `name: 林默`），登记回「基础形象」并把 `refImage` 指回去；
  「战斗装束」留作第二张。**没有重新生成、没有额外花你的额度。**
- **形象名没能真的换掉衣服**：第一次生成的「战斗装束」还是一身灰卫衣——
  因为角色设定里写着"穿着宽大卫衣"，而提示词只说了"只换该变的部分"，模型听设定的话。
  现在这一张的提示词写死：**服装与装备以「战斗装束」为准，覆盖角色设定里的服装描述**
  （那段设定是基础形象，不是这一张）；脸/发型/体格仍须一致。

顺带在真项目上验了"提到哪张形象就挂哪张"（纯函数、只读）：
普通段落挂基础形象、出现「战斗装束」或「（战斗装束）」挂战斗装束、两个角色同时出现两张都挂。
新增一条回归测试锁住"老角色加新形象不许覆盖 refImage"。`npm test` **1322/1322**。

## [2.38.0] - 2026-09-15

### 修：连续创作页「排版乱了」——状态条挤进了两列网格的第一列
你报的排版问题，根因是我上一版自己埋的：`.story-layout` 是**两列网格**（时间线 200px | 编辑器），
我把流水线状态条当成了它的**第一个 grid 子元素**——于是状态条占掉了时间线那一列，
时间线、编辑器、结果整列被挤偏。现在状态条移到网格**外面**、横跨整页。
真机量过几何：状态条 `x=256 w=1104`（整宽），下面才是 `[时间线 200 | 主区 876]`，
主区里再分 `[编辑器 | 结果]`。

### 角色形象变体（基础形象 / 战斗装束），并把"第二张图"接进参考图挑选
一个角色只锁一张脸不够：换装段落没有对应形象图，模型只能靠文字猜，一致性立刻掉。

- 角色新增 `looks: [{id,name,refImage}]`：第一次生成定妆照时自动登记为「基础形象」，
  之后「新增形象」输入名字就建一张新的；**同名是重做那一张**，不会重复堆。
- 定妆照提示词带上「这一张形象 + 脸/发型/体格必须与角色设定完全一致」——换装不许换脸。
- 参考图挑选升级：这一段**提到哪张形象就挂哪张**（正文写 `@林默-战斗装束` 或直接出现「战斗装束」），
  没提到才挂基础形象。
- 顺手修一个真 bug：`assetRefsForShot` 把 `looks`（对象数组）当字符串拼，
  会拼出 `@林默-[object Object]`（新增的形象等于白生成）。
- 连续性体检与流水线状态机跟着算：有形象图就算有定妆照，不再误报"还没有定妆照"。
- 界面：设定面板的角色卡列出这个人的所有形象（「已添加形象 N/M」），点形象名就重做那一张。

验证：`npm test` **1322/1322**；真机截图核对排版已恢复（状态条整宽在上，下接时间线/编辑器/结果）。

## [2.37.0] - 2026-09-15

### 产物版本并排对比 + 「采用这一版」（Pavo 右栏那排"历史记录"的做法）
同一段生成好几版镜头之后，用户要的是**比出来**挑一版，而此前只能一版一版往下滚。
产物本来就在 `scene.outputs` 里全留着（只追加不覆盖），缺的是把它们摆到一起的手段。

- `StoryResults` 顶部新增**版本条**：所有版本的缩略图横排（视频出静帧），
  点选 2~4 版进入**并排对比**；每版的关键元数据（模型 / seed / 状态 / 生成时间 / 参数 / 耗时）
  对齐成一张对照表——差异一眼可见，不用来回滚。列表里勾「加入对比」也能选。
- **采用这一版**：写回 `beat.chosenRunId`；再点同一版就是取消采用（选择必须能撤销）。
- 服务端跟着走：`filmPlan` 的 `recommendedRunId` **优先用采用过的那一版**（用户挑过的别再替他挑），
  没有才退回"最新可用"；采用的那版已不可用（被删/外链失效）时同样退回最新，而不是给一个空推荐。
  候选里带 `chosen` 标记，`StoryFilm` 会显示「你采用的」而不是「最新」。
- 测试：`story-film-picks` 增一条（采用优先于最新、采用的那版不在候选里时退回最新）。

## [2.36.1] - 2026-09-15

### 修：状态条在首屏是空的（列表接口没带 flow）
在真 GUI 上截图核对时发现的：连续创作页**先拉列表、再选项目**，而 flow 只加在了详情接口上，
于是 `project.flow` 是 undefined，状态条直接不渲染——功能写好了、用户却一个都看不到。
现在 `GET /api/story/projects` 的每个项目也带 `flow`（纯函数算的，不产生额外 IO）。
真机截图确认：四步胶囊 + 「还有 4 段没有成品（共 7 段）」+ 推荐动作三枚 + 「1 个动作现在做不了」都出来了。

## [2.36.0] - 2026-09-15

### 流水线状态机：把"现在该干什么、为什么这个按钮点不了"交给服务端说
学自 Pavo 的 `flow`：`{ current_step, allowed_actions, blocked_actions[{action, reason_code, message}],
recommended_actions, failed_recovery_actions }`。元枢此前是反过来的——十几个按钮各自 `disabled`，
用户得自己猜"我现在该干到哪一步了""为什么合成成片是灰的"。

- 新增 `engine/story-flow.mjs`（纯函数）：四个阶段（剧本大纲 → 资产库 → 分镜成片 → 成片），
  **完成度只用真实产物判断**，不看"用户点过什么"；被挡住的动作必须带 `reason_code` + 人话，
  且理由要具体到数字（「可用视频片段只有 1 段，合成至少要 2 段」，不是"不可用"）。
- 失败与进行中优先：`failed_recovery_actions` 分别给 `retry_failed` / `check_running`；
  两者同时存在时 headline 两件都说——只报"进行中"会把失败藏起来。
- 外链产物会被认出来并推荐"拉到本地"（`http(s)` 且非本机才算外链，工作区自己的产物不算）。
- `GET /api/story/projects/:id` 现在随项目下发 `flow`（算出来的，不落盘）。
- 前端新增 `StoryFlowBar`：四步胶囊 + 一句人话总状态 + 推荐动作按钮 +
  「N 个动作现在做不了（点开看原因）」。**界面不自己写一份规则**，只渲染服务端给的三张表；
  动作按钮复用既有处理器，做完重新取一次项目（`patch`/`run` 的返回里没有 flow，
  不重取就会一直显示旧状态——"刚做完还提示你做同一件事"比没有状态条更让人困惑）。
- 测试：`tests/unit/story-flow.test.mjs` 6 条（阶段推进、理由必须带数字、失败/进行中优先、
  外链识别、每个动作都有 label+panel、推荐不能推荐被挡住的、产物在 outputAssets 里也要算数）。
- **真机验收又抓到一个**：产物的 url **不在 `run.url` 上**，而在 `run.outputAssets[].url`
  （一个项目 14 条 run 里 `run.url` 全是 undefined）。只看 `run.url` 会把"已经出片的段落"算成
  "没有成品"——状态条于是催你重做已经做好的段，还把「合成成片」误判成"可用片段 0 段"挡住。
  现在按 `outputAssets` 统计，`degraded`（片子到了但带降级说明）也算有成品：
  三个真实项目的段落完成度从「0/N」修正为「3/7」「4/23」，合成动作也正确放开了。

## [2.35.1] - 2026-09-15

### 修：景别前缀重复（真机验收里发现的）
拿真实项目跑 `run-preview` 一眼看到的：老分镜把景别写在内容开头（「镜头特写：林默趴在…」），
景别被提到句首后就重复了——编译出来是「**特写。镜头特写：**林默趴在…」。
现在只削**开头那一个**景别前缀（削完是空就还原，宁可重复也不吃内容）。
新增一条测试；`npm test` **1315/1315**。

## [2.35.0] - 2026-09-15

### 把"人家的镜头提示词"学进来：镜头提示词编译器（`engine/story-shot-prompt.mjs`）
上一轮拿 Pavo 的真实产物对照出的结论是：**同一个视频模型，出片像不像电影，差在发过去的提示词**。
它一个 4 秒镜头是「全景，俯拍斜角机位。画面采用…柔和自然光，呈现温暖复古的色调…具有 90 年代
写实电影质感与轻微的胶片颗粒，使用 35mm Panavision 镜头。…镜头缓缓推近。**落幅定格在**她落寞
无助的侧脸。」，下一镜开头写「承接上一个镜头…的落点」；而我们此前只把 `beat.prompt`（一段散文）发出去，
缺景别、机位、光线、色调、质感、运镜、**落幅**、**承接**这八样。

新增 `engine/story-shot-prompt.mjs`：

- **18 条风格库**（`code` / 中文名 / 别名都认，认不出来返回 `null`——**绝不替你选风格**），
  每条都落在"用什么镜头、什么颗粒、什么光比"上，不是形容词堆砌；
- `compileShotPrompt()` 按**固定顺序**编译成一条散文（视频模型吃的是一段话，不是带标签的规格表）：
  景别+机位 → 光线/色调/质感 → 场景与人物（含 `@资产` 引用）→ 运镜 → **落幅** → **承接**；
  另起短行给【台词】【时长】【资产引用】【全局约束】【必须避免】；
- **老项目也受益**：没有结构化字段时，从散文里**认**出景别/机位/运镜/落幅（`inferShotFromProse`）；
  **认不出来就不写**，绝不编造一个不存在的机位；
- 全局约束按类型分：视频怕"字幕/静音段"，图片怕"画面里冒出文字"；
- `@资产` 引用只挂**名字真的出现在这一段里**的资产，带形象变体（`@林晚-基础形象`），
  并标明**有没有参考图**（没挂图的会写"（未挂参考图）"，别让人以为挂了）。

接线（三处，缺一处这套东西就是空转）：

- `engine/story-prompts.mjs`：镜头规格块放在提示词**最前面**（上游对开头的权重最高），
  原有的 `## 必须避免` / 台词 / 素材等块一个不少；
- `engine/story-orchestrator.mjs`：`buildRunPlan` 为画面/视频编译，**预览与实跑共用同一份**，
  所以点「检查生成输入」看到的就是真正发出去的那段话；
- `engine/story-assist.mjs`：分镜提示词改为要求模型把镜头语言**拆成字段**
  （景别/机位/运镜/光线/色调/落幅/承接 + 秒数），解析器新增白名单 `cleanShotFields`，
  并让时长落到 `params.seconds`；`一键分镜`与`原著改编`两条写回路径都带上它们；
- 前端 `StoryWorkbench`：新增「镜头规格」六格可编辑（景别/机位/运镜/光线 + 落幅/承接），
  落幅那格的提示写着"不写，剪起来就是跳的"。

验证：`npm test` **1314/1314 通过**（新增 `tests/unit/story-shot-prompt.test.mjs` 8 条，
含"不许编造没写的字段""老项目从散文里认得出落幅""@资产只挂真出场的"）；
`story-workflow-params` 增一条断言：`## 镜头规格` 必须排在 `## 必须避免` 之前。

## [2.34.0] - 2026-09-15

### 拿别人家的成品剧本做对照：补上「重复检测」，修掉动作行被当成说话人
你给的那份从别的工具（Pavo）导出的 10 集剧本，我先用元枢自己的机检跑了一遍，跑出两个真问题：

- **"生成在原地打转"没有任何环节在报**：那份剧本**第 5 集与第 6 集逐字相同**
  （5-gram 重合 100%），全剧 **24% 的正文段落跨集重复**（同一段"记忆画面"在第 4/5/6 集各出现一次）。
  这不是文笔问题，是长文生成卡住后的退化——而且纯算力就能发现，之前却没人说。
  → `engine/story-craft.mjs` 新增 `repeatCheck()`（单元间 5-gram 重合度 + 段落逐字重复 + 占比），
  接进 `engine/story-lint.mjs` 体检（`repeat-unit` / `repeat-paragraph` 两条 warn），
  体检 summary 增加 `repeatParagraphs` / `repeatRatio`。
- **动作行被当成了说话人**：那份剧本用 `△ 记忆画面：年轻的林默穿着工程服…` 写动作，
  旧解析器把"△ 记忆画面"当作说话人，还把后面所有无冒号的动作行都并进这一句，
  整段动作变成一句 **190 字的"台词"**——我第一次跑出来的 258 条语速 warn 里一多半是这个误报。
  → `engine/story-screenplay.mjs`：`△ ▲ ○ ● ◇ ◆ ・ · ※` 一律当动作行，并清掉上一句说话人
  （否则下一行没有冒号的动作还是会被算进上一句台词里）。
  这条对"用户从别的工具导剧本进来"是真需求。

顺手记下、**还没做**的：那份剧本里"异常值"出现了 40% 和 75% 两个阈值、
"区域稳定性"从 72% 跳到 15% 再到"归零"——**数值/设定冲突是纯规则可查的，元枢还没有这一项**。

验证：`npm test` **1306/1306 通过**（原 1300 + 重复检测 5 条 + 动作行 1 条）。
对照报告：`工程/_research/pavo-短剧设计/剧本-机器体检.md`。

## [2.33.0] - 2026-09-15

### 顺手挖出的真问题：你的技能写好了，但**主引擎根本看不见它们**
打磨完照着"技能到底会不会被用上"真跑了一遍（建临时会话 → 走真服务 → 读 SSE → 删会话），
结果两条任务**都没激活技能**。翻模型的思考记录才看明白：

- 主引擎是 `pi`（兼容适配器 / Pi SDK），它扫描技能的根只有两个 —— 会话工作目录的
  `cwd/skills`（你的技能商店 `D:\pi-workspace\skills`）和 `~/.agents/skills`。
  **仓库根的内置技能 `D:\pi-web\skills` 不在它任何一条扫描路径上**：
  「元枢技能」页面上列得出来（那个走的是另一条读取路径 `listBuiltinSkills()`），
  模型却看不见。实测原话："I don't see a `prompt-architect` skill in the list"，
  然后转身去读了 SDK 那份目录里的 `multi-agent-meeting`。
- `activate_skill` 工具一直好好注册着（`engine/session-manager.mjs:322`），
  但目录里没有名字，这个工具就永远等不到调用 —— 上一轮所有技能打磨，在这条通道上等于白做。
- 连带的：`engine/context-loader.mjs` 里的 `makeLoader()`（本来是给 SDK 会话追加技能目录的）
  **是死代码，全仓库没有任何调用点**。

修法（`server.mjs` `handleChat` + `engine/yuanshu-protocol.mjs`）：

- 每个会话**注入一次**「元枢内置技能库」目录（`skillCatalogSent` 记账，不重复灌上下文），
  走的是和情绪/时间/PPT 护栏同一条 `nextTurn` 通道，不污染会话历史；
- 每轮再做一次技能匹配，命中就补一句"本轮任务可能匹配：xxx"，把候选点名给模型；
- 匹配器补了两族"概念名"规则：实测 `用提示词架构师的办法…` 原本**零命中**、
  `用多AI角色扮演系统…` 命中的是无关技能，现在分别稳定命中 `prompt-architect` / `multi-ai-roleplay`。

验证（改完重启服务再跑）：问"你上下文里有没有元枢内置技能库"，模型**逐行复述出全部 17 个技能名**。
新增 `tests/unit/skill-catalog-injection.test.mjs`（3 条）：目录文本能用（每条注入 ≤90 字且触发语在 90 字内）、
匹配器认得这两族、`handleChat` 真的走"每会话一次 + nextTurn"注入。
顺带修正 `diagram-design` 的 description：118 字时"use when…"正好被 90 字截口切掉，现已压到 89 字。

### 继续打磨你写的那些技能：17 份 description 全部进 120 字 + 3 份源文抽出可读文本 + 全技能契约测试
上一轮只查了万像两份。这一轮把 `skills/` 下**全部 17 个技能**按同一把尺子过了一遍，问题还是老几样：

- **description 超过 120 字，超出部分等于没写**：`engine/context-loader.mjs:181` 只把前 120 字
  塞进技能目录，那是模型唯一能看见的一行。实测有 9 个在 121–220 字之间（`seedance-25` 713、
  `diagram-design` 579、`video-prompt-library` 220、`gpt-image-2` 191…）。现在 17 份**全部 ≤120 字
  且触发语前置**（"当用户…"/"Use when…"）。
- **三份技能指向读不到的东西**：`multi-ai-roleplay` / `prompt-architect` / `shortform-genesis`
  都只给了 `.docx`（Word 二进制，`read` 工具读不了），其中两份还引用了**根本不存在**的抽取文本。
  → 新增 `scripts/extract-docx.mjs` 抽出 `src_full.txt`（3926 / 65 / 750 行），
  `scripts/gen-skill-index.mjs` 生成 `INDEX.md`（章 → 行号区间）；SKILL.md 的「参考文档」段改成
  **先查索引、按行号 read**，`.docx` 降级为"只在逐字核对措辞时看"。
- **索引生成器把"重号章"当成样例藏了**：平面文档第 89 行和第 116 行都是"第四章"
  （全域色彩美学引擎 / 设计生成基本范式与核心模板库），旧逻辑把第二个丢进"非序列标题"，
  缺陷被"多半是正文里的样例"这句话盖掉。现在**重号章既进章节表（行号区间才对）又报"章号重复"**。
- **死链**：`gpt-image-2` 的 7 个 `cases/*.md` 是外部库
  （`D:/pi-workspace/工程/awesome-gpt-image-2-prompts/`）里的相对路径——改成绝对路径，
  并写明"没 clone 到本地时就别去 read"；`diagram-design` 多处写 `style-guide.md` 却没带 `references/`
  目录，还有 3 处让 agent 去跑**本机不存在**的 `verify-geometry.py` / `verify-motion.py`
  （上游脚本没随技能过来）→ 改指随技能发布的 `scripts/self_check.py`。
- **"运行时产物"要说清**：`novel-forge-v10` / `novel-writing-pipeline` 里的
  `current_state.json`、`canon.md`、`souls/<角色>.json` 是**你小说工程目录里**的文件，
  不是技能自带的——现在都标了"运行时产物，别去 `skills/` 下找"。
- **契约测试从"只看万像两个"扩到全部 17 个**：`tests/unit/wanxiang-skills.test.mjs`
  → `tests/unit/skills-contract.test.mjs`（11 条），新增三条最值钱的：
  **加载器必须真的读到每个技能的 description**（CRLF/引号/frontmatter 坏掉就红——实测
  `diagram-design`、`seedance-25` 整份是 CRLF，旧正则只认 `\n`，这两个技能的说明一直是正文第一行）、
  **引用不到的文件必须写明是运行时产物**、**原文 docx 不得被抽取文本顶掉**。
- 加载器容 CRLF（`engine/context-loader.mjs:177`），两个 CRLF 技能归一为 LF。

**仍未做（跟你报备过，没动你的材料）**：`wx_full.txt` 里 35 处【旧版补充】块**只标出位置，
没有物理搬动**——搬动等于改写你的合并稿；`万像平面` 的第 17/18 章在 `D:\遗产` 里找不到正文
（现有 16 章正文 / 17 个标题，第 4 章重号）。

验证：`npm test` **1300/1300 通过**（原 1292 + 新增 8：技能契约 11 条替换旧的 6 条 + 目录注入 3 条）；
另外真机跑了两轮技能激活验收（临时会话，跑完删除），并复述出全部 17 个内置技能名。

## [2.32.0] - 2026-09-15

### 打磨：你写的那两个万像技能（`skills/wanxiang-portrait` / `wanxiang-design`）
先按"能不能被执行"的标准逐条查了一遍，问题不在文笔，在**指令做不到**：

- **写着"复杂需求必须读取完整文档"，但没法执行**：人物源的 `wx_full.txt` 有 6652 行
  （13.3 万字），整份读进来十几万 token，等于没读；平面源更糟——它指向一个 **.docx**，
  而 `read` 工具**读不了二进制**，那条强制要求从一开始就是空话。
  → 新增 `skills/scripts/gen-index.mjs`，**从源文机械生成**两份 `INDEX.md`（章 → 行号区间 + 每章首句）。
  实测：只读第五章（5234 字）就拿到证件照全部参数，**省下 96% 的上下文**。
  平面那份同时抽出 `wx_design_full.txt`（1553 行可读文本，docx 保留为原文）。
- **数字是错的**：平面技能写着"18 章 / 8.2 万字"，实测是 **17 个章标题、16 个编号、3.8 万字**；
  而且**第 4 章重号**（第 89 行"全域色彩美学引擎"、第 116 行"设计生成基本范式与核心模板库"）——
  引用时必须带行号，否则指不清是哪一章。索引里如实写明。
- **人物源文的合并缺陷被藏住了**：35 处【旧版补充】块被放在了**下一个章标题之前**
  （读到某一章会觉得"顺序怪"），另有 3 处断句残片（丢掉前半句的孤行）。索引把位置逐行标出并写明"不要引用"。
- **description 超长会被截断**：`engine/context-loader.mjs:178` 只取前 120 字进技能目录，
  原文 145/161 字——触发语虽然活下来了，出处却被切断。现在两份都压到 120 字内，触发语前置。
- **补上缺的东西**：两份 SKILL.md 都加了**输出契约**（五要素齐全 / 符号只用那 7 个 / 不留占位符 /
  必带负面词）、**什么时候必须去读原文**的对照表（带行号）、**常见失败**、
  以及**本机出图通路**——原文讲的 MJ/SD/即梦/Imagen 是**提示词写法**的参考，
  本机实际能调的是 `agnes-image-*`，走 `generate_image` / 对话里说"画一张"。
- 新增 `tests/unit/wanxiang-skills.test.mjs`（6 条）：description ≤120 且触发语在前 120、
  引用的仓库内路径必须存在、**`INDEX.md` 必须与生成器现算结果逐字一致**（改了源文不重跑 → 红灯）、
  索引行号必须真的落在章节标题上、源文缺陷必须如实声明、旧的不实数字不得回潮。

### 验证（照技能真跑一次）
按索引定位第五章 → 取参数 → 按输出契约组装提示词 → 真出图（`agnes-image-2.5-flash`，29 秒）：
产物**四角亮度 246–250（纯白背景达标）、中心亮度 188（主体入画）**，符合技能里"纯白 L≥90"的硬指标。
顺带记下一个实测坑并写进技能：本机出图是 **1024×1024 方图**，而证件照要 35×45mm 竖幅、
商务照要 4×5——**规格靠出图后裁剪**，别指望一句 `350dpi` 写出来。

## [2.31.0] - 2026-09-15

### 新增：台词专科 + 深度构思（用户："人物场景搭上了，对话和构思还是不行"）
去 GitHub 把同行的东西翻了一遍，真正有用的**不是更长的提示词，是把手艺变成能逐条过的规则，
而且能机检的先机检**：

- [manju-laoli-skill](https://github.com/lixiaoxiao9888-create/manju-laoli-skill)：
  **台词语速自检**（标准 3.5~5 字/秒、慢速/快速档、**标点不计入字数**、单句 >24 字强制拆镜、
  快档连续不超 3 秒、金句后留 0.3~0.8s）+ **台词七维诊断**（辨识度/潜台词/冲突推进力/类型语感/
  信息效率/节奏/金句潜力，输出要求"每条改写 ≥3 条维度依据"）。
- [short-drama-factory](https://github.com/lixiaoxiao9888-create/short-drama-factory)：
  **情绪契约**（全剧不变的是那口气，矛盾只是单元载具）、**单条矛盾硬帽 20~30 集**、
  换单元的**三缝合一**、**四账台账**（伏笔/人物/道具/规则）、单集机检（前 3 秒冲突 / 断章存在）。
- [drama-skills](https://github.com/zenstory-ai/drama-skills)（漫剧工作室产线，上千项目）：
  **因果节拍**（每场必须改变信息/权力/关系/情绪/物理/风险之一）、"刻意把确认放在生产之前"。
- [Dramatron](https://github.com/google-deepmind/dramatron)：**台词是独立的一层**，不和情节揉在一起生成。

落地成两块，都是"机检先上、模型只负责改写和给方案"：

- **台词专科**（`engine/story-craft.mjs` + `StoryDialogue.tsx`）：
  ① **体检不花模型钱**——字数（只数可发音字符）、需要的秒数（含标点停顿预算）、
     哪句超 24 字要拆几镜、这句在不在这一段的时长预算内、有没有连续快语超 3 秒、整段有没有短句气口；
  ② 七维里能机检的那几维：**0 层直白**（"我很愤怒，我要杀了你"）、**自报家门**（"我是你师兄"）、
     寒暄附和、**古装出现现代词**、说话人句子长短无差别（语言指纹不清）；
  ③ **诊断才调模型**：把机检事实一并喂进去，逐句改写并要求每条 ≥3 条维度依据——
     只给一句"更自然了"的条目会被标成"依据不足"；
  ④ **采纳只替换那一句**，同段其它台词一个字不动。
  机检边界写在界面上：**金句潜力机检查不了，别把体检通过当成台词好**。
- **深度构思**（`StoryCraft.tsx`）：情绪契约（含"绝不违背"清单）、人物四件套
  （欲望/秘密/弧光/**语言指纹**+功能位）、矛盾单元（含硬帽与接缝三合一：火种/对手/弧光）、
  分集地图（目标 + 前 3 秒冲突 + 断章钩子）、因果节拍（因此/但是，标的改变项）、四账台账。
  **草稿不落盘**，确认后才保存；分集地图能**一键建成集**（目标与钩子写进集里，已存在的集不覆盖）。
  机检：伏笔没写回收集 / 回收集不晚于埋设集 / 单元硬帽超 30 集 / 每集缺钩子 / "然后"式并列 /
  **地图覆盖不全**（真机：要 60 集，模型只给了 3 集，当时体检一声不吭）。
- 台词体检并进**连续性体检**（不花钱就能看到"这一句 37 字，最快也要 7.4 秒，而这段只有 5 秒"）；
  缺深度构思只算 **advisory**，不把体检总级别染黄——总级别的含义是"现在能不能生成"。
- 这套规则也写进了**内置方法包**（竖屏短剧 / 小说改编），一键分镜与原著改编会带着它走。

### 验证（真模型 + 真界面）
- 刻意喂一份"外行稿"（台词直白、在解释情绪、46 字长句）：体检零成本指出 5 处问题；
  真模型诊断把「我很愤怒，我要杀了你！」改成「师兄，三年了，刀还是冷的。」、
  把说教句改成「雨停了吗？」、把 46 字长句拆成两句并给出**逐镜拆镜方案**（含每镜字数），
  每条都给了 3 条维度依据。
- 深度构思真模型 145 秒产出：5 条"绝不违背"（含"反转必须建立在前文伏笔上"）、4 个人物各有语言指纹、
  3 个矛盾单元（硬帽 25/25/10）带接缝三合一、分集地图的钩子具体到"车窗倒影里站着一个人却没有脸"、
  伏笔账（埋 1→收 25）与因果节拍（因此/但是）。
- 真浏览器：两个面板渲染正确、控制台零错误；**已保存的构思也会当场体检**（"1 集 / 计划 12 集"、
  "伏笔没写回收集"两条都报了出来——这是真机上抓到并当场补的缺口）。
- 新增 9 条手艺契约 + 2 条界面契约；全量 **1284/1284**。探针项目与脚本全部清理。

## [2.30.0] - 2026-09-15

### 新增：删项目；选项目不再用下拉框
用户的原话是「加个项目删除吧，或者项目那款不用下拉框了」。两件一起做了，因为它们是同一个问题：
项目一多，`<select>` 里只看得见标题——哪个有成片、哪个是随手建的草稿、哪个是上周的，全看不出来，
更不能删。

- **项目架**（`StoryProjects.tsx`）取代下拉框：每个项目一行，显示**标题 + 规模（场/段/集/成片）+
  最近改动时间 + 梗概**，行内「打开」「删除」；当前项目高亮并带「当前」徽标。
  刻意**点「打开」才切换**——翻一下列表就把正在写的一段换掉，是最烦人的那种"顺手"。
- **删项目**（`DELETE /api/story/projects/:id`）按"克制"来设计：
  - **先留副本**到 `story-projects/.trash/<id>.<时间>.json`（`.trash` 是子目录，不会出现在项目列表里）。
    删整部戏是不可逆的，副本成本只有几 KB；
  - **产物文件默认不动**（还在工作区里，能从「资产」找到），要一起清得显式勾选；
    勾了也只清**没有被别的项目引用**的，被引用的保留并说清被谁挡住；
  - 确认块里写清会删掉什么、文件会怎样、副本在哪。

### 修复：删完列表那一行还在
真机点验时抓到的：删掉项目之后弹了「已删除」，**项目架里那一行却还在**（API 已经删了，界面没跟上）。
现在按 id 从本地列表里直接剔除，而不是重新拉一遍——重新拉会走到 `load()` 的
"找不到当前项目就选第一个"分支，把用户顺手带到别的项目上。

### 修复：「当前」徽标的字看不见（而且我一开始改错了方向）
截图里那个蓝色胶囊里的「当前」是看不见的。第一反应是颜色写错了（写的是 `var(--pi-bg1)`），
改成 `#fff` 后**计算样式说白色、放大截图看着仍是深色**。最后用像素定案：
取胶囊底色与字形区的平均亮度——**底色 91、字形区 190**（连拍两次一致），字确实是白的。
真正的病根是选择器权重：徽标是个 `<span>`，被 `.story-project-main span { color: var(--pi-dim2) }`
盖掉了（同权重、后者在后）。修成 `.story-project-main .story-project-badge` 才生效。
那张"看着还是深色"的放大图是 `deviceScaleFactor:3` × `clip.scale:4` 双重缩放出来的假象——
**截图会骗人，像素不会**：以后遇到"样式改了但看着没变"，先量像素再下结论。

### 验证（真机）
- 真浏览器：旧下拉框确实不存在了；项目架列出 5 行，含规模、时间、当前徽标；
  点「打开」切到 A（顶栏随之变成「当前「项目架A」」）；点 B 的「删除」→ 确认块（含"连产物文件一起删"勾选）
  → 确认后**列表里那一行立刻消失、计数从 5 变 4**，与 API 列表一致；提示文案准确；
  `.trash/` 里确有 B 的副本、原记录确实不在；用户自己的 3 个项目一个没动；控制台零错误。
- 新增 4 条契约（删项目留副本+默认不碰文件、deleteFiles 只清没被引用的、不碰别的项目的产物、
  界面契约：下拉框必须真的撤掉）；全量 **1275/1275**。

## [2.29.0] - 2026-09-15

### 修复：「智能填充返回的内容不是有效 JSON」
用户真机上撞到的报错。根因是**同一个文件里两套宽容度**：`parseStoryboard`（一键分镜）与
`parseAdapt`（原著改编）早就从"先复述一段再给 JSON"里把答案抠出来了，而
`parseStoryAssist`（智能填充）还是一次 `JSON.parse` 定生死——模型多写一句
"好的，我来补一份草稿"就整条路断掉，而错误信息里一条线索都没有。

- **解析对齐宽容度**：围栏、前后解释、包一层 `assist/draft/data/result`、
  先复述提示词里的「已有状态」再给答案，全部能解析。
  候选的挑选顺序有个讲究：**优先取带 `scene`/`beat` 的那一个**——
  被复述的「已有设定」里也有 characters/locations（看名字完全像一份合格回答），
  唯一的区别是它没有 scene/beat（bible 里根本没这两个字段）。只按"有没有内容"挑，
  就会把复述当草稿（写这条时实测即踩到）。
- **"解析成功"要有底线**：模型回 `{}` 或 `{"error":"…"}` 不再算成功——
  以前会一路变成一份空草稿，用户点了「采用」才发现是空的。
- **先重试同一个模型，再换模型**：解析失败时把"只输出那一个 JSON 对象、不要开场白、
  不要代码块"说到不能再直白，再问一次；两次都不行才换备选模型
  （换模型往往更慢更贵，不该是第一反应）。重试过就在响应里如实标 `retried`。
- **错误信息要能排障**：说清"找不到 JSON / JSON 里没有可用内容"、试过几个候选、顶层键、
  原文开头，并给出下一步（换构思模型）；模型侧问题回 **502**，不再混成 400 的"用户输入错"。
  原文同时打进服务端日志，形状不符还是模型跑偏一眼可分。

### 验证（真机 + 用户的那个项目）
- 用**用户自己那个报错留下的项目**（`648a813d…`，只有占位骨架、没有设定）和**同一个想法**
  复验：HTTP 200、19 秒、`agnes-3.0-flash` 一次成功，给出 2 个人物、1 个地点、场景
  「觉醒仪式上的异类」、画面段落与台词；并核对项目文件前后一致（智能填充只出草稿、不落盘）。
- 另跑两个新想法（常规 / 抽象）：10 秒与 14 秒都成功，草稿还能走真实 PATCH 存下来。
- 新增 5 条契约（宽容度 4 种形状 + 挑候选的规则 + 空的 JSON 不算成功 + 报错线索 +
  重试/换模型的顺序 + 502）；全量 **1271/1271**。

## [2.28.0] - 2026-09-15

### 修复：参考图（定妆照 / 场景 / 道具）也受本地化契约约束
上一轮把"外链先搞到本地"只补到了成片合成与产出上，参考图这一块还是同一个坑——
而参考图是**长期复用的锚点**，挂在会过期的外站链接上，几天后人物一致性就悄悄失效，
界面上却一直显示"已生成"。三处一起补：

- **全项目补下载**：新增 `POST /localize`（`scope: refs | runs | all`），一次把还挂在外站的
  参考图与每一次生成的产出都拉到本地并写回（bible 条目 / run.outputAssets）。
  只补下载、**不重新生成**——产物还在，重生成要多花一次钱。已经在本地的不重下（幂等），
  **全下完了才清掉"没能存到本地"那条降级说明**（留着就是假话）。
  段落挂载的素材刻意不落盘（它引用的是别的工作台的产物，再存一份只是重复占地方），
  但返回里**明说**这件事——"没做"和"忘了做"看起来一样。
- **入库路径本身也说真话**：`generateAssetRef` 此前不把 `localizeError` 带出来，
  于是"参考图没落到本地"和"落好了"在界面上长得一模一样。现在返回 `localizeError` + 一句
  note（说清会过期、并指向补下载入口）。
- **界面**：参考图卡片上标「外链 · 会过期」；设定面板顶部在有外链产物时出现
  「把外站的产物拉到本地」，并说明覆盖范围（参考图与产出都算）。

### 验证（真外链 + 真界面）
- 真 https 参考图（`img_girl.jpg` 36KB / `lamp.jpg` 653B）：补下载后两条都变成本地文件
  （磁盘上确有 36KB / 1KB），第二次补下载 0 项（幂等）。
- 真浏览器点一遍：未补下载时设定面板显示「有 1 个产物还挂在外站临时链接上（会过期）」+
  卡片徽标「外链 · 会过期」；点「把外站的产物拉到本地」→ **1 秒后** API 里定妆照已是本地地址、
  文件 36KB、`<img>` 正常显示；提示块与徽标同时消失；提示文案为
  「已把 1 个外站产物下载到本地并写回项目——外站链接失效也不影响了。（挂载素材不重复落盘：…）」。
  全程控制台零错误。
- 新增 6 条契约；全量 **1266/1266**。探针项目与它拉下来的产物文件全部删除，
  你自己的产物一个没动。

## [2.27.0] - 2026-09-15

### 修复：外链产物没有"优先搞到本地"——这是本地化契约（docs/NAMING.md 第三节）
用户指出来的：**外站产物必须先下载到本地再入库**，这条早就写在契约里，
我却在挑片段时把外链判成"拼不进去"——等于把契约做丢了。改法不是"允许外链"，而是**先把它搞到本地**：

- **合成成片时**：每一段先看本地有没有文件；没有就**当场下载到本地**（`saveArtifact`，含 data URL
  解码），下载完再拼。下载成功就把地址**写回项目**、并清掉那条"没能存到本地"的降级说明
  （已经落盘了还留着旧说法就是假话）；下一次合成不会再重下。
- **候选清单**：本地已有 vs 可下载的外链分开标（`exists` / `external` / `localable`），
  外链版本从此**算可用、也能被推荐**——它的错是"还没下"，不是"拼不了"。
- **没挑片段的老路径同样修了**：以前它只收集本地已有的文件，外链片段是**静默漏掉**的
  （少拼一段而一字不提，比报错难查得多）。现在两条路走同一段逻辑，漏掉的都会进 `skipped` 带原因。
- **只有两种情况才真拼不了**，而且都说清原因：落盘实现没接入（配置问题）、下载失败
  （SSRF 守卫拦内网地址、上游 404/502、超过 50MB——`saveArtifact` 会给出 reason）。
- **新增补下载入口**：某一版入库时下载失败留下的外站临时链接，此前除了"重新生成一次"
  （再花一次钱）没有任何补救办法。现在版本卡片上有「外链产物 · 下载到本地」，
  只补下载、不重新生成；失败了保留原链接与原说明，成功了才改地址。

### 验证（这一条专门用真外链跑）
- 一次性项目里放两段外链：一个 `data:` URL（走不需要网络的解码分支）、
  一个**真 https**（`https://www.w3schools.com/html/mov_bbb.mp4`，788KB，公开测试片）。
  SSRF 守卫会（正确地）拦回环地址，所以用本地服务器测等于测不到真实下载路径。
- 合成前候选：两段都标 `external: true, localable: true`、各推荐了外链版本（旧代码这里是"可用 0"）。
- 合成（3 秒）：`localized` 两条，工作区里真的落下 2KB 与 **770KB** 两个文件；
  两条产出的地址都换成 `/api/ws/file?...`，那条"没能存到本地"的降级说明被清掉。
- **第二次合成 `localized: 0`**——已经落本地了，不重复下载。
- 探针项目、临时目录与它生成的产物文件全部删除（`生成物/视频/2026-09-15` 里你自己的产物一个没动）。
- 新增 3 条契约（外链先下载再拼并写回、下载失败如实报因、补下载幂等）；全量 **1261/1261**。

## [2.26.0] - 2026-09-15

### 新增：删掉不要的那几版 + 合成成片时挑片段
用户的原话是「有些剧情片段我生成了好几个镜头了」——同一段生成好几版之后，
此前只能全留着、合成时也只能"按分镜顺序自动拼"，等于把挑片子的权利拿走了。

- **删掉某一版产出**（`POST /run-delete`）：记录必删，但**删文件必须克制**，三条边界写在代码里：
  - 文件还被别处引用（别的段、**别的项目**、设定里的参考图、段落挂的素材）→ **只移除记录、保留文件**，
    并说清是被谁挡住的。生成物目录是所有项目共享的，只查当前项目就会把别人还指着的那张图删掉；
  - 还在排队的版本**默认拒绝**（上游那个任务还在跑，删了记录就永远收不回来），要删得点「强制删除」；
  - 外链与 data URL **永不碰磁盘**。
  界面上是不可逆操作该有的样子：先出一个确认块，里面对「文件留不留」给出选项，并写明后果。
- **合成成片时挑片段**（`GET /film-plan` + `POST /film` 带 `clips`）：
  每一段的下拉里列出这一段生成过的**每一版**（第几版、seed、状态），可以换版本、整段不要、上移下移调顺序
  （顺序就是成片里的先后）。**默认值仍是原来那套**（每段取最新可用的一版、按分镜顺序），不改也能一键合成。
  挑不出来的（文件被移走、这一版刚被删、还是排队中）**逐条说明原因**，并记进 `skipped`——
  少拼一段而没有任何提示，比直接报错难查得多。
  成片记录里从此带上 `picks`（这一版成片用的是**哪一版的镜头**），刷新后仍能对得上。

### 修复：界面上的字面星号
顺着上一条检查时发现：`**强调**` 写在 JSX 文本里是**字面星号**，不是加粗——界面上真的显示了 `**工艺**`、
`**没有真机打开验证过**` 这种字样（分镜/剧本/配方几处都有，本次一并改成 `<strong>` 或「」）。
这类问题只有在界面上看一眼才会发现。

### 验证（真机）
这一轮的真机装置做到了"可核对的证据"，而不只是"点了一下"：
- 用 ffmpeg lavfi 现造四段纯色 1 秒片子（红/蓝 = 第 1 段两版，绿/黄 = 第 2 段两版），
  在真浏览器里挑「第 1 段用蓝色（第 2 版）、第 2 段用绿色（第 1 版）」再整体倒序 → 合成。
  成片记录 `picks` 与所选一致，**抽帧核对主色：第 0.5 秒是绿、第 1.5 秒是蓝**——
  顺序与版本是真的按挑的来，不靠界面上的一句话。
- 删除：界面出确认块（含「文件留不留」勾选）→ 确认后该段从 2 版变 1 版、提示「文件也删了 1 个」、
  API 核对一致；跨项目引用保护另用探针单独验证（引用方项目还指着时，文件被保留并说明被谁挡住）。
- 全程控制台零错误；一次性项目与探针素材用完即删，用户真实项目未动。
- 新增 12 条契约；全量 **1258/1258**。

## [2.25.0] - 2026-09-15

### 修复：真机跑出来的三个真问题（其中一个是上一版引入的回归）
上一版交付后只做了"构建产物里有这段字符串"的核对，没有真的点一遍界面、真的跑一次生成。
这轮装了零依赖的 CDP 驱动（系统 Chrome + Node 内置 WebSocket）把界面点了一遍、真花额度跑了批量，
结果打出三件单测永远发现不了的事：

- **seed 越界，每一张画面都被上游 400 拒掉。** 上一版"seed 真的上送"改对了方向，
  但 `randomSeed()` 掷的是 `0..2^31-1`，而 Agnes 图像接口的原文要求是
  `seed must be between -1 and 999`。于是真机上**两段画面全部失败**，界面上只有一句
  "图像模型未返回图片"。现在：随机 seed 落在 `[0, 999]`，用户填的越界值**夹进区间并如实告知**
  （`run.seed` 记的是实际发出去的那个——不然"填 123456 能复现"就是假话），
  变体 seed 到顶绕回开头（999+变体数会直接换一个 400）。
  同时改掉一条**把 bug 写进断言的旧测试**：它断言的正是 `[1000, 1001, 1002]` 这个上游不收的范围。
- **图像失败被吞成 `return null`。** `generateImage` 原先 `if (!r.ok) return null`，
  于是"Key 无效 / 模型名不对 / 参数不认 / 被限流"四种原因在界面上长得一模一样，用户没法处理、
  我也没法排查。现在带上**状态码与响应体**；并且 400/401/403/429 **不再换地址重试**
  （那是请求或账号的问题，换 endpoint 一样被拒，还把那句真正的错误挤出错误信息）。
- **上游"忙"被当成"拒绝"。** 真机撞到 `video_queue_full 503 — video queue is full, please retry later`——
  上游明明让我们稍后再试，实现却把它当终局失败报给用户。现在瞬时错误退避重试两次
  （2s/5s），**计费/权限/参数类错误一次就返回**；重试过就在错误里说清重试过。
- 顺带修：集标题里已经写了「第 N 集」时，界面会显示成「第 1 集 · 第 1 集 · 站台」。

### 新增：创作方法包（Skill —— 程序性知识）
研究 [Lovart](https://www.lovart.ai/)（[国内版](https://www.lovart.art/)）时最值得抄的一条不是
"接了多少模型"，而是它的 **Skill**：封装的是一个**创作任务的整套方法**——任务怎么拆、按什么顺序执行、
遵循什么专业标准、修改时从哪一步入手、交付物要满足什么要求；而且**用户跑通的一次过程能一键存成个人技能**。
官方口径（[极客公园专访](https://hub.baai.ac.cn/view/57618)）："垂直 Agent 的竞争，正从'谁接了最强的模型'
走向'谁积累了最多专业工作方法'"。

元枢此前的"配方"只存**工艺参数**（类型/模型/尺寸/负向/seed/参考图策略）。
"一集 90 秒、每集 6 场、前 3 秒出冲突、每集结尾留钩子"这些是**方法**，不是参数，此前没有任何落点——
每次都要在提示词里重新交代，于是同一部戏的第一集和第五集往往不是一个打法。

- 新增 `engine/story-methods.mjs`：4 个预置方法包（竖屏短剧 / 小说改编成短剧 / 品牌短片 / 纪实短片），
  每个都有**能对应到界面上真有的动作**的步骤、必须遵守的规则、交付前能逐条看的自检项、该拿出的东西。
- **用户能把自己跑通的打法存成方法包**（从当前项目反推）：只抄结构与配比
  （几集、每集几场、每场几段、目标时长、类型配比），**不抄提示词与台词**——抄了内容就不能跨项目用。
- 方法包真的进到模型调用里：一键分镜与原著改编会带上方法段与规则，并按 `fast / thinking`
  两档决定输出预算（照 Lovart 的 `--mode`）。空方法包不该让调用多花 token。
- 内置方法包**不可改不可删**（改它等于改所有项目的标准，升级还会被覆盖），要改就"复制成我的"。
- 边界写在代码里：方法包**只给建议**——套用时只给"还没排过时长"的集写目标时长，
  **不覆盖**用户自己排过的时长，也不覆盖人工调好的画风。

### 新增：高消耗先确认 + 收尾查询合并（照 Lovart 的接口级约定）
- **高消耗操作先 confirm**：批量生成里有视频段、或一次要跑 4 段以上时，先摊开
  "这次会真实调用：N 张画面 + M 段视频（合计约 X 秒）"，确认了才跑。别的直接跑——
  每次都弹确认，确认就会变成闭眼点掉的东西。刻意**不编单价**：我们没有各平台的单价数据，
  只报"会真实调用多少次、要等多久"，额度归各平台各自计费。
- **收尾查询合并 + 退避**：批量挂着 N 个视频任务号时，原来是逐个 run 去问（N 个请求 + N 轮上游问答）。
  现在 `POST /run-check-many` 一次收尾全部：一次读盘、一轮问答、**一次写盘**；
  轮询间隔按剩余数量退避（5s → 最长 30s）。这与 Lovart 把"查询"单独分一档限流
  （写 60/分、读 300/分）是同一个道理。查不到的运行 id 如实回报，不假装查过。

### 验证：真机（这次是真的点了、跑了）
- 装了零依赖的 CDP 驱动（系统 Chrome + Node 内置 WebSocket），三趟真机：真浏览器、真点击、真额度。
  - 两段画面 + 一段视频的批量：视频**真出片**（152 秒，落盘 `/api/ws/file?...`），
    两段画面当时因 seed 越界全失败——**失败原因逐条显示在批量表里**，这才有了上面第一条修复。
  - 修完 seed 后重跑：两段画面 **2/2 出片**（seed 846 / 646，都在区间内）；
    视频这次撞上游队列满，错因完整显示（`video_queue_full 503`），催生了退避重试。
  - 确认步骤真的拦住了：点「开始批量生成」只弹确认清单、行数为 0，点「确认，跑这 3 段」才真的开跑。
  - 方法包面板：4 个预置可选、步骤/规则/自检项都渲染出来、套用成功、从项目存成方法包成功。
  - 全程**控制台零错误**；用户真实项目（`44ee44b2…`）文件 mtime/sha256 未变。
- 新增 14 条契约（方法包 7 条 + 合并收尾 / 图像错误可见 / 视频退避重试 / 集标题 / seed 区间）；
  全量 **1246/1246**。

## [2.24.0] - 2026-09-15

### 新增：分集（短剧/系列的组织单位）
- 对照 [PINNGOO AI](https://www.pinngoo.cn/)「小说转分集短剧」与 [LibTV](https://www.liblib.tv/)
  按集拆镜头时最明显的一条：**短剧是以"集"为组织单位的**，而元枢此前只有「场」——
  做短剧时手里是一堆平铺的场，没有"第 3 集"这个概念，"这一集多长/有哪些场/还差多少没生成"都答不上来。
- 新增 `engine/story-episodes.mjs`：集是项目上的独立实体（`project.episodes`），场用 `scene.episodeId` 归属。
  三条刻意的取舍：**允许"未分集"**（单集短片照旧能用，未分组的场永远排在最后而不是被塞进某一集）；
  **删集只解绑不删场**（删一个集把内容一起带走是最不能接受的顺手）；**一集一本账**
  （场/段/已出/失败/排队，界面与批量生成共用同一个数，不各算一遍）。
- 剧本导出带上集标题：`# 第 1 集 …`（Fountain）/`【第 1 集 …】`（中文剧本）/Action 段落（FDX），
  未分集的场显式标为「（未分集）」——不许因为数据缺一块就静默跳过。
- 制作台新增分集面板（建/改/删/拖归集）。顺带修掉一个静默失灵：从分集面板点某一场会被落到第 1 场，
  因为 `selected` 只按**段 id** 定位（场 id 匹配不上就回退第一场）。

### 新增：原著改编（小说 → 分集大纲 → 分集剧本）
- PINNGOO 的主线是「导入原著 → 总览/人物关系/分集大纲 → 批量分集剧本 → 视觉制作」；
  元枢此前只能把小说工坊的**某一章**当文本素材挂在某一段上，一整本书进来仍是一个大平铺。
- 新增 `POST /api/story/projects/:id/adapt`：按**粘贴正文**或**小说工坊的书**（可选哪几章）改编，
  一次落成 集 + 场 + 段（含台词），段落继承链跨集串好，人物与关系并进设定。
  另加 `adaptations` 留档：刷新之后仍能回答"这个项目是从哪本小说、哪几章改出来的"。
- 三条边界：**先预览再花钱**（`preview:true` 只读书、只报字数，一次模型调用都不发）；
  **原文超 6 万字就明说**（界面照实显示会截断，不让人以为整本都改编了）；
  **解析不全不算整体失败**（能出几集就落几集，"哪几集没解析出来""模型第一次少给了几集"全部如实上报）。
- 段落类型由改编决定：与一键分镜**相反**，改编给的 novel/image/video 分工**不被项目默认配方的 kind 覆盖**
  ——那是改编结果本身，拿一个全局 kind 抹平等于把结果毁了（负向仍照旧套用）。
- 场景/段落的清洗**只此一份**（`cleanScenes`，分镜与改编共用）：真实模型返回的形状五花八门，
  写成两份必然漂移。同时补上场次标题的解析——模型给一行「外景 站台 夜」或「INT. 车内 - 凌晨」
  也要拆成 内外景/地点/时间，而不是整行丢给"地点"。

### 新增：跨段批量生成（这一场 / 这一集 / 全项目没出的都补上）
- 对照 LibTV「批量脚本→分镜图→视频片段」与 PINNGOO「批量生成分集剧本」：
  此前只能一段一段点，一集十几段就是十几轮点击加十几轮等待。
- 新增批量面板：**逐段串行提交**（不并发轰炸上游——视频任务本来就慢，并发只会一起排队一起超窗），
  提交完统一短轮询；视频创建成功只算"排上队"，**超窗不算失败**（任务号留着，可单条「查一次」）；
  每段成败逐条列在表里，失败的如实列出，不把整批说成"完成"；随时可停，未提交的段落保持原样。

### 新增：风格预设（统一画风）
- 此前只有一个 `bible.style` 自由文本框，每次都要重新描述一遍画风，不同段落很容易写得不一样
  ——而画风一旦漂移，人物锁得再准也救不回来。
- 新增 `frontend/src/lib/story-styles.ts`：10 种预置画风（电影感写实 / 3D 动画 / 国漫二维 / 水墨武侠 /
  纪录片手持 / 黑色电影 / 赛博朋克 / 日式动画 / 治愈日常 / 短剧强钩子），每套给 `visual` + `tone`，
  在设定面板选完写进 `bible.style` 且仍可手改。

### 验证
- 新增 26 条契约（改编解析的宽容形状与"哪几集塌了要如实说"、分集账与"删集不删场"、
  改编不被默认配方抹平、预览零调用、批量串行与超窗语义、台前入口是否真的接上通路）；
  全量 **1232/1232**。
- 真机验证（一次性丢弃项目，用完即删；用户真实项目 mtime/sha256 未变）：
  按小说工坊的书预览 2 章 4953 字（零模型调用）→ 真跑一次改编，`agnes-3.0-flash` 首次调用即给出
  2 集 / 6 场 / 11 段，台词 16 行、说话人 3 人，继承链 10/11，Fountain 与中文剧本都带上了集标题。

## [2.23.0] - 2026-09-15
### 修复
- **「名字出现在提示词里就挂参考图」这条规则在真实调用里是退化的。** 追查它时发现：
  `compileStoryPrompt` 会把**整个 bible**（角色/场景/道具全列）写进提示词，于是"名字出现"对
  **每一个**条目都成立——一段视频原本会把所有参考图全挂上（每张内联成 base64 都是 MB 级，
  既烧钱又把噪音塞给模型）。现在判定改为基于**段落自己的文本**（`beatReferenceText` =
  prompt + dialogue + action），并且"一个都没命中"时退回**主角一张**（不是"前 N 张"）。
  这条是给场景/道具扩参考图时被自己的测试逼出来的，改动同时修好了角色侧的老问题。

### 新增：场景与道具参考图（对手都在解决的"场景漂移"）
- 研究 [LibTV](https://www.liblib.tv/)（LiblibAI 的无限画布视频工作台）与
  [PINNGOO AI](https://www.pinngoo.cn/)（屏菓悟）时，两边都在解决同一件事：
  **角色/场景/道具作为资产复用，保证跨镜头不漂移**（PINNGOO 叫"资产库"，LibTV 有"角色三视图 + 资产复用"）。
  而我们此前**只有角色有参考图**（定妆照），场景和道具只有文字——同一间屋子在两段里长得不一样。
- `generatePortrait` 泛化成 `generateAssetRef`（`assetType: character | location | prop`），
  三件共用一套流程与一条通路（`POST /api/story/projects/:id/asset-ref`），
  写回 `bible` 对应条目的 `refImage`；老的 `generatePortrait` 保留为它的特例（返回形状兼容）。
- 新增 `buildAssetPrompt`：场景图要求**构图中性、把人排除在外**（有人的场景图当不了场景锚点），
  道具图要求单一物件、完整入画、纯净背景。理由与定妆照一致：参考图必须是**可复用的锚点**，
  不能是"一张有剧情有情绪的插画"。
- 挂载规则与角色完全相同（谁被这一段提到就挂谁），界面在设定面板里按「角色定妆照 /
  场景参考图 / 道具参考图」三组列出，各自显示"已有 N/M"。

### 验证
- 新增 2 条契约（挂载规则含"没提到就不挂"、三件生成与写回各归其位）+ 修 2 条被退化行为污染的旧期望；
  全量 **1206/1206**。


## [2.22.0] - 2026-09-15
### 新增：照 Laper 补三件（剧本要素与导出 / 全剧上下文 / 试戏）
Laper（[laper.ai](https://laper.ai/)）是 AI 原生剧本创作平台，它的地基是**剧本要素 + 工业格式导出**，
外加**200K 全剧本上下文**与**Playground（和 AI 扮演的角色对台词）**。逐项对照后元枢三件都缺：
能出图、出片、出正文，却拿不出一个能给人看的剧本文件；生成时只看得到继承链上的前 3 段；没有任何"试戏"手段。

- **剧本要素与导出**（`engine/story-screenplay.mjs` + `StoryScript.tsx`）：
  - 要素：场景标题（场景上的内外景/地点/时间三要素 → `EXT. 站台 - 夜` / `外景 站台 夜`）、
    **动作**（新增 `beat.action`；留空才退回画面描述）、角色与台词（从 `beat.dialogue` 按
    「角色名：台词」拆，认括号提示 `阿宁（低声）：…`，没冒号的行按"同一人续行 / 否则动作"处理）、
    **转场**（新增 `beat.transition`）。
  - 为什么动作要单独一个字段：`prompt` 是发给图像/视频模型的「动作、构图、镜头、光线」，
    把它整段当剧本动作，会把"镜头怎么推"写进剧本——那不是剧作该写的东西。
  - 导出三种：**中文剧本 .txt**、**Fountain .fountain**（`= 梗概` / `> 转场` 等前缀按规范）、
    **Final Draft .fdx**（XML，六种段落类型）。
    三种导出**共用同一份要素流**（`projectToElements`），谁也不许自己另算一遍。
  - **不发明屏幕排版引擎**（缩进/等宽/分页），也不硬塞没用的要素（字幕这类当前没有数据来源的先不做）。
  - 诚实标注：FDX 结构按公开约定生成、XML 合法且段落配平，但**本机没有 Final Draft，没真机打开验证过**——
    这句写在界面上，不藏着。
- **全剧上下文**（`engine/story-context.mjs`）：把"全剧至今"压成一份有上限的材料带进提示词。
  此前只有继承链上的前 3 段（各截 1.2 万字），写到第 8 段时第 2 段发生的事模型看不见。
  三条原则：**有上限**（默认 2 万字，`STORY_CONTEXT_CHARS` 可配）、
  **先保"发生了什么"再给正文**（场摘要永远保留，正文按段截断）、
  **砍了要说出来**（`truncated` 会显示在执行链上，不让人以为模型看到了全部）。
  执行链新增一行「全剧上下文：N 字（M 场、含 K 段已写正文）」。
- **试戏（Playground）**（`engine/story-playground.mjs` + `StoryPlayground.tsx`）：
  拿一句话去试，看这个角色会不会这么说。角色只能依据设定与「全剧至今」说话，
  不许编新设定、不许替别的角色说话、不许给创作建议；对话留档在 `beat.playground`（最多 20 句）。

### 试戏上线时踩到的三个真问题（都不是"模型不行"一句话能盖过去的）
1. **失败说不清**：`directChat` 在"没有密钥/没有端点/上游非 2xx"时返回 **null**，
   我却把它和"返回了内容但为空"混成一句「角色没有回话」。现在分成三句：
   `没有可用凭据或端点（模型没被调用）` / `返回了内容但里面没有台词（可能是思考占满了输出）` / `在自述而不是演`。
2. **没关思考**：仓库自己写着"填充/短任务必须显式关掉"，我却漏了 `thinking:false`，
   1–3 句台词会被推理模型的思考吃光。已补，并用测试锁住。
3. **自述不是台词**：弱模型把「用户让我扮演角色"阿宁"，和编剧对台词。编剧问"你为什么不走？"」
   当成回答交上来。**绝不能把这种话当角色的台词显示给编剧看**——现在识别出来就换下一个模型
   （按 `handleStoryAssist` 同一套路试 2 个），全都这样就如实说清是哪几个模型在自述。
   另外把"直接说那 1–3 句、不要解释"压到提示词**最后一行**（弱模型对末尾指令更敏感）。
   修完实测：编剧问「你为什么不走？」，阿宁回「走哪儿。」

### 验证
- 新增 14 条契约（剧本要素/三种导出/FDX 转义与配平/上下文上限与截断提示/试戏提示词与三种失败区分）
  + 1 条前端契约；全量 **1203/1203**。
- **上线实测**（探针项目用完即删）：要素统计 `2 场 / 3 动作 / 3 台词 / 1 转场 / 说话人 阿宁、老周`；
  三种导出都拿到（fdx 1139 字、段落配平、六种段落类型齐）；
  第 2 场生成时执行链显示「全剧上下文 964 字（2 场、含 1 段已写正文）」且提示词里确实有第 1 场摘要与第 1 段正文；
  试戏拿到在角色里的回复并落盘 2 回合。

## [2.21.0] - 2026-09-15
### 修复
- **连续创作的视频不再「一条请求干等三分钟」。** 用户真实项目里的一次失败暴露了它：
  `视频生成超时（180s）`（seed 48494449）。查下来是两个问题叠在一起：
  1. `media-api.generateVideo` 的轮询是 36×5s = **180 秒硬上限**，上游排队常常比这久；
  2. 更关键：**视频工坊早就改成了「创建 + 短轮询」**（躲 Cloudflare 100s/524），
     而连续创作还是"一条请求干等"——同一条上游、两种姿势，后者正是仓库自己警告过的写法。

  现在连续创作的视频也走两段式：
  - `runGeneration` 对视频**只创建**，立刻返回 `status: 'running'` + `taskId` + `queuedAt`，
    并把当时发的提示词存进 `run.promptText`（异步收尾还要用它）；
  - 新增 `checkRun` / `POST /api/story/projects/:id/run-check`：**一次调用只查一次**，
    出片就落盘收尾、上游说失败就记失败、还在排队就原样返回；
  - 前端启动后每 5 秒轮询，并给每个排队中的版本一个「查一次（任务号 …）」按钮。
- **「还没好」不再被说成「失败」。** 以前超时直接标 failed；现在超窗（默认 10 分钟，
  `STORY_VIDEO_POLL_MS` 可配）只是**不再自动等**，任务号留着、状态仍是 running，随时可以再问。
  界面文案也分开：创建成功只说「已排上队」，出片才说「片子到了」。
- **启动清扫给"可续查"的运行让路**：带 `taskId` 的 running **不再**被标成「生成被中断」——
  任务还在上游跑着，拿号问一次就能收尾；把它标失败才是真的丢东西。清扫结果里单列 `resumable` 计数。
- 同步路径（聊天旁路）的轮询上限也可配：`VIDEO_POLL_ATTEMPTS` / `VIDEO_POLL_INTERVAL_MS`，
  且超时的说法改成「等上游出片超时（Ns 内没等到，任务号 X 仍可继续查）」——不再伪装成失败。

### 验证
- 新增 5 条（异步契约 / 状态区分 / 配置 / 清扫例外）+ 1 条前端契约；全量 **1189/1189**。
- **上线实测**（探针项目，用完即删）：创建请求 **1923ms 返回**（以前最多等 180s），
  `status=running`、真实任务号 `task_8SJqnJ4k6Y1LJDfFXjmb1BVs3HvIqV8l`、`queuedAt` 已记、
  产物 0 个、`pollWindowMs=600000`；紧接着「查一次」→ `settled=false / status=running /
  upstream=pending / waitedMs=1062`（**排队没有被说成失败**）；
  人为塞一条没有任务号的 running → 「查一次」如实标 failed 并说明「没有任务号，多半是创建阶段就断了」。
- 如实说明：这次验收**真的创建了一个上游视频任务**（agnes），会产生一次计费；
  探针项目已删除，但那笔消耗已经发生。以后这类验收应当用不存在的模型 id 来避免。

## [2.20.0] - 2026-09-15
### 新增（把配方推完整：参考图策略 + 项目默认）
- **参考图策略进配方**：用几张（0–4）、谁优先（挂载素材 / 定妆照）。
  此前这条策略**硬编码在编排层**——画面固定"素材优先且只 1 张"、视频固定"定妆照优先且 4 张"，
  用户在界面上既看不到也改不了，更存不进配方。现在 `buildRunPlan` 按策略算顺序与张数，
  执行链上单列一行「参考图策略」，`run.reference` 记下这一趟实际用的（几张/谁优先/真用了几张）
  ——事后要能回答"为什么这张图没带定妆照"。
  默认值跟"用户显式做了什么"对齐：画面只有一个入口，所以**素材优先**（否则"挂了素材却没发生"
  是最坏的静默失败）；视频能吃多张，所以**定妆照在前**（保人物）、素材跟在后面给场景依据。
  前端 `lib/story-ref.ts` 与后端 `story-recipes.defaultRefStrategy` 是同一套默认值，
  契约测试锁住两处一致——不一致的话界面显示的和真正发出去的就不是一回事。
- **项目级默认配方**：把一套设置设为**项目的默认**，新段落自动套用、段落没说的地方由它兜底。
  同一部片子 10 个段落不必再点 10 遍。
  - 兜底范围：模型 / 参数 / 负向 / 变体数 / 参考图策略；优先级 **显式 > 段落 > 项目默认配方**。
  - **seed 刻意不从默认配方取**：配方里锁一个 seed 是"这一镜要复现"，若它悄悄成了全项目默认，
    每个段落都会拿到同一个 seed——那是没人要的副作用。想锁就在制作台上明写。
  - 新段落自动盖上的只有**类型与负向**（一键分镜在服务端盖、「从此处继续」在前端盖）；
    模型/尺寸/seed 是每次生成时的选择，写进段落会把它们变成看不见的既成事实。
  - 默认配方被删掉时**当作没有，不报错**——一条配方被删不该让整个项目生成不了。
  - 界面：配方卡上「设为项目默认 / 取消项目默认」，当前默认那张会高亮并标「项目默认」。

### 修复
- **`createProject` 把 `defaultRecipeId` 静默丢掉了。** 它是白名单式构造，漏一个字段就等于
  这个功能不存在：第一版"设为项目默认"存进去、再读出来就没了。**测试当场抓到**（新增的
  默认配方契约第一次跑就红）。这已经是同一个坑的第二次（上一次是 `films`），所以在那里
  补了注释：`createProject` 加字段时要一起加。
- **配方的 `seed: null`（没锁）被静默变成了 `seed 0`。** `Number(null) === 0` 会走进
  "是数字"那条分支，于是"没锁 seed"变成了"锁死在 0"——界面上表现为配方卡凭空显示 `seed 0`。
  **这是台前探针抓到的**（单测当时只覆盖了 `undefined`，漏了 `null`，而前端正是传 `null`）。
  现在三态分清：null / undefined / 空串 = 没锁，**0 是合法 seed**，并补了覆盖三态的测试。

### 验证
- 新增 7 条（参考图策略 / 项目默认配方）+ 1 条前端契约 + 1 条配方模块契约；全量 **1182/1182**。
- 真实浏览器：参考图张数/优先可选（画面默认「1 张 · 素材优先」、视频默认「4 张 · 定妆照优先」）；
  配方卡显示「参考图 1 张 · 素材优先」；「设为项目默认」后卡片高亮标「项目默认」、项目上出现
  `defaultRecipeId`；刷新后仍在。

## [2.19.0] - 2026-09-15
### 修复
- **卡在 `running` 的孤儿运行再也不会永远"正在生成"了。** 一次生成由某个进程持有，进程没了
  （重启、崩溃、被打断），那条 run 就永远停在 `running`：既不会成功，也不会失败，界面上一直显示
  「正在生成」。真实案例就是用户项目里那条 `07:40:12` 的 running——被我重启服务时打断，
  一直挂在那里。现在服务启动时统一清扫：`status === 'running' && !finishedAt` → `failed` +
  「生成被中断：服务在生成过程中重启，这一版没有产出」+ `finishedAt`。
  **判据是确定的**：启动这一刻还挂着的必然是孤儿（持有它的进程已经死了），不需要超时猜测，
  也不可能误杀正在跑的任务。幂等，且只写真正被改动的项目。
- **一键分镜「要 4 段只给 1 段」会自动再要一次。** 实测同一提示词、请求 4 段，三次真实调用给的是
  **4 / 1 / 4** 段（`agnes-3.0-flash`）——模型侧抖动，对用户就是"这功能时好时坏"。现在段数
  **少于请求的一半**时自动重试一次，第二次把话说明白（「上一次你只给了 N 段，这次必须给足 M 段」），
  并把 `attempts` / `retried` / `firstBeatCount` 如实回给界面；两次都不够时给 `short: true` 和
  一句「两次都只给了 N 段，可以再点一次或把想法写具体些」——**不假装完成**。

### 新增：配方（对标 ComfyUI「工作流即资产」）
这一轮把 ComfyUI 最值钱的那条补上了：**调好的工作流本身是资产**。
- 新增 `engine/story-recipes.mjs` + `/api/story/recipes`（GET 列 / POST 存 / DELETE 删 /
  `/export` 导出 / `/import` 导入）。配方落在**工作区根**（`story-recipes.json`），
  所以跨项目共用——同一部片子 10 个段落、以及以后的新项目，都不用再点 10 遍。
- **只存工艺，不存故事**：类型 / 模型 / 尺寸或时长 / 负向 / seed / 变体数。
  提示词、台词、素材属于故事，混进配方就变成"换个配方顺手把台词也换了"。
- 同名即覆盖（用户的意图是"更新它"而不是"再来一份"），并保留 `createdAt`。
- 导出是**自足**的一份文件（带 `format`/`version`/`exportedAt`），换台机器/发给别人导入即可用；
  导入认三种形状（导出文件 / 裸数组 / 单个配方对象），坏条目**逐条如实报出**，不静默丢弃。
- 配方文件损坏时报「配方文件读不出来」而不是装作"没有配方"——后者等于让用户以为配方凭空消失。
- **顺带把 `params` 做实了**：此前它是个**没人填的空字段**（界面没有尺寸/时长），
  配方要携带它就必须先有它。现在画面可选尺寸、视频可选尺寸与时长，进 `buildRunPlan` → 适配器 → 请求体。
- 界面：`StoryRecipes.tsx`（保存当前设置 / 套用 / 套用到本项目所有段落 / 导出全部 / 导入 JSON / 删除）。
  「套用到全项目」只写**能存在段落上的**工艺（类型与负向）——模型/尺寸/seed 是每次生成时的选择，
  不该被悄悄改掉。

### 验证
- 新增 6 条配方契约 + 1 条前端契约、2 条（孤儿运行 / 分镜重试）契约；全量 **1173/1173**。
- 上线后实测：探针项目里造一条 `running` → 重启服务 → 该条变成 `failed` +
  「生成被中断…」+ `finishedAt`，别的运行一条没动。
- 真实浏览器：尺寸/时长可选；配方面板能保存 → 列表出现 → 套用回制作台 → 导出下载 → 删除。

## [2.18.0] - 2026-09-15
### 修复
- **「固定 seed」是一条假通路——声称支持，实际一次都没生效过。** 对照 ComfyUI 的产线纪律
  （seed 摆在 KSampler 上当必填项）去查，发现元枢这边三处都在假装：
  `model-probe.mjs` 给每个图/视频模型写 `caps.seed = true`（注释说"本管道会原样转发给上游"）；
  `run.seed` 也存了；`story-orchestrator` 也把它传给了适配器。**但**：
  图像分支 `story-adapters.mjs` 调 `generateImage(provider,id,prompt,size,ref)`——参数里根本没有 seed，
  只在提示词里插了一句 `seed=123` 文本（模型不认那个东西）；视频分支的适配器连 `seed` 都没解构，
  于是 `video-request.mjs:32` 的 `body.seed` 永远走不到。界面上也没有 seed 输入框。
  现在两条通道都真的上送（`generateImage` 加 seed → 请求体；视频适配器 → `params.seed` → 创建体），
  **run 里记的是实际用出去的那个值**（没指定就现掷一个并记下来）——否则"可复现"是句空话。
- **预览路由没拿到模型目录，会报一条假的「不支持参考资产」。** `server.mjs` 的
  `/run-preview` 只注入了 `root`（`getModelList` / `getDefaultModel` 都没给），于是"自动选择模型"
  解析不出来、回落到 `auto/auto`，执行链上顶着一条「当前模型不支持参考资产；不支持固定 seed」，
  而真实生成（有目录）会正常选到 `agnes/agnes-image-2.5-flash`。现在两边注入同一份目录，
  预览显示的模型与注意项和实跑一致。**这条是探针里发现的，不是推断。**

### 新增（按 ComfyUI 的产线纪律补的三样）
- **负向提示词**：段落级可编辑，进 `## 必须避免` 提示词块（所有通道都吃、都看得见），
  同时作为 `negative` 进上送参数（图像通道据此发 `negative_prompt`；**只在真有值时带**，
  不赌每家上游都认这个字段），并记进 `run.negative`，同参重跑才有依据。
- **一次出多版（batch）**：`variants` 1–4，seed 依次递增，**每版一条独立 run**——
  于是天然落进「本段结果」与「作品列表」，不需要发明新的展示概念。上限压到 4，批量不该变成手滑烧钱。
- **「同参重跑」**：本段结果每版一个按钮，照这版的模型 / seed / 负向 / 参数再跑一次。
  有了它，一次偶然的好结果才算真的可复现。
- **执行链可见**：预览与实跑**共用同一份 `buildRunPlan`**（不是各算一遍），
  界面按它列出「输出类型 / 模型 / seed / 参考图（几张 + 清单）/ 挂载素材 / 负向 / 上送参数 / 注意」，
  再附编译后的提示词全文。ComfyUI 的价值有一半在于"整张图摆在画布上，每一步都看得见"。

### 明确**没有**搬过来的（说明理由，免得被当成漏做）
元枢调的是**远端 API**（agnes / 火山方舟 / Cloudflare / ModelScope…），不是本地扩散采样，所以：
- **节点画布本身**：没有可编排的本地采样节点，搬一张图只会变成假的可视化；
- **steps / cfg / sampler_name / scheduler / denoise**：本地采样参数，远端通道不暴露，传了也没用——**不假装支持**；
- **LoRA / ControlNet / IPAdapter / 放大 / 修脸**：需要本地模型与后处理链；定妆照已经承担了
  "参考图锁定"的角色（reference / 图生图）；
- **每个节点的中间结果**（latent 预览、逐节点输出）：远端只返回成品；
- **工作流 json 的导出/导入/跨项目套用**（把"配方"当资产）——这是 ComfyUI 最有价值的一条，
  也是**这一轮刻意没做**的：它需要新的数据模型（配方）+ 套用/导出入口，值得单独一轮，不适合塞在这里。

### 验证
- 新增 9 条产线契约（`tests/unit/story-workflow-params.test.mjs`）+ 1 条前端契约；全量 **1163/1163**。
- 真实浏览器（探针项目，用完即删，只点预览不花钱）：负向/seed/变体三件控件都在；
  填 `12345` + `3 版` + 两条负向 → 执行链显示「模型 agnes/agnes-image-2.5-flash」「seed 12345 ~ 12347（3 个变体）」
  「负向 多余的手指 / 文字水印」「上送参数 {"negative":"…"}」，编译后提示词里有 `## 必须避免`；
  `pageErrors=0`、失败请求 0。

## [2.17.0] - 2026-09-15
### 新增
- **剧本重视对话创作。** 此前三个提示词（`story-prompts.compileStoryPrompt`、`story-assist` 的
  `buildStoryboardPrompt` / `buildStoryAssistPrompt`）里**一个"对话"字都没有**，分镜提示词还明确
  只要求"动作、构图、镜头、光线"——出来的是一串漂亮的画面说明，一句人话都没有，戏不成戏，
  后续配音也没东西可念。现在：
  - 分镜/智能填充要求每段给出 `dialogue`，并**禁止**"两人交谈了几句"这类概述；
  - beat 新增 `dialogue` 字段（`StoryBeat.dialogue`），制作台有独立的「本段台词 · 对白」编辑框；
  - 台词**单独成块**、按 kind 分流：文字段落（novel）当戏写、视频（video）当台词上送、
    画面（image）**不进提示词**——台词写进画面描述会被生图模型当内容画出来，或把视觉指令稀释掉；
  - 解析宽容：`dialogue` / `台词` / `对白` / `line` / `lines` / 数组 / `{name,text}` 都收；
    **只有台词没有画面描述的段不再被丢掉**（那是编剧刚写好的对白，画面可以后补）。
- **别的工作台的产出能被连续创作调用了。** 此前连续创作能用的素材只有「角色定妆照」
  （因为它是设定里的一个字段）；AI 绘画出的图、视频工坊出的片、小说工坊写的正文就在同一个
  工作区里，却一个字都进不来。现在制作台有「本段素材」面板，两个来源：
  - **工作台产物**（`生成物/图片|视频`，走 `/api/ws/artifacts`）；
  - **小说正文**（小说工坊章节，走 `/api/novel/chapter`）。
  接线：画面素材 → 参考图（图生图 / reference 模式）；视频素材 → `videos[]`（reference 输入）；
  文本素材 → 作为创作依据写进提示词（截断 4000 字，**内容存在项目里**，源文件删了也不影响）。
  素材沿继承链累积：续写的段落默认看得见上一段挂的东西。

### 说明（刻意的取舍，不是遗漏）
- **画面（图生图）只有一张入口，所以用户显式挂的素材优先，定妆照退居其次**。
  反过来会让"挂了素材却什么都没发生"——那是最坏的一种静默失败。视频（能吃多张）则是
  定妆照在前、素材在后。
- 产物历史 `run.referenceImages` / `run.inputAssets` 记的是**原始引用**，不是内联后的 base64
  （一张定妆照的 base64 就是 2MB 级，写进项目 JSON 会把文件撑爆）。
- 素材地址解析不到时不硬塞坏地址，按上一版建立的方式如实写进 `run.degradation`。

### 验证
- 新增 12 条契约（`tests/unit/story-dialogue-materials.test.mjs`）+ 1 条前端契约；全量 **1153/1153**。
- **真实浏览器**（探针项目，用完即删）：台词框可编辑；素材面板给出 **200 个产物**、
  **3 本小说 / 2 章**；挂上画面 + 文本各一条 → 2 个 chip；点「检查生成输入」后编译出的提示词里
  确实有 `## 本段台词（必须按这些台词写成戏，不要改写成旁白）` 与 `## 本段已挂载素材`，
  且文本素材正文进了提示词；`pageErrors=0`（唯一失败请求是外部字体 CDN `fonts.loli.net`，与本次无关）。
- **真实模型**（`agnes-3.0-flash`，3 次分镜调用）：有台词的段 **4/4、1/1、4/4**（覆盖率 100%），
  台词 10/3/12 行，**台词混进画面描述 0 段**；bible 角色登记 2/2/2。
- 顺带量到的一个**既有**问题（不是本次引入，也没有顺手改）：同一提示词下模型给的段数不稳定——
  三次分别是 4、1、4 段（请求的是 4 段）。界面会如实显示「已追加 1 段分镜」，所以是可见的，
  但"要 4 段只给 1 段"值得单独处理（例如段数明显不足时重试一次）。等你说要不要做。

## [2.16.1] - 2026-09-15
### 修复
- **连续创作里带参考图的任务必失败：`视频任务创建失败 400: {"code":"invalid_request","message":"media must be
  a public http(s) URL or base64 data. Local file paths are not supported; ..."}`**
  根因不是猜的：真实项目「制作台验收」里 5 次视频运行的 `degradation` 全是这一条，
  而**同一模型**在不带参考图时能正常出片。差别只有一处——带参考图时，元枢把角色定妆照以
  **自己的相对地址** `/api/ws/file?path=...` 塞进 `images[]`（`run.referenceImages`，
  由 `story-adapters.mjs:57` 放进创建体、`video-request.mjs:29` 原样转发）。
  Agnes 只认**公网 http(s) URL 或 base64 data URI**，把相对地址一律当"本地文件路径"拒掉。
  本地部署给不出公网地址（上游抓不到 127.0.0.1，隧道又是本地系统管的），所以按上游自己给出的
  另一个选项做：**读文件内联成 base64**。新增 `engine/media-inline.mjs`，在 `startVideoJob`
  与 `generateImage` 两个出口前落地 media（`media-inline.mjs`、`media-api.mjs`）。
- **同一模型被报「当前模型不支持参考资产」——一条假降级。** 前端模型下拉只能给 `{provider,id}`，
  `capabilities` 整个丢掉，于是 `negotiateCapabilities` 对**每个显式选中的模型**都判"不支持参考图"，
  参考图通路被这条假降级关掉（真实项目里 4 次视频运行就是这样"成功但没带参考图"）。
  现在按 provider+id 回目录补齐能力（`story-orchestrator.mjs` 的 `withCatalogCapabilities`）。

### 改动
- **请求体超限不再只留一句 `fetch failed`。** `readBody` 以前超限时 `req.destroy()`——
  在响应写回**之前**掐断连接，客户端什么都拿不到（实测：2.8MB 的 `/api/media` 请求报的就是
  光秃秃的 `fetch failed`，排查方向被带偏到网络层）。现在停止累积、排掉剩余流量、抛带
  `statusCode` 的错，分发层尊重该状态码 → 客户端收到 **413 +「超过 2MB 上限（已收到 2.8MB）」**。
  顺带：非法 JSON 也从 500 归正为 400（那是客户端的错）。
  注意 `/api/media` 用的是默认 2MB，而连续创作**不把 media 放进请求体**（内联在服务端做），
  所以这道墙不在连续创作的路上——它是同一类"错误说不清"的另一处。
- **参考图落不下来就摘掉并如实降级**，不静默发一趟没有参考图的请求，也不硬失败：
  超过 8MB 内联上限、文件缺失、空文件、地址解析不到，都会变成 `run.degradation` 里的一句人话。
  `mode` 跟着修正——不给上游发一个没有 media 的 `reference`/`keyframe` 请求。
- 适配器如实上报的降级（上游摘掉参考图时 `startVideoJob` 的 `notes`）会并进 `run.degradation`，
  那一趟于是显示「已生成 · 请核对连续性」，而不是一个看不出问题的"成功"。
- `run.referenceImages` 记录的仍是**原始引用**：内联后的 base64 一张就是 2MB 级，写进产物历史会把
  项目 JSON 撑爆；原始引用保留可追溯性。上送用的那份单独算。

### 验证
- payload 级：把 `globalThis.fetch` 换成记录器，用真实项目里那张真实定妆照（1.51MB）
  跑真实的 `startVideoJob`。实测 POST 体 2.02MB、`mode=reference`、`images[0]` 以
  `data:image/png;base64,` 开头、体内**不再出现** `/api/ws/file`，
  且内联字节与原文件 **sha256 一致**。
- 真实上游（零成本探针）：全部用**不存在的模型 id**，模型校验必然失败，绝不会建出任务。
  实测内联后 0.3／0.8／1.5／2.0／2.5／3.0MB 以及真实定妆照（2.02MB）**每一次都带着完整
  body 到达 Agnes**（返回 `503 model_not_found`，而不是任何 media 报错）。
  真实定妆照重复 8 次：**8/8 到达**。
- 全量 `npm test`：1142/1142 通过（新增 10 条 media-inline 契约 + 4 条编排层契约 + 3 条 readBody 契约）。
- **没有做**、也**不能**由免费探针得出的事，要说清：Agnes **先校验模型、后校验 media**
  （用一个损坏的 base64 也是先返回 `model_not_found`），所以免费探针**证明不了**"上游接受这个 base64"。
  "接受 base64" 是它自己那条错误信息里给出的选项，payload 已按该格式发出并逐字节核对。
  要真正坐实，需要一次真实生成。
- **一次未复现的失败也记下来**：切换新代码后第一次打 Agnes 时，同一个 2.02MB 请求返回过一次
  `{"error":"fetch failed"}`；随后完全相同的请求 8/8 成功（首次调用耗时 4.9s，其余 0.7～1.0s，
  像是重启后代理活性探测那个窗口）。**一个样本不算证据**，我没有把它算成已修或已知原因。
- 探针里自己踩到的两个坑也补上了守卫（`resolveLocalMedia`）：`wsRoot` 为空时不再按进程 CWD
  解析（`story-film` 的 `withinRoot` 在空 root 下会放行任意路径），解不出来的 URL 形态值不再被
  `path.resolve` 拼成 `<wsRoot>\api\ws\file?...` 并谎报"文件不存在"。

## [2.16.0] - 2026-09-15
### 修复
- **成片合成完刷新就没了。** `assembleFilm` 只把文件存进产物库就返回（`story-orchestrator.mjs`），
  项目 JSON 里**一个字都没写**；前端也只 `setFilmUrl` 存在内存里，
  于是"打开成片"这个链接在刷新/换项目之后必然消失，看起来像合成失败。合成真的成功了，是**没落账**。
- **产物卡上的"第 N 段"会被后来的重排改写。** 段号此前是界面按**当前**分镜顺序现算的
  （`StoryProducts.tsx` 的 `noOf`）。重排一次分镜，昨天导出的"第 3 段"今天就指向别的内容——
  产物是历史，不该被后续编辑改写。现在 `run.beatNo` / `run.sceneTitle` 在**生成时刻**定格并随产物落盘
  （`createGenerationRun`，调用点 `runGeneration`），界面优先用定格值，只对旧数据回退到现算。
- **同一段既有正文又有画面时，正文被缩略图吞掉。** `StoryProducts.tsx` 的条件是
  `prose && !headUrl`——只要出了图，这一段实际写出来的文字就再也不显示了。改为两者都给。
- **失败且无产物的运行完全不可见。** 作品列表只收 `outputAssets.length > 0` 的运行，
  失败的那几次连"失败"两个字都看不到。现在默认仍然只看成品（不占屏），
  但给一个「只看有产物的」开关，勾掉就能看到失败运行与它的 `degradation` 原因。

### 改动
- `project.films` 落盘：每合成一版成片追加一条 `{id,url,clipCount,method,beatIds,createdAt}` 并 `writeProject`；
  `createProject` 补 `films: []`，`types.ts` 补 `StoryFilm`。成片是作品，必须有账。
- 作品列表新增「成片 · N 版」区（内嵌播放 + 打开 + 下载，新的在前），成片计数并入表头。
- 制作台「打开成片」改为读 `project.films`（不再依赖一次性的本地状态），多版时标注「第 N 版」。
- `StoryResults.tsx` 契约测试更新：原断言锁的是 `版本对比`（那个已被删除的版本下拉），
  改为锁**新意图**——所有版本平铺、代码里不再有隐藏版本的 `<select>`（注释不算证据，按行剥离后再断言）。

### 验证
- 新增 3 条测试：段号定格且重排后不变、成片写回并能在磁盘上重新读到、作品列表的段号/失败可见/成片来源契约。
- 全量 `npm test` 通过。

## [2.15.0] - 2026-09-15
### 修复
- **连续创作里旧产物看不见了。** 先说清数据是好的：服务端 `story-orchestrator.mjs:50-51`
  是 `scene.outputs.push(run)`——**只追加、从不覆盖**，旧产物一直都在磁盘上。
  问题纯在展示：`StoryResults.tsx` 把该段所有 run 都取出来了，却**只渲染 `current`（最新一版）**，
  旧版藏在一个 `<select>` 版本对比下拉里。而同一页 `StoryWorkbench.tsx:192` 还写着
  「结果不满意时可以修改内容重新生成，**旧版本会保留**」——保留了，但一生成新的就从视野里消失。
- **没有任何地方能一眼看全"这个项目到底产出过什么"。** 分镜时间线和「本段结果」都只看当前那一段。

### 改动
- **「本段结果」改为全部版本平铺**（`StoryResults.tsx`）：新的在最上面，每版一块，
  带版次 / 状态 / 模型 / 时间 / 降级提示 / 各资产与「下载这一版」「打开原文件」。
  版本下拉框**已移除**（它正是"旧的看不见"的来源）。
- **新增「作品列表」**（`frontend/src/components/story/StoryProducts.tsx`，接在创作页底部、默认展开）：
  把整个项目里**跑出过产物的运行**跨场景摊平，新的在前。每张卡带：第 N 段 · 类型 · 状态 ·
  场景 · 时间 · 缩略图（图/视频）或正文摘要 · 原提示词 · **定位到本段** / 下载 / 打开。
  表头给出计数：`N 次生成 · 成品 X 个（画面 a / 视频 b / 正文 c）`。
  只有**真正产出过成品**的运行才进列表（失败且无产物的不计入，但它在「本段结果」里仍能看到）。
- 卡片可点击「定位到本段」跳回该段继续生成，形成"列表 → 段落"的回环。

### 验证（真实项目「制作台验收」：10 镜头 / 7 条运行记录 / 4 个视频产物）
```
作品卡: 4 个（跨 2 个段落，新的在前）
本段结果: 3 个版本平铺 —— 第 3 版 / 第 2 版 / 第 1 版，各自带时间与模型
标题: 3 个版本 · 全部保留，新的在最上面
版本下拉框: 0 个（已移除）
pageErrors=0
```

## [2.14.0] - 2026-09-15
### 修复
- **定时任务的自我反思只谈后端，谈不了聊天。** 查下来不是它"选择"只谈后端，是**手里只有
  后端的材料**：会话材料是 `formatSession` 给的一行「名字 + preview」，而 `preview` 是
  **第一条用户消息截 60 字**（`session-files.mjs:113`）；同类材料里记忆日志却给了
  **6000 字**（`time-task-run.mjs` 的 `CLIP_CAP`）。信息量差两个数量级。
  证据是反思自己抱怨过：「PPT 会话数量多但深度信息为零」「记忆日志严重欠账」。

- **反思的行动清单写完就没有下文。** 原先结果只做两件事：追加进 `文档/时间引擎日志.md`、
  存进 `time-tasks.json` 的 `history[].result`。而那份日志**全仓没有一处读它**；唯一自动
  通道是 `onTaskDone → nudgeSkill` 生成技能提案——实测 5 条提案**全是 `open`，一条都没落地**，
  且 09-07 与 09-14 提的是**同名技能** `daily-retrospective`（重复提案、无去重）。
  最关键的缺口：**反思写的「今天要做的」不变成任何可追踪对象**。

- **反思不读上一次的行动清单**，只能每天从头复盘，**无法检查兑现**。

### 改动
- **会话正文进材料**（`sessionDigest`）：按会话读取目标日期的用户提问 + 小语结论正文
  （工具调用/结果不入正文），每会话 1800 字、总量 9000 字、最多 6 个会话。
- **上次兑现喂回下一次**（`collectReflectionCommitments`）：把承诺账里 `source=reflect`
  的那些连同"挂了多久 / 已结清及证据"一起注入，并硬性要求「先逐条交代兑现，再说今天」。
- **行动清单落成承诺**（`parseReflectionActions` + `recordReflectionActions`）：复盘结尾
  必须附一个 JSON 块，解析后写进承诺账（`sessionId=reflect`）——于是它会被下一轮复盘追问、
  被台前「待兑现承诺」列出。**结清仍只能由人给结论**（承诺账的既有规矩，模型自述不算证据）。
  解析取**最后**一个 json 块（模型常先给示例），解析失败就是无操作、不写空条目。
- **技能提案按名字去重**（`nudgeSkill`）：同名且未 dismissed 的提案已存在就跳过，
  不再并排堆两条 `open`。
- prompt 增硬约束：**必须覆盖会话侧**（用户在聊天里要了什么、交付到什么程度），
  「不能只谈引擎/后端的工作——会话正文就在上方」。

### 端到端验证（真触发一次反思，跑完还原）
```
① 谈到会话: 是   ← 开头即「## 一、会话侧交付情况」，在分析用户聊天里那十遍重复约定
③ 行动 JSON 块: 1 个 → 解析 5 条
④ 承诺账 0 → 1488 字节，source=reflect 共 5 条，全部 pending
   例：排查后端 POST /api/runs 是否按 clientRequestId 去重…／核查视频交付通知链路：确认用户为何「没看到」…
```
②（上次兑现）在这一次里没显示——因为当时承诺账是空的（探针跑完即还原），该段是条件注入；
其行为由单测锁住（prompt 含清单与"先逐条交代兑现"）。

### 验证
测试 1122/1122（新增 `tests/unit/reflection-loop.test.mjs` 18 条）。

## [2.13.0] - 2026-09-15
### 更正（重要）
- **上一版我在给用户的回复里说错了元枢的沙箱默认值。** 我写的是"元枢当前是
  `danger-full-access` 默认，恰好相反"——**这是错的**，那句话来自调研线的一句未核实转述，
  我没有验证就转给了用户。实测：`checkSandboxCall` 默认 `mode = "workspace-write"`，
  非法值也回落到它（`engine/yuanshu-sandbox.mjs:44,51`）；**全仓没有任何地方默认
  `danger-full-access`**。元枢的默认是阶梯中档，不是宽松那端。
- **同时澄清一句我说得不完整的话**：元枢的沙箱阶梯**只管 yuanshu 这条路**——
  `gateSandboxCall` 全仓只在 `engine/yuanshu-loop.mjs:117` 被调用；默认主驾的 pi 路径
  用的是 Pi SDK 自己的工具，不经这道阶梯（元枢在那边只注入审批拦截）。
  所以"把默认收成 read-only"收的是一条非默认路径。

### 新增
- **会话级沙箱模式**（`engine/sandbox-session.mjs`）。补齐的是真缺口：原先模式是**写死的**
  （`unified-chat.mjs` 里 `isPlanLock ? "read-only" : "workspace-write"`），前端没有任何
  控件、config 也没有开关——想让整个会话只看不改做不到，想整会话放开也做不到，
  模式什么时候变过更没有记录。

  按 dsh 的"**会话事件即状态、投影出有效值**"（`dsh-sandbox-policy:41-43,114-120`）实现成
  一条 append-only 的 JSONL，最新一条生效——每次变化都可审计、可回放。两条 dsh 的规矩：
    · **收紧随时可以，放宽必须写明理由**（dsh 单次升级必须配 justification，会话级同理：
      没有理由的放宽，在日志里就是一句无据可查的授权）。
    · **切换的是具名预设，不是一个裸值**（dsh-permission-presets）：`cautious`(read-only) /
      `standard`(workspace-write) / `trusted`(danger)，各带 label 与说明。
  另有一条元枢自己的不变量：**计划模式永远强制 read-only**，不会借用会话的放宽。

- **台前**：引擎页新增「沙箱模式（本会话）」——显示当前档、切换预设、放宽时收一句理由、
  把变化史摊开（时间 / 从哪到哪 / 放宽还是收紧 / 人工还是模型 / 理由）。

### 刻意不改的一项
**默认值保持 `standard`（workspace-write），没有按 dsh 改成 read-only。** 三条理由写在
`DEFAULT_PRESET` 旁边：① 阶梯只管 yuanshu 这条非默认路径；② read-only 下写操作转审批、
无人值守按 fail-closed 拒绝，而元枢的常态是跑长任务，会在第一次写就停住；③
workspace-write 本身不宽松（禁工作区外路径，`BASH_DANGER_RE` 仍需单独升级）。
要改成最严只改 `DEFAULT_PRESET` 一行；测试里留了回归锁，改它会挂，逼改的人先读那三条理由。

### API
`GET /api/sandbox/mode`、`POST /api/sandbox/mode`（缺 sessionId 时回落到最近会话）。

### 验证
测试 1104/1104（新增 `tests/unit/sandbox-session.test.mjs` 14 条）。

## [2.12.0] - 2026-09-15
### 新增
- **fork 型子智能体**（`engine/subagent-fork.mjs` + `delegate_fork` 工具）。元枢原先只有
  spawn 型（`delegate_task`）：干净上下文 + 自包含 prompt。dsh 的 fork 变体只差一个
  seed——取父会话里**已完成**的那段前缀当子智能体的起始对话。差别不是"多一点上下文"：
  spawn 要你把子代理该知道的全写进 prompt（写漏就答歪），fork 的子代理已经看过前面
  聊了什么（省掉重复总结，也少一处"总结失真"）。

  两条来自 dsh 的硬规矩：
    · **只取已完成的前缀**（dsh 是 `events.slice(0, lastEnd.seq + 1)`）。等价实现：
      取到"最后一条有正文的助手消息"为止，其后的工具调用/结果/本轮用户消息全丢——
      在飞的回合重放会得到半截状态。
    · **深度只能加深，不能降低**（`engine/subagent-depth.mjs`，对应 dsh
      `subagent/types/depth.js:18-25` 的 `max(持久化, 运行期)`）：否则被恢复的子智能体
      按 0 重算深度，`maxDepth` 就形同虚设。

  **与 dsh 的一处刻意分歧**：dsh 的 fork 继承完整事件流；元枢的历史里工具结果可能很大，
  全带过去会吃光子智能体预算。所以这里丢弃工具结果，但**必须自报**——在种子最前面插
  一条"其中 N 条工具结果没有带过来，需要时自己重新查，不要假设它没发生过"，回执里
  也带上同一句。省略必须自报，否则就是"没被观测的说法"。

  **诚实说明**：元枢的子智能体没有工具（SYSTEM 里明写"你只有分析能力"），所以它派生
  不出下一层——**深度闸门目前是空转的**，保留它是为了将来 fork 子代理拿到工具时不会
  绕过上限，以及让 `depth` 在 trace 里可见。这条不构成当前的安全收益。

  **工具是两条而不是一条**（对应 dsh 的两条硬编码工具行 `dsh-base/cordis.patch.yml:349-367`）：
  `delegate_task`（独立判断）/ `delegate_fork`（接着前文干）。这样"要不要把前文带过去"
  是模型可见的选择，而不是自动挑。种子只能来自宿主传进来的历史，模型无法伪造"前文"。

  接线：`history` 加进 `executionContext`（`yuanshu-loop.mjs:121` 把 `executionContext`
  展开进工具 ctx）。**已知限制**：Gateway 旁路（`unified-chat.mjs:617/625`）没有传 ctx，
  那条路径上 `delegate_fork` 会走到"没有可继承的前文"的明确拒绝，不会静默。

### 验证
测试 1090/1090（新增 `tests/unit/subagent-fork.test.mjs` 15 条，含"种子真的进了模型请求
且顺序为 系统 → 种子 → 补充 → 子任务"的断言，以及"被闸门挡下时一次请求都不发"）。

## [2.11.0] - 2026-09-15
### 新增
- **跨轮目标 + 三重闸门**（`engine/goals.mjs`，借 `dsh-goal-round-driver`）。
  元枢原先没有"目标跨轮推进"：任务看板是给人看的，时间引擎是按点触发的，都不是
  "一个目标自己往下走"。dsh 值得借的不是"自动跑"，而是它围绕自动跑立的三重闸门
  ——那三条才是让这个能力不变成烧钱自转的东西，一条不落全搬：
    · **闸门① 回合上限**：到顶自动转 blocked，不再"再来一轮"
    · **闸门② 单轮预约**：同一 `(goalId, revision, round)` 只驱动一次；`revision`
      不匹配则本轮作废（目标被改过）；轮号必须连续
    · **闸门③ 错误即解除**：一轮出错立刻 disarm，不带着错误继续转
  另有两条 dsh 的规矩照搬：**`complete` / `blocked` 只接受人类输入**（模型自己说
  "我做完了"是不可验证的说法，不算证据）；**重启后一律回到未武装态**，必须人类重新确认。

  **与 dsh 的一处刻意分歧**：dsh 的 round-driver 默认就自动推进（默认上限 256 轮）。
  元枢把自动推进做成显式开关 `autoAdvance`，**默认 false**——默认只注入轮次上下文、
  不自己起轮次；上限也取小值（默认 20，可设到 200 封顶）。个人伙伴跑无人看管的
  256 轮是成本隐患，不该是默认值。

  接线：`advanceGoalTurn` 在 `handleChat` 的分支**之前**调一次，pi 与 yuanshu 两条
  路径因此看到同一个轮号；到顶/重放/revision 不匹配时注入为空，不占上下文。
  pi 走 nextTurn、yuanshu 走 prompt 区段（`plan`，只读状态不重复推进）；两条路径的
  catch 都接 `noteGoalError`。
- **台前**：记忆园丁页新增「跨轮目标」——新建、武装、暂停、已完成（收证据）、受阻。
  台前只能做人类这一侧：武装与结清都只可能来自点击，服务端没有任何路径会自己调它们。

### API
`GET /api/goals`、`POST /api/goals/create`、`POST /api/goals/action`（arm/pause/complete/block）。

### 验证
测试 1075/1075（新增 `tests/unit/goals.test.mjs` 20 条）。端到端实跑：建→paused、
人类武装→active、真实对话推进到第 1 轮、目标区段的 4 个标记全部出现在模型请求里、
人类结清带证据；被碰文件全部还原并校验一致。

## [2.10.2] - 2026-09-14
### 修复
- **dsh 不再标记为可主驾**（`canLead: true` → `false`）。这个标记的依据是
  `engine-pair.test.mjs` 里那句断言——「dsh 对话适配器**写完后**应能主驾」，
  那是**完成度目标**，不是能力判断。适配器确实写完了（实跑通），但"写完"这个门槛
  从没被定义过。而同一个测试的第 28 行又断言 dsh 的 `cannot` 必须提到记忆/出图/多轮
  ——两句话同时被锁在测试里，互相矛盾。
  实测元枢调用它的方式（`dsh --profile headless <task>`，每轮新起子进程、只回收
  stdout、历史压成近 8 条 × 800 字纯文本）**预热后约 4.7 秒/轮、冷启动约 27 秒**，
  且无流式（`dsh-chat.mjs` 只在进程 close 后推一次 delta）、无记忆/情绪/出图/工具轨迹、
  180 秒硬超时。作为日常主驾不成立。
  > 更正：本条目最初写的是「回两个字要 26.9 秒」。那是**单次冷启动**的数字，被当成了
  > 常态。复测六次为 4.4–5.0 秒，`--dump-config`（只装配配置）0.25 秒。已按实测改正
  > 代码注释、测试注释与本条目。教训：一次测量不是"实测"，要么给分布要么标清单次。

### 改动
- `ENGINE_CATALOG.dsh.canLead = false`，并把边界写实：不适合当主驾的原因（每轮冷启动
  子进程、无流式、历史只能压成摘要）与真正的用途（`dsh_task` 执行臂）分开列。
  前端早就有「（暂不能主驾）」这个状态（`EnginePairPanel.tsx`），无需新代码：
  引擎对会自动回落到能主驾的次席，并播报「本轮主引擎 · X（dsh 主驾让路）」。
- `canLead` 全仓只在 `engine-pair.mjs` 的 resolveLead 里被读，所以 **`dsh_task`
  执行臂完全不受影响**。
- 测试意图同步改写：原断言「写完后应能主驾」→「作为执行臂可用，但不作为主驾」，
  并新增一条锁：不可主驾的引擎占主驾位时必须让路且**播报可见**（不能静默换引擎）。

## [2.10.1] - 2026-09-14
### 修复
- **记忆结算只跑在 Pi 一条路上，而声明说它属于元枢循环。** 流水账 / 承诺 / 纠正 / 关系 /
  每-20-轮快照计数原先都写在 `server.mjs` 的 Pi 分支 `try` 里，而那个分支位于 `handleChat`
  early-return **之后**——于是：
    · `yuanshu`(unified) 路径静默不写记忆；
    · `dsh` 路径同样不写；
    · **Pi 失败降级到 unified 时也被一起跳过**（那段代码在 try 内，catch 走了降级不再回头）。
  而 `engine/engine-pair.mjs` 的引擎目录声明的恰恰相反：yuanshu「记忆、出图、规划…都挂在这条上」、
  pi「记忆…不在这条循环里长」——**实现与声明是反的**。默认主驾是 pi，所以一直能跑，缺口才没被发现。
  实测：同一条必定触发记忆信号的消息，走 unified 时记忆日志一字不动，走 Pi 时正常追加。

### 改动
- 结算逻辑抽成 `engine/turn-memory.mjs`（可单测，不再内联在 server.mjs），由两个分支的
  `finally` 各调一次：`pi` 与 `yuanshu` 都结算，`dsh` 按它自己声明的边界不结算。
  放在 `res.end()` 之后，不拖慢客户端收尾；被新请求顶掉的轮次跳过（否则按偏移切片会切到新轮次的回复）。
- 助手文本改为**按字节偏移只读本轮新追加的那段**，不再整份回读会话找"最后一条助手消息"。
  这是必须的：中止/报错的轮次也会因为用户消息落盘而让文件变长，只看"变长了"会把**上一轮的
  助手回复重复记进记忆**。回归锁见 `tests/unit/turn-memory.test.mjs`。
- 引擎目录里 pi 那条不实声明改成「记忆、出图、规划由元枢外壳结算，不在这条适配器内部实现」——
  适配器内部确实没有，但外壳会替它结算，原话会让人以为 pi 路径下记忆是坏的。

## [2.10.0] - 2026-09-14
### 新增
- **作息与节律**（`engine/activity-rhythm.mjs`）。时间感此前只有"现在几点"和"距上次多久"，
  缺一层"对这个人来说现在算不算正常时间"——凌晨 3 点说早上好和上午 10 点说早上好不是一回事。
  从**会话文件名自带的时间戳**与记忆日志标题观测真实作息，给出活跃时段、当天轮次、深夜占比，
  以「（作息）」段并入提示词。样本不足返回 null（不观测），绝不编一句"你通常早睡早起"。
- **承诺兑现**（`engine/promises.mjs` + 台前）。模型最廉价的讨好方式是承诺（"下次给你补上"），
  说完就滑过去。现在把助手自己的延迟承诺落成账：提取 → 入库 → 下一轮提示词里提醒主动交代。
  两条硬规矩：**绝不自动标记"已兑现"**（结清只能由人给结论），**宁可漏不可灌水**
  （没有一人称主语的"下次再X比较好"是在给用户建议，不能进账）。
- **记忆快照可回退**。此前快照只有写入方、全仓没有任何读取方：`restoreSnapshot` 零调用、
  无端点、前端零引用——98.7MB 攒着却一份都回退不了。现补 `GET /api/memory/snapshots`
  与 `POST /api/memory/snapshot/restore`（回退前自动存 pre-restore），并在记忆园丁页给出
  列表与回退按钮。
- **AIBody 土壤真实读数上台前**。"系统脉络"此前对身份/基因/情绪/记忆/治理只显示"可用/不可用"，
  而真实读数（`state[].summary`）早已算好、只被渲染层丢掉。现在五项读数全文可见。

### 修复
- **记忆快照无界增长**：`archiveStateSections` 在归档循环**之前**无条件 `saveSnapshot`，
  而它挂在 server 启动块里 → 每次重启落一份（每份内嵌记忆日志全文）→ 29 天攒到 **521 份 / 98.7MB**。
  现在只在真有归档动作时写，并加保留上限（30 份 / 12MB，从最旧端双重截断，至少留 1 份）。
- **"每 20 轮存一份"是死代码**：原来读 `listSnapshots().length % 20`，0 触发后份数恒为 1，
  1..19 都不满足条件，该分支第一次之后永远进不去。改用落盘计数器。
- **快照 id 只到分钟**：同一分钟两次快照会静默互相覆盖（回退前的 pre-restore 安全网会被
  随后的 archive-before 盖掉，等于没有安全网）。改为精确到秒 + 冲突加序号。
- **`autoMemorize` 几乎每轮灌一条流水账**：项目信号连助手回复一起匹配，而"完成/做好/搞定"
  在助手回复里几乎是口头禅。改为只认用户侧，助手侧只认可核查里程碑；并在**写入前按要点去重**
  （园丁的"一键去重"只是事后补救，一天能落 18 个 `.bak`）。
- **`dedupeLog` 的 `.bak` 无上限**：每次去重落一份全量日志副本，一天被调用 18 次就是 3.2MB。
  改为保留最近 5 份，并在启动自维护里一并收敛。
- **作息数据源污染**：最初用 `记忆/情绪感受.jsonl`（768 条，看着最细），实际被批量任务污染——
  2026-09-07 单日 578 条、09-06 单日 103 条（占 88%），相邻记录间隔中位数正好 60 秒，是定时跑出来的。
  拿它算作息会得出"3:00–9:00 全是 60 条"这种假结论。改用会话文件名后分布立刻正常
  （03:00–08:00 为 0，峰值 23:00）。测试里留了回归锁，防止再被当回数据源。

## [2.9.0] - 2026-09-14
### 新增
- **AIBody 的「土壤」接回主角色**。原先这份运行协调 directive（模式策略 + 五个状态提供者摘要）
  只经 `executionContext` 传给**子智能体**，主角色根本收不到——而 directive 里写的正是
  "母体协调身份、记忆与角色治理""主角色负责综合和交付"。现在 Pi 路径用 nextTurn 注入，
  兜底路径走 `runtime` 提示区段（该区段本来就存在，只是一直没人往里传）。
- **五个状态提供者改为真实读数**（`engine/aibody-soil.mjs`）。此前 identity / governance
  是纯硬编码字符串，genes 的 summary 硬编码，memory 只证明文件存在——而这些摘要每轮都会
  拼进 directive 交给模型，等于让模型读没被观测过的说法。现在：
  identity=人格附录文件大小与更新时间、genes=条数与**真实漂移**、emotion=**本会话**快照、
  memory=条目数与最后写入时间、governance=**待批提案真实条数**；读不到就返回 null，
  由 policy 标 `not_observed`，不再冒充 observed。
- **时间感**：`promptTimeText` 补上"距上次对话多久"与"本次会话已持续多久"。
  原先三个重复实现（yuanshu-seams / time-engine.nowContext / server.mjs 内联）都只说
  "现在几点"——是时钟，不是时间感。现在收敛成一份；`nowContext` 全仓零调用者，已删。

### 变更
- 会话写操作与 Pi 共用同一把按文件队列（`withFileMutationQueue`），并叠加跨进程锁文件；
  元枢自己的 `edit` 是同步临界区本就原子，真窗口在**跨实现**（Pi 的 edit 走 fs/promises，
  读写之间有真 await）。
- AIBody 记录的 `engine` 不再硬编码 "yuanshu"——主驾是 pi 时记录会是错的；改为真实观测值。
- 工具结果压缩：给出可执行回读指针（归档原件 + 读法）、大结果先发全文 2 次再压缩。
  详见 `docs/NAMING.md` 第四节与 `docs/sol-pi-notes.md`。

## [2.8.0] - 2026-09-14
### 新增
- **命名契约**（`docs/NAMING.md`）：产物名统一为 `{提示词摘要}_{类型}_{时间戳}-{id}_{v版本}.{扩展名}`，
  视频/音乐/文本/图片/文档一体适用。此前聊天里的视频下载名回退成 `元枢视频-N.mp4`（所有视频同名、
  互相覆盖），文件链接下载名是字面量 `download`（连扩展名都没有），主题文件还叫 `pi-theme-*.css`。
- **版本唯一来源** `version.json`：此前版本号散在 9 个文件里各写各的，产品版本 `2.7.1` 与壳版本
  `0.2.4` 两条线互不相干（壳版本还倒退过一次 `1.0.0 → 0.2.2`），且没有任何机制强制推进——
  09-12 到 09-14 发了一整批功能，一个号都没动。现在全部由 `version.json` 派生或被测试钉死。
  发版用 `npm run version:bump <major|minor|patch>`；`version:bump sync` 用于对齐历史漂移。
- **版本进产物名**：升级后产出的文件不再与旧版混淆，看名字就知道是哪个版本做的。
- 系统页与看板显示同一个版本号（此前系统页读 `package.json` 显示 `v0.2.4`、看板显示 `2.7.1`，
  两个数字都叫「版本」）。

### 变更
- **仓库改名**：`pi-web` → `yuanshu`（GitHub `xueshao1716/yuanshu`、Gitee `linxinyu520xue/yuanshu`）。
  旧地址实测仍可跳转（网页 301/302，raw 与 git 端点跟随重定向后 200），旧安装命令不会失效；
  但仓库内所有链接已改为新地址，不再依赖跳转。历史条目保留旧地址作为当时的记录。
- **环境变量前缀**：`PI_WEB_*` → `YUANSHU_*`。旧名继续被读取（新名优先），老安装无需改动。
  环境变量表见 README；实现见 `engine/env.mjs`。
- 计划任务 `pi-web-watchdog` → `yuanshu-watchdog`；`autostart.ps1` 原先写的任务名与机器上
  实际注册的完全不符（重跑会多出一个 watchdog 抢 8787），已按真实配置重写。

### 新增（承接 2.7.1 之后）
- 系统级 AIBody 运行协调：主聊天统一记录规划、工具、子智能体、记忆、产物与治理状态，验收页只作为观察入口。
- 子智能体持久台账：关联父任务与会话，记录角色、状态、证据、失败、取消和重启恢复，不保存原始提示词或隐藏推理。
- 连续创作编排阶段一：故事项目、Story Bible、分镜 beat、生成运行记录与能力降级信息，提供时间线 + 节点侧栏工作台；本阶段只创建可追溯编排记录，不直接触发外部生成。
- 连续创作·制作台：一键分镜（自带设定登记）、角色定妆照参考图锁定、连续性体检、成片合成；跨场景继承打通。
- 改动验收并入工作台、连续创作并入创作，去掉重复板块。

- 聊天区接入运行观测状态栏：展示阶段、工具数量、情绪摘要与失败重试入口。
- 运行摘要补充记忆命中与上下文预览，聊天完成态支持详情查看并自动收起。
- 引擎控制台拆分配置、运行诊断、工具搜索与 Gateway 旁路，补齐刷新失败提示和移动端可读性。

## [2.7.1] - 2026-09-12
### 新增
- 下载中心支持桌面壳一键打开系统下载目录，下载记录可重试与清理。
- Android 0.2.4 接入系统保存窗口，资产与会话导出支持选择保存位置；确认保存后记录状态，并可重新打开文件。
### 修复
- 资产库 PPT 直接预览文字大纲，移动端预览层关闭后自动收起。
- 修复 Windows 打包完成后等待进程组不退出、导致交付步骤未执行的问题；安装包按当前版本精确交付并核对校验值。

## [2.7.0] - 2026-09-12
### 新增
- 会话库按真实更新时间排序，历史会话自动补发稳定编号，新建会话立即落盘编号。
- Pi SDK 会话恢复搜索、分享、媒体、技能和执行臂工具，PPT 任务可真实生成并交付文件。
- HTML 作品集缺失或损坏 `deck.json` 时自动从页面恢复；并发 PPT 任务按本轮目录隔离产物。
### 修复
- 壁纸模式下新建对话工作画布保持透明，避免遮住主题壁纸。
- 作品集路径前缀边界和文件交付路径校验，避免误读兄弟目录或误交付其他任务文件。

## [2.6.0] - 2026-09-04
### 新增
- 工作台看板（桌面侧栏 + 手机「更多」）、进化引擎与跨会话回忆
- 水墨 / 竹影主题；本机 Aieyra Anthropic 网关给 Claude Code
- ppt-html 模板路径可配置（`PPT_HTML_TEMPLATES`）
### 修复
- PPT 生成与单页 refine 的 SSE 断连会 abort/dispose agent
- htmlPath 前缀穿越；小说目录与自愈提示词不再写死 D 盘路径
- CHANGELOG 看板实际能读到仓库根的更新日志

## [2.5.0] - 2026-08-15
### 新增
- 正式版本体系：版本常量 + CHANGELOG 更新日志，看板顶部显示 pi-web 版本与最近更新
### 修复
- 时间引擎崩溃：server.mjs 缺失 `createTimeEngine` 导入与 `timeEngine` 变量声明，前端一用时间引擎即崩（反复重启 42 次的诱因）
- 更新：pi 引擎 0.84.1 → 0.84.2

## [2.4.0] - 2026-08-15
- 安装一条命令化：极简版（纯 zip）+ 全自动版（装 git）双脚本，`irm … | iex` 一条命令部署
- 全局安装支持：`npm i -g git+https://gitee.com/linxinyu520xue/pi-web.git` 后 `pi-web` 即用（setup.mjs 自初始化）
- 外网分享服务固化：start-share.cmd 一键启动 8644 + cloudflared 隧道

## [2.3.0] - 2026-08-14
- 断线自恢复：任务进度快照（/api/tasks/active）+ 前端断流自动恢复轮询
- OpenIM 评估结论：不需要，SSE 心跳 + 断线重连已够

## [2.2.0] - 2026-08-12
- 经验沉淀台：提案制（plan/approve/reject/rollback）+ 基因反馈闭环（效率/可靠/适应三滑块）
- pi-reasonix 扩展：DeepSeek 前缀缓存优化（命中率实测 94%+）

## [2.1.0] - 2026-08-11
- 多 provider 模型通道策略：默认 opencode-go 套餐 deepseek-v4-flash，图像自动降级切换
- 技能体系接入：web-search / image-generation / voice-transcribe / session-export-redacted

## [2.0.0] - 2026-08-10
- 记忆系统：固定记忆 + 记忆日志自动沉淀 + 经验库（跨会话长期有效）
- 情绪引擎：VAD 三维情绪感知，对话自适应语气
- 工作空间分类视图（工程/文档/生成物/交付）+ 一键交付 + 文件断点续传
- 外网分享 share_project：稳定域名 + 自动复制目录
- 会话分支 / 模板 / 项目分组 / 主题系统
## 2026-09-06

- 第一批主驾工作台升级：新增统一运行观测快照与 `/api/run/overview`，Board/Engine 展示主驾健康和判断→执行→记忆→交付阶段。
## 2026-09-06

- 第一批主驾工作台升级：新增统一运行观测快照与 `/api/run/overview`，Board/Engine 展示主驾健康和判断→执行→记忆→交付阶段。
