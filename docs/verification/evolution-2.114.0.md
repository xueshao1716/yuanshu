# 2.114.0 自进化机制验收记录

日期：2026-09-21。范围：本机 `D:\pi-web`，后台端口 8787。

## 自动检查

- `node scripts/verify-workbench.mjs`：1773 项测试通过，0 失败，0 跳过；前端类型检查通过；隔离生产构建通过。
- 新增回归覆盖执行成功但验证失败、标签隔离、分支覆盖不足、保留集泄漏、同时间戳切分、未来时间戳、试运行启用、完整配置回滚、旧记录撤销保护、失败配置写入及天团人工验收重新核对。
- Impeccable detect 对新增机制模块返回空问题列表。这是代码静态检测，不代表浏览器视觉验收。
- 服务端语法检查及源码差异空白检查通过。暂存新增资源后，全仓库 `git diff --cached --check` 提示生成的主入口包有 5 处模板空白行（三份镜像共 15 处）；保留构建原件，不手改带哈希文件。
- 正式前端构建完成，静态资源同步到 `public/` 和 `app/dist/`。保留当前和最近两次发布可达资源，清理 285 个中间构建文件，约 9.8 MB。
- 构建仍有现有的插件弃用、大分块及混合静态/动态导入警告，不阻断构建。

## 本机运行验收

- 重启前实时任务总览无活动任务。使用项目重启入口成功重启。
- `/api/health` 成功；运行实例报告版本 2.114.0，端口持有者为 PID 8736，最终活跃实例 1，无过期端口占用声明。
- 服务返回的首页与磁盘 `frontend/dist/index.html` 一致，主入口 `index-PhMLyUho.js` 返回 200，包含 2.114.0。
- 实际调用 `/api/dream/run` 成功，技能与修复重试均处于 collecting；没有改变现役策略。
- 历史技能观察 9 条，可评分的核验记录 0 条；修复重试轨迹 0 条。数据不足时没有自动制造证据或强行启用候选。
- 天团最新真实运行归档 1 条，人工接受 0 条。该运行模型清单 10/12，验收状态 quality_failed，未被记成成功。耗时来自实际记录；未记录的调用预算、token、金额保持 null。

## 验收边界

本次完成机制回归和真实本机接口验收，未用合成测试声称模型能力已经提升，也未新增付费模型任务充数。后续需要真实核验样本才能评估策略收益。后台每 6 小时检查一次，回滚在检查时执行。随后已重新制作 Windows 与 Android 2.114.0 安装包，包内版本、签名及校验结果见 [客户端打包核验](../packaging-2.114.0-20260921.md)。未执行覆盖安装或本轮真机界面测试。

设计、门槛及证据限制见 [自进化证据说明](../EVOLUTION-EVIDENCE.md)。
