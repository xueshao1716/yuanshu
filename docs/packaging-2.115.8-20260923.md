# 元枢 2.115.8 客户端打包核验

日期：2026-09-23。按用户要求打包 Windows x64、Android arm64，并将本轮版本源码同步至 GitHub、Gitee。旧原生 JS 前端暂不调整；安装包本地分发，不提交二进制、密钥、日志或临时验证目录。

## 发布验收

- 最终暂存源码导出至隔离发布目录，以独立 `YUANSHU_CWD` 运行全量测试：**1910/1910 通过，0 失败、0 跳过**，退出码 0。前端 TypeScript 检查、生产构建、Impeccable 检查通过。
- 首轮工作树检查暴露两项无关的未跟踪技能触发词配置问题，以及一项取消测试时序问题。未修改/删除用户技能，也未纳入本次发布；取消测试改为等待请求实际启动后再取消。隔离验证树严格由暂存文件导出。
- 发布副本曾因忽略规则遗漏 `app/src-tauri/Cargo.lock` 未通过版本契约；已纳入本轮实际构建使用的锁文件，再执行上述完整测试。依赖版本未重新升级。
- 审查补齐长任务检查点：保留当前用户需求、修复旧快照缺用户的工具轨迹落盘；截断后的片段及接续指令在预算暂停前保存；已完成工具交换不得留下孤立结果。
- 待执行工具恢复依据权威计划重建完整调用组，保留完整参数、轮次和步骤编号；已有结果不重复执行；匹配的原生签名块原样保留。新增 10 项检查点/恢复测试，关键修复经过先红后绿验证，独立审查无剩余阻塞项。
- 源码 `diff --check` 通过。带哈希的编译分块有构建器生成的模板字符串尾空白，未手工修改产物以免破坏内容哈希。

七套浏览器回归全部通过，使用隔离 fixtures，不调用付费模型或修改真实会话：

1. 长任务：桌面/手机模拟 11 分钟无事件，不自动停止；恢复结果只出现一次，显式停止有效。
2. 消息流：完成、缓存恢复、刷新/焦点恢复均不重复。
3. 图片：桌面缓存、手机缓存和实时场景均只显示一份。
4. 手机辅助面板：390/820 宽度全屏、关闭和重开正常，桌面不强制全屏。
5. 模型归属：所选模型和实际回答模型区分，刷新保留，替换回答不重复。
6. 任务证据：验收、持久化、撤销、旧状态冲突和天团边界验证通过。
7. 模型接入：鉴权失败保留表单、手动原生协议保存、验证刷新、编辑重探和删除通过。

## 前端与服务

- `frontend/dist`、`app/dist` 均为 415 个文件，逐文件 SHA256 一致；`public` 中对应文件全部一致，其余资源保留。
- 三处入口 HTML SHA256：`505D3973C41E8B41148B6F5484C0490D7A101CD1D657AEB0AABF7A0D67D6B169`。
- 当前入口：`assets/index-DTFElMUs.js`、`assets/index-wpReDLKy.css`。
- 本机首页、公网首页及本机健康接口均返回 200，两处首页入口与磁盘一致；8787 检查时只有一个监听进程。
- 清理不可达中间构建分块，保留当前和前两次发布，清理前已有本地备份；没有删除旧安装包。
- 本轮未重启服务。前端已由服务读盘，**本轮新增 engine 修复需下次正常重启后加载**，不将磁盘更新描述为运行进程已经生效。

## Windows x64

- 安装包：`D:/pi-workspace/安装包/元枢_2.115.8_x64-setup.exe`。
- 大小：9,192,012 字节。
- SHA256：`054ED0600B407D9F06A8DE5F94A014D892F5690B4535D0A45595FDD025AC1832`。
- NSIS 构建退出码 0；构建原件、安装包目录和 `交付/元枢桌面客户端/` 副本哈希一致。
- 安装包与主程序 FileVersion / ProductVersion 均为 `2.115.8`；主程序 PE machine 为 `0x8664`。
- Authenticode 为 `NotSigned`，沿用本机未签名分发方式。

## Android arm64

- 安装包：`D:/pi-workspace/安装包/元枢-v2.115.8-arm64.apk`。
- 大小：16,505,363 字节。
- SHA256：`F0CFC5633C63A1AFF562DFD351ABAA0CE33E6DE6990B997C2610CEC602A79F38`。
- 从签名 APK 读取：applicationId=`com.yuanshu.app`、versionName=`2.115.8`、versionCode=`2115008`、ABI=`arm64-v8a`、minSdk=`24`、targetSdk=`36`。
- apksigner 验证 v2/v3 签名有效，zipalign 16 KB 页面及 4 字节对齐通过。
- 沿用本机 Android Debug 证书，与 2.114.0 相同，非商店发布签名。证书 SHA256：`794311754e7420551ff205eef390facea93b051e40b0d58c220512a5ad7bfe33`。
- APK 内原生库、编译原件和 JNI 副本 SHA256 一致：`7557185C0036EB094CDC360C52778591806580A453EB4E6D09882AD10F39478F`。
- Rust 编译成功后由现有 Gradle 兜底处理本机符号链接权限限制；构建退出码 0，168 项任务（28 执行、140 已是最新）。DownloadBuffer 测试 3/3，0 失败、错误或跳过。
- ASCII 路径签名原件与中文交付路径 APK 哈希一致。

## 使用与边界

Windows 连接 `http://127.0.0.1:8787/`，Android 沿用 `https://pi.myxinyu.xin/`。客户端依赖现有后端，不包含独立离线后端；构建后的本轮检查点修复只涉及服务端，不改变已核验的客户端资源。

本轮未覆盖安装用户正在使用的客户端，未做安装后 GUI 或手机真机功能测试。上述浏览器手机模拟不能替代真机验收。旧安装包及无关本地技能保留，延后统一集成设计未纳入本次提交。
