# 系统2第3项：Claude方案后续修正

日期：2026-09-26。承接 `claude-review-reconciliation-2026-09-25.md`。

## 顺序和范围

- 第1项：按用户要求暂缓，预览已接入、形象待验收。状态已写入人像验收文档。
- 第2项：保留已完成的首屏/会话恢复优化，见 `superpowers/specs/2026-09-26-startup-performance.md`。
- 当前只推进第3项，不自动进入第4–7项，不启动暂缓的跨系统整合。
- “推第三个”按推进事项理解，本批不推送远端、不打安装包。

## 本批：请求解析和缓存边界

### 1. 中文与表情被网络分块损坏

根因：`readBody` 把每块 Buffer 独立转成字符串。UTF-8 字符跨块时被替换为乱码，JSON仍可能解析成功，因此内容会无声损坏。

修正：使用 Node 内置 StringDecoder 跨块解码；按原始字节累计请求体大小，在累积内容前检查上限。保留空请求体、非法JSON的400，以及超限时排空剩余请求并返回413的既有约定，不主动毁掉连接。

回归：遍历中文、表情、中文路径JSON的每一个两段切分点；验证恰好达到字节上限能通过、多一个字节返回413。

### 2. 缓存规则污染接口和错误响应

根因：入口包裹所有响应的 `writeHead`，仅根据URL后缀/目录强制缓存。实测 `/api/private.png` 的401响应带 `public, max-age=31536000, immutable`；无版本人像、图标和缺失资源也被一年强缓存。

修正：移除入口中的缓存覆写器；默认 `no-store`，由成功的静态文件服务设置具体缓存规则。保留HTML、service worker、无指纹资源的 `no-cache` 重验证；带构建指纹的 assets 文件和现有版本查询参数继续长期缓存，304保持一致。鉴权、CSP、路由注册与签名文件流程不变。

回归：用真实本地HTTP运行生产请求回调，覆盖401、已授权私有API、缺失指纹资源404、普通图片/图标、现行人像路径、HTML、构建指纹资源200与304。无模型、任务或真实工作空间副作用。

### 验证记录

- 修改前隔离基线：2205/2205通过，类型检查与隔离构建通过。
- 新回归先红：6项按预期失败，3项原有请求体行为通过。
- 修正后定向回归：23/23通过；语法检查和差异空白检查通过。
- 隔离分支全量复验：2211/2211通过，类型检查与隔离构建通过，命令退出0。输入指纹 `b853a19e4af2e691f54ced9bcccbff4ed51cc546097545f6bde5a493dda4f7c0`；证据在本工作树 `tmp/review-verification.json` 和 `tmp/verification-{unit,types,build}.log`。构建仍有既有大chunk提示，不等于无警告。
- 本批不改UI，未执行前端视觉检测；未重启后端，运行中进程不会自动载入这些源码修改。

### 本地主分支合并复验

- 修复提交 `5117022e` 已快进合入本地 main；原有7处已跟踪未提交改动保留，未推送远端。
- 合并后 `npm run verify` 于2026-09-26T09:58:49.474Z退出0：2254/2254测试通过，0失败、0跳过；类型检查与隔离构建通过。本地主目录包含本机附加技能与既有测试改动，所以测试数不同于隔离分支。
- 输入指纹：`45df5b1087ebc1b98351171fabb9d2ab9809443e47a906a924e8aff1897a9c65`。证据：主目录 `tmp/review-verification.json` 与三份 `tmp/verification-*.log`。
- 构建未覆盖线上 `frontend/dist`；本批提交差异空白检查通过。全工作区检查仍报告既有 `frontend/dist/index.html:13` 尾随空白，未修改他人产物。
- 版本仍为2.116.6；未重启、部署或打包，不将代码合并视为线上生效。保留隔离工作树及测试证据，不强制清理。

## 第3项仍未完成的边界

1. 服务入口仍大，聊天/SSE路由尚未完成分模块迁移；未启用 `server-new.mjs` 草稿。
2. 全API错误schema、生成类型、后端注释覆盖仍需按路由分批推进；流已开始后的异常终结和完成日志可作为下一批核查项，尚未在本批声称修复。
3. 全局结构化日志和操作审计覆盖仍待盘点；已有运行事件不等于所有行为可审计。
4. 凭据加密/轮换、OS级沙箱是独立安全工程，未完成。
5. Android真机与远端CI/签名发布需另行验证，本机测试不替代。

已存在的React主入口、CI配置、版本同步、桌面壳和分享不重复建设；不删除兼容环境变量或仅因产品名而切换默认引擎。
