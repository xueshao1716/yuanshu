# 2.116.2 补充验收：Step-5 与 Android（2026-09-25）

本记录补充 [运行验收](runtime-acceptance-2026-09-25.md) 与 [发布记录](packaging-2.116.2-20260925.md)。历史失败不覆盖为成功；每项只说明本次实际覆盖的范围。

## Step-5 通道与真实工具任务

- 同一配置 `stepfun-plan/step-5-preview`，协议 `anthropic-messages`，端点 `https://api.stepfun.com/step_plan/v1/messages`。未修改账号、密钥、默认模型、引擎或协议。
- 最小请求本次返回 HTTP 200，耗时约 1.3 秒；实际响应模型为 `step-5-preview`。32 token 的小预算在思考阶段触及 `max_tokens`，仅用于连通性检查，不据此判定空回复故障。
- 真实模型工具任务通过：读取临时账单、写入凭据、读回凭据，3 个工具各执行一次，核对总额 68 与随机标记。4 轮实际模型均为 `stepfun-plan/step-5-preview`。
- 首次工具执行时断开 HTTP/SSE 订阅，服务端仍完成；从序号 2 重连补收 23 个事件，与持久化事件逐项一致，完成事件恰好一次，无重复或遗漏。
- 以上证据为 `yuanshu-stream-live-915pgt/report.json`。它验证服务端与真实模型链路，**不是 Android 锁屏/后台保活验收**。
- 先前 HTTP 403 本次没有复现；尚无证据确定先前失败来自账号、额度还是上游策略，不能写成“403 根因已修复”。

## 工坊续做与慢响应诊断

- 从旧检查点复制到独立临时目录，继续第二个区块；设计计划、已完成第一区块和原始检查点摘要保持一致，没有重跑已完成工作。
- 第一次补测 `yuanshu-website-live-iI8Ykf`：第二个区块在 120 秒后超时，长任务验收未通过；没有自动采用候选、导出作品或进入局部修改测试。
- 最后一次有界复测 `yuanshu-website-live-T65pJS`：仍只续做第二个区块，2026-09-25 07:31:47–07:33:47 UTC 再次在约 120 秒后超时；`passed:false`、`checkpointsReused:true`、`sourceUnchanged:true`。没有进入选区修改和导出阶段，也没有完成新的区块；报告中的历史成功模型字段不代表这次超时调用收到了成功响应。
- 单独以同一模型、相同资料与区块要求改用流式诊断（非工坊产品路径）：HTTP 200，约 1.9 秒开始返回思考，约 99.8 秒开始正文，112.8 秒完成。思考 21,135 字符，正文 4,012 字符，`end_turn`，区块结构校验通过。
- 这说明该次请求并非完全无响应，但不能从一次成功推出流式更快或通道稳定。工坊现有 120 秒单次截止与 15 分钟总截止没有被暗中提高。
- 检查到工坊传入 `thinking:false`，原生 Messages 编码器保留上游默认思考策略。另一次最小请求显式发送 `thinking:{type:"disabled"}` 仍返回 thinking 块；不能把“补传此参数”当作已验证的解决方案，也未据此修改公共协议适配。
- 诊断脚本仅写隔离临时数据，不改用户画布；思考正文不纳入归档报告，仅保留字符数、时序和最终区块正文。
- 结论：Step-5 通道与短工具任务本次可用，但工坊长任务仍未通过。两次工坊补测均超时；独立流式诊断一次成功不能替代产品路径验收。到此停止重复付费尝试，不把截止线放宽或静默换模型当作已完成修复。

## 补充证据归档

以下目录已复制到 `D:/pi-workspace/.build-cache/release-evidence-2.116.2-20260925/`，逐文件 SHA256 与原始临时目录一致；本机归档不作为远端仓库附带这些产物的承诺。

| 目录 | 文件数 | 结果与范围 |
| --- | ---: | --- |
| `yuanshu-stream-live-915pgt` | 5 | 真实 Step-5 工具执行、断连与事件重放通过 |
| `yuanshu-website-live-iI8Ykf` | 2 | 首次续做补测超时，检查点保留 |
| `yuanshu-step-section-diagnostic-kfAANA` | 2 | 独立流式区块诊断通过，不是工坊完整验收 |
| `yuanshu-website-live-T65pJS` | 2 | 最后一次续做补测仍超时，检查点及源文件不变 |

## Android 当前边界

- `adb devices -l` 没有列出设备；本机没有已配置 AVD，SDK 中未发现 emulator 目录。未额外下载模拟器，未覆盖安装用户手机。
- 重新执行后台恢复、恢复集成、续做端到端、Run API、Android 壳和键盘的 6 个测试文件：**47/47，通过；0 失败、跳过或取消**。使用统一验证环境与 preload，将状态隔离到临时目录。
- 收尾时再次执行同一组 6 个文件，仍为 47/47、exit 0；独立目录 `yuanshu-mobile-followup-final-W6lDKt`，不将其算成新增 47 个不同用例。再次查询 ADB 仍无设备。
- 覆盖预算续做、三次自动续做上限、停止意图、审批/副作用/范围屏障、会话隔离、事件游标、手机壳服务器地址和键盘约定。
- 前端切后台处理不调用停止；切回时刷新本地缓冲和会话状态。此为源码检查，不能代替 Android 厂商节电、进程回收或锁屏网络实测。
- 待接入可调试真机后：核对旧版与待装 APK 签名 → 保留数据覆盖安装 → 验证版本与登录 → 新建专用测试会话 → 工具任务期间切后台/锁屏 → 解锁检查完成结果、事件补收与重复副作用 → 测试断网重连。通知及时性与任务是否完成分别记录。

## 发布边界

- 本轮只补验收与诊断，没有修改产品代码、默认配置或生产数据。版本仍为 **2.116.2**，无需重复打包或为文档变化重启服务。
- 当前产品源码指纹仍与之前全量验证证据一致：`3d8caa78791a374e08cd270568f1f54896d1da1bca74fdb5a935417a85a04440`，检查记录状态为 `passed`。本轮重跑的 47 项检查与真实模型测试单独记录，不冒充重新执行全量 2164 项。
- 收尾复查 `/api/health` 返回 `{"ok":true}`。服务存活不等同于 Step-5 工坊或 Android 真机验收通过。
- 不启动既有延期的服务器拆分与跨系统集成。
