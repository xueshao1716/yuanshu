# 元枢工作台本批发布记录

日期：2026-09-21。用户已授权实施与重启。本记录更新此前各验证文档中“仅候选、未部署”的状态；不把 P1–P4 全路线图标记为完成。

## 已部署

- 正式目录：`D:/pi-web`；服务工作区：`D:/pi-workspace`。
- 候选目录：`D:/pi-web/.worktrees/verification-20260920`。
- 本批整合 39 个源码、测试及说明文件。保留主目录原有的其他未提交修改，没有提交或推送。
- 待审改动增加目标内容冲突检查、重复应用防护、写入失败恢复状态；文件操作限定范围，拒绝危险链接和保护记录。
- 历史回滚先另存当前内容，区分文件恢复与审计记录失败。
- 天团运行入口改为仓库内维护的 runner，增加独占启动、启动标识匹配、运行快照与验收状态读取；脚本完成不等于人工验收。
- 天团区分历史快照、当前启动状态、清单检查及交付验收；未知流程版本不强行套用 V20/V25 阶段定义。
- 改动验收将 `public/assets/` 构建快照正确归入生成物，保留真正源码分类。新增测试先复现误分类失败，再修正通过。

## 部署与恢复证据

- 备份：`D:/pi-web/backups/release-20260921-0015/`。
- `source/` 保存本批修改前的 10 个已有文件；`frontend-dist/` 保存切换前完整前端。
- `manifest.json` 是最初 37 文件的候选清单；`deployed-manifest.json` 是最终 39 文件的实际 SHA-256 清单。TeamRunView 候选末尾空行与实际文件不同，功能内容一致，实际哈希以后者为准。
- 重启前查询无运行任务；通过 `scripts/restart-pi-web.ps1` 执行一次后端重启，退出 0。8787 监听 PID 从 36296 变为 8420，仅一个回环监听。
- 前端临时构建后先部署资源、最后切换 index，未删除旧 hash 资源，未误发布到 public。
- 最终首页与磁盘 index 一致；入口脚本 `index-BiHScc94.js`、工作台 `Board-Ds_OBRhy.js` 及另外四项入口依赖均 HTTP 200 且逐字节一致。

## 验证

| 检查 | 结果 |
| --- | --- |
| 候选全量测试 | 1692/1692，通过 |
| 主目录首次全量测试 | 1692/1692，通过 |
| 最后分类修复后的主目录全量测试 | 1693/1693，50 suites，0 失败、取消、跳过，128.8 秒 |
| 最后前端类型检查与构建 | 使用 frontend 自身依赖，退出 0 |
| Impeccable | 初始五个改动组件/页面及最后 ReviewWorkbench 检查均返回空问题清单 |
| 语法与空白检查 | server 语法检查通过；git diff --check 通过，存在既有 CRLF 提醒 |
| 在线接口 | health、agent-status、team/run、pending、history、run/overview 均 200；未授权 team/run 为 401 |
| 实际页面 | 概览、改动验收、天团可进入；旧天团 8/12 清单显示需复核，不冒充验收通过 |
| 响应式 | 检查桌面及 360×800 窄屏；窄屏使用视图下拉框，三个视图可切换，文档宽度 360，无整页横向溢出 |
| 浏览器日志 | 本次检查未捕获 error/warn |

全量测试将 YUANSHU_CWD、PI_WEB_CWD、PI_WORKSPACE 指向本批临时工作区；没有以真实产物执行写入、接受或回滚测试。14 项 HTTP 回归使用真实处理器与临时工作区，但不是完整生产 server 端到端测试。

日志：

- `D:/pi-web/.worktrees/verification-20260920/tmp/release-tests-20260921.log`
- `D:/pi-web/tmp/release-main-tests-20260921.log`
- `D:/pi-web/tmp/release-final-tests-20260921.log`
- 最终构建：`D:/pi-web/.worktrees/verification-20260920/tmp/release-build-20260921-final/`

## 保留边界与后续工作

- 本轮未触发付费模型/视频生成，没有验证真实生成质量或完整付费任务链路。
- 天团接入统一 run-manager、取消/恢复、检查点/事件重放与预算控制，视频/小说/技能专项及反馈评估仍未完成。
- 纯 delivery-contract 已测试，但尚未成为全部业务发布授权来源。
- 独立最终复审工具未成功接收任务正文，不记作审查通过；现有自动测试和主执行者检查不等于独立复审。
- 改动验收 API 的测试结果回写尚未接通，页面仍显示“尚未执行”；不伪造已验收状态。本次真实执行证据以本记录和日志为准。
- Git 快照仍有 300 文件上限，当前项目含大量旧构建文件，页面不是完整变更清单。
- 窄屏 ReviewWorkbench 内部 scrollWidth 大于 clientWidth，但未发现元素边框越出视口或整页横向滚动；长内容截断/内部滚动没有逐条穷尽验证。
- 构建有非阻断警告：allow-scripts 配置、Vite 插件废弃选项、Toast 混合导入、大于 500 kB 分包。没有为消除提示进行无关依赖升级。
- 未重打包手机/桌面安装包；已安装客户端快照不等同于此次网页发布。

## 必要时回退

1. 先查询是否有正在运行的任务，保存现场；不要在长任务中途回退或重启。
2. 按 deployed-manifest 核对当前文件哈希，若发布后又有修改，先单独保存，禁止整仓覆盖。
3. 从 source/ 恢复清单中已有文件；本批新增模块可暂留未被引用，不必为了回退删除。
4. 从 frontend-dist/ 先恢复资源，再恢复 index.html；保留额外 hash 资源无害。
5. 使用正规 restart-pi-web.ps1 重启并复查 health、鉴权、单监听和线上资源版本。

回退不包含删除真实工作区里的历史记录、用户产物或待审数据。此次未实际执行回退。
