# 元枢工作台 2.111.0 发布与验证

日期：2026-09-21（北京时间）。本记录接续 2.110.0；不将整个 AIBody P1–P4 路线图标记完成。

## 已上线

- 正式目录 D:/pi-web，运行内容目录 D:/pi-workspace。运行版本 2.111.0，最终服务 PID 54236，8787 仅一个回环监听。
- 本批在隔离工作树实现并将 38 个源码、版本与测试文件逐项核对后合入主目录，保留原有未提交改动。最终又补齐前端样式配置的验证指纹。
- 验收页读取真实的单元测试、类型检查与构建记录；记录绑定源码和前端构建配置指纹，内容变化显示过期。缺少检查不显示通过。300 文件预览上限现在明确显示总数与省略范围。
- 天团新增持久化阶段检查点、调用步骤上限、合作式停止及保守恢复。完成步骤复用，不确定调用拒绝自动重放；网络失败不自动再发付费请求。停止信号传递到模型请求，但不能保证撤销远端执行或计费。
- 小说修订、续写和节点推进要有新的有效内容证据，取消、超时及只有旧产物不再被判成功。修订备份失败阻止继续执行。
- 提示词进化限制模板路径，提案绑定原文摘要，拒绝旧提案覆盖新内容；必须成功备份后才写入。旧的无摘要提案需重新生成。
- 延续并核实 2.110.0 的展示修正：飞天舞旧任务默认折叠为历史，不冒充当前任务；身份可读不等于本轮已加载，记忆标题数不冒充语义条目数，情绪说明来源会话与时间，治理计数限定队列，重启恢复记录不宣称自动续跑。

## 验证证据

| 检查 | 最终结果 |
| --- | --- |
| 主目录全量单元测试 | 1724/1724，50 suites，0 失败、取消、跳过；129.79 秒 |
| 前端类型检查 | 退出 0 |
| 前端构建 | 退出 0，输出 tmp/verification-dist |
| 证据状态 | 线上 /api/git/review 为 passed；记录时间 2026-09-21 01:41:56 |
| 新增配置失效回归 | 先复现 passed 不失效的失败，再修正为 stale，通过 |
| 真实 runner 恢复回归 | 子进程连接本机模拟模型服务；第二次恢复模型调用数不增加，送审总数仍为 1 |
| Impeccable | TeamRunControls、TeamRunStatus、ReviewWorkbench 返回空问题清单 |
| 语法与差异 | server.mjs 语法检查及指定改动文件 diff --check 退出 0；保留原有换行符警告 |
| 在线 API | health 200；无鉴权 team/run 401；停止/恢复不存在的任务均 409 |
| 在线资源 | 首页与磁盘逐字节一致；入口 JS、三个依赖 JS、样式共五项资源 HTTP 200 且内容一致 |
| 页面交互 | 桌面历史默认折叠，展开为历史任务；手机可切换概览、改动验收、天团 |
| 隔离按钮测试 | 拦截全部 team 写请求，停止/恢复错误均显示提示；未操作真实任务 |
| 响应式 | 桌面 1440、窄屏 360×800 的整页宽度均等于视口；无 pageerror |

日志在 tmp/verification-unit.log、tmp/verification-types.log、tmp/verification-build.log；机器可读证据为 tmp/review-verification.json。最终源码指纹 ce29ec1db43d8b68548b991d97e0dae7257c6eaf875cf38d6c74eb3da53c0b3b。

全量测试的工作区变量指向临时目录 C:/Users/XUEXIA~1/AppData/Local/Temp/yuanshu-verification-B26rcb，未拿真实创作产物进行写入测试。未触发真实付费模型或视频生成。

浏览器检查基于本机无头 Chromium 的 DOM、点击和布局断言。截图保存在 tmp/workbench-2111-*.png；当前图像查看工具不支持图像输入，未将截图保存算作视觉审查。一次模拟按钮检查因刷新后未重新选择天团视图而超时，重试显式选择视图后通过。独立复审工具此前任务传递失效，本次是主执行者检查，不宣称独立复审通过。

## 发布与恢复

- 发布前备份：D:/pi-web/backups/workbench-2.111.0，包含被替换的已有文件与旧 frontend-dist；不是整仓完整快照。最后两个验证文件的中间版本另存为 .pre-final。
- 前端部署先复制资源、最后切换 index；保留旧 hash 文件，不删除历史，不覆盖 public 快照。入口 index-BsPXG_Ow.js，样式 index-DXf1HqpT.css。
- 两次重启前都确认聊天无 busy、运行概览 active 为空且天团无运行任务。使用正规 restart-pi-web.ps1，退出 0；最终 PID 54236。短暂旧心跳是残留声明，不是第二个监听。
- 未提交、推送、外部通知或重建安装包。保留隔离工作树及原有脏工作区。
- 回退前先保存发布后的新修改，按文件核对备份；资源先恢复、index 最后恢复，再用正规脚本重启核验。不要整仓覆盖，不删除用户工作记录。本次未演练回退。

## 仍未完成，不能列作已验收能力

- 天团尚未接入统一 run-manager / 事件重放架构；当前步骤上限不是货币或 token 预算，尚无完整专项 profile 与真实质量反馈评测。
- delivery-contract 仍不是全部业务共同使用的发布授权来源。
- 小说的完整 V25/V26 产物映射、跨章节连续性评测、代理创建期间取消竞争等未完成全面验证。
- 技能版本治理、完整评测样本与反馈改进未完成；本次模板安全修复不等同全套技能进化。
- 真实视频生成、付费链路、远端计费取消效果未验收，需要明确样例与费用上限。
- 桌面和手机安装包未重新构建，旧包的内置页面不会因网页发布自动更新。
- 当前测试证据覆盖声明的源码、前端输入及本地检查，不代表安装器、所有外部依赖或全部运行数据已验收。

非阻断事项：构建仍有大分包与 Toast 静态/动态混合导入提示；Impeccable 配置存在旧键，本次没有顺手改动无关配置。
