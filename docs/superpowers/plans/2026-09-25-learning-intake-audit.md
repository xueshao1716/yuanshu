# 工作台与知识库入账链路审查

检查时间：2026-09-25（UTC；本机 UTC+8 已为9月26日）。范围：只读检查当前在线接口、源代码及工作空间记录。未触发付费分析、重新验收、自动审批或自主探索。

## 已确认事实

1. **经验数据存在，但经验沉淀台没有展示。** `/api/refine/status` 返回 experience.exists=true、49个二/三级标题、updatedAt=`2026-09-25T17:37:16.358Z`。49是标题数，不能当作49条已验证知识。`frontend/src/pages/Apps.tsx` 的 RefineView 请求了 status，但渲染只使用提案列表；提案为待审1、已采纳17、已拒绝3。该面板不是完整经验库浏览器。
2. **活动流不是持久账本。** `engine/unified-chat.mjs` 的 agentEventRing 只在内存保存最近200条，GET返回80条，页面展示40条；重启后清空。检查时GET返回0条。全局 `event-broadcast.ts` 从 pi SDK 扩展向此接口上报；元枢内部执行另有 run-event-log/work-explanation 链路，不能由此活动环为空推断普通会话未执行。终端 notify.py 也能向这个环上报，但本次没有调用。
3. **后台采集近期确实发生过，但没有合格学习标签。** 在线 `/api/dream/status` 显示20条观察、0条合格样本、phase=collecting。对loadEpisodes返回的逻辑去重记录按UTC日期聚合：9/16=1、9/19=3、9/20=4、9/21=4、9/22=1、9/24=2、9/25=5。不能据此证明这些日期用户没有使用Codex，只能证明采集并非仅有检查当天的记录。
4. **周期回放不等于主动获取新知识。** server在启动2分钟后及每6小时运行已有会话的增量采集与回放。游标为 `2026-09-25T17:48:16.711Z`。`evolution-cycle.mjs` 只有result.ok才写做梦日志，日志旧不能单独证明定时器停了。技能样本需要真实任务验收及独立/人工技能有效标签；0合格时不应自动晋升策略。
5. **自动技能沉淀触发范围有限。** 仓库内自动 nudgeSkill 挂在 time-engine 的 onTaskDone；未发现普通对话结束统一挂钩。refine分析有显式POST入口和前端按钮，不能认为每次正常聊天都会生成提案。未对所有外部脚本/计划任务做完整审计，因此不声称机器上完全没有其他调用来源。

## 额外代码风险（未证明已造成线上丢失）

`runDreamCycle` 单文件采集异常被吞掉，但批次游标仍推进至最大mtime；若确实发生读写失败，可能越过失败文件。当前没有足够错误记录确认曾发生。应增加每周期状态、失败计数及安全重试，不把这个风险写成已证实事故。

## 建议下一批小步修正

1. 知识库显示现有经验标题、更新时间及来源，并明确区分观察、待验证、已验证与已采用；不把条目数当作能力提升。
2. 工作台基于已有持久执行记录汇总日常活动，覆盖元枢/pi/定时任务；保留旧外部事件兼容，避免双计数。
3. 每轮采集持久记录成功/失败/跳过原因及上次时间；失败文件可重试，不被总游标越过。
4. 普通任务完成后先生成本地、去重的沉淀候选队列；模型提炼另设明确预算及触发条件，不能每轮无上限追加付费调用。采用仍经过原有验收和人工审批。

以上为改动前的审查快照。随后用户批准继续实施；本地已补上以下链路，但不宣称后台已具备持续无人值守自进化。

## 已实施的本地修正

1. **经验沉淀台**：显示现有 experience.md 的来源、修改时间和最近500个标题摘要，支持关键词搜索、每20条展开；明确标题数量不是验证通过数。加载、空结果、读取错误分别显示，刷新失败保留上次内容。
2. **持久活动**：从执行账本读取当前工作空间的任务摘要，最多60条，与最多20条带来源标记的外部上报合并，页面显示最近40条。有 runId 的重复上报优先采用账本。执行中数量单独计算，不从最后一条历史事件推断；恢复后的任务不再显示旧失败时间。
3. **可靠采集**：按会话文件的修改时间及大小记录完成指纹，不再以全局时间游标跨过失败文件。每5个已处理文件保存进度并让出事件循环；并发采集合并，解析/写入失败或文件仍在变化时留待重试。状态包含上次时间、成功、失败、跳过及原因；损坏的状态文件保留并报错，不自动覆盖。
4. **普通任务候选**：持久任务管理器完成任务后，写本地去重候选，再写账本标记；失败可在后台周期补记。仅收录明确属于当前工作空间的已完成记录，不存整段会话或工具输出，不新增模型调用、不自动写经验、不自动采纳。展示待补记及上次写入失败数量。

## 边界与发布状态

- 候选保留最近500条，每周期最多补记100条；移出旧候选不删除原任务账本，也不重新入队。每个候选只是待提炼的任务引用，不是已掌握能力。
- 使用既有启动约2分钟、之后每6小时的后台周期；服务未运行时不会执行。不是新增全天候自主探索，也未配置任何外部提醒。
- 持久活动和候选覆盖走当前任务管理器、且明确记录工作空间的路径；没有归属信息的旧记录不猜测归属。外部终端事件仍可能仅存于内存，不能说所有历史任务都已补齐。
- 经验展示为原始文件摘要检索，不负责判断经验冲突或过时；观察采集不代替此前的任务验收和人工审批。
- 此处原为2.116.5本地实施快照。2026-09-26发布2.116.6的部署、安装包与验证结果另见 `2026-09-26-learning-intake-release.md`；以下测试数量保留原审查口径，不代替发布副本的验收。

## 验证证据

- 隔离全量验收：2231/2231测试通过，类型检查、非线上目录构建通过。新增回归覆盖失败重试、同时间戳重启、并发采集、变化中的文件、损坏状态保留、候选去重与跨工作空间隔离、保留上限及恢复任务时间。
- 页面契约检查覆盖加载/错误/资格文案及服务接线；Impeccable检测本轮三个前端文件，无发现。
- 隔离真实模块串联验收：任务管理器完成任务→候选落盘→重新创建存储与候选服务→补记去重→重建工作台活动，共一条候选、一条任务摘要、零待补记；零模型调用。
- `scripts/verify-learning-intake-ui.mjs` 用隔离HTTP夹具与真实Chrome实测1440px/390px：搜索、分页、空状态、长标题不横向溢出、按钮触控尺寸、刷新失败保留记录、来源标记、活动状态及断连提示。不是生产数据端到端验收，也没有为测试调用付费模型。
- 截图产物保存于 `tmp/learning-intake-1440.png`、`tmp/learning-intake-390.png`；当前工具不能回读图片，未声称完成截图人工视觉检查。
