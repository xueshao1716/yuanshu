# 天团入口迁移验证记录

日期：2026-09-20。目录：`D:/pi-web/.worktrees/verification-20260920`。

## 本批结果

- 仓库内 `scripts/team-run-live.mjs` 成为候选服务唯一固定启动脚本，工作区、端口、凭据通过服务配置注入。移除 D 盘固定依赖及外部角色/清单依赖。
- 本地视频脚本 profile 明确为 `yuanshu-video-legacy@2`，不冒充完整 V20/V25/V26。实际五角色、十二项清单；验收前只保存草稿。
- `engine/team-launch.mjs` 以独占创建的启动锁覆盖启动窗口，跨实例重复请求被拒绝。旧锁中的存活进程不按 20 分钟超时放行；损坏、null、权限或链接等不确定情况拒绝猜测处理。
- 实际 spawn 事件后才返回启动成功；错误返回失败。退出码为零且快照 launchId 匹配才能称为执行结束，不等于人工验收通过。
- 页面分别展示启动状态、历史快照、profile 和草稿投递状态；未知模式不自动解释成演练。当前验收结果仍应查待审区/历史记录。
- 修复跨实例读到旧状态、误报启动中断，以及旧锁 JSON null 被当成缺失的问题。
- VIDEO 和 ARBIT_FINAL 提示补齐画幅、一致性键、负面清单、文字处理，取消与本地检查清单矛盾的字段禁止要求。未据此宣称模型内容质量已实测通过。

## 新鲜验证证据

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| 全量测试 | 1656/1656，50 suites，0 fail，约 96 秒 | `tmp/p1-entry-reviewed-tests.log` |
| 定向回归 | 31/31 | `tmp/p1-entry-focused.log` |
| TypeScript + Vite build | 退出 0 | `tmp/p1-entry-build.log` |
| 构建文件 | index.html 存在，1280 字节；不覆盖现有 dist | `tmp/p1-entry-build-20260920/` |
| Impeccable | 两个改动组件返回 `[]` | TeamRunView.tsx、TeamRunStatus.tsx |
| 语法检查 | server、launcher、runtime config、video profile、runner 通过 | `node --check` |
| diff 空白检查 | 退出 0，仅既有 CRLF 提醒 | `git diff --check` |

测试使用新建临时工作区以及临时文件夹，不直接执行付费 runner。新增真实无害子进程成功退出与不存在可执行文件的 OS 错误测试；其余事件由进程边界测试替身控制，实际锁文件与状态写入仍走真实实现。全量运行前配置了 YUANSHU_CWD、PI_WEB_CWD、PI_WORKSPACE 到本批 tmp 路径。

首次全量暴露旧源码断言仍要求 runner 内直接读 token；现在改为验证配置模块读取及 runner 调用，配套行为测试继续覆盖优先级。修正后重新全量通过，不把早先失败记录当作最终结果。

构建仍有 Toast 静态/动态混合导入和大 chunk 警告，本批未扩大到打包架构改造。

## 审查

独立审查 `p1_review_with_context` 阅读入口、runner、配置、页面和测试，独立 20/20 定向回归通过，未发现入口切片阻断项。其指出的提示/清单矛盾已随后用失败测试复现并修正，再由主任务完成 31 项定向和 1656 项全量验证。

最终提示修订的第二次独立复审因工具传递空正文未完成；不能把第一次独立结论扩展为最终提示修订已独立审查。已有记录与验证证据保留，未直接发布。

## 剩余边界与发布门槛

- 这是 P1 入口切片，不是完整 P1，也未把团队流程接入 run-manager 的取消、恢复、事件重放、预算控制。
- 启动锁不自动回收异常服务退出后的不确定状态。应核对运行进程与快照后人工处理；不得为重试直接删除锁。旧服务不理解新锁，发布时不得混跑旧入口与新入口。
- 旧锁检查与新锁排他保证适用于迁移后的入口；无法禁止用户直接在命令行运行外部历史脚本。
- 未进行真实模型调用、浏览器点击/小屏交互、生产重启或在线部署验证。
- 模型输出形状的完整运行时验证、真实视频适配器、验收状态动态回写仍未完成。先前契约模块尚未成为业务发布授权来源。
- 只构建 tmp 产物；没有修改在线主工作区、提交、推送、外部通知或写入长期记忆。

下一步先完成最终提示独立复审与隔离浏览器检查，再做运行状态/验收联动和结构化产物验证；部署前重新核对主目录差异及无活动任务，按既定正规重启入口执行。
