# 元枢 2.110.0 工作台发布核验

发布日期：2026-09-21。本记录补充此前工作台发布记录，不代表完整 V20/V25/V26 路线已实现。

## 本次交付

- 产品版本从 2.109.3 升至 2.110.0，同步九处版本声明、四份 npm 锁文件根包版本及 Cargo.lock 根包版本。升级脚本新增锁文件同步，未升级第三方依赖。
- 天团当前运行必须同时满足启动标识匹配及启动器处于 launching/running；其他快照归入历史。旧飞天舞记录保留，默认折叠，展开标注“历史任务（不在运行）”。
- 未标注流程版本的提示仅对缺少版本的快照显示，不把历史流程强行解释为 V20/V25。
- AIBody 身份读取确认文件可读，不再冒称本轮已加载；记忆数量按标题统计，不冒称语义条目或本轮已引用。
- 情绪展示最近会话记录时间和来源；人格/基因治理队列与其他审批队列明确分开。缺失或无效数据不作为健康零值。
- 系统脉络数量标为“有读数”，不代表健康检查通过。历史引擎记录不代表当前在线。
- 重启恢复记录并标记中断，不承诺自动续跑。进化原则标注为设计约束；本页未验证的审批/回滚能力不再硬编码为已保证。

## 验证证据

| 项目 | 结果 |
| --- | --- |
| 新增状态回归 | 13 项先失败后通过 |
| 版本一致性 | 命名契约 12 项通过，包含新增锁文件根版本检查 |
| 全量单元测试 | 1707/1707，50 suites，0 失败、取消、跳过，106.38 秒，退出 0 |
| 前端类型检查、构建 | 均退出 0 |
| Impeccable | 三个改动 UI 文件检查返回空问题列表；文案明确区分事实、记录和能力边界 |
| 语法、差异空白 | server 语法检查与 git diff --check 通过；有既有 CRLF 提醒 |
| 在线接口 | health、team/run、aibody 返回 200；无鉴权 team/run 返回 401 |
| 运行版本 | system/instances 返回 2.110.0、PID 21972、8787 端口；仅一个回环监听 |
| 前端部署 | 在线 index 与新构建逐字节一致，五项入口 JS/CSS 资源也一致 |
| 桌面浏览器 | 历史默认折叠，展开标签正确；AIBody 新文案与来源显示正确 |
| 360×800 窄屏 | 天团、改动验收切换正常，document.scrollWidth=360，无整页横向溢出 |
| 浏览器脚本异常 | 本次页面检查 pageerror 列表为空 |

测试工作区变量统一指向 `D:/pi-web/tmp/workbench-2110-tests`，未拿真实工作产物执行写入测试。没有触发付费生成，也没有删除旧任务记录。

浏览器验收使用本机无头 Chromium/Playwright 完成 DOM 与交互断言，不是像素级视觉验收。应用浏览器工具因 token 不可用未能使用；截图已保存，但本次图像查看工具不支持图像输入，不计作人工视觉审查。

## 发布与恢复

- 修改前备份：`D:/pi-web/backups/workbench-2.110.0/`，包含本次主要已有源码、版本声明、锁文件及旧 frontend-dist；不是整个脏工作区的完整快照。升级脚本旧版没有单独文件备份，可从 Git 原始版本与本次差异核对。
- 构建输出：`D:/pi-web/tmp/workbench-2110-build/`。
- 测试日志：`D:/pi-web/tmp/workbench-2110-tests.log`。
- 新入口脚本：`index-DNxw-2tJ.js`；样式：`index-DXf1HqpT.css`。
- 部署先复制资源后切换 index，保留旧 hash 资源，不覆盖 public 快照。
- 重启前 agent anyBusy=false、run overview 无 active、team launch=null。正规重启脚本退出 0，PID 从 8420 变为 21972。
- 未执行 Git 提交、推送或外部通知；保留原有未提交改动。未重建桌面或手机安装包。
- 如需回退，先保存发布后的新修改，逐文件核对备份，恢复前端资源后恢复 index，再用正规脚本重启验证。不要整仓覆盖，也不要删除用户任务历史。本次未演练回退。

## 仍未完成的能力（不能据此发布宣称完成）

- 天团统一 run-manager、取消/恢复、检查点/事件重放与预算控制，以及视频/小说/技能专项和反馈评估。
- 真实视频生成及完整付费任务链路未验收。
- delivery-contract 尚未成为全部业务发布授权来源。
- 改动验收页面的测试结果回写尚未接通，真实测试结果以本记录及日志为准。
- Git 快照有 300 文件上限；本次没有将页面变更清单升级为完整仓库审计。
- 桌面/手机安装包未重打包。网页版本更新不意味着旧安装包内部快照更新。
- 自动测试及主执行者检查不等于独立复审；本次不宣称独立复审通过。

此前累计修复与边界详见 `2026-09-21-workbench-release.md`。
