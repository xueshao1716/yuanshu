# 元枢：诊断纠偏与质量基线

状态：实现完成，隔离分支已验证，主项目集成后复验待完成；基于 2.116.1 / 7d605689，2026-09-25。

## 本轮目标

把桌面 Claude 诊断中的有效建议落成可复现的检查，不采用其中与当前代码冲突的架构判断。保留生产服务、未提交草稿和本地扩展技能。

1. 修正本地 boundary-subject-vs-stance、retro-ledger-audit、self-retro-review 三个技能描述缺触发语的问题；不改正文，不顺带收编本地技能。
2. 复用 verify-workbench 验证入口：区分仓库随附技能与本机附加技能，隔离测试工作空间，串联单测、类型检查和前端构建；失败必须非零退出。
3. 加自动检查工作流，验证干净检出，不接生产密钥、不调用付费模型、不部署、不发消息。
4. 依据现有代码补全架构/审计与后续实施文档，明确已实现、待验证与未实现；为安全拆分提供可核对的边界。
5. 运行完整验证、检查差异与副作用；集成到主工作区前确认没有并发改动。

## 不在本轮冒进的事项

- 不用 server-new.mjs 替换生产入口，不把未完成的 server/ 草稿当已交付。
- 不切默认引擎，不启动暂缓的跨系统整合。
- 不把本地冻结测试等同真实模型稳定性，Step-5 超时仍需单列验收。
- 不把“已有错误/审计机制”重写成另一套；后续逐条补覆盖。
- 不宣称本轮完成客户端打包、远端 CI 或服务重启，除非实际执行且验证。

## 验收证据

- 技能契约在本机完整技能目录和仓库随附目录分别给出真实结果。
- 验证入口具备自身回归测试，失败传播、路径隔离、报告均可检查。
- 全量单测、TypeScript 与前端构建分别记录退出码；保留失败日志，不用跳过断言换绿灯。
- 主工作区的用户文件、生产状态及未跟踪拆分草稿不变。

## 本轮执行记录

- 隔离分支最终 `npm run verify`：2125/2125 测试通过，0 跳过；类型检查与构建通过。证据指纹复核为 passed。
- 主项目实际 43 技能通过新版目录格式化验证：3991 字符，无缺失触发语。三个本地技能的描述修正在主机保留，不纳入代码提交。
- 独立子审查未完成：调用返回 403 Insufficient account balance；不能算作审查通过。主任务已检查实现差异和隔离边界，详见 `docs/quality-baseline-2026-09-25.md`。
- 本地依赖来自已安装目录；远端 CI、干净安装、客户端发版与真实模型长任务不是此次测试通过所能证明的内容。

## 下一阶段（依赖本轮基线）

按真实用户路径补“超时/空回复/继续执行/流式恢复/交付验收”的端到端证据；随后从无状态路由开始小步拆分原 server.mjs，聊天路由最后迁移。对引擎保留/切换只按实际任务成功率、耗时、成本与可追溯性判断。
