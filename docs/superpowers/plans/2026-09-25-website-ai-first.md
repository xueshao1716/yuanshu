# 网站工坊 AI-first 执行清单

按已批准方案直接执行，采用测试先行和完成前验证。

- [x] 在 `tests/unit/model-client.test.mjs` 补协议预算、部分正文、结束原因和 Responses 历史测试；先运行确认失败，再修 `engine/model-client.mjs`。
- [x] 在 `tests/unit/website-ai-first.test.mjs` 补无模板创建、分区计划、截断恢复、检查点继续、取消与不覆盖测试；先红后改生成模块与 `website-service.mjs`。
- [x] 在 `tests/unit/website-ui-contract.test.mjs` 补入口和进度契约；更新 WebsiteBoard、Workspace、RunPanel 和 API 类型，保留既有视觉体系。
- [x] 运行隔离的专项回归和真实浏览器桌面/手机验收；检查旧作品只读快照不变。
- [x] 运行类型检查、全量单测、构建、Impeccable detect；如遇既有失败按证据记录，不改无关文件。
- [x] 更新版本/变更记录，同步前端快照，检查在途任务后重启验证，审查改动范围。
- [x] 双推并核对两个远端提交。

## 2026-09-25 验证记录 · 2.116.1

- 专项回归：38/38 通过，覆盖三协议预算/截断正文、继续任务、取消、候选不覆盖、选区边界与版本声明。
- 全量测试：2064 项，2062 通过、2 失败。失败均来自既有未跟踪技能 `skills/boundary-subject-vs-stance/SKILL.md` 的触发语格式，涉及 `skill-catalog-injection` 和 `skills-contract`；未修改无关技能。
- `npm run build:frontend` 类型检查与生产构建通过；Impeccable detect 返回空数组。构建仍有既有大分块及动态导入提示。
- 发布产物桌面 1440×1050、手机 390×844 浏览器验收：想法创建、过程展示、刷新、模拟第二区块失败、继续、候选预览、显式采用全部通过；已完成区块只调用一次，无横向溢出及页面异常。样式检查覆盖根元素、子元素、伪元素、媒体查询、动画及兄弟区块隔离。截图已保存，但未声称做过截图视觉审美验收。
- 真实 `zai-coding-cn/glm-5.3-flash`：计划与两个区块完成，实际模型一致；原始页面手机端有 3px 装饰溢出，真实选区修改修正后桌面/手机均无溢出、无页面异常。断言确认其他 HTML 和全局 CSS 未改变，候选未自动采用。
- 真实 `stepfun-plan/step-5-preview`：计划完成；首次第一区块超时，续跑复用计划并完成第一区块；第二区块连续两次 120 秒超时。保留计划和 1/2 区块，已完成区块未重跑，无模型替换。此通道完整生成验收未通过，不宣称响应稳定，也未通过无限等待掩盖问题。
- 所有生成验收使用临时作品目录；真实工作空间作品文件 SHA256 快照不变。验收脚本与原始结果保存在本机 `.verification/`，不将含本机路径的临时产物作为发布资源。
- 前端已构建、清理不可达旧分块并同步至 `public/`、`app/dist/`，保留最近版本资源。未重打 Windows/Android 安装包。
- 确认聊天、子任务、网站生成均无运行任务后重启；健康检查 200，8787 只有一个监听进程。线上版本接口 `appVersion=2.116.1`，首页与 `frontend/dist/index.html` 完全一致，SHA256 为 `2cca6490dfa2f39207d1c19091d06a4c332da1fdd2609dd08296fc1b276e06b9`。只读浏览器加载线上工坊通过，新入口分块为 `WebsiteBoard-CIDYN8-x.js`，页面错误为零。
- 发布提交 `9ecfc8f622877d2592b9cc9ce7d32b596289f486` 已推送至 GitHub、Gitee；分别读取两个远端 `main`，与本地发布提交一致。未暂存或改动其他会话的服务重构及技能文件；本方案不发送微信通知。
