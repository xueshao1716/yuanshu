# 元枢工作台第一批修正与验证记录

日期：2026-09-20。用户“可以，你弄”后实施。

## 状态

改动仅位于 `D:/pi-web/.worktrees/verification-20260920`，不代表线上已生效。保留主工作区和既有候选改动，没有提交、推送、外部通知、记忆更新或重启服务。

## 本批实际改动

- 待审与回溯：限制工作区内容路径；拒绝目录链接、硬链接、越界及 Windows 大小写别名绕过；目标变化时禁止旧提议覆盖；原子写入和唯一备份。
- 验收事务：写入前记录 applying，完成审计后才 accepted；异常保留 needs_recovery 或 applying，阻止重复接受并在列表显示。回溯完成审计失败返回明确恢复警告。
- 界面：待恢复记录禁用接受/拒绝；操作异常后刷新列表；未知版本历史记录不再默认按 V20 解释 S8.5。
- 候选天团 runner：移除源码字面量访问凭据，统一读取配置；运行目录加唯一 ID，文件名固定避免模型标题控制路径；草稿单独保存，通过检查只进入待审，人工接受才写正式目标；投递失败不能说已交付。
- 仲裁结果必须在 JSON 中明确返回 unresolved 数组，只有显式空数组可通过该项门槛；缺失、格式错误或非空均阻止自动提交验收。修复提示复用实际任务上下文，删除固定“4镜”等示例裁决。
- 新增纯规则模块：明确 V20/V25 的 S8.5 含义，交付判定绑定产物摘要、真实运行证据和人工验收摘要。该模块尚未接入业务，不是凭据或验收身份认证机制。

## 验证证据

- 核心改动遵循先失败再修正：草稿模块缺失、凭据字面量、链接写入、审计失败、UI 恢复提示以及 5 个大小写绕过均出现预期失败，修正后通过。
- 最新针对性回归：119/119，覆盖安全、界面源码契约、草稿交付、清单、AIBody、run-store、run-manager 与交付规则。日志：`tmp/p0-reviewed-focused.log`。
- 独立流程规则通过规格审查与质量审查，各自确认 42 项通过。
- 最终独立审查曾发现未决事项字段缺失被误当为空、修复提示夹带固定示例两项问题；均补回归并修正。复审确认两项关闭，独立运行草稿测试 6/6 及 runner 语法检查通过；该复审范围无未关闭阻断项。
- 第一遍全量：1629/1629；该次运行期间仍在补充大小写检查，不能充当最终版本证据。最终重跑结果见下方更新。
- 前端类型检查和生产构建重新执行成功；日志 `tmp/p0-reviewed-build.log`，输出到 `tmp/p0-build-20260920`，没有清空或替换已有 `frontend/dist`。构建仍有既存的大包、插件弃用及混合导入提示，未扩大为本批依赖升级。
- Impeccable 对四个工作台相关前端文件输出空问题列表；语法检查与受影响文件 diff 空白检查通过。
- 全量测试前核对了记忆同步环境变量读取和相关临时目录夹具；测试进程把 YUANSHU_CWD、PI_WEB_CWD、PI_WORKSPACE 指向新建隔离目录。没有以真实工作产物作为写入靶子；这不等于所有历史测试均已完全环境无关。

## 限制与发布前必做

1. 线上 `server.mjs` 的天团启动入口仍指向 `D:/pi-workspace/工程/多AI角色扮演系统/scripts/team-run-live.mjs`。本批候选 runner 未调用真实模型，也未替换外部脚本。
2. 当前 runner 仍是视频脚本特定实现，不应当作完整小说/通用任务执行器；模型清单通过不等于真实视频生成验收。完整流程接线、预算、真实适配器属于下一批。
3. 早先部分独立审查任务返回“任务正文为空”；后续独立审查成功执行，对待审/回溯安全与界面未发现新增阻断问题，且发现并推动修复上述 runner 两项问题。审查与测试只覆盖列明范围，不等于完整安全认证。
4. 本轮没有浏览器实际点击、小屏、断线恢复或部署端到端验证；源码契约断言不替代这些检查。
5. 路径检查防止已存在的链接绕过，不宣称防住拥有本机文件写权限者在校验与写入之间主动换路径的全部竞争情况。验收仍依赖现有认证和可信存储；独立摘要规则尚未形成发布授权链。
6. 回溯审计失败有错误提示与预备份，但尚无完整自动恢复执行器；needs_recovery 必须核对文件、备份和审计记录后人工处理，不可盲目重放。
7. 上线前先审查并整合主目录差异，确认无运行任务，再用约定重启入口，核验健康、单实例、前端实际资源及验收链。P1–P4 尚未完成，不发布全项目“全部升级完成”的结论。

## 最终全量结果

审查修补后的最终重跑：1635/1635 通过，50 个 suite，0 失败、0 跳过，退出码 0，约 102 秒。日志：`tmp/p0-reviewed-tests.log`。该次开始前已完成未决事项门槛、提示与 JSON 示例修正，运行期间没有再改业务代码。

上一轮 1634/1634 的日志 `tmp/p0-final-tests.log` 保留作过程证据，不再作为最终版本结果。当前六个核心模块语法检查退出码均为 0；四个工作台前端文件 Impeccable 输出 `[]`，退出码 0。当前前端构建产物 `tmp/p0-build-20260920/index.html` 存在且为 1280 字节。
