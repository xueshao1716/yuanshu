# 元枢写真人像陪伴形象

用户于 2026-09-26 确认：按已提出方案制作素材并接入现有公仔，允许使用当前配置的 GPT 绘图 API。

## 范围与定义

以桌面《系统2.txt》第 1 项为依据：明确成年的东亚女性、超长黑直发、齐刘海、黑色长袖系带服装、烟黑薄丝袜、无鞋，冷白光下的黑楼梯与白墙。文中的 barefoot 解释为不穿鞋，脚仍被丝袜覆盖。人物为虚构形象，不改变系统身份、人格、权限或真实情绪记录。

母图为 9:16 写实楼梯摄影。公仔从同一母图编辑提取，保持面孔、服装与坐姿一致；透明素材若通过色键移除得到，明确记录而不冒称模型原生透明。不得用卡通或矢量占位代替写实图。

## 接入

在 XiaoyuWidget 新增 portrait 皮肤，保留旧四种皮肤及用户选择。首次安装仍沿用现有默认；已有用户可在外观中切换。拖动与位置记忆继续工作。坐姿皮肤停止自动游走、不水平翻转，不修改其他皮肤的游走偏好。允许轻微呼吸，减少动态设置下静止。

设置面板显示原始 9:16 场景和明确的“AI 生成形象”说明，并提供原图链接。原有展台、合影等功能仅适用于旧皮肤，真人皮肤不强套玩具展台。不改变 MoodOrb/MoodPanel 的在途改动。

## 验收

素材检查尺寸、解码、透明通道与文件体积，并人工查看人物/边缘。单测覆盖皮肤兼容和真人固定姿态；源码契约覆盖面板与动作接入。运行隔离全量验证、类型检查、构建、前端审查和桌面/窄屏浏览器检查。图片加载失败要保留文字回退，不出现空白公仔。

本次不做独立 Windows 透明桌宠窗口、不改原生客户端、不重启后端，也不处理系统2中的第2–7项。
