# 元枢 2.113.0 天团与 AIBody 共享链路验收

日期：2026-09-21。范围为当前 D:/pi-web 工作区与本机在线服务；保留原有未提交修改。安装包另见 [打包核验](packaging-2.113.0-20260921.md)。

## 本轮实现

天团使用统一的会话、任务管理、持久化事件和 AIBody 记录。工作台「天团」可关联当前会话或新建会话启动任务，聊天可用 `/team 需求` 启动。各角色接收受限长度的会话上下文和 AIBody 指导；角色进度、草稿进入同一 run/session，草稿回到原会话，之后普通聊天可继续调用工具。

停止和恢复沿用统一任务管理，保留原编号及步骤检查点。已完成步骤复用，结果不确定的付费调用拒绝自动重放。任务私有绑定、上下文不通过公开运行接口返回；草稿读取限制在本次运行的固定路径并检查链接边界。

当前范围是 legacy-video v2 的 10 秒、3 镜头脚本流程，不表示完整 V26 已实现。默认主驾仍为 Pi；天团角色编排使用元枢引擎。共享运行记录不意味着角色可自行改写人格或获得额外权限，人工验收与治理边界保留。

## 真实模型验收

使用独立 group=test 会话 `01a0c30f-b30f-71b0-811f-bab62fe89739`，没有使用真实业务产物作测试靶子。

| 步骤 | 实际引擎 / 文本模型 | 结果 |
| --- | --- | --- |
| 天团雨夜书店脚本 | yuanshu / zai-coding-cn/glm-5.3 | 7 个角色步骤完成，草稿回原会话 |
| 读取并修订草稿 | pi / zai-coding-cn/glm-5.3-flash | read、write、bash 三次工具成功 |
| 否定媒体请求复测 | pi / zai-coding-cn/glm-5.3-flash | 正确回忆阿澄、黄色伞、门边；没有媒体事件或出片提示 |
| 实时接流及只读核验 | pi / zai-coding-cn/glm-5.3-flash | 页面自动连接事件流，read 成功，最终回复落盘一次 |

天团 run：`6a73a85a-aed6-4db5-9306-99f3ecfbe6da`；launch：`7828c075-ddba-42e3-a667-6c668ec2a4d9`。实际用时约 7 分钟。AIBody 使用相同 run/session，记录 7 个子角色步骤及 1 个草稿产物，状态 completed；这表示执行结束，不表示质量通过。

质量检查实际为 **10/12，quality_failed**。系统保留未通过状态，未将草稿冒充正式交付。Pi 后续修订补齐逐镜声音提示、将第二镜收束为收伞动作并修正时间单位，但未自动改写原质量记录，也没有重新进行完整角色审阅或人工批准。

原始草稿：`D:/pi-workspace/工程/多AI角色扮演系统/runs/live-VIDEO-7828c075-ddba-42e3-a667-6c668ec2a4d9/草稿/终稿.md`。

修订 run：`45dd3536-0fca-4f55-8629-6cc1229d3a0f`。修订文件：`D:/pi-workspace/tmp/team-shared-21130-revised.md`，4152 字节。原稿保留。

否定媒体复测 run：`73dbe5a3-37cf-40f6-a4e0-5451395d6d26`。最终实时接流 run：`7bb35dbb-6d6f-44e6-9a39-c597e6b17c1c`。均在原测试会话完成，mediaModels 为空。最后一次 read 的 tool_end.isError=false，页面最终验收标记精确匹配 1 份，服务端非空助手回复也为 1 份。

## 实测发现并修复

1. 角色已有真实模型记录，但工作说明显示未记录：补充子角色模型作为无主模型记录时的来源；主模型记录仍优先。
2. 模型返回的镜头时间已带 s，渲染器再次追加导致 ss：统一格式化，新增回归测试，保留历史原稿。
3. “不生成视频”“不生成图片”“不配音”漏过否定识别：分别识别媒体否定，且不跨标点误伤另一种媒体。修复前确实进入了媒体旁路并出现出片提示，不能仅凭没有媒体交付事件断言从未尝试上游调用。修复后两次真实续聊均无出片提示或媒体事件。
4. 单会话任务概览被缓存 60 秒，页面可能漏接刚启动的任务：会话查询绕过缓存，全局总览保留缓存；真实页面从工作台返回后观察到新 run 的 events 请求、实时输出和完成状态。

## 自动验证

- `node scripts/verify-workbench.mjs` 隔离工作区执行完整单元测试：**1755/1755**，50 suites，无失败、取消、跳过。
- TypeScript 类型检查、隔离 Vite 构建均退出 0；正式前端构建及同步也已成功。
- 最终验证源码指纹：`7602faa032b055a930749f7cd4d095048f896bdcbe3883903f0f38290211a8db`；记录时间 `2026-09-21T08:53:00.820Z`；重新计算指纹仍匹配，state=passed。
- 证据文件：`tmp/review-verification.json`、`tmp/verification-unit.log`、`tmp/verification-types.log`、`tmp/verification-build.log`。
- 本地集成测试覆盖真实子进程接本地模型替身、同会话上下文、停止、已完成检查点恢复不重放、结果不确定时拒绝重试，以及运行接口脱敏与草稿路径边界。
- 四个改动的前端组件执行 Impeccable detect，结果 `[]`。`git diff --check` 无空白错误。

## 页面与部署

- 使用本机 Chromium 检查桌面页面及 390×844 手机宽度。天团入口、返回关联会话可用，手机无横向溢出，页面错误数为 0。
- 移动端工作台使用视图选择框，桌面使用「天团」页签。
- 实时接流验证使用页面真实网络请求及真实模型，未用预制回复替代；工具读稿完成后，最终回复只显示一份。
- 本地截图：`tmp/team-shared-21130-mobile.png`、`tmp/team-shared-21130-stream.png`。截图只用于本地诊断，可能包含侧栏，不对外发布。
- 无活动任务时通过正式重启脚本加载服务端修复。最终 /api/health 返回 ok=true，/api/frontend-version 返回 appVersion=2.113.0、version=2113000。
- 8787 唯一监听 PID=36296；服务首页与磁盘 frontend/dist/index.html 内容一致。最终测试会话无活动任务。

## 验收边界

本轮交付的是共享执行链路与脚本协作能力。真实草稿质量未通过的事实继续显示，未生成或声称交付真实视频。没有进行本轮 Windows 覆盖安装或 Android 真机覆盖安装，没有提交、推送仓库或对外发送通知。安装包的签名状态和依赖现有后端的限制见打包报告。
