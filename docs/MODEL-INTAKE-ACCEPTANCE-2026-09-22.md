# 模型接入与探测验收 · 2026-09-22

目标版本：2.115.5。范围：模型中心、快捷模型管理，以及 API 接入 → 目录发现 → 保存 → 单模型文本验证。

## 已实现

- 接入表单统一，明确选择 OpenAI Chat / Anthropic Messages；支持完整版本路径和手动模型 ID。编辑密钥留空沿用，出错保留输入。
- 目录发现仅读取列表，支持去重与原生分页；不再批量发起文本请求或生成图片。新增目录模型不受旧精选名单隐藏。
- 分开呈现已配置、尚未验证、文本通过、失败、预算不足；记录时间、协议和上游报告模型。名称推断的能力与默认上下文不冒充实测能力。
- 上游 401/429 不再判为健康；目录鉴权失败返回本地 422，避免把上游错误误当成元枢登录失效。
- 发现失败不替换原密钥；持久化失败检测、回滚与探测期间配置变更检查。重新发现为增量合并，保留手动模型和已有元数据。
- 旧配置只有模型级协议时，管理界面和重新发现沿用该协议；已有模型级协议不被无意覆盖。地址优先采用现行模型配置，避免读取旧鉴权地址。
- 文本验证使用所选模型和协议，不静默切模型，不生成媒体。成功必须收到实际文本；短预算耗尽单独记为未确认。
- 支持接入后全局列表刷新、重新发现、编辑和移除确认；修复多能力和视觉筛选。

## 验证记录

- 核心逻辑与回归覆盖：只读目录、原生鉴权、自定义版本路径、分页、非法/空目录、401/429、实际文本、短预算、输出参数兼容、旧配置协议、密钥保留、写盘失败回滚、向导复用。
- 桌面 1440×960 与手机 390×844 的浏览器自动化通过：目录失败保留登录和输入、手动原生模型登记、保存与列表刷新、文本验证及上游模型显示、编辑、重新发现、确认移除。无页面脚本错误、无横向溢出。
- 浏览器验收拦截配置写接口，仅使用虚拟服务商，不删除或覆盖实际密钥。
- 前端类型检查与生产构建通过；Impeccable 静态检查无发现。已有构建体积和导入方式警告仍存在。
- 截图已生成，但本次截图查看工具不支持图像输入，因此未完成目视验收；布局结论来自浏览器自动化。

## 真实上游抽测

2026-09-22 22:48，本机已保存配置，小额真实请求，无密钥输出、不保存回答正文：

| 配置 | 目录读取 | 文本调用 |
| --- | --- | --- |
| stepfun-plan / step-5-preview | 原生目录返回 401 | Messages 调用通过，约 1.55 秒；上游报告 step-5-preview |
| zai-coding-cn / glm-5.3-flash | 返回 11 个模型，包含目标模型 | 约 2.32 秒，64 token 短预算耗尽，记为未确认，未误判健康 |

这证明目录读取权限与模型调用权限需要分开判断。Step 5 目录 401 不意味着该密钥不能调用模型；可手动登记模型 ID，再验证实际文本。

## 边界

- 本次验证不证明工具、视觉、图片、视频或语音能力；媒体模型需在对应工具里验证。
- 接入表单提供 OpenAI Chat / Anthropic Messages；既有 Responses 模型可验证文本，但本次没有新增 Responses 接入选项。
- 真实抽测只覆盖上表配置，状态会随上游权限、余额和限流变化；不会绕过上游限制。
- 网页构建与服务更新不等于重新打包桌面或手机安装包。

## 最终回归与上线

- 全量测试：1,877 / 1,877 通过，50 个测试组，零失败、零跳过。测试工作目录隔离到临时目录。
- 最终日志：`C:/Users/xuexiaofeng/AppData/Local/Temp/yuanshu-models-669b8c3a494b403192ff299dbb39e764/tests.log`。
- 23:04 确认运行中任务为零，通过正式重启脚本重启。新接口已在 8787 生效。
- 本机与公网 `https://pi.myxinyu.xin` 健康检查成功，均加载本次构建 `index-CQXgT-lw.js`。
- 23:05 通过正式 HTTP 接口验证：Step 服务商现有 OpenAI 目录返回 10 个模型；显式选择 Messages 的目录仍返回上游 401，元枢转为 422，随后本地鉴权请求仍为 200。
- 同次 Step 5 文本验证走 `anthropic-messages`，993ms，通过；上游报告 `step-5-preview`。结果已保存并从 `/api/models` 读回。目录协议与单个模型调用协议可以不同，重新发现会保留模型级设置。
- 本次没有重新打包安装包、提交或推送代码。
