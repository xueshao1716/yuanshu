# Claude 系统诊断逐项对账

来源：桌面 `系统.txt`。核查日期：2026-09-25；基线提交 `8a17d41d` / 2.116.2。
这里的“已落地”不等于本轮已部署，也不等于已完成全系统安全验收。

## 已修复或已有实现

| 建议 | 结论和证据 |
| --- | --- |
| 修复技能描述和目录预算导致的测试失败 | 前轮已修复；技能清单区分随仓库与本机附加，全部参加校验。见 `docs/quality-baseline-2026-09-25.md`。 |
| 可重复的质量守门 | `npm run verify` 隔离应用状态，执行全量测试、类型检查、非线上构建，保存输入指纹与日志；不是操作系统沙箱。 |
| CI | `.github` 中已有 Windows 工作流；不代表远端 CI 已运行，也不包含自动发布。 |
| 版本同步 | `scripts/bump-version.mjs` 已存在；“没有同步机制”是过时判断。 |
| React 转正 | 生产入口已经服务 `frontend/dist`，不是备用存档。不重复迁移、不删除旧入口。 |
| 桌面壳和分享 | `app/src-tauri` 和分享实现已经存在；不能由此推定所有终端、外网环境均可用。 |
| 工坊长任务 | 本轮新增 Messages/Chat 流式进度与片段检查点；90 秒无有效内容、单次 5 分钟、整轮 15 分钟上限；主动取消可打断读取。Responses 暂仍等整包，不伪造流式进度。 |
| 本次模型与历史模型混淆 | 每次发请求清除本次响应模型，历史成功模型单独标记；失败不再沿用上一段实际模型。返回模型名仍是上游自报，不是提供商内部路由证明。 |
| 思考调试接口协议分叉 | 复用 directChat：Messages 读取 thinking 字段，Responses 走对应协议，减少重复实现。 |
| 环境变量密钥兼容 | 修复统一调用时仅读取 auth 文件的回归；保留已有 provider 环境变量解析路径。回归测试先复现 502，再验证恢复 200，未输出真实凭据。 |
| 局部编辑验收误判 | 原脚本混用存储 HTML 和 DOM 序列化片段，遇到 `<br />` / `<br>` 时替换失败。改为 DOM 同口径比较，并保留选区位置标记；仍拒绝选区外文字、祖先属性、位置、全局 CSS 和重复 ID 变化。 |
| 文档准确性 | Android 验收文档去掉 0.2.4 的陈旧当前版本、固定首屏和未证实的签名连续性结论，改用隔离验收入口。 |

## 有价值但尚未全面完成

| 工作 | 当前边界 / 后续验收门槛 |
| --- | --- |
| 服务入口拆分 | 入口仍大；`server-new.mjs`、`server/` 为未完成草稿，不接管生产。先抽无状态路由，逐个比对鉴权、状态码和响应体，聊天/SSE 最后迁移。 |
| 全局统一错误契约 | 本轮补工坊流错误代码和取消语义，不声称所有 API 已统一。避免一次性改变已有客户端依赖的响应格式。 |
| 后端类型与 API 文档 | 前端类型检查与部分契约测试已有；全 API schema、生成类型、后端 JSDoc 覆盖尚未完成。 |
| 依赖注入与日志监控 | 已能隔离本轮测试，运行事件和审计机制已有；所有模块消除全局依赖、全局结构化日志和覆盖盘点仍待做。 |
| 安全边界 | 工具安全检查不等于容器/VM 隔离。操作审计覆盖、凭据加密与轮换、OS 级沙箱未完成，不能写成“安全已全过”。 |
| 真机体验 | 没有连接 Android 设备；键盘、后台恢复、下载、覆盖安装均待真机验收。桌面浏览器证据不能替代。 |
| CI 与发布 | 本地验证不等同远端干净安装；远端 CI、签名、安装包和运行中版本应分别记录。 |

## 不按原建议直接实施

- `website-ai-first` 的旧失败没有复现，不修改已正确的可选参考模板逻辑。
- 不因产品叫“元枢”就删引擎切换或换默认主驾；按真实任务能力决定。
- 不删除兼容环境变量 `PI_WEB_*`，不为了命名整齐破坏已有配置。
- 不把多人团队、协同编辑、iOS、离线模型视为当前缺陷；属于独立产品范围。
- 不启动已明确暂缓的 OpenClaw/Hermes/曦/linxinyu 整合。
- 不直接照搬全局格式化、全仓重构或自动发布示例。

## 本轮验收

源码修复、自动化验证、真实模型、线上部署、客户端安装是不同状态，不合并成“都完成”。

- 定向回归：模型调用、流式读取、局部编辑验收共 23 项通过；新增环境密钥回归 1 项、选区验收 7 项。
- 全量验证：首轮 2182/2182 测试、类型检查和构建通过，但验证过程中新增了离线复查脚本，输入指纹失效，命令退出 1；这次结果不作为最终输入指纹的验收。随后冻结代码完整重跑，结果见下方最终记录。
- 真实模型：隔离任务中的 Step-5（`stepfun-plan/step-5-preview`，Messages 协议）复用已有检查点完成下一段生成，耗时约 143 秒；随后约 6.5 秒完成局部修改。模型身份是上游返回值。仅代表这一次实际场景，不能外推所有模型稳定性。
- 初次真实任务报告保留失败状态：失败来自上述验收脚本误判，不改写旧报告。离线复查存量产物通过，选区外未变、仅增加指定边框，原项目文件与报告字节未改，导出 HTML 11663 字节；没有额外调用付费模型。
- 浏览器自动检查：1440×1050 和 390×844 两种视口通过进度、重载、恢复、继续、不重复已完成分段、候选版本、显式采纳和无横向溢出检查；页面错误为空。使用测试模型，不等于真实手机或跨模型测试。
- 前端规则检测：`WebsiteRunPanel.tsx` 的 Impeccable 检测结果为空。截图已保存，但本次未做截图视觉审阅，不据此声称完整视觉或无障碍验收。
- 发布状态：本轮未部署、未重启、未打客户端安装包；当前版本仍为 2.116.2。

### 本机证据位置

- 原始真实任务与复查：`C:/Users/xuexiaofeng/AppData/Local/Temp/yuanshu-website-live-AVJjL6/`，分别为 `report.json`、`recheck-report.json`、`delivery-rechecked.html`。
- 原始报告 SHA-256：`da840eb6be12ca1c0a5e3c6da1c8164169cafde00f214a43f451e8738952bc1c`。
- 浏览器产物：首次 `C:/Users/xuexiaofeng/AppData/Local/Temp/yuanshu-website-browser-SQsCte/`；收尾复跑 `C:/Users/xuexiaofeng/AppData/Local/Temp/yuanshu-website-browser-U1xnbp/`，两种视口均通过，页面错误为空，退出 0。
- 全量日志与输入指纹：`tmp/verification-{unit,types,build}.log`、`tmp/review-verification.json`；构建写入 `tmp/verification-dist`，不覆盖线上 `frontend/dist`。

### 最终记录

2026-09-25T08:54:48.782Z，冻结代码后 `npm run verify` 退出 0；随后重新计算当前输入指纹，证据状态为 `passed`。

- 单元测试 2182/2182，0 失败、0 跳过，约 168 秒。
- 类型检查退出 0，约 9 秒；隔离构建退出 0，约 7 秒。
- 输入 SHA-256：`e9634f676fbc2afcf7028b2ef66f7e99efaaaaf867d927cd34526cbb48f6d0ed`。
- 构建仍有已有的大包和混合动态/静态引用警告，不表示已完成性能优化。
- 线上 `/api/health` 返回 `ok: true`。本輪未提交、推送、部署、重启或打包；修复仍在本地工作区，版本不变。

### 后续发布记录：2.116.3

以上“未发布”描述保留为当时状态。随后按继续完成授权升版到 2.116.3：重新全量验证 2182/2182 通过，类型检查与隔离构建通过；最终输入指纹 `9dc1176357cd637af2ca4b4ce49e16de1e4269be6e933404fa7c4820fb6f802c`。

已部署并正规重启，在线版本 2.116.3 / 2116003；最终生产构建的桌面/手机尺寸浏览器回归通过。Windows x64 与 Android arm64 安装包均已构建和核验，详情及 SHA256 见 [发布记录](packaging-2.116.3-20260925.md)。真机、全仓架构与安全改造仍按上表保留边界，不把发布完成等同这些事项完成。
