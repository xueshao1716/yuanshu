# 系统2第六项：工具能力认知与过程显示

日期：2026-09-26。范围仅限桌面任务书第六项，不扩大工具权限、不调整模型路由、不处理第七项。

## 事实与修正

- 当前默认配置和工具装配代码并非仅有bash，包含read/write/edit等；本机watchdog日志也曾记录首轮包含这些工具。未拿到用户所指历史回合的完整请求，不能断言该次误报的唯一原因。
- 元枢自制循环在实际工具选择之后注入“本轮工具事实”，续跑时移除同类旧提示再刷新，避免沿用历史能力描述。提示不能授予权限；规划模式、无工具模式、审批与安全拦截保持原样。
- pi适配器引导明确以实际工具定义为准，文件操作优先使用可用的read/write/edit，命令和批处理再用bash；禁止通过bash绕过限制。

## 界面行为

1. 消息新增“显示/隐藏工具执行”，包含bash及其他工具，切换对已加载消息同步生效；工具默认显示，保留原有行为。
2. “过程与依据”保持默认折叠，增加明确的显示/隐藏状态，与工具详情开关独立。共享此组件的工作说明同步切换。
3. 本设备通过本地存储记住选择，支持同源跨标签同步；存储不可用时仍可在当前页面切换，但不承诺刷新后保留。
4. 只控制展示，不取消任务、不删除记录。隐藏时保留运行/失败数量，交付正文、错误重试入口、问题及下一步不被包进隐藏区域。
5. 单工具详情改用原生按钮，可键盘操作，工具开关及详情按钮保留至少44px触控高度。

## 验收

- 新增工具事实与UI边界用例，修复前对应失败；完整隔离测试2263/2263通过，类型检查和非线上目录构建通过。最终运行证据保留于本工作树tmp/review-verification.json及verification-*.log。
- 最终固定代码复验退出0，记录时间2026-09-26T11:48:46.247Z，输入指纹`8ee74bdd0f341cc9d8c08e283481fe75836de14f3b727673f126bc3c4b9c82b3`。此前一次在验证期间新增验收脚本，三项检查虽通过但整体验证返回过期；本次完整重跑取代该记录。相关定向测试另通过10/10。
- 真浏览器加载实际Message/WorkExplanation组件，桌面1280×900及手机390×844尺寸验证：一键切换、键盘展开、错误不隐藏、隐藏期间结果更新、刷新记忆、跨标签同步、无横向溢出；受限存储回退验证通过。此为隔离测试数据，不是手机真机或真实模型验收。
- Impeccable detector对四个改动前端源文件返回空问题列表，git diff --check通过。
- 截图已保存于本机临时验收目录，但当前图片查看工具不可用，未做人工逐图视觉确认；不将自动布局断言称为视觉审美验收。

## 发布边界

本轮只完成本地源码实现与验收，未重启、未覆盖线上frontend/dist、未打包、未远端推送，版本仍2.116.7。后续前端构建发布和服务重启后才会生效；未承诺提示词能消除所有模型能力误报，真实模型行为仍需部署后观察。
