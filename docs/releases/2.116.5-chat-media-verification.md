# 2.116.5 会话素材保存与连续查看

日期：2026-09-25

## 改动

- 同会话图片和视频共用查看器，按素材路径去重；方向键、左右按钮、触屏横滑切换。
- 视频播放控件底部 56px 保留原生操作，不用于翻页。切换会话关闭旧查看器。
- 保存时保留中文文件名和扩展名；支持 File System Access 选择位置，优先保留 Android 原生桥。
- 写入完成才标记已保存；普通浏览器下载只能确认已发起，不宣称已经落盘。取消、404 和网络错误均有反馈。

## 验证

- 新增 7 个单元测试；全量 2202 个单元测试通过，类型检查和构建通过。
- `node scripts/verify-chat-media.mjs`：真实 Chromium，1280×900 / 390×844 两种布局。
- 使用隔离测试页和临时 PNG/WebM，不操作真实用户文件。图片、视频下载文件名与每字节内容均一致。
- 验证上一项/下一项、键盘、列表边界、图片/视频合成触摸事件、播放控件防误翻页、返回键关闭、会话切换清除旧素材。
- 验证浏览器下载记录为 started；模拟文件选择器写入并关闭后才为 saved；取消和 404 提示正常。
- 两种布局无横向溢出，保存按钮至少 44×44 且位于屏幕内，无页面运行错误。Impeccable 检测结果为空。
- 截图已生成，但当前工具不支持图像输入，未宣称完成肉眼截图验收。

## 部署与边界

- 已构建 frontend/dist，同步 public 与 app/dist；保留旧哈希资源供已打开的页面使用。
- 正式重启后健康检查正常，单监听进程返回版本 2.116.5，HTTP 首页与磁盘构建一致。
- 未连接手机实机；触摸为浏览器合成事件，Android 系统保存对话框仍需真机复核。
- 最初修复轮未生成新安装包；同日后续打包与补充验收见 [2.116.5 打包记录](../packaging-2.116.5-20260925.md)。现行 Tauri 壳读取服务端页面，重新加载后获取新版前端；原生桥或客户端版本更新仍需安装新版。
