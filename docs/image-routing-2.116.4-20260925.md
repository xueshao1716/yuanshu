# 2.116.4 对话绘图选路验收 — 2026-09-25

## 修复范围

- `generate_image` 增加 provider/modelId，pi 与元枢共用；指定模型不静默换通道。
- 自动媒体入口保留用户原话，避免提取画面提示词后丢失 OpenAI/GPT Image 选择。家族名称映射本机已配置的 gpt-image 模型，不假定第三方别名都是官方型号。
- 未指定模型时保留原默认排序；429/502/503/504 最多尝试另一个通道。参数/策略拒绝、鉴权失败、模糊超时及无图片的成功响应不触发跨通道重试。
- 工具反馈、完成通知、媒体块及 SDK 兼容历史保留实际模型和尝试记录。
- 独立生图面板原本已传选择参数，本轮未改前端组件。

## 本地验证

- 新增绘图选路测试：13/13，通过本机 HTTP 测试服务验证实际请求模型与反馈。
- 最终 `npm run verify`：2195/2195，0 失败、0 跳过；类型检查通过；隔离构建通过。测试应用状态与真实工作空间隔离。
- 生产前端构建通过，版本 2.116.4；同步 frontend/dist、public、app/dist，三份入口 SHA-256 一致。
- 无前端源组件改动，Impeccable 前端设计检测不适用；不以此声明 UI 重做或视觉验收。
- 清理 99 个不可达中间构建分块（4.4 MB），保留当前和历史入口依赖。清理前备份：`D:/pi-workspace/.build-cache/image-routing-2.116.4-frontend-backup`。

## 真实通道结果（没有出图成功）

| 配置模型 | 实际结果 |
| --- | --- |
| aieyra-image/gpt-image-2.5-sunburst | 约 4.76 秒，上游 HTTP 502：Upstream request failed |
| aieyra/gpt-image-1 | 约 1.61 秒，标准地址 HTTP 404：当前账户组无支持该模型的账号；既有备用地址返回 502 |

以上证明更新后的选路到达所选通道，不证明通道可用。未反复购买图片、未改密钥、未将不可用型号伪报可用。
本机脱敏记录：`tmp/image-routing-live-evidence.json`、`tmp/image-routing-live-gpt-image-1.json`（不纳入仓库）。

## 发布边界

验收结束时 `/api/agent-status` 仍报告一个正在执行的任务；本轮不强制重启，不声明运行中的后端已加载修复。代码与网页快照发布后，仍需在任务结束后重启服务。

本轮没有重新生成 Windows/Android 安装包。核心修复在服务端，既有客户端连接重启后的服务即可使用；安装包自带版本展示不会因本次网页快照同步而改变。
