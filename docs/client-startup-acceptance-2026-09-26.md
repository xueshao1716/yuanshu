# 第7项：Windows独立启动验收

日期：2026-09-26。工作分支`feat/client-startup`，隔离目录`.worktrees/client-startup`。候选版本2.116.9。

## 本批实现

- Windows首先打开打包内的启动页，不依赖8787在线；检查健康后进入本机工作台。
- 原生启动只操作固定的`yuanshu-watchdog`计划任务，命令仅授予本地页面；不接受用户命令、路径和端口，不提供公网启动API，不自动提权。
- 任务缺失/无法访问、禁用、启动被拒绝、健康超时均有可重试提示；原生等待有60秒上限，只终止自己创建的辅助进程。
- 守护改为检查HTTP200和JSON布尔值`ok:true`，合并并发启动，健康或已占端口时不强杀、不重复启动。
- 自启脚本使用单一绝对Node路径，开机、登录、无限期每五分钟兜底，忽略重复实例；只迁移元枢自身两个旧任务名。
- 启动页源文件保存在`app/startup`，前端同步清理旧资源后恢复三份启动文件，避免后续发包丢失。

## 已有证据

- 最终隔离全量2277/2277、类型检查、构建通过；记录`2026-09-26T13:22:55.683Z`，摘要`4083d2018fabd7c900b959015da341cf5df943e406625dafcd623e5d261fbbea`，已包含资源同步与多Node回归。
- 新增资源同步测试先因`startup.html`丢失失败，修复后通过；新增多Node选择测试先失败、修复后通过。
- Rust原生编译成功；`cargo test --lib --offline`1/1通过。
- 两种窗口尺寸1280×820、420×360的浏览器夹具通过等待、失败、重试、键盘、无横向溢出、调用顺序检查；原生桥被模拟，不等于安装后的原生端到端验收。
- 实际执行只读健康分支返回`READY`，服务监听仍为PID29400；没有停止生产服务。
- Impeccable检测已运行，但缺HTML解析模块，退化为正则；不把空结果视为完整无障碍/视觉通过。截图生成但未完成视觉查看。
- 原生debug程序可启动并存活8秒，随后只关闭了本次测试进程；原生WebView调试端点未连上，不能确认完整页面进入链路。

## Windows候选安装包

- NSIS发布构建退出0，交付文件`D:/pi-workspace/交付/元枢桌面客户端/元枢_2.116.9_x64-setup.exe`，9,994,538字节。
- 构建目录与交付副本SHA-256一致：`D3C66D1D2961C182BC2CCBFAC4708FD6A90E07C108878CB93D3D9DC81E4CDCAA`。
- 签名状态`NotSigned`；未覆盖安装用户当前客户端，不能将成功打包等同于安装/离线恢复验收。
- `app/startup`与`app/dist`三份启动资源逐字节一致；生成权限仅增加本地启动命令与Windows本地能力。

## 本机任务部署状态

现有`yuanshu-watchdog`为Highest任务，状态Ready，没有48787守护监听。重复触发仍是`PT5M/P1D`，不是本次修复后的无限期触发。

已备份到`D:/pi-workspace/tmp/yuanshu-watchdog-before-client-startup-20260926.xml`。2026-09-26尝试只修改该任务的动作、触发器、设置并保留运行主体，Windows返回“拒绝访问”。未发起UAC、未绕过权限、未删除原任务、未停止后端。

合并到正式服务目录后，需要管理员从`D:/pi-web`执行经过审查的任务配置（不能在工作树注册，否则运行路径会错误）。当前仅新客户端包不能证明此任务已修好。

## 仍未完成

- 手机/网页在后台服务离线时的恢复：需要独立于主服务的鉴权控制面；手机本身不能直接启动另一台电脑的进程。
- Windows安装/覆盖升级与真实离线拉起验收；本机计划任务修复。
- Android真机、签名发布、远端CI；本批不生成无原生变化的APK。
- 前期第4项真实行为实验对照、第5项真实模型长稿复验；C3-01至C3-05长期欠账继续保留，不以本地夹具替代。

本批不调用付费模型、不修改人格/记忆/技能、不执行通知脚本。
